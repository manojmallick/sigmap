{
  "_comment": "ContextForge configuration — all keys are optional (defaults shown)",

  "output": ".github/copilot-instructions.md",

  "outputs": ["copilot"],

  "srcDirs": ["src", "app", "lib", "packages", "services", "api"],

  "exclude": [
    "node_modules", ".git", "dist", "build", "out",
    "__pycache__", ".next", "coverage", "target", "vendor",
    ".context"
  ],

  "maxDepth": 6,

  "maxSigsPerFile": 25,

  "maxTokens": 6000,

  "secretScan": true,

  "monorepo": false,

  "diffPriority": true,

  "mcp": {
    "autoRegister": true
  },

  "_strategy_comment": "Context output strategy. Options: 'full' | 'per-module' | 'hot-cold'",
  "_strategy_full":       "  full       — single file, all signatures, always injected (default)",
  "_strategy_per_module": "  per-module — one .github/context-<module>.md per srcDir + thin overview",
  "_strategy_hot_cold":   "  hot-cold   — recently changed files injected; rest in context-cold.md (MCP)",
  "strategy": "full",

  "_hotCommits_comment": "Only used by hot-cold strategy: how many recent commits count as 'hot'",
  "hotCommits": 10
}
