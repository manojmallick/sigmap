{
  "_comment": "Claude Code settings — both MCP servers configured. Copy to .claude/settings.json in your project root.",
  "mcpServers": {
    "context-forge": {
      "_comment": "ContextForge: fast always-on code signatures (~200-4K tokens per call)",
      "command": "node",
      "args": ["/absolute/path/to/your/project/gen-context.js", "--mcp"]
    },
    "repomix": {
      "_comment": "Repomix: full file content for deep analysis sessions (install: npm i -g repomix)",
      "command": "npx",
      "args": ["-y", "repomix", "--mcp"]
    }
  }
}
