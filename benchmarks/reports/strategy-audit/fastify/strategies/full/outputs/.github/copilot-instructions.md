

## Auto-generated signatures
<!-- Updated by gen-context.js -->
<!-- Generated by SigMap gen-context.js v3.5.0 -->
<!-- Updated: 2026-04-14T22:00:53.478Z -->
<!-- Do not edit below — regenerate with: node gen-context.js -->

# Code signatures

<!-- Generated by SigMap gen-context.js v3.5.0 -->
<!-- DO NOT EDIT below the marker line — run gen-context.js to regenerate -->

# Code signatures

## todos
```
lib/promise.js:15  # TODO: (20.x): remove when node@20 is not supported
lib/reply.js:382  # TODO: Explore a central cache for all the schemas shared across
lib/reply.js:850  # TODO: we may protect multiple callback calls
lib/request.js:332  # TODO: Explore a central cache for all the schemas shared across
lib/route.js:479  # TODO: remove return503OnClosing after Node v18 goes EOL
```

## lib

### lib/config-validator.js
```
function validate10(data, {instancePath="", parentData, parentDataProperty, rootData=data}={})
```

### lib/content-type-parser.js
```
function ContentTypeParser(bodyLimit, onProtoPoisoning, onConstructorPoisoning)
function rawBody(request, reply, options, parser, done)
function getDefaultJsonParser(onProtoPoisoning, onConstructorPoisoning)
function defaultPlainTextParser(req, body, done)
function Parser(asString, asBuffer, bodyLimit, fn)
function buildContentTypeParser(c)
function addContentTypeParser(contentType, opts, parser)
function hasContentTypeParser(contentType)
function removeContentTypeParser(contentType)
function removeAllContentTypeParsers()
function validateRegExp(regexp)
```

### lib/content-type.js
```
class ContentType
  constructor(headerValue)
  if(headerValue == null || headerValue === '' || headerValue === 'undefined')
  if(sepIdx === -1)
  if(sepIdx === -1)
  if(sepIdx === -1)
  while(matches)
  if(value[0] === '"')
  isEmpty()
```

### lib/context.js
```
function Context({ schema, handler, config, requestIdLogLabel, childLoggerFactory, errorHandler, bodyLimit, logLevel, logSerializers, attachValidation, validatorCompiler, serializerCompiler, replySerializer, schemaErrorFormatter, exposeHeadRoute, prefixTrailingSlash, server, isFastify, handlerTimeout })
function defaultSchemaErrorFormatter(errors, dataVar)
```

### lib/decorate.js
```
module.exports = { add: decorateFastify, exist: checkExistence, existRequest: checkRequestExistence, existReply: checkReplyExistence, dependencies: checkDependencies, decorateReply, decorateRequest, getInstanceDecorator, hasKey }
function decorate(instance, name, fn, dependencies)
function getInstanceDecorator(name)
function decorateConstructor(konstructor, name, fn, dependencies)
function checkReferenceType(name, fn)
function decorateFastify(name, fn, dependencies)
function checkExistence(instance, name)
function hasKey(fn, name)
function checkRequestExistence(name)
function checkReplyExistence(name)
function checkDependencies(instance, name, deps)
function decorateReply(name, fn, dependencies)
function decorateRequest(name, fn, dependencies)
function assertNotStarted(instance, name)
```

### lib/error-handler.js
```
module.exports = { buildErrorHandler, handleError }
function handleError(reply, error, cb)
function defaultErrorHandler(error, request, reply)
function fallbackErrorHandler(error, reply, cb)
function buildErrorHandler(parent = rootErrorHandler, func)
function setErrorHeaders(error, reply)
```

### lib/error-status.js
```
module.exports = { setErrorStatusCode }
function setErrorStatusCode(reply, err)
```

### lib/errors.js
```
function appendStackTrace(oldErr, newErr)
```

### lib/four-oh-four.js
```
function fourOhFour(options)
```

### lib/handle-request.js
```
function handleRequest(err, request, reply)
function handler(request, reply)
function preValidationCallback(err, request, reply)
function validationCompleted(request, reply, validationErr)
function preHandlerCallback(err, request, reply)
function preHandlerCallbackInner(err, request, reply, store)
```

### lib/head-route.js
```
module.exports = { parseHeadOnSendHandlers }
function headRouteOnSendHandler(req, reply, payload, done)
function parseHeadOnSendHandlers(onSendHandlers)
```

### lib/hooks.js
```
module.exports = { Hooks, buildHooks, hookRunnerGenerator, preParsingHookRunner, onResponseHookRunner, onSendHookRunner, preSerializationHookRunner, onRequestAbortHookRunner, hookIterator, hookRunnerApplication, onListenHookRunner, preHandlerHookRunner, preValidationHookRunner, onRequestHookRunner, onTimeoutHookRunner, lifecycleHooks, supportedHooks }
function Hooks()
function buildHooks(h)
function hookRunnerApplication(hookName, boot, server, cb)
function onListenHookRunner(server)
function hookRunnerGenerator(iterator)
function onResponseHookIterator(fn, request, reply, next)
function onSendHookRunner(functions, request, reply, payload, cb)
function preParsingHookRunner(functions, request, reply, cb)
function onRequestAbortHookRunner(functions, request, cb)
function hookIterator(fn, request, reply, next)
```

### lib/initial-config-validation.js
```
function validateInitialConfig(options)
function deepFreezeObject(object)
```

### lib/logger-factory.js
```
module.exports = { createChildLogger, defaultChildLoggerFactory, createLogger, validateLogger, now }
function createChildLogger(context, logger, req, reqId, loggerOpts) → object
function defaultChildLoggerFactory(logger, bindings, opts) → import('../types/logger.j
function validateLogger(logger, strict) → boolean
function createLogger(options)
function now()
```

### lib/logger-pino.js
```
module.exports = { serializers, createPinoLogger }
function createPinoLogger(opts)
```

### lib/plugin-override.js
```
function buildRoutePrefix(instancePrefix, pluginPrefix)
```

### lib/plugin-utils.js
```
module.exports = { getPluginName, getFuncPreview, kRegisteredPlugins, getDisplayName, registerPlugin }
function getMeta(fn)
function getPluginName(func)
function getFuncPreview(func)
function getDisplayName(fn)
function shouldSkipOverride(fn)
function checkDependencies(fn)
function checkDecorators(fn)
function _checkDecorators(that, instance, decorators, name)
function checkVersion(fn)
function registerPluginName(fn)
function checkPluginHealthiness(fn, pluginName)
function registerPlugin(fn)
```

### lib/promise.js
```
module.exports = { withResolvers: typeof Promise.withResolvers === 'function'
    ? Promise.withResolvers.bind(Promise) 
    
    : withResolvers, [kTestInternals]: {
    withResolvers }
function withResolvers()
```

### lib/reply.js
```
function Reply(res, request, log)
function preSerializationHook(reply, payload)
function preSerializationHookEnd(err, _request, reply, payload)
function wrapSerializationError(error, reply)
function onSendHook(reply, payload)
function wrapOnSendEnd(err, request, reply, payload)
function safeWriteHead(reply, statusCode)
function onSendEnd(reply, payload)
function logStreamError(logger, err, res)
function sendWebStream(payload, res, reply)
function sendStream(payload, res, reply)
function sendTrailer(payload, res, reply)
function sendStreamTrailer(payload, res, reply)
function onErrorHook(reply, error, cb)
function setupResponseListeners(reply)
function onResponseCallback(err, request, reply)
function buildReply(R)
function notFound(reply)
function serialize(context, data, statusCode, contentType) → string
function noop()
```

### lib/req-id-gen-factory.js
```
module.exports = { getGenReqId, reqIdGenFactory }
function reqIdGenFactory(requestIdHeader, optGenReqId) → string
function getGenReqId(contextServer, req)
function buildDefaultGenReqId()
function buildOptionalHeaderReqId(requestIdHeader, genReqId)
```

### lib/request.js
```
function Request(id, params, req, query, log, context)
function getTrustProxyFn(tp)
function buildRequest(R, trustProxy)
function buildRegularRequest(R)
function getLastEntryInMultiHeaderValue(headerValue)
function buildRequestWithTrustProxy(R, trustProxy)
function assertsRequestDecoration(request, name)
```

### lib/route.js
```
module.exports = { buildRouting, validateBodyLimitOption, buildRouterOptions }
function buildRouting(options)
function handleOnRequestAbortHooksErrors(reply, err)
function handleTimeout()
function normalizeAndValidateMethod(method)
function validateSchemaBodyOption(method, path, schema)
function validateBodyLimitOption(bodyLimit)
function validateHandlerTimeoutOption(handlerTimeout)
function runPreParsing(err, request, reply)
function buildRouterOptions(options, defaultOptions)
function removeTrackedSocket()
function noop()
```

### lib/schema-controller.js
```
class SchemaController
  constructor(parent, options)
  if(parent)
  add(schema)
  getSchema(schemaId)
  getSchemas()
  setValidatorCompiler(validatorCompiler)
  setSerializerCompiler(serializerCompiler)
  getValidatorCompiler()
function buildSchemaController(parentSchemaCtrl, opts) → object
```

### lib/schemas.js
```
module.exports = { buildSchemas (initStore) { return new Schemas(initStore) }
function Schemas(initStore)
function isCustomSchemaPrototype(schema) → boolean
function normalizeSchema(routeSchemas, serverOptions)
function generateFluentSchema(schema)
function getSchemaSerializer(context, statusCode, contentType) → function|false
```

### lib/server.js
```
function defaultResolveServerListeningText(address)
function createServer(options, httpHandler)
function multipleBindings(mainServer, httpHandler, serverOpts, listenOptions, onListen)
function listenCallback(server, listenOptions)
function listenPromise(server, listenOptions)
function getServerInstance(options, httpHandler)
function getAddresses(address) → string[]
function logServerAddress(server, listenTextResolver)
function createCloseHttp2SessionsByHttp2Server(http2Server) → () => void
```

### lib/validation.js
```
module.exports = { symbols: { bodySchema, querystringSchema, responseSchema, paramsSchema, headersSchema }
function compileSchemasForSerialization(context, compile)
function compileSchemasForValidation(context, compile, isCustom)
function validateParam(validatorFunction, request, paramName)
function validate(context, request, execution)
function validateAsyncParams(validatePromise, context, request)
function validateAsyncBody(validatePromise, context, request)
function validateAsyncQuery(validatePromise, context, request)
function validateAsyncHeaders(validatePromise, context, request)
function wrapValidationError(result, dataVar, schemaErrorFormatter)
```

### lib/warnings.js
```
module.exports = { FSTWRN001, FSTWRN003, FSTWRN004, FSTSEC001, FSTDEP022 }
```

### lib/wrap-thenable.js
```
function wrapThenable(thenable, reply, store)
```
