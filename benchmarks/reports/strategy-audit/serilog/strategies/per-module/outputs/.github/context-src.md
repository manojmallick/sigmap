<!-- Generated by SigMap gen-context.js v3.3.2 -->
<!-- DO NOT EDIT below the marker line — run gen-context.js to regenerate -->

# Code signatures

## src

### src/Serilog/Core/Pipeline/ByReferenceStringComparer.cs
```
class ByReferenceStringComparer
  Equals(object? x, object? y) → new bool
  GetHashCode(object obj) → int
  Equals(string? x, string? y) → bool
  GetHashCode(string obj) → int
```

### src/Serilog/Core/Pipeline/MessageTemplateCache.cs
```
class MessageTemplateCache
  Parse(string messageTemplate) → MessageTemplate
```

### src/Serilog/Core/Pipeline/SilentLogger.cs
```
class SilentLogger
  ForContext(ILogEventEnricher enricher) → ILogger
  ForContext(IEnumerable<ILogEventEnricher> enrichers) → ILogger
  ForContext(string propertyName, object? value, bool destructureObjects = false) → ILogger
  ForContext(Type source) → ILogger
  Write(LogEvent logEvent) → void
  Write(LogEventLevel level, string messageTemplate) → void
  Write(LogEventLevel level, string messageTemplate, params object?[]? propertyValues) → void
  Write(LogEventLevel level, Exception? exception, string messageTemplate) → void
```

### src/Serilog/Core/PropertiesInlineArray.cs
```
struct PropertiesInlineArray
  AsSpan(int count) → Span<object?>
```

### src/Serilog/Core/Sinks/AggregateSink.cs
```
class AggregateSink
  Emit(LogEvent logEvent) → void
```

### src/Serilog/Core/Sinks/Batching/FailureAwareBatchScheduler.cs
```
class FailureAwareBatchScheduler
  MarkSuccess() → void
  MarkFailure(out bool shouldDropBatch, out bool shouldDropQueue) → void
```

### src/Serilog/Core/Sinks/Fallback/FailureListenerSink.cs
```
class FailureListenerSink
  Emit(LogEvent logEvent) → void
```

### src/Serilog/Core/Sinks/FilteringSink.cs
```
class FilteringSink
  Emit(LogEvent logEvent) → void
```

### src/Serilog/Core/Sinks/SafeAggregateSink.cs
```
class SafeAggregateSink
  Emit(LogEvent logEvent) → void
```

### src/Serilog/Data/LogEventPropertyValueRewriter.cs
```
class LogEventPropertyValueRewriter
  VisitScalarValue(TState state, ScalarValue scalar) → LogEventPropertyValue
  VisitSequenceValue(TState state, SequenceValue sequence) → LogEventPropertyValue
  VisitStructureValue(TState state, StructureValue structure) → LogEventPropertyValue
  VisitDictionaryValue(TState state, DictionaryValue dictionary) → LogEventPropertyValue
  VisitUnsupportedValue(TState state, LogEventPropertyValue value) → LogEventPropertyValue
```

### src/Serilog/Data/LogEventPropertyValueVisitor.cs
```
class LogEventPropertyValueVisitor
  Visit(TState state, LogEventPropertyValue value) → TResult
  VisitScalarValue(TState state, ScalarValue scalar) → abstract TResult
  VisitSequenceValue(TState state, SequenceValue sequence) → abstract TResult
  VisitStructureValue(TState state, StructureValue structure) → abstract TResult
  VisitDictionaryValue(TState state, DictionaryValue dictionary) → abstract TResult
  VisitUnsupportedValue(TState state, LogEventPropertyValue value) → TResult
```

### src/Serilog/Debugging/LoggingFailedException.cs
```
class LoggingFailedException
```

### src/Serilog/Debugging/SelfLog.cs
```
class SelfLog
  Enable(TextWriter output) → void
  Enable(Action<string> output) → void
  Disable() → void
  WriteLine(string format, object? arg0 = null, object? arg1 = null, object? arg2 = null) → void
  OnLoggingFailed(object sender, LoggingFailureKind kind, string message, IReadOnlyCollection<LogEvent>? events, Exception? exception) → void
class SelfLogFailureListener
  OnLoggingFailed(object sender, LoggingFailureKind kind, string message, IReadOnlyCollection<LogEvent>? events, Exception? exception) → void
```

### src/Serilog/Events/DictionaryValue.cs
```
class DictionaryValue
  Render(TextWriter output, string? format = null, IFormatProvider? formatProvider = null) → void
```

### src/Serilog/Events/LevelAlias.cs
```
class LevelAlias
```

### src/Serilog/Events/LogEvent.cs
```
class LogEvent
  RenderMessage(TextWriter output, IFormatProvider? formatProvider = null) → void
  RenderMessage(IFormatProvider? formatProvider = null) → string
  AddOrUpdateProperty(LogEventProperty property) → void
  AddOrUpdateProperty(in EventProperty property) → void
  AddPropertyIfAbsent(LogEventProperty property) → void
  AddPropertyIfAbsent(in EventProperty property) → void
  AddPropertyIfAbsent(ILogEventPropertyFactory factory, string name, object? value, bool destructureObjects = false) → void
  RemovePropertyIfPresent(string propertyName) → void
```

### src/Serilog/Events/LogEventLevel.cs
```
enum LogEventLevel
```

### src/Serilog/Events/LogEventProperty.cs
```
class LogEventProperty
  IsValidName([NotNullWhen(true) → bool
  EnsureValidName(string name) → void
```

### src/Serilog/Events/LogEventPropertyValue.cs
```
class LogEventPropertyValue
  Render(TextWriter output, string? format = null, IFormatProvider? formatProvider = null) → abstract void
  ToString() → string
  ToString(string? format, IFormatProvider? formatProvider) → string
```

### src/Serilog/Events/MessageTemplate.cs
```
class MessageTemplate
  ToString() → string
  Render(IReadOnlyDictionary<string, LogEventPropertyValue> properties, IFormatProvider? formatProvider = null) → string
  Render(IReadOnlyDictionary<string, LogEventPropertyValue> properties, TextWriter output, IFormatProvider? formatProvider = null) → void
```

### src/Serilog/Events/ScalarValue.cs
```
class ScalarValue
  Render(TextWriter output, string? format = null, IFormatProvider? formatProvider = null) → void
  Render(object? value, TextWriter output, string? format = null, IFormatProvider? formatProvider = null) → void
  Equals(object? obj) → bool
  GetHashCode() → int
```

### src/Serilog/Events/SequenceValue.cs
```
class SequenceValue
  Render(TextWriter output, string? format = null, IFormatProvider? formatProvider = null) → void
```

### src/Serilog/Events/StructureValue.cs
```
class StructureValue
  Render(TextWriter output, string? format = null, IFormatProvider? formatProvider = null) → void
```

### src/Serilog/Filters/Matching.cs
```
class Matching
  FromSource(string source) → Func<LogEvent, bool>
  WithProperty(string propertyName) → Func<LogEvent, bool>
  WithProperty(string propertyName, object scalarValue) → Func<LogEvent, bool>
```

### src/Serilog/Formatting/Display/LevelOutputFormat.cs
```
class LevelOutputFormat
  GetLevelMoniker(LogEventLevel value, string? format = null) → string
```

### src/Serilog/Formatting/Display/MessageTemplateTextFormatter.cs
```
class MessageTemplateTextFormatter
  Format(LogEvent logEvent, TextWriter output) → void
```

### src/Serilog/Formatting/Display/OutputProperties.cs
```
class OutputProperties
```

### src/Serilog/Formatting/Display/PropertiesOutputFormat.cs
```
class PropertiesOutputFormat
  Render(MessageTemplate template, IReadOnlyDictionary<string, LogEventPropertyValue> properties, MessageTemplate outputTemplate, TextWriter output, string? format, IFormatProvider? formatProvider = null) → void
```

### src/Serilog/Formatting/ITextFormatter.cs
```
interface ITextFormatter
```

### src/Serilog/Formatting/Json/JsonFormatter.cs
```
class JsonFormatter
  Format(LogEvent logEvent, TextWriter output) → void
```

### src/Serilog/Formatting/Json/JsonValueFormatter.cs
```
class JsonValueFormatter
  Format(LogEventPropertyValue value, TextWriter output) → void
  VisitScalarValue(TextWriter state, ScalarValue scalar) → bool
  VisitSequenceValue(TextWriter state, SequenceValue sequence) → bool
  VisitStructureValue(TextWriter state, StructureValue structure) → bool
  VisitDictionaryValue(TextWriter state, DictionaryValue dictionary) → bool
  FormatLiteralValue(object? value, TextWriter output) → void
```

### src/Serilog/Guard.cs
```
class NoEnumerationAttribute
class Guard
```

### src/Serilog/ILogger.cs
```
interface ILogger
```

### src/Serilog/Log.cs
```
class Log
  CloseAndFlush() → void
  CloseAndFlushAsync() → ValueTask
  ForContext(ILogEventEnricher enricher) → ILogger
  ForContext(ILogEventEnricher[] enrichers) → ILogger
  ForContext(string propertyName, object? value, bool destructureObjects = false) → ILogger
  ForContext(Type source) → ILogger
  Write(LogEvent logEvent) → void
  Write(LogEventLevel level, string messageTemplate) → void
```

### src/Serilog/LoggerConfiguration.cs
```
class LoggerConfiguration
  CreateLogger() → Logger
```

### src/Serilog/LoggerExtensions.cs
```
class LoggerExtensions
```

### src/Serilog/Parsing/AlignmentDirection.cs
```
enum AlignmentDirection
```

### src/Serilog/Parsing/Destructuring.cs
```
enum Destructuring
```

### src/Serilog/Parsing/MessageTemplateParser.cs
```
class MessageTemplateParser
  Parse(string messageTemplate) → MessageTemplate
```

### src/Serilog/Parsing/MessageTemplateToken.cs
```
class MessageTemplateToken
  Render(IReadOnlyDictionary<string, LogEventPropertyValue> properties, TextWriter output, IFormatProvider? formatProvider = null) → abstract void
```

### src/Serilog/Parsing/PropertyToken.cs
```
class PropertyToken
  Render(IReadOnlyDictionary<string, LogEventPropertyValue> properties, TextWriter output, IFormatProvider? formatProvider = null) → void
  TryGetPositionalValue(out int position) → bool
  Equals(object? obj) → bool
  GetHashCode() → int
  ToString() → string
```

### src/Serilog/Parsing/TextToken.cs
```
class TextToken
  Render(IReadOnlyDictionary<string, LogEventPropertyValue> properties, TextWriter output, IFormatProvider? formatProvider = null) → void
  Equals(object? obj) → bool
  GetHashCode() → int
  ToString() → string
```

### src/Serilog/Policies/ByteArrayScalarConversionPolicy.cs
```
class ByteArrayScalarConversionPolicy
  TryConvertToScalar(object value, [NotNullWhen(true) → bool
```

### src/Serilog/Policies/ByteMemoryScalarConversionPolicy.cs
```
class ByteMemoryScalarConversionPolicy
  TryConvertToScalar(object value, [NotNullWhen(true) → bool
```

### src/Serilog/Policies/DelegateDestructuringPolicy.cs
```
class DelegateDestructuringPolicy
  TryDestructure(object value, ILogEventPropertyValueFactory propertyValueFactory, [NotNullWhen(true) → bool
```

### src/Serilog/Policies/EnumScalarConversionPolicy.cs
```
class EnumScalarConversionPolicy
  TryConvertToScalar(object value, [NotNullWhen(true) → bool
```

### src/Serilog/Policies/PrimitiveScalarConversionPolicy.cs
```
class PrimitiveScalarConversionPolicy
  TryConvertToScalar(object value, [NotNullWhen(true) → bool
```

### src/Serilog/Policies/ProjectedDestructuringPolicy.cs
```
class ProjectedDestructuringPolicy
  TryDestructure(object value, ILogEventPropertyValueFactory propertyValueFactory, [NotNullWhen(true) → bool
```

### src/Serilog/Policies/ReflectionTypesScalarDestructuringPolicy.cs
```
class ReflectionTypesScalarDestructuringPolicy
  TryDestructure(object value, ILogEventPropertyValueFactory propertyValueFactory, [NotNullWhen(true) → bool
```

### src/Serilog/Policies/SimpleScalarConversionPolicy.cs
```
class SimpleScalarConversionPolicy
  TryConvertToScalar(object value, [NotNullWhen(true) → bool
```

### src/Serilog/Rendering/Casing.cs
```
class Casing
  Format(string value, string? format = null) → string
```

### src/Serilog/Rendering/MessageTemplateRenderer.cs
```
class MessageTemplateRenderer
  Render(MessageTemplate messageTemplate, IReadOnlyDictionary<string, LogEventPropertyValue> properties, TextWriter output, string? format = null, IFormatProvider? formatProvider = null) → void
  RenderTextToken(TextToken tt, TextWriter output) → void
  RenderPropertyToken(PropertyToken pt, IReadOnlyDictionary<string, LogEventPropertyValue> properties, TextWriter output, IFormatProvider? formatProvider, bool isLiteral, bool isJson) → void
```

### src/Serilog/Rendering/Padding.cs
```
class Padding
  Apply(TextWriter output, string value, in Alignment? alignment) → void
  Apply(TextWriter output, StringBuilder value, in Alignment? alignment) → void
```

### src/Serilog/Rendering/ReusableStringWriter.cs
```
class ReusableStringWriter
  GetOrCreate(IFormatProvider? formatProvider = null) → StringWriter
  Dispose(bool disposing) → void
  Write(ReadOnlySpan<char> buffer) → void
  WriteLine(ReadOnlySpan<char> buffer) → void
```

### src/Serilog/Settings/KeyValuePairs/CallableConfigurationMethodFinder.cs
```
class CallableConfigurationMethodFinder
  FindConfigurationMethods(IEnumerable<Assembly> configurationAssemblies, Type configType) → IList<MethodInfo>
```

### src/Serilog/Settings/KeyValuePairs/KeyValuePairSettings.cs
```
class KeyValuePairSettings
  Configure(LoggerConfiguration loggerConfiguration) → void
class ConfigurationMethodCall
```

### src/Serilog/Settings/KeyValuePairs/SettingValueConversions.cs
```
class SettingValueConversions
  ConvertToType(string value, Type toType) → object?
  TryParseStaticMemberAccessor(string input, [NotNullWhen(true) → bool
```

### src/Serilog/Settings/KeyValuePairs/SurrogateConfigurationMethods.cs
```
class SurrogateConfigurationMethods
  Sink(LoggerSinkConfiguration loggerSinkConfiguration, ILogEventSink sink, LogEventLevel restrictedToMinimumLevel = LevelAlias.Minimum, LoggingLevelSwitch? levelSwitch = null) → LoggerConfiguration
  Sink(LoggerAuditSinkConfiguration auditSinkConfiguration, ILogEventSink sink, LogEventLevel restrictedToMinimumLevel = LevelAlias.Minimum, LoggingLevelSwitch? levelSwitch = null) → LoggerConfiguration
  With(LoggerEnrichmentConfiguration loggerEnrichmentConfiguration, ILogEventEnricher enricher) → LoggerConfiguration
  FromLogContext(LoggerEnrichmentConfiguration loggerEnrichmentConfiguration) → LoggerConfiguration
  With(LoggerDestructuringConfiguration loggerDestructuringConfiguration, IDestructuringPolicy policy) → LoggerConfiguration
  AsScalar(LoggerDestructuringConfiguration loggerDestructuringConfiguration, Type scalarType) → LoggerConfiguration
  ToMaximumCollectionCount(LoggerDestructuringConfiguration loggerDestructuringConfiguration, int maximumCollectionCount) → LoggerConfiguration
  ToMaximumDepth(LoggerDestructuringConfiguration loggerDestructuringConfiguration, int maximumDestructuringDepth) → LoggerConfiguration
```

### src/Serilog/Util/TimeProvider.cs
```
class TimeProvider
  GetLocalNow() → DateTimeOffset
  GetUtcNow() → DateTimeOffset
  GetTimestamp() → long
  GetElapsedTime(long startingTimestamp, long endingTimestamp) → TimeSpan
  GetElapsedTime(long startingTimestamp) → TimeSpan
```
