<!-- Generated by SigMap gen-context.js v3.3.2 -->
<!-- DO NOT EDIT below the marker line — run gen-context.js to regenerate -->

# Code signatures

## todos
```
actionpack/test/abstract_unit.rb:10  # TODO: Have these tests support other combinations of these values
```

## actionpack

### actionpack/lib/action_dispatch/testing/assertions.rb
```
module ActionDispatch
  def html_document
```

### actionpack/lib/action_dispatch/testing/integration.rb
```
module ActionDispatch
  def get(path, **args)
  def post(path, **args)
  def patch(path, **args)
  def put(path, **args)
  def delete(path, **args)
  def head(path, **args)
  def options(path, **args)
  def follow_redirect
  def host
  def cookies
  def initialize(app)
  def url_options
  def reset
  def https
  def https
  def process(method, path, params: nil, headers: nil, env: nil, xhr: false, as: nil, query: nil, body: nil)
  def build_full_uri(path, env)
  def build_expanded_path(path)
  def initialize(*args, &blk)
  def before_setup
  def integration_session
  def reset
  def create_session(app)
  def remove
```

### actionpack/lib/action_dispatch/testing/request_encoder.rb
```
module ActionDispatch
  def content_type
  def accept_header
  def encode_params(params)
  def response_parser
  def initialize(mime_name, param_encoder, response_parser, content_type)
  def accept_header
  def encode_params(params)
  def self.parser(content_type)
  def self.encoder(name)
  def self.register_encoder(mime_name, param_encoder: nil, response_parser: nil, content_type: nil)
```

### actionpack/lib/action_dispatch/testing/test_helpers/page_dump_helper.rb
```
module ActionDispatch
  def save_and_open_page(path = html_dump_default_path)
  def save_page(path = html_dump_default_path)
  def open_file(path)
  def html_dump_default_path
```

### actionpack/lib/action_dispatch/testing/test_process.rb
```
module ActionDispatch
  def file_fixture_upload(path, mime_type = nil, binary = false)
  def assigns(key = nil)
  def session
  def flash
  def cookies
  def redirect_to_url
```

### actionpack/lib/action_dispatch/testing/test_request.rb
```
module ActionDispatch
  def self.create(env = {})
  def self.default_env
  def request_method
  def host
  def port
  def request_uri
  def path
  def action
  def if_modified_since
  def if_none_match
  def remote_addr
  def user_agent
  def accept
```

### actionpack/lib/action_dispatch/testing/test_response.rb
```
module ActionDispatch
  def self.from_response(response)
  def parsed_body
  def response_parser
```

### actionpack/lib/action_dispatch.rb
```
module Rack
module ActionDispatch
  def self.resolve_store(session_store)
  def eager_load
```

### actionpack/lib/action_pack/gem_version.rb
```
module ActionPack
  def self.gem_version
```

### actionpack/lib/action_pack/version.rb
```
module ActionPack
  def self.version
```

### actionpack/test/abstract_unit.rb
```
module Rails
module ActionPackTestSuiteUtils
module ActionDispatch
module ActiveSupport
class RoutedRackApp
class ActionDispatch::IntegrationTest
class Rack::TestCase
module ActionController
class ::ApplicationController
module ActionDispatch
module ActionDispatch
module RoutingTestHelpers
class ResourcesController
class ActiveSupport::TestCase
module CookieAssertions
module HeadersAssertions
  def env
  def application
  def root
  def self.require_helpers(helpers_dirs)
  def before_setup
  def initialize(routes, &blk)
  def call(env)
  def config
  def self.build_app(routes = nil)
```

### actionpack/test/lib/controller/fake_controllers.rb
```
module Admin
module Api
```

### actionpack/test/lib/controller/fake_models.rb
```
class Comment
  def to_xml(options = {})
  def to_js(options = {})
  def errors
  def persisted
  def cache_key
  def initialize(*args)
  def author_attributes
  def comments_attributes
  def tags_attributes
  def initialize(id = nil, post_id = nil)
  def to_key
  def save
  def persisted
  def to_param
  def name
  def relevances_attributes
```

### actionpack/test/lib/test_renderable.rb
```
class TestRenderable
  def render_in(_view_context)
  def format
```

### actionpack/test/support/etag_helper.rb
```
module EtagHelper
  def weak_etag(record)
  def strong_etag(record)
```

### actionpack/test/support/path_helper.rb
```
module ActionDispatch
  def path_from_string(string)
  def build_path(path, requirements, separators, anchored, formatted = true)
```

### actionpack/test/support/rack_parsing_override.rb
```
module RackParsingOverride
  def parse_nested_query(*)
  def self.permitted_caller
  def params
  def GET
  def POST
```

### actionpack/test/support/system_helper.rb
```
class DrivenByRackTest
class DrivenBySeleniumWithChrome
class DrivenBySeleniumWithHeadlessChrome
class DrivenBySeleniumWithHeadlessFirefox
```
