<!-- Generated by SigMap gen-context.js v3.3.2 -->
<!-- DO NOT EDIT below the marker line — run gen-context.js to regenerate -->

# Code signatures

## deps
```
tests/browser/adapter.browser.test.js ← ../index
tests/browser/basicAuth.browser.test.js ← ../index
tests/browser/cancel.browser.test.js ← ../index
tests/browser/defaults.browser.test.js ← ../index, ../lib/defaults/index, ../lib/core/AxiosHeaders
tests/browser/formdata.browser.test.js ← ../index
tests/browser/headers.browser.test.js ← ../index
tests/browser/instance.browser.test.js ← ../index
tests/browser/interceptors.browser.test.js ← ../index
tests/browser/options.browser.test.js ← ../index
tests/browser/progress.browser.test.js ← ../index
tests/browser/promise.browser.test.js ← ../index
tests/browser/requests.browser.test.js ← ../index
tests/browser/transform.browser.test.js ← ../index, ../lib/core/AxiosError
tests/browser/xsrf.browser.test.js ← ../index, ../lib/helpers/cookies
```

## todos
```
tests/browser/requests.browser.test.js:267  # XXX: ');
tests/browser/requests.browser.test.js:271  # XXX: ',
```

## tests

### tests/browser/adapter.browser.test.js
```
class MockXMLHttpRequest
  constructor()
  addEventListener()
  async open(method, url, async = true)
  setRequestHeader(key, value)
  addEventListener()
  getAllResponseHeaders()
  send(data)
  respondWith({ status = 200, statusText = 'OK', responseText = '' } = {})
```

### tests/browser/basicAuth.browser.test.js
```
class MockXMLHttpRequest
  constructor()
  addEventListener()
  async open(method, url, async = true)
  setRequestHeader(key, value)
  addEventListener()
  getAllResponseHeaders()
  send(data)
  respondWith({ status = 200, statusText = 'OK', responseText = '', responseHeaders = '' } = {})
```

### tests/browser/cancel.browser.test.js
```
class MockXMLHttpRequest
  constructor()
  addEventListener()
  async open(method, url, async = true)
  setRequestHeader(key, value)
  addEventListener()
  getAllResponseHeaders()
  send(data)
  respondWith({ status = 200, statusText = 'OK', responseText = '', responseHeaders = '' } = {})
```

### tests/browser/defaults.browser.test.js
```
class MockXMLHttpRequest
  constructor()
  addEventListener()
  async open(method, url, async = true)
  setRequestHeader(key, value)
  addEventListener()
  getAllResponseHeaders()
  send(data)
  respondWith({ status = 200, statusText = 'OK', responseText = '', responseHeaders = '' } = {})
```

### tests/browser/formdata.browser.test.js
```
class MockXMLHttpRequest
  constructor()
  addEventListener()
  async open(method, url, async = true)
  setRequestHeader(key, value)
  addEventListener()
  getAllResponseHeaders()
  send(data)
  respondWith({ status = 200, statusText = 'OK', responseText = '', responseHeaders = '' } = {})
```

### tests/browser/headers.browser.test.js
```
class MockXMLHttpRequest
  constructor()
  addEventListener()
  async open(method, url, async = true)
  setRequestHeader(key, value)
  addEventListener()
  getAllResponseHeaders()
  send(data)
  respondWith({ status = 200, statusText = 'OK', responseText = '' } = {})
function testHeaderValue(headers, key, val)
```

### tests/browser/instance.browser.test.js
```
class MockXMLHttpRequest
  constructor()
  addEventListener()
  async open(method, url, async = true)
  setRequestHeader(key, value)
  addEventListener()
  getAllResponseHeaders()
  send(data)
  respondWith({ status = 200, statusText = 'OK', responseText = '', responseHeaders = '' } = {})
```

### tests/browser/interceptors.browser.test.js
```
class MockXMLHttpRequest
  constructor()
  addEventListener()
  async open(method, url, async = true)
  setRequestHeader(key, value)
  addEventListener()
  getAllResponseHeaders()
  if(typeof this.responseHeaders === 'string')
  send(data)
```

### tests/browser/options.browser.test.js
```
class MockXMLHttpRequest
  constructor()
  addEventListener()
  async open(method, url, async = true)
  setRequestHeader(key, value)
  addEventListener()
  getAllResponseHeaders()
  send(data)
  respondWith({ status = 200, statusText = 'OK', responseText = '', responseHeaders = '' } = {})
```

### tests/browser/progress.browser.test.js
```
class MockXMLHttpRequest
  constructor()
  async open(method, url, async = true)
  setRequestHeader(key, value)
  addEventListener(type, listener)
  getAllResponseHeaders()
  send(data)
  getListenerCount(type, target = 'request')
  emit(type, target = 'request', event = {})
```

### tests/browser/promise.browser.test.js
```
class MockXMLHttpRequest
  constructor()
  addEventListener()
  async open(method, url, async = true)
  setRequestHeader(key, value)
  addEventListener()
  getAllResponseHeaders()
  send(data)
  respondWith({ status = 200, statusText = 'OK', responseText = '', responseHeaders = '' } = {})
```

### tests/browser/requests.browser.test.js
```
class MockXMLHttpRequest
  constructor()
  addEventListener()
  async open(method, url, async = true)
  setRequestHeader(key, value)
  addEventListener()
  getAllResponseHeaders()
  send(data)
  respondWith({ status = 200, statusText = 'OK', responseText = '', response = null, headers = {}, responseURL = '', } = {})
```

### tests/browser/transform.browser.test.js
```
class MockXMLHttpRequest
  constructor()
  addEventListener()
  async open(method, url, async = true)
  setRequestHeader(key, value)
  addEventListener()
  getAllResponseHeaders()
  send(data)
  respondWith({ status = 200, statusText = 'OK', responseText = '', responseHeaders = '' } = {})
```

### tests/browser/xsrf.browser.test.js
```
class MockXMLHttpRequest
  constructor()
  addEventListener()
  async open(method, url, async = true)
  setRequestHeader(key, value)
  addEventListener()
  getAllResponseHeaders()
  send()
  if(this.onloadend)
```

### tests/module/cjs/tests/helpers/fixture.cjs
```
module.exports = { createTempFixture, cleanupTempFixture }
```

### tests/module/cjs/tests/helpers/run-command.cjs
```
module.exports = { runCommand }
```

### tests/module/esm/tests/helpers/fixture.js
```
export const createTempFixture = (suiteRoot, name, sourcePath, tsconfig, packageJson) =>
export const cleanupTempFixture = (dirPath) =>
```

### tests/module/esm/tests/helpers/run-command.js
```
export const runCommand = (command, args = [], options = {}) =>
```

### tests/setup/server.js
```
export const SERVER_HANDLER_STREAM_ECHO = (req, res) =>
export const setTimeoutAsync = (ms) =>
export const startHTTPServer = async (handlerOrOptions, options) =>
export const stopHTTPServer = async (server, timeout = 10000) =>
export const stopAllTrackedHTTPServers = async (timeout = 10000) =>
export const handleFormData = (req) =>
export const generateReadable = (length = 1024 * 1024, chunkSize = 10 * 1024, sleep = 50) =>
export const makeReadableStream = (chunk = 'chunk', n = 10, timeout = 100) =>
export const makeEchoStream = (echo) =>
export const startTestServer = async (port) =>
```
