

## Auto-generated signatures
<!-- Updated by gen-context.js -->
<!-- Generated by SigMap gen-context.js v3.3.2 -->
<!-- Updated: 2026-04-14T06:21:05.980Z -->
<!-- Do not edit below — regenerate with: node gen-context.js -->

# Code signatures

<!-- Generated by SigMap gen-context.js v3.3.2 -->
<!-- DO NOT EDIT below the marker line — run gen-context.js to regenerate -->

# Code signatures

## deps
```
lib/adapters/adapters.js ← utils, http, xhr, fetch, core/AxiosError
lib/adapters/fetch.js ← platform/index, utils, core/AxiosError, helpers/composeSignals, helpers/trackStream
lib/adapters/http.js ← utils, core/settle, core/buildFullPath, helpers/buildURL, env/data
lib/axios.js ← utils, helpers/bind, core/Axios, core/mergeConfig, defaults/index
lib/cancel/CancelToken.js ← CanceledError
lib/cancel/CanceledError.js ← core/AxiosError
lib/core/Axios.js ← utils, helpers/buildURL, InterceptorManager, dispatchRequest, mergeConfig
lib/core/AxiosError.js ← utils
lib/core/AxiosHeaders.js ← utils, helpers/parseHeaders
lib/core/InterceptorManager.js ← utils
lib/core/dispatchRequest.js ← transformData, cancel/isCancel, defaults/index, cancel/CanceledError, core/AxiosHeaders
lib/defaults/index.js ← utils, core/AxiosError, transitional, helpers/toFormData, helpers/toURLEncodedForm
lib/helpers/AxiosTransformStream.js ← utils
lib/helpers/AxiosURLSearchParams.js ← toFormData
lib/helpers/buildURL.js ← utils, helpers/AxiosURLSearchParams
lib/helpers/formDataToJSON.js ← utils
lib/helpers/formDataToStream.js ← utils, readBlob, platform/index
lib/helpers/progressEventReducer.js ← speedometer, throttle, utils
lib/helpers/toFormData.js ← utils, core/AxiosError, platform/node/classes/FormData
lib/helpers/validator.js ← env/data, core/AxiosError
lib/utils.js ← helpers/bind
sandbox/client.js ← index
tests/browser/adapter.browser.test.js ← ../index
tests/browser/basicAuth.browser.test.js ← ../index
tests/browser/cancel.browser.test.js ← ../index
tests/browser/defaults.browser.test.js ← ../index, ../lib/defaults/index, ../lib/core/AxiosHeaders
tests/browser/formdata.browser.test.js ← ../index
tests/browser/headers.browser.test.js ← ../index
tests/browser/instance.browser.test.js ← ../index
tests/browser/interceptors.browser.test.js ← ../index
tests/browser/options.browser.test.js ← ../index
tests/browser/progress.browser.test.js ← ../index
tests/browser/promise.browser.test.js ← ../index
tests/browser/requests.browser.test.js ← ../index
tests/browser/transform.browser.test.js ← ../index, ../lib/core/AxiosError
tests/browser/xsrf.browser.test.js ← ../index, ../lib/helpers/cookies
```

## todos
```
tests/browser/requests.browser.test.js:267  # XXX: ');
tests/browser/requests.browser.test.js:271  # XXX: ',
```

## examples

### examples/abort-controller/index.html
```
title: axios - abort controller example
button#startBtn
button#cancelBtn
div#singleStatus
input#searchInput
div#searchStatus
ul#searchLog
```

### examples/all/index.html
```
title: axios - all example
img#useravatar
strong#username
ul#orgs
```

### examples/amd/index.html
```
title: AMD
img#useravatar
strong#username
```

### examples/get/index.html
```
title: axios - get example
ul#people
```

### examples/network_enhanced.js
```
export function createEnhancedClient(config = {})
function enhanceNetworkError(error)
```

### examples/post/index.html
```
title: axios - post example
textarea#data
button#post
div#output
```

### examples/postMultipartFormData/index.html
```
title: Axios - Multipart Form Example
button#themeToggle
input#someString
input#someNumber
input#someSmallFile
input#formData
input#json
button#post
div#checkNetwork
div#errorOutput
div#resultContent
```

### examples/server.js
```
function listDirs(root)
function getIndexTemplate()
function sendResponse(res, statusCode, body)
function send200(res, body)
function send404(res, body)
function pipeFileToResponse(res, file, type)
```

### examples/transform-response/index.html
```
title: axios - transform response example
img#useravatar
strong#username
span#created
span#updated
```

### examples/upload/index.html
```
title: axios - file upload example
input#file
button#upload
div#output
```

## lib

### lib/adapters/adapters.js
```
function getAdapter(adapters, config) → string
```

### lib/adapters/fetch.js
```
export const getFetch = (config) =>
```

### lib/adapters/http.js
```
class Http2Sessions
  constructor()
  getSession(authority, options)
  if(authoritySessions)
  for(let i = 0; i < len; i++)
  if(removed)
  while(i--)
  if(entries[i][0] === session)
  if(len === 1)
function dispatchBeforeRedirect(options, responseDetails) → Object<string, any>
function setProxy(options, configProxy, location) → http.ClientRequestArgs
```

### lib/axios.js
```
function createInstance(defaultConfig) → Axios
```

### lib/cancel/CancelToken.js
```
class CancelToken
  constructor(executor)
  if(typeof executor !== 'function')
  while(i-- > 0)
  executor(function cancel(message, config, request)
  if(token.reason)
  throwIfRequested() → CancelToken
  if(this.reason)
  subscribe(listener)
```

### lib/cancel/CanceledError.js
```
class CanceledError
  constructor(message, config, request)
```

### lib/core/Axios.js
```
class Axios
  constructor(instanceConfig)
  async request(configOrUrl, config) → Axios
  if(err instanceof Error)
  if(!dummy.stack)
  if(!err.stack)
  if(typeof configOrUrl === 'string')
  if(transitional !== undefined)
  if(paramsSerializer != null)
```

### lib/core/AxiosError.js
```
class AxiosError
  static from(error, code, config, request, response, customProps)
  if(error.status != null && axiosError.status == null)
  constructor(message, code, config, request, response)
  if(response)
  toJSON()
```

### lib/core/AxiosHeaders.js
```
class AxiosHeaders
  constructor(headers)
  set(header, valueOrRewrite, rewrite)
  if(!lHeader)
  for(const entry of header)
  get(header, parser)
  if(header)
  if(key)
  if(!parser)
function assertValidHeaderValue(value, header)
function normalizeHeader(header)
function stripTrailingCRLF(str)
function normalizeValue(value)
function parseTokens(str)
function matchHeaderValue(context, value, header, filter, isHeaderNameFilter)
function formatHeader(header)
function buildAccessors(obj, header)
```

### lib/core/InterceptorManager.js
```
class InterceptorManager
  constructor()
  use(fulfilled, rejected, options) → Number
  eject(id) → void
  if(this.handlers[id])
  clear() → void
  if(this.handlers)
  forEach(fn) → void
  if(h !== null)
```

### lib/core/dispatchRequest.js
```
function throwIfCancellationRequested(config) → void
```

### lib/defaults/index.js
```
function stringifySafely(rawValue, parser, encoder) → string
```

### lib/helpers/AxiosTransformStream.js
```
class AxiosTransformStream
  constructor(options)
  if(event === 'progress')
  if(!internals.isCaptured)
  if(internals.onReadCallback)
  if(maxRate)
  if(maxRate)
  if(bytesLeft <= 0)
  if(bytesLeft < maxChunkSize)
```

### lib/helpers/AxiosURLSearchParams.js
```
function encode(str) → string
function AxiosURLSearchParams(params, options) → void
```

### lib/helpers/ZlibHeaderTransformStream.js
```
class ZlibHeaderTransformStream
  if(chunk.length !== 0)
  if(chunk[0] !== 120)
```

### lib/helpers/buildURL.js
```
function encode(val) → string
```

### lib/helpers/formDataToJSON.js
```
function parsePropPath(name)
function arrayToObject(arr)
function formDataToJSON(formData) → Object<string, any> | nul
```

### lib/helpers/formDataToStream.js
```
class FormDataPart
  constructor(name, value)
  if(isStringValue)
  static escapeName(name)
```

### lib/helpers/progressEventReducer.js
```
export const progressEventReducer = (listener, isDownloadStream, freq = 3) =>
export const progressEventDecorator = (total, throttled) =>
export const asyncDecorator = async (fn) =>
```

### lib/helpers/speedometer.js
```
function speedometer(samplesCount, min) → Function
```

### lib/helpers/throttle.js
```
function throttle(fn, freq) → Function
```

### lib/helpers/toFormData.js
```
function isVisitable(thing) → boolean
function removeBrackets(key) → string
function renderKey(path, key, dots) → string
function isFlatArray(arr) → boolean
function toFormData(obj, formData, options) → Object
```

### lib/helpers/trackStream.js
```
export const trackStream = (stream, chunkSize, onProgress, onFinish) =>
```

### lib/helpers/validator.js
```
function assertOptions(options, schema, allowUnknown) → function
```

### lib/utils.js
```
function isBuffer(val) → boolean
function isArrayBufferView(val) → boolean
function getGlobal() → boolean
function forEach(obj, fn, { allOwnKeys = false } = {}) → boolean
function findKey(obj, key) → ?string
function merge() → Object
function isSpecCompliantForm(thing) → Object
```

## sandbox

### sandbox/client.html
```
title: AXIOS | Sandbox
input#url
select#method
textarea#params
textarea#data
textarea#headers
button#submit
pre#request
pre#response
pre#error
span#year
```

### sandbox/client.js
```
function handleSuccess(data)
function handleFailure(data)
```

### sandbox/server.js
```
function pipeFileToResponse(res, file, type)
function handleApiRequest(req, res)
function requestHandler(req, res)
```

## tests

### tests/browser/adapter.browser.test.js
```
class MockXMLHttpRequest
  constructor()
  addEventListener()
  async open(method, url, async = true)
  setRequestHeader(key, value)
  addEventListener()
  getAllResponseHeaders()
  send(data)
  respondWith({ status = 200, statusText = 'OK', responseText = '' } = {})
```

### tests/browser/basicAuth.browser.test.js
```
class MockXMLHttpRequest
  constructor()
  addEventListener()
  async open(method, url, async = true)
  setRequestHeader(key, value)
  addEventListener()
  getAllResponseHeaders()
  send(data)
  respondWith({ status = 200, statusText = 'OK', responseText = '', responseHeaders = '' } = {})
```

### tests/browser/cancel.browser.test.js
```
class MockXMLHttpRequest
  constructor()
  addEventListener()
  async open(method, url, async = true)
  setRequestHeader(key, value)
  addEventListener()
  getAllResponseHeaders()
  send(data)
  respondWith({ status = 200, statusText = 'OK', responseText = '', responseHeaders = '' } = {})
```

### tests/browser/defaults.browser.test.js
```
class MockXMLHttpRequest
  constructor()
  addEventListener()
  async open(method, url, async = true)
  setRequestHeader(key, value)
  addEventListener()
  getAllResponseHeaders()
  send(data)
  respondWith({ status = 200, statusText = 'OK', responseText = '', responseHeaders = '' } = {})
```

### tests/browser/formdata.browser.test.js
```
class MockXMLHttpRequest
  constructor()
  addEventListener()
  async open(method, url, async = true)
  setRequestHeader(key, value)
  addEventListener()
  getAllResponseHeaders()
  send(data)
  respondWith({ status = 200, statusText = 'OK', responseText = '', responseHeaders = '' } = {})
```

### tests/browser/headers.browser.test.js
```
class MockXMLHttpRequest
  constructor()
  addEventListener()
  async open(method, url, async = true)
  setRequestHeader(key, value)
  addEventListener()
  getAllResponseHeaders()
  send(data)
  respondWith({ status = 200, statusText = 'OK', responseText = '' } = {})
function testHeaderValue(headers, key, val)
```

### tests/browser/instance.browser.test.js
```
class MockXMLHttpRequest
  constructor()
  addEventListener()
  async open(method, url, async = true)
  setRequestHeader(key, value)
  addEventListener()
  getAllResponseHeaders()
  send(data)
  respondWith({ status = 200, statusText = 'OK', responseText = '', responseHeaders = '' } = {})
```

### tests/browser/interceptors.browser.test.js
```
class MockXMLHttpRequest
  constructor()
  addEventListener()
  async open(method, url, async = true)
  setRequestHeader(key, value)
  addEventListener()
  getAllResponseHeaders()
  if(typeof this.responseHeaders === 'string')
  send(data)
```

### tests/browser/options.browser.test.js
```
class MockXMLHttpRequest
  constructor()
  addEventListener()
  async open(method, url, async = true)
  setRequestHeader(key, value)
  addEventListener()
  getAllResponseHeaders()
  send(data)
  respondWith({ status = 200, statusText = 'OK', responseText = '', responseHeaders = '' } = {})
```

### tests/browser/progress.browser.test.js
```
class MockXMLHttpRequest
  constructor()
  async open(method, url, async = true)
  setRequestHeader(key, value)
  addEventListener(type, listener)
  getAllResponseHeaders()
  send(data)
  getListenerCount(type, target = 'request')
  emit(type, target = 'request', event = {})
```

### tests/browser/promise.browser.test.js
```
class MockXMLHttpRequest
  constructor()
  addEventListener()
  async open(method, url, async = true)
  setRequestHeader(key, value)
  addEventListener()
  getAllResponseHeaders()
  send(data)
  respondWith({ status = 200, statusText = 'OK', responseText = '', responseHeaders = '' } = {})
```

### tests/browser/requests.browser.test.js
```
class MockXMLHttpRequest
  constructor()
  addEventListener()
  async open(method, url, async = true)
  setRequestHeader(key, value)
  addEventListener()
  getAllResponseHeaders()
  send(data)
  respondWith({ status = 200, statusText = 'OK', responseText = '', response = null, headers = {}, responseURL = '', } = {})
```

### tests/browser/transform.browser.test.js
```
class MockXMLHttpRequest
  constructor()
  addEventListener()
  async open(method, url, async = true)
  setRequestHeader(key, value)
  addEventListener()
  getAllResponseHeaders()
  send(data)
  respondWith({ status = 200, statusText = 'OK', responseText = '', responseHeaders = '' } = {})
```

### tests/browser/xsrf.browser.test.js
```
class MockXMLHttpRequest
  constructor()
  addEventListener()
  async open(method, url, async = true)
  setRequestHeader(key, value)
  addEventListener()
  getAllResponseHeaders()
  send()
  if(this.onloadend)
```

### tests/module/cjs/tests/helpers/fixture.cjs
```
module.exports = { createTempFixture, cleanupTempFixture }
```

### tests/module/cjs/tests/helpers/run-command.cjs
```
module.exports = { runCommand }
```

### tests/module/esm/tests/helpers/fixture.js
```
export const createTempFixture = (suiteRoot, name, sourcePath, tsconfig, packageJson) =>
export const cleanupTempFixture = (dirPath) =>
```

### tests/module/esm/tests/helpers/run-command.js
```
export const runCommand = (command, args = [], options = {}) =>
```

### tests/setup/server.js
```
export const SERVER_HANDLER_STREAM_ECHO = (req, res) =>
export const setTimeoutAsync = (ms) =>
export const startHTTPServer = async (handlerOrOptions, options) =>
export const stopHTTPServer = async (server, timeout = 10000) =>
export const stopAllTrackedHTTPServers = async (timeout = 10000) =>
export const handleFormData = (req) =>
export const generateReadable = (length = 1024 * 1024, chunkSize = 10 * 1024, sleep = 50) =>
export const makeReadableStream = (chunk = 'chunk', n = 10, timeout = 100) =>
export const makeEchoStream = (echo) =>
export const startTestServer = async (port) =>
```
