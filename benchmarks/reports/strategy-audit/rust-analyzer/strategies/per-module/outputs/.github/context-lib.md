<!-- Generated by SigMap gen-context.js v3.3.2 -->
<!-- DO NOT EDIT below the marker line — run gen-context.js to regenerate -->

# Code signatures

## deps
```
lib/smol_str/src/gdb_smolstr_printer.py ← gdb
```

## todos
```
lib/text-size/src/range.rs:126  # HACK: for const fn: math on primitives only
lib/text-size/src/range.rs:133  # HACK: for const fn: math on primitives only
```

## lib

### lib/la-arena/src/lib.rs
```
pub struct RawIdx
pub struct Idx
pub struct IdxRange
pub struct Arena
pub struct IntoIter
impl RawIdx
impl RawIdx
impl RawIdx
impl Idx
impl Idx
impl Idx
impl Idx
impl Idx
impl Idx
impl Idx
impl Idx
impl Idx
impl IdxRange
  pub fn new(range: Range<Idx<T>>) → Self
  pub fn new_inclusive(range: RangeInclusive<Idx<T>>) → Self
  pub fn is_empty(&self) → bool
  pub fn start(&self) → Idx<T>
  pub fn end(&self) → Idx<T>
impl IdxRange
impl IdxRange
```

### lib/la-arena/src/map.rs
```
pub struct ArenaMap
pub struct ArenaMapIter
pub struct VacantEntry
pub struct OccupiedEntry
pub enum Entry
impl ArenaMap
  pub fn with_capacity(capacity: usize) → Self
  pub fn reserve(&mut self, additional: usize)
  pub fn clear(&mut self)
  pub fn shrink_to_fit(&mut self)
  pub fn contains_idx(&self, idx: Idx<T>) → bool
  pub fn remove(&mut self, idx: Idx<T>) → Option<V>
  pub fn insert(&mut self, idx: Idx<T>, t: V) → Option<V>
  pub fn get(&self, idx: Idx<T>) → Option<&V>
impl ArenaMap
impl Extend
impl FromIterator
impl ArenaMap
impl ArenaMapIter
impl ArenaMapIter
impl ArenaMapIter
impl Entry
  pub fn or_insert(self, default: V) → &'a mut V
impl VacantEntry
  pub fn insert(self, value: V) → &'a mut V
```

### lib/line-index/src/lib.rs
```
pub struct LineCol
pub struct WideLineCol
pub struct LineIndex
pub enum WideEncoding
impl WideEncoding
  pub fn measure(&self, text: &str) → usize
impl WideChar
impl LineIndex
  pub fn new(text: &str) → LineIndex
  pub fn line_col(&self, offset: TextSize) → LineCol
  pub fn try_line_col(&self, offset: TextSize) → Option<LineCol>
  pub fn offset(&self, line_col: LineCol) → Option<TextSize>
  pub fn to_wide(&self, enc: WideEncoding, line_col: LineCol) → Option<WideLineCol>
  pub fn to_utf8(&self, enc: WideEncoding, line_col: WideLineCol) → Option<LineCol>
  pub fn line(&self, line: u32) → Option<TextRange>
  pub fn lines(&self, range: TextRange) → impl Iterator<Item = TextRa...
```

### lib/lsp-server/examples/manual_test.sh
```
# Simple nine-packet LSP test for examples/minimal_lsp.rs
function send()
```

### lib/lsp-server/src/error.rs
```
pub struct ProtocolError
pub enum ExtractError
impl ProtocolError
  pub fn channel_is_disconnected(&self) → bool
impl ProtocolError
impl ProtocolError
impl ExtractError
impl ExtractError
impl ExtractError
impl ExtractError
```

### lib/lsp-server/src/lib.rs
```
pub struct Connection
impl Connection
  pub fn stdio() → (Connection, IoThreads)
  pub fn connect(addr: A) → io::Result<(Connection, IoT...
  pub fn listen(addr: A) → io::Result<(Connection, IoT...
  pub fn memory() → (Connection, Connection)
  pub fn initialize_start(&self) → Result<(RequestId, serde_js...
  pub fn initialize_start_while(&self, running: C,) → Result<(RequestId, serde_js...
  pub fn initialize_finish(&self, initialize_id: RequestId, initialize_result: serde_json::Value,) → Result<(), ProtocolError>
  pub fn initialize_finish_while(&self, initialize_id: RequestId, initialize_result: serde_json::Value, running: C,) → Result<(), ProtocolError> w...
```

### lib/lsp-server/src/msg.rs
```
pub struct RequestId
pub struct Request
pub struct Response
pub struct ResponseError
pub struct Notification
pub enum Message
pub enum ErrorCode
impl RequestId
impl Message
  pub fn read(r: &mut impl BufRead) → io::Result<Option<Message>>
  pub fn write(&self, w: &mut impl Write) → io::Result<()>
impl Response
  pub fn new_ok(id: RequestId, result: R) → Response
  pub fn new_err(id: RequestId, code: i32, message: String) → Response
impl Request
  pub fn new(id: RequestId, method: String, params: P) → Request
  pub fn extract(self, method: &str,) → Result<(RequestId, P), Extr...
impl Notification
  pub fn new(method: String, params: impl serde::Serialize) → Notification
  pub fn extract(self, method: &str,) → Result<P, ExtractError<Noti...
```

### lib/lsp-server/src/req_queue.rs
```
pub struct ReqQueue
pub struct Incoming
pub struct Outgoing
impl ReqQueue
impl ReqQueue
  pub fn has_pending(&self) → bool
impl Incoming
  pub fn register(&mut self, id: RequestId, data: I)
  pub fn cancel(&mut self, id: RequestId) → Option<Response>
  pub fn complete(&mut self, id: &RequestId) → Option<I>
  pub fn is_completed(&self, id: &RequestId) → bool
  pub fn has_pending(&self) → bool
impl Outgoing
  pub fn register(&mut self, method: String, params: P, data: O) → Request
  pub fn complete(&mut self, id: RequestId) → Option<O>
  pub fn has_pending(&self) → bool
```

### lib/lsp-server/src/stdio.rs
```
pub struct IoThreads
impl IoThreads
  pub fn join(self) → io::Result<()>
```

### lib/smol_str/src/borsh.rs
```
impl SmolStr
impl SmolStr
```

### lib/smol_str/src/gdb_smolstr_printer.py
```
class SmolStrProvider
  def __init__(val)
  def to_string()
  def display_hint()
class SmolStrSubPrinter(gdb.printing.SubPrettyPrinter)
  def __init__()
class SmolStrPrettyPrinter(gdb.printing.PrettyPrinter)
  def __init__()
def register_printers(objfile)
```

### lib/smol_str/src/lib.rs
```
pub struct SmolStr
pub struct SmolStrBuilder
pub trait ToSmolStr
pub trait StrExt
impl SmolStr
  pub fn new(text: impl AsRef<str>) → SmolStr
  pub fn as_str(&self) → &str
  pub fn len(&self) → usize
  pub fn is_empty(&self) → bool
  pub fn from_utf8(bytes: &[u8]) → Result<SmolStr, core::str::...
impl SmolStr
impl SmolStr
impl SmolStr
impl SmolStr
impl SmolStr
impl SmolStr
impl SmolStr
impl SmolStr
impl SmolStr
impl From
impl SmolStr
impl InlineSize
impl Repr
impl str
impl SmolStrBuilder
```

### lib/smol_str/src/serde.rs
```
impl SmolStr
```

### lib/text-size/src/range.rs
```
pub struct TextRange
impl TextRange
impl TextRange
impl TextRange
impl TextRange
  pub fn contains(self, offset: TextSize) → bool
  pub fn contains_inclusive(self, offset: TextSize) → bool
  pub fn contains_range(self, other: TextRange) → bool
  pub fn intersect(self, other: TextRange) → Option<TextRange>
  pub fn cover(self, other: TextRange) → TextRange
  pub fn cover_offset(self, offset: TextSize) → TextRange
  pub fn checked_add(self, offset: TextSize) → Option<TextRange>
  pub fn checked_sub(self, offset: TextSize) → Option<TextRange>
```

### lib/text-size/src/serde_impls.rs
```
impl TextSize
impl TextRange
```

### lib/text-size/src/size.rs
```
pub struct TextSize
impl TextSize
impl TextSize
  pub fn of(text: T) → TextSize
impl TextSize
```

### lib/text-size/src/traits.rs
```
pub trait TextLen
impl char
impl char
```

### lib/text-size/tests/constructors.rs
```
impl BadRope
```

### lib/ungrammar/src/error.rs
```
pub struct Error
impl Error
impl Error
impl Error
```

### lib/ungrammar/src/lexer.rs
```
impl Location
```

### lib/ungrammar/src/lib.rs
```
pub struct Node
pub struct Token
pub struct Grammar
pub struct NodeData
pub struct TokenData
pub enum Rule
impl Grammar
impl Grammar
  pub fn iter(&self) → impl Iterator<Item = Node> ...
  pub fn tokens(&self) → impl Iterator<Item = Token>...
```

### lib/ungrammar/src/parser.rs
```
impl Parser
```
