

## Auto-generated signatures
<!-- Updated by gen-context.js -->
<!-- Generated by SigMap gen-context.js v3.3.2 -->
<!-- Updated: 2026-04-14T06:21:26.628Z -->
<!-- Do not edit below — regenerate with: node gen-context.js -->

# Code signatures

<!-- Generated by SigMap gen-context.js v3.3.2 -->
<!-- DO NOT EDIT below the marker line — run gen-context.js to regenerate -->

# Code signatures

## todos
```
android-test/src/androidTest/java/okhttp/android/test/OkHttpTest.kt:501  # TODO: map to expected versions automatically, test ignored for now.  Run man
android-test/src/androidTest/java/okhttp/android/test/OkHttpTest.kt:901  # TODO: remove most of this code after https://github.com/bcgit/bc-java/issues
android-test/src/androidTest/java/okhttp/android/test/letsencrypt/LetsEncryptClientTest.kt:82  # TODO: reenable in official answers
mockwebserver/src/test/java/mockwebserver3/internal/http2/Http2Server.kt:104  # TODO: send bad request error
okhttp/src/commonJvmAndroid/kotlin/okhttp3/internal/-HostnamesCommon.kt:336  # TODO: Must not begin with a combining mark.
okhttp/src/commonJvmAndroid/kotlin/okhttp3/internal/-HostnamesCommon.kt:337  # TODO: Each character must be 'valid' or 'deviation'. Not mapped.
okhttp/src/commonJvmAndroid/kotlin/okhttp3/internal/-HostnamesCommon.kt:338  # TODO: CheckJoiners from IDNA 2008
okhttp/src/commonJvmAndroid/kotlin/okhttp3/internal/-HostnamesCommon.kt:339  # TODO: CheckBidi from IDNA 2008, RFC 5893, Section 2.
okhttp/src/commonJvmAndroid/kotlin/okhttp3/internal/cache/DiskLruCache.kt:575  # TODO: check null behaviour
okhttp/src/commonJvmAndroid/kotlin/okhttp3/internal/http2/Header.kt:30  # TODO: search for toLowerCase and consider moving logic here.
okhttp/src/commonJvmAndroid/kotlin/okhttp3/internal/http2/Http2.kt:181  # TODO: Avoid allocation.
okhttp/src/commonJvmAndroid/kotlin/okhttp3/internal/http2/Http2.kt:185  # TODO: Avoid allocation.
okhttp/src/commonJvmAndroid/kotlin/okhttp3/internal/http2/Http2Connection.kt:433  # TODO: propagate exception message into debugData.
okhttp/src/commonJvmAndroid/kotlin/okhttp3/internal/http2/Http2Connection.kt:434  # TODO: configure a timeout on the reader so that it doesn’t block forever.
okhttp/src/commonJvmAndroid/kotlin/okhttp3/internal/http2/Http2Connection.kt:802  # TODO: If we don't get this callback after sending settings to the peer, SETT
okhttp/src/commonJvmAndroid/kotlin/okhttp3/internal/http2/Http2Connection.kt:845  # TODO: log the debugData
okhttp/src/commonJvmAndroid/kotlin/okhttp3/internal/http2/Http2Connection.kt:889  # TODO: honor priority.
okhttp/src/commonJvmAndroid/kotlin/okhttp3/internal/http2/Http2Connection.kt:908  # TODO: register alternate service.
okhttp/src/commonJvmAndroid/kotlin/okhttp3/internal/http2/Http2Reader.kt:176  # TODO: Concat multi-value headers with 0x0, except COOKIE, which uses 0x3B, 0
okhttp/src/commonJvmAndroid/kotlin/okhttp3/internal/http2/Http2Reader.kt:191  # TODO: checkState open or half-closed (local) or raise STREAM_CLOSED
```

## android-test

### android-test/src/androidTest/java/okhttp/android/test/OkHttpTest.kt
```
class OkHttpTest
  fun setup()
  fun testPlatform()
  fun testRequest()
  fun testLocalhostInsecure()
  fun testRequestWithSniRequirement()
  fun testConscryptRequest()
  fun connectionAcquired(call, connection)
  fun testConscryptRequestLocalhostInsecure()
```

### android-test/src/androidTest/java/okhttp/android/test/SingleAndroidTest.kt
```
class SingleAndroidTest
  fun testHttpsRequest()
  fun testHttpRequest()
```

### android-test/src/androidTest/java/okhttp/android/test/StrictModeTest.kt
```
class StrictModeTest
  fun cleanup()
  fun testInit()
  fun testNewCall()
```

### android-test/src/androidTest/java/okhttp/android/test/alpn/AlpnOverrideTest.kt
```
class AlpnOverrideTest
  fun configureSocket(sslSocket) → SSLSocket
  fun getWithCustomSocketFactory()
  fun connectionAcquired(call, connection)
```

### android-test/src/androidTest/java/okhttp/android/test/letsencrypt/LetsEncryptClientTest.kt
```
class LetsEncryptClientTest
```

### android-test/src/androidTest/java/okhttp/android/test/sni/SniOverrideTest.kt
```
class SniOverrideTest
  fun getWithCustomSocketFactory()
  fun configureSocket(sslSocket) → SSLSocket
  fun getWithDns()
```

### android-test/src/test/kotlin/okhttp/android/test/AndroidLoggingTest.kt
```
class AndroidLoggingTest
  fun testHttpLoggingInterceptor()
  fun testLoggingEventListener()
```

### android-test/src/test/kotlin/okhttp/android/test/AndroidSocketAdapterTest.kt
```
class AndroidSocketAdapterTest
  fun testMatchesSupportedSocket()
  fun testMatchesSupportedAndroidSocketFactory()
  fun testDoesntMatchSupportedCustomSocketFactory()
  fun testCustomSocket()
  fun data() → Collection<SocketAdapter>
```

### android-test/src/test/kotlin/okhttp/android/test/BaseOkHttpClientUnitTest.kt
```
class BaseOkHttpClientUnitTest
  fun setUp()
  fun testRequestExternal()
```

### android-test/src/test/kotlin/okhttp/android/test/DisabledInitialiserTest.kt
```
class DisabledInitialiserTest
  fun setContext()
  fun testWithoutContext()
  fun resetContext()
```

### android-test/src/test/kotlin/okhttp/android/test/NonRobolectricOkHttpClientTest.kt
```
class NonRobolectricOkHttpClientTest
  fun testPublicSuffixDb()
```

### android-test/src/test/kotlin/okhttp/android/test/RobolectricOkHttpClientTest.kt
```
class RobolectricOkHttpClientTest
  fun setContext()
```

### android-test/src/test/kotlin/okhttp/android/test/ShadowDnsResolver.kt
```
class ShadowDnsResolver
  fun query(network, domain, nsType, flags, executor, cancellationSignal, callback)
  fun getInstance() → DnsResolver
```

## android-test-app

### android-test-app/src/androidTest/kotlin/okhttp/android/testapp/IdnaTest.kt
```
class IdnaTest
  fun testHostnameFunction()
```

### android-test-app/src/androidTest/kotlin/okhttp/android/testapp/PublicSuffixDatabaseTest.kt
```
class PublicSuffixDatabaseTest
  fun testTopLevelDomain()
```

### android-test-app/src/main/kotlin/okhttp/android/testapp/MainActivity.kt
```
class MainActivity
  fun onCreate(savedInstanceState)
  fun onFailure(call, e)
  fun onResponse(call, response)
```

### android-test-app/src/main/kotlin/okhttp/android/testapp/TestApplication.kt
```
class TestApplication
  fun onCreate()
```

## build-logic

### build-logic/src/main/kotlin/AlpnVersions.kt
```
fun alpnBootVersion() → String?
```

### build-logic/src/main/kotlin/BndBuildAction.kt
```
class BndBuildAction
  fun execute(task)
```

### build-logic/src/main/kotlin/okhttp.jvm-conventions.gradle.kts
```
fun library(alias)
fun version(alias)
```

### build-logic/src/main/kotlin/okhttp.quality-conventions.gradle.kts
```
fun library(alias)
fun version(alias)
```

### build-logic/src/main/kotlin/okhttp.testing-conventions.gradle.kts
```
fun library(alias)
```

## container-tests

### container-tests/src/test/java/okhttp3/containers/BasicLoomTest.kt
```
class BasicLoomTest
  fun setUp()
  fun checkForPinning()
  fun testHttpsRequest()
```

### container-tests/src/test/java/okhttp3/containers/BasicMockServerTest.kt
```
class BasicMockServerTest
  fun testRequest()
  fun testHttpsRequest()
```

### container-tests/src/test/java/okhttp3/containers/BasicProxyTest.kt
```
class BasicProxyTest
  fun testOkHttpDirect()
  fun testOkHttpProxied()
  fun testOkHttpSecureDirect()
  fun testOkHttpSecureProxiedHttp1()
  fun testUrlConnectionDirect()
  fun testUrlConnectionPlaintextProxied()
  fun testUrlConnectionSecureDirect()
  fun testUrlConnectionSecureProxied()
```

### container-tests/src/test/java/okhttp3/containers/SocksProxyTest.kt
```
class SocksProxyTest
  fun testLocal()
```

## maven-tests

### maven-tests/src/main/java/com/squareup/okhttp3/maventest/SampleHttpClient.java
```
class SampleHttpClient
  makeCall(HttpUrl url) → void
```

### maven-tests/src/test/java/com/squareup/okhttp3/maventest/AppTest.java
```
class AppTest
  testApp() → void
```

## mockwebserver

### mockwebserver/src/main/kotlin/mockwebserver3/Dispatcher.kt
```
class Dispatcher
```

### mockwebserver/src/main/kotlin/mockwebserver3/MockResponse.kt
```
class MockResponse
  fun newBuilder() → Builder
```

### mockwebserver/src/main/kotlin/mockwebserver3/MockResponseBody.kt
```
interface MockResponseBody
  fun writeTo(sink)
```

### mockwebserver/src/main/kotlin/mockwebserver3/MockWebServer.kt
```
class MockWebServer
  fun url(path) → HttpUrl
  fun useHttps(sslSocketFactory)
  fun noClientAuth()
  fun requestClientAuth()
  fun requireClientAuth()
  fun takeRequest() → RecordedRequest
  fun takeRequest(timeout, unit) → RecordedRequest?
  fun enqueue(response)
```

### mockwebserver/src/main/kotlin/mockwebserver3/QueueDispatcher.kt
```
class QueueDispatcher
  fun dispatch(request) → MockResponse
```

### mockwebserver/src/main/kotlin/mockwebserver3/RecordedRequest.kt
```
class RecordedRequest
```

### mockwebserver/src/main/kotlin/mockwebserver3/SocketEffect.kt
```
interface SocketEffect
```

### mockwebserver/src/main/kotlin/mockwebserver3/SocketHandler.kt
```
interface SocketHandler
  fun handle(socket)
```

### mockwebserver/src/main/kotlin/mockwebserver3/internal/BufferMockResponseBody.kt
```
class BufferMockResponseBody
  fun writeTo(sink)
```

### mockwebserver/src/main/kotlin/mockwebserver3/internal/MockWebServerSocket.kt
```
class MockWebServerSocket
  fun close()
  fun close()
  fun shutdownInput()
  fun shutdownOutput()
  fun sleepWhileOpen(nanos)
  fun cancel()
  fun close()
  fun awaitClosed()
```

### mockwebserver/src/main/kotlin/mockwebserver3/internal/RecordedRequestFactory.kt
```
class RequestLine
  fun toString()
fun RecordedRequest(requestLine, headers, chunkSizes, bodySize, body, connectionIndex, exchangeIndex, socket, failure) → RecordedRequest
fun decodeRequestLine(requestLine) → RequestLine
```

### mockwebserver/src/main/kotlin/mockwebserver3/internal/ThrottledSink.kt
```
class ThrottledSink
  fun write(source, byteCount)
```

### mockwebserver/src/main/kotlin/mockwebserver3/internal/TriggerSink.kt
```
class TriggerSink
  fun write(source, byteCount)
```

### mockwebserver/src/test/java/mockwebserver3/CustomDispatcherTest.kt
```
class CustomDispatcherTest
  fun simpleDispatch()
  fun dispatch(request) → MockResponse
  fun outOfOrderResponses()
  fun dispatch(request) → MockResponse
```

### mockwebserver/src/test/java/mockwebserver3/MockResponseSniTest.kt
```
class MockResponseSniTest
  fun clientSendsServerNameAndServerReceivesIt()
  fun domainFronting()
  fun ipv6()
  fun ipv4()
  fun regularHostname()
```

### mockwebserver/src/test/java/mockwebserver3/MockWebServerTest.kt
```
class MockWebServerTest
  fun setUp()
  fun tearDown()
  fun defaultMockResponse()
  fun setResponseMockReason()
  fun setStatusControlsWholeStatusLine()
  fun setBodyAdjustsHeaders()
  fun mockResponseAddHeader()
  fun mockResponseSetHeader()
```

### mockwebserver/src/test/java/mockwebserver3/RecordedRequestTest.kt
```
class RecordedRequestTest
  fun getInputStream()
  fun getOutputStream()
  fun getInetAddress()
  fun getLocalAddress()
```

### mockwebserver/src/test/java/mockwebserver3/internal/http2/Http2Server.kt
```
class Http2Server
  fun onStream(stream)
```

## mockwebserver-deprecated

### mockwebserver-deprecated/src/main/kotlin/okhttp3/mockwebserver/Dispatcher.kt
```
class Dispatcher
```

### mockwebserver-deprecated/src/main/kotlin/okhttp3/mockwebserver/MockResponse.kt
```
class MockResponse
  fun getStatus() → String
  fun setStatus(status)
  fun setResponseCode(code) → MockResponse
  fun clearHeaders()
  fun addHeader(header)
  fun addHeader(name, value)
  fun addHeaderLenient(name, value)
  fun setHeader(name, value)
```

### mockwebserver-deprecated/src/main/kotlin/okhttp3/mockwebserver/MockWebServer.kt
```
class MockWebServer
  fun getPort() → Int
  fun toProxyAddress() → Proxy
  fun setServerSocketFactory(serverSocketFactory)
  fun url(path) → HttpUrl
  fun setBodyLimit(bodyLimit)
  fun setProtocolNegotiationEnabled(protocolNegotiationEnabled)
  fun setProtocols(protocols)
  fun protocols() → List<Protocol>
```

### mockwebserver-deprecated/src/main/kotlin/okhttp3/mockwebserver/PushPromise.kt
```
class PushPromise
  fun method() → String
  fun path() → String
  fun headers() → Headers
  fun response() → MockResponse
```

### mockwebserver-deprecated/src/main/kotlin/okhttp3/mockwebserver/QueueDispatcher.kt
```
class QueueDispatcher
  fun dispatch(request) → MockResponse
  fun peek() → MockResponse
  fun enqueueResponse(response)
  fun shutdown()
  fun setFailFast(failFast)
  fun setFailFast(failFastResponse)
```

### mockwebserver-deprecated/src/main/kotlin/okhttp3/mockwebserver/RecordedRequest.kt
```
class RecordedRequest
  fun getUtf8Body() → String
  fun getHeader(name) → String?
  fun toString() → String
```

### mockwebserver-deprecated/src/test/java/okhttp3/mockwebserver/KotlinSourceModernTest.kt
```
class KotlinSourceModernTest
  fun dispatcherFromMockWebServer()
  fun dispatch(request) → MockResponse
  fun peek() → MockResponse
  fun shutdown()
  fun mockResponse()
  fun mockWebServer()
```

### mockwebserver-deprecated/src/test/java/okhttp3/mockwebserver/MockWebServerTest.kt
```
class MockWebServerTest
  fun setUp()
  fun tearDown()
  fun defaultMockResponse()
  fun setResponseMockReason()
  fun setStatusControlsWholeStatusLine()
  fun setBodyAdjustsHeaders()
  fun mockResponseAddHeader()
  fun mockResponseSetHeader()
```

## mockwebserver-junit4

### mockwebserver-junit4/src/main/kotlin/mockwebserver3/junit4/MockWebServerRule.kt
```
class MockWebServerRule
  fun before()
  fun after()
```

### mockwebserver-junit4/src/test/java/mockwebserver3/junit4/MockWebServerRuleTest.kt
```
class MockWebServerRuleTest
  fun evaluate()
```

## mockwebserver-junit5

### mockwebserver-junit5/src/main/kotlin/mockwebserver3/junit5/internal/StartStopExtension.kt
```
class StartStopExtension
  fun beforeAll(context)
  fun beforeEach(context)
```

### mockwebserver-junit5/src/test/java/mockwebserver3/junit5/StartStopTest.kt
```
class StartStopTest
  fun happyPath()
  fun dispatch(request)
  fun close()
```

## module-tests

### module-tests/src/main/java/okhttp3/modules/Main.java
```
class Main
  main(String[] args) → void
```

### module-tests/src/main/java/okhttp3/modules/OkHttpCaller.java
```
class OkHttpCaller
  callOkHttp(HttpUrl url) → Call
```

### module-tests/src/test/java/okhttp3/modules/test/JavaModuleTest.java
```
class JavaModuleTest
  testVisibility() → void
  testMockWebServer() → void
  testModules() → void
```

## native-image-tests

### native-image-tests/src/test/kotlin/okhttp3/nativeimage/PublicSuffixDatabaseTest.kt
```
class PublicSuffixDatabaseTest
  fun testResourcesLoaded()
```

### native-image-tests/src/test/kotlin/okhttp3/nativeimage/SampleTest.kt
```
class SampleTest
  fun passingTest()
  fun testMockWebServer()
  fun testExternalSite()
```

### native-image-tests/src/test/kotlin/okhttp3/nativeimage/WithArgumentSourceTest.kt
```
class WithArgumentSourceTest
  fun passingTest(value)
class FakeArgumentsProvider
  fun provideArguments(parameters, context) → Stream<out Arguments>
```

## okcurl

### okcurl/src/main/kotlin/okhttp3/curl/Main.kt
```
class Main
  fun help(context) → String
  fun run()
  fun createRequest() → Request
  fun createClient() → Call.Factory
  fun close()
  fun checkClientTrusted(chain, authType)
  fun checkServerTrusted(chain, authType)
  fun getAcceptedIssuers() → Array<X509Certificate>
```

### okcurl/src/main/kotlin/okhttp3/curl/MainCommandLine.kt
```
fun main(args)
```

### okcurl/src/main/kotlin/okhttp3/curl/logging/LoggingUtil.kt
```
class LoggingUtil
  fun configureLogging(debug, showHttp2Frames, sslDebug)
  fun publish(record)
  fun getLogger(name) → Logger
```

### okcurl/src/main/kotlin/okhttp3/curl/logging/MessageFormatter.kt
```
object MessageFormatter
  fun format(record) → String
```

### okcurl/src/main/kotlin/okhttp3/curl/logging/OneLineLogFormat.kt
```
class OneLineLogFormat
  fun format(record) → String
```

### okcurl/src/test/kotlin/okhttp3/curl/MainTest.kt
```
class MainTest
  fun simple()
  fun put()
  fun dataPost()
  fun dataPut()
  fun contentTypeHeader()
  fun referer()
  fun userAgent()
  fun defaultUserAgent()
```

## okhttp

### okhttp/src/androidMain/kotlin/okhttp3/internal/platform/Android10Platform.kt
```
class Android10Platform
  fun trustManager(sslSocketFactory) → X509TrustManager?
  fun newSSLContext() → SSLContext
  fun buildTrustRootIndex(trustManager) → TrustRootIndex
  fun configureTlsExtensions(sslSocket, hostname, protocols)
  fun getSelectedProtocol(sslSocket) → String?
  fun getStackTraceForCloseable(closer) → Any?
  fun logCloseableLeak(message, stackTrace)
  fun isCleartextTrafficPermitted(hostname) → Boolean
```

### okhttp/src/androidMain/kotlin/okhttp3/internal/platform/AndroidPlatform.kt
```
class AndroidPlatform
  fun connectSocket(socket, address, connectTimeout)
  fun newSSLContext() → SSLContext
  fun trustManager(sslSocketFactory) → X509TrustManager?
  fun configureTlsExtensions(sslSocket, hostname, protocols)
  fun getSelectedProtocol(sslSocket) → String?
  fun isCleartextTrafficPermitted(hostname) → Boolean
  fun buildCertificateChainCleaner(trustManager) → CertificateChainCleaner
  fun buildTrustRootIndex(trustManager) → TrustRootIndex
```

### okhttp/src/androidMain/kotlin/okhttp3/internal/platform/ContextAwarePlatform.kt
```
interface ContextAwarePlatform
```

### okhttp/src/androidMain/kotlin/okhttp3/internal/platform/PlatformInitializer.kt
```
class PlatformInitializer
  fun create(context) → Platform
  fun dependencies() → List<Class<Initializer<*>
```

### okhttp/src/androidMain/kotlin/okhttp3/internal/platform/android/Android10SocketAdapter.kt
```
class Android10SocketAdapter
  fun matchesSocket(sslSocket) → Boolean
  fun isSupported() → Boolean
  fun getSelectedProtocol(sslSocket) → String?
  fun configureTlsExtensions(sslSocket, hostname, protocols)
  fun buildIfSupported() → SocketAdapter?
  fun isSupported()
```

### okhttp/src/androidMain/kotlin/okhttp3/internal/platform/android/AndroidCertificateChainCleaner.kt
```
class AndroidCertificateChainCleaner
  fun clean(chain, hostname) → List<Certificate>
  fun equals(other) → Boolean
  fun hashCode() → Int
  fun buildIfSupported(trustManager) → AndroidCertificateChainCl
```

### okhttp/src/androidMain/kotlin/okhttp3/internal/platform/android/AndroidLog.kt
```
object AndroidLogHandler
  fun publish(record)
  fun flush()
  fun close()
object AndroidLog
  fun androidLog(loggerName, logLevel, message, t)
  fun enable()
```

### okhttp/src/androidMain/kotlin/okhttp3/internal/platform/android/AndroidSocketAdapter.kt
```
class AndroidSocketAdapter
  fun isSupported() → Boolean
  fun matchesSocket(sslSocket) → Boolean
  fun configureTlsExtensions(sslSocket, hostname, protocols)
  fun getSelectedProtocol(sslSocket) → String?
  fun factory(packageName) → DeferredSocketAdapter.Fac
  fun matchesSocket(sslSocket) → Boolean
  fun create(sslSocket) → SocketAdapter
```

### okhttp/src/androidMain/kotlin/okhttp3/internal/platform/android/BouncyCastleSocketAdapter.kt
```
class BouncyCastleSocketAdapter
  fun matchesSocket(sslSocket) → Boolean
  fun isSupported() → Boolean
  fun getSelectedProtocol(sslSocket) → String?
  fun configureTlsExtensions(sslSocket, hostname, protocols)
  fun matchesSocket(sslSocket) → Boolean
  fun create(sslSocket) → SocketAdapter
```

### okhttp/src/androidMain/kotlin/okhttp3/internal/platform/android/ConscryptSocketAdapter.kt
```
class ConscryptSocketAdapter
  fun matchesSocket(sslSocket) → Boolean
  fun isSupported() → Boolean
  fun getSelectedProtocol(sslSocket) → String?
  fun configureTlsExtensions(sslSocket, hostname, protocols)
  fun matchesSocket(sslSocket) → Boolean
  fun create(sslSocket) → SocketAdapter
  fun atLeastVersion(major, minor, patch) → Boolean
```

### okhttp/src/androidMain/kotlin/okhttp3/internal/platform/android/DeferredSocketAdapter.kt
```
class DeferredSocketAdapter
  fun isSupported() → Boolean
  fun matchesSocket(sslSocket) → Boolean
  fun configureTlsExtensions(sslSocket, hostname, protocols)
  fun getSelectedProtocol(sslSocket) → String?
  fun matchesSocket(sslSocket) → Boolean
  fun create(sslSocket) → SocketAdapter
```

### okhttp/src/androidMain/kotlin/okhttp3/internal/platform/android/SocketAdapter.kt
```
interface SocketAdapter
  fun isSupported() → Boolean
  fun trustManager(sslSocketFactory) → X509TrustManager?
  fun matchesSocket(sslSocket) → Boolean
  fun matchesSocketFactory(sslSocketFactory) → Boolean
  fun configureTlsExtensions(sslSocket, hostname, protocols)
  fun getSelectedProtocol(sslSocket) → String?
```

### okhttp/src/androidMain/kotlin/okhttp3/internal/platform/android/StandardAndroidSocketAdapter.kt
```
class StandardAndroidSocketAdapter
  fun matchesSocketFactory(sslSocketFactory) → Boolean
  fun trustManager(sslSocketFactory) → X509TrustManager?
  fun buildIfSupported(packageName) → SocketAdapter?
```

### okhttp/src/androidMain/kotlin/okhttp3/internal/publicsuffix/AssetPublicSuffixList.kt
```
class AssetPublicSuffixList
  fun listSource() → Source
```

### okhttp/src/commonJvmAndroid/kotlin/okhttp3/Address.kt
```
class Address
  fun url() → HttpUrl
  fun dns() → Dns
  fun socketFactory() → SocketFactory
  fun proxyAuthenticator() → Authenticator
  fun protocols() → List<Protocol>
  fun connectionSpecs() → List<ConnectionSpec>
  fun proxySelector() → ProxySelector
  fun proxy() → Proxy?
```

### okhttp/src/commonJvmAndroid/kotlin/okhttp3/Cache.kt
```
class Cache
  fun get(request) → Response?
  fun put(response) → CacheRequest?
  fun remove(request)
  fun update(cached, network)
  fun initialize()
  fun delete()
  fun evictAll()
  fun urls() → MutableIterator<String>
```

### okhttp/src/commonJvmAndroid/kotlin/okhttp3/CacheControl.kt
```
class CacheControl
  fun noCache() → Boolean
  fun noStore() → Boolean
  fun maxAgeSeconds() → Int
  fun sMaxAgeSeconds() → Int
  fun mustRevalidate() → Boolean
  fun maxStaleSeconds() → Int
  fun minFreshSeconds() → Int
  fun onlyIfCached() → Boolean
```

### okhttp/src/commonJvmAndroid/kotlin/okhttp3/Call.kt
```
interface Call
  fun request() → Request
  fun execute() → Response
  fun enqueue(responseCallback)
  fun cancel()
  fun isExecuted() → Boolean
  fun isCanceled() → Boolean
  fun timeout() → Timeout
  fun addEventListener(eventListener)
```

### okhttp/src/commonJvmAndroid/kotlin/okhttp3/Callback.kt
```
interface Callback
  fun onFailure(call, e)
  fun onResponse(call, response)
```

### okhttp/src/commonJvmAndroid/kotlin/okhttp3/CertificatePinner.kt
```
class CertificatePinner
  fun check(hostname, peerCertificates)
  fun check(hostname, cleanedPeerCertificatesFn)
  fun check(hostname, vararg peerCertificates)
  fun findMatchingPins(hostname) → List<Pin>
  fun withCertificateChainCleaner(certificateChainCleaner) → CertificatePinner
  fun equals(other) → Boolean
  fun hashCode() → Int
  fun matchesHostname(hostname) → Boolean
```

### okhttp/src/commonJvmAndroid/kotlin/okhttp3/Challenge.kt
```
class Challenge
  fun withCharset(charset) → Challenge
  fun scheme() → String
  fun authParams() → Map<String?, String>
  fun realm() → String?
  fun charset() → Charset
  fun equals(other) → Boolean
  fun hashCode() → Int
  fun toString() → String
```

### okhttp/src/commonJvmAndroid/kotlin/okhttp3/CipherSuite.kt
```
class CipherSuite
  fun javaName() → String
  fun toString() → String
  fun compare(a, b) → Int
```

### okhttp/src/commonJvmAndroid/kotlin/okhttp3/CompressionInterceptor.kt
```
class CompressionInterceptor
  fun intercept(chain) → Response
  fun decompress(response) → Response
  fun lookupDecompressor(encoding) → DecompressionAlgorithm?
  fun decompress(compressedSource) → Source
```

### okhttp/src/commonJvmAndroid/kotlin/okhttp3/Connection.kt
```
interface Connection
  fun route() → Route
  fun socket() → Socket
  fun handshake() → Handshake?
  fun protocol() → Protocol
```

### okhttp/src/commonJvmAndroid/kotlin/okhttp3/ConnectionPool.kt
```
class ConnectionPool
  fun idleConnectionCount() → Int
  fun connectionCount() → Int
  fun evictAll()
```

### okhttp/src/commonJvmAndroid/kotlin/okhttp3/ConnectionSpec.kt
```
class ConnectionSpec
  fun cipherSuites() → List<CipherSuite>?
  fun tlsVersions() → List<TlsVersion>?
  fun supportsTlsExtensions() → Boolean
  fun apply(sslSocket, isFallback)
  fun isCompatible(socket) → Boolean
  fun equals(other) → Boolean
  fun hashCode() → Int
  fun toString() → String
```

### okhttp/src/commonJvmAndroid/kotlin/okhttp3/Cookie.kt
```
class Cookie
  fun matches(url) → Boolean
  fun equals(other) → Boolean
  fun hashCode() → Int
  fun toString() → String
  fun name() → String
  fun value() → String
  fun persistent() → Boolean
  fun expiresAt() → Long
```

### okhttp/src/commonJvmAndroid/kotlin/okhttp3/CookieJar.kt
```
interface CookieJar
  fun saveFromResponse(url, cookies)
  fun loadForRequest(url) → List<Cookie>
  fun saveFromResponse(url, cookies)
  fun loadForRequest(url) → List<Cookie>
```

### okhttp/src/commonJvmAndroid/kotlin/okhttp3/Credentials.kt
```
object Credentials
  fun basic(username, password, charset) → String
```

### okhttp/src/commonJvmAndroid/kotlin/okhttp3/Dispatcher.kt
```
class Dispatcher
  fun enqueue(call)
  fun cancelAll()
```

### okhttp/src/commonJvmAndroid/kotlin/okhttp3/EventListener.kt
```
class EventListener
  fun create(call) → EventListener
  fun callStart(call)
  fun dispatcherQueueStart(call, dispatcher)
  fun dispatcherQueueEnd(call, dispatcher)
```

### okhttp/src/commonJvmAndroid/kotlin/okhttp3/FormBody.kt
```
class FormBody
  fun size() → Int
  fun encodedName(index) → String
  fun name(index) → String
  fun encodedValue(index) → String
  fun value(index) → String
  fun contentType() → MediaType
  fun contentLength() → Long
  fun writeTo(sink)
```

### okhttp/src/commonJvmAndroid/kotlin/okhttp3/Gzip.kt
```
object Gzip
  fun decompress(compressedSource) → Source
```

### okhttp/src/commonJvmAndroid/kotlin/okhttp3/Handshake.kt
```
class Handshake
  fun tlsVersion() → TlsVersion
  fun cipherSuite() → CipherSuite
  fun peerCertificates() → List<Certificate>
  fun peerPrincipal() → Principal?
  fun localCertificates() → List<Certificate>
  fun localPrincipal() → Principal?
  fun equals(other) → Boolean
  fun hashCode() → Int
```

### okhttp/src/commonJvmAndroid/kotlin/okhttp3/Headers.kt
```
class Headers
  fun getDate(name) → Date?
  fun getInstant(name) → Instant?
  fun size() → Int
  fun name(index) → String
  fun value(index) → String
  fun names() → Set<String>
  fun values(name) → List<String>
  fun byteCount() → Long
```

### okhttp/src/commonJvmAndroid/kotlin/okhttp3/HttpUrl.kt
```
class HttpUrl
  fun toUrl() → URL
  fun toUri() → URI
  fun queryParameter(name) → String?
  fun queryParameterValues(name) → List<String?>
  fun queryParameterName(index) → String
  fun queryParameterValue(index) → String?
```

### okhttp/src/commonJvmAndroid/kotlin/okhttp3/MediaType.kt
```
class MediaType
  fun charset(defaultValue) → Charset?
  fun parameter(name) → String?
  fun type() → String
  fun subtype() → String
  fun toString() → String
  fun equals(other) → Boolean
  fun hashCode() → Int
  fun get(mediaType) → MediaType
```

### okhttp/src/commonJvmAndroid/kotlin/okhttp3/MultipartBody.kt
```
class MultipartBody
  fun part(index) → Part
  fun isOneShot() → Boolean
  fun contentType() → MediaType
  fun type() → MediaType
  fun boundary() → String
  fun size() → Int
  fun parts() → List<Part>
  fun contentLength() → Long
```

### okhttp/src/commonJvmAndroid/kotlin/okhttp3/MultipartReader.kt
```
class MultipartReader
  fun nextPart() → Part?
  fun close()
  fun read(sink, byteCount) → Long
  fun timeout() → Timeout
  fun close()
```

### okhttp/src/commonJvmAndroid/kotlin/okhttp3/OkHttpClient.kt
```
class OkHttpClient
```

### okhttp/src/commonJvmAndroid/kotlin/okhttp3/Request.kt
```
class Request
  fun header(name) → String?
  fun headers(name) → List<String>
  fun tag() → Any?
  fun newBuilder() → Builder
  fun url() → HttpUrl
  fun method() → String
  fun headers() → Headers
  fun body() → RequestBody?
```

### okhttp/src/commonJvmAndroid/kotlin/okhttp3/RequestBody.kt
```
class RequestBody
  fun sha256() → ByteString
  fun contentType()
  fun contentLength()
  fun writeTo(sink)
  fun contentType()
  fun isOneShot() → Boolean
  fun writeTo(sink)
  fun contentType()
```

### okhttp/src/commonJvmAndroid/kotlin/okhttp3/Response.kt
```
class Response
  fun request() → Request
  fun protocol() → Protocol
  fun code() → Int
  fun message() → String
  fun handshake() → Handshake?
  fun headers(name) → List<String>
  fun header(name, defaultValue) → String?
  fun headers() → Headers
```

### okhttp/src/commonJvmAndroid/kotlin/okhttp3/ResponseBody.kt
```
class ResponseBody
  fun byteStream() → InputStream
  fun bytes()
  fun byteString()
  fun charStream() → Reader
  fun string() → String
  fun close()
  fun read(cbuf, off, len) → Int
  fun close()
```

### okhttp/src/commonJvmAndroid/kotlin/okhttp3/Route.kt
```
class Route
  fun address() → Address
  fun proxy() → Proxy
  fun socketAddress() → InetSocketAddress
  fun requiresTunnel() → Boolean
  fun equals(other) → Boolean
  fun hashCode() → Int
  fun toString() → String
```

### okhttp/src/commonJvmAndroid/kotlin/okhttp3/TrailersSource.kt
```
interface TrailersSource
  fun peek() → Headers?
  fun get() → Headers
  fun peek()
  fun get()
```

### okhttp/src/commonJvmAndroid/kotlin/okhttp3/WebSocket.kt
```
interface WebSocket
  fun request() → Request
  fun queueSize() → Long
  fun send(text) → Boolean
  fun send(bytes) → Boolean
  fun close(code, reason) → Boolean
  fun cancel()
  fun newWebSocket(request, listener) → WebSocket
```

### okhttp/src/commonJvmAndroid/kotlin/okhttp3/WebSocketListener.kt
```
class WebSocketListener
```

### okhttp/src/commonJvmAndroid/kotlin/okhttp3/internal/-HeadersCommon.kt
```
fun commonHeadersGet(namesAndValues, name) → String?
fun headersCheckName(name)
fun headersCheckValue(value, name)
fun commonHeadersOf(vararg inputNamesAndValues) → Headers
```

### okhttp/src/commonJvmAndroid/kotlin/okhttp3/internal/-HostnamesCommon.kt
```
fun decodeIpv6(input, pos, limit) → ByteArray?
fun decodeIpv4Suffix(input, pos, limit, address, addressOffset) → Boolean
fun inet6AddressToAscii(address) → String
fun canonicalizeInetAddress(address) → ByteArray
fun inet4AddressToAscii(address) → String
fun idnToAscii(host) → String?
```

### okhttp/src/commonJvmAndroid/kotlin/okhttp3/internal/-NormalizeJvm.kt
```
fun normalizeNfc(string) → String
```

### okhttp/src/commonJvmAndroid/kotlin/okhttp3/internal/-UtilCommon.kt
```
fun isSensitiveHeader(name) → Boolean
fun checkOffsetAndCount(arrayLength, offset, count)
```

### okhttp/src/commonJvmAndroid/kotlin/okhttp3/internal/-UtilJvm.kt
```
fun threadFactory(name, daemon) → ThreadFactory
fun format(format, vararg args) → String
fun checkDuration(name, duration, unit) → Int
fun checkDuration(name, duration) → Int
```

### okhttp/src/commonJvmAndroid/kotlin/okhttp3/internal/NativeImageTestsAccessors.kt
```
fun buildCache(file, maxSize, fileSystem) → Cache
```

### okhttp/src/commonJvmAndroid/kotlin/okhttp3/internal/Tags.kt
```
class Tags
object EmptyTags
  fun toString()
```

### okhttp/src/commonJvmAndroid/kotlin/okhttp3/internal/UnreadableResponseBody.kt
```
class UnreadableResponseBody
  fun contentType()
  fun contentLength()
  fun source()
  fun read(sink, byteCount) → Long
  fun timeout()
  fun close()
```

### okhttp/src/commonJvmAndroid/kotlin/okhttp3/internal/authenticator/JavaNetAuthenticator.kt
```
class JavaNetAuthenticator
  fun authenticate(route, response) → Request?
```

### okhttp/src/commonJvmAndroid/kotlin/okhttp3/internal/cache/CacheInterceptor.kt
```
class CacheInterceptor
  fun intercept(chain) → Response
```

### okhttp/src/commonJvmAndroid/kotlin/okhttp3/internal/cache/CacheRequest.kt
```
interface CacheRequest
  fun body() → Sink
  fun abort()
```

### okhttp/src/commonJvmAndroid/kotlin/okhttp3/internal/cache/CacheStrategy.kt
```
class CacheStrategy
  fun compute() → CacheStrategy
```

### okhttp/src/commonJvmAndroid/kotlin/okhttp3/internal/cache/DiskLruCache.kt
```
class DiskLruCache
  fun sink(file, mustCreate) → Sink
  fun runOnce() → Long
  fun initialize()
```

### okhttp/src/commonJvmAndroid/kotlin/okhttp3/internal/cache/FaultHidingSink.kt
```
class FaultHidingSink
  fun write(source, byteCount)
  fun flush()
  fun close()
```

### okhttp/src/commonJvmAndroid/kotlin/okhttp3/internal/concurrent/Lockable.kt
```
interface Lockable
```

### okhttp/src/commonJvmAndroid/kotlin/okhttp3/internal/concurrent/Task.kt
```
class Task
  fun initQueue(queue)
  fun toString() → String
```

### okhttp/src/commonJvmAndroid/kotlin/okhttp3/internal/concurrent/TaskLogger.kt
```
fun formatDuration(ns) → String
```

### okhttp/src/commonJvmAndroid/kotlin/okhttp3/internal/concurrent/TaskQueue.kt
```
class TaskQueue
  fun schedule(task, delayNanos)
  fun schedule(name, delayNanos, block)
  fun runOnce() → Long
  fun execute(name, delayNanos, cancelable, block)
  fun runOnce() → Long
  fun idleLatch() → CountDownLatch
  fun runOnce() → Long
  fun scheduleAndDecide(task, delayNanos, recurrence) → Boolean
```

### okhttp/src/commonJvmAndroid/kotlin/okhttp3/internal/concurrent/TaskRunner.kt
```
class TaskRunner
  fun run()
  fun kickCoordinator(taskQueue)
  fun awaitTaskToRun() → Task?
```

### okhttp/src/commonJvmAndroid/kotlin/okhttp3/internal/connection/BufferedSocket.kt
```
interface BufferedSocket
```

### okhttp/src/commonJvmAndroid/kotlin/okhttp3/internal/connection/ConnectInterceptor.kt
```
object ConnectInterceptor
  fun intercept(chain) → Response
```

### okhttp/src/commonJvmAndroid/kotlin/okhttp3/internal/connection/ConnectPlan.kt
```
class ConnectPlan
  fun connectTcp() → ConnectResult
  fun connectTlsEtc() → ConnectResult
```

### okhttp/src/commonJvmAndroid/kotlin/okhttp3/internal/connection/ConnectionListener.kt
```
class ConnectionListener
```

### okhttp/src/commonJvmAndroid/kotlin/okhttp3/internal/connection/Exchange.kt
```
class Exchange
  fun writeRequestHeaders(request)
  fun createRequestBody(request, duplex) → Sink
  fun flushRequest()
  fun finishRequest()
  fun responseHeadersStart()
  fun readResponseHeaders(expectContinue) → Response.Builder?
  fun responseHeadersEnd(response)
  fun openResponseBody(response) → ResponseBody
```

### okhttp/src/commonJvmAndroid/kotlin/okhttp3/internal/connection/ExchangeFinder.kt
```
interface ExchangeFinder
  fun find() → RealConnection
```

### okhttp/src/commonJvmAndroid/kotlin/okhttp3/internal/connection/FailedPlan.kt
```
class FailedPlan
  fun connectTcp()
  fun connectTlsEtc()
  fun handleSuccess()
  fun cancel()
  fun retry()
```

### okhttp/src/commonJvmAndroid/kotlin/okhttp3/internal/connection/FastFallbackExchangeFinder.kt
```
class FastFallbackExchangeFinder
  fun find() → RealConnection
  fun runOnce() → Long
```

### okhttp/src/commonJvmAndroid/kotlin/okhttp3/internal/connection/ForceConnectRoutePlanner.kt
```
class ForceConnectRoutePlanner
  fun plan() → RoutePlanner.Plan
```

### okhttp/src/commonJvmAndroid/kotlin/okhttp3/internal/connection/InetAddressOrder.kt
```
fun reorderForHappyEyeballs(addresses) → List<InetAddress>
```

### okhttp/src/commonJvmAndroid/kotlin/okhttp3/internal/connection/RealCall.kt
```
class RealCall
  fun timedOut()
  fun timeout() → Timeout
  fun addEventListener(eventListener)
  fun clone() → Call
  fun request() → Request
  fun cancel()
  fun isCanceled() → Boolean
  fun execute() → Response
```

### okhttp/src/commonJvmAndroid/kotlin/okhttp3/internal/connection/RealConnection.kt
```
class RealConnection
  fun noNewExchanges()
  fun noCoalescedConnections()
  fun incrementSuccessCount()
  fun start()
  fun isEligible(address, routes) → Boolean
  fun newCodec(client, chain) → ExchangeCodec
```

### okhttp/src/commonJvmAndroid/kotlin/okhttp3/internal/connection/RealConnectionPool.kt
```
class RealConnectionPool
  fun runOnce() → Long
  fun idleConnectionCount() → Int
  fun connectionCount() → Int
  fun callAcquirePooledConnection(doExtensiveHealthChecks, address, call, routes, requireMultiplexed) → RealConnection?
  fun put(connection)
  fun connectionBecameIdle(connection) → Boolean
  fun evictAll()
  fun closeConnections(now) → Long
```

### okhttp/src/commonJvmAndroid/kotlin/okhttp3/internal/connection/RealRoutePlanner.kt
```
class RealRoutePlanner
  fun isCanceled() → Boolean
  fun plan() → Plan
  fun planConnect() → ConnectPlan
  fun planReusePooledConnection(planToReplace, routes) → ReusePlan?
  fun planConnectToRoute(route, routes) → ConnectPlan
```

### okhttp/src/commonJvmAndroid/kotlin/okhttp3/internal/connection/RetryTlsHandshake.kt
```
fun retryTlsHandshake(e) → Boolean
```

### okhttp/src/commonJvmAndroid/kotlin/okhttp3/internal/connection/ReusePlan.kt
```
class ReusePlan
  fun connectTcp()
  fun connectTlsEtc()
  fun handleSuccess()
  fun cancel()
  fun retry()
```

### okhttp/src/commonJvmAndroid/kotlin/okhttp3/internal/connection/RouteDatabase.kt
```
class RouteDatabase
```

### okhttp/src/commonJvmAndroid/kotlin/okhttp3/internal/connection/RoutePlanner.kt
```
interface RoutePlanner
  fun isCanceled() → Boolean
  fun plan() → Plan
  fun hasNext(failedConnection) → Boolean
  fun sameHostAndPort(url) → Boolean
  fun connectTcp() → ConnectResult
  fun connectTlsEtc() → ConnectResult
  fun handleSuccess() → RealConnection
  fun cancel()
```

### okhttp/src/commonJvmAndroid/kotlin/okhttp3/internal/connection/RouteSelector.kt
```
class RouteSelector
  fun selectProxies() → List<Proxy>
```

### okhttp/src/commonJvmAndroid/kotlin/okhttp3/internal/connection/SequentialExchangeFinder.kt
```
class SequentialExchangeFinder
  fun find() → RealConnection
```

### okhttp/src/commonJvmAndroid/kotlin/okhttp3/internal/http/BridgeInterceptor.kt
```
class BridgeInterceptor
  fun intercept(chain) → Response
```

### okhttp/src/commonJvmAndroid/kotlin/okhttp3/internal/http/CallServerInterceptor.kt
```
object CallServerInterceptor
  fun intercept(chain) → Response
```

### okhttp/src/commonJvmAndroid/kotlin/okhttp3/internal/http/ExchangeCodec.kt
```
interface ExchangeCodec
  fun createRequestBody(request, contentLength) → Sink
  fun writeRequestHeaders(request)
  fun flushRequest()
  fun finishRequest()
  fun readResponseHeaders(expectContinue) → Response.Builder?
  fun reportedContentLength(response) → Long
  fun openResponseBodySource(response) → Source
  fun peekTrailers() → Headers?
```

### okhttp/src/commonJvmAndroid/kotlin/okhttp3/internal/http/GzipRequestBody.kt
```
class GzipRequestBody
  fun contentType()
  fun contentLength()
  fun writeTo(sink)
  fun isOneShot()
```

### okhttp/src/commonJvmAndroid/kotlin/okhttp3/internal/http/HttpHeaders.kt
```
fun hasBody(response) → Boolean
```

### okhttp/src/commonJvmAndroid/kotlin/okhttp3/internal/http/HttpMethod.kt
```
object HttpMethod
  fun invalidatesCache(method) → Boolean
  fun requiresRequestBody(method) → Boolean
  fun permitsRequestBody(method) → Boolean
  fun redirectsWithBody(method) → Boolean
  fun redirectsToGet(method) → Boolean
  fun isCacheable(requestMethod) → Boolean
```

### okhttp/src/commonJvmAndroid/kotlin/okhttp3/internal/http/RealInterceptorChain.kt
```
class RealInterceptorChain
  fun copy(index, exchange, request, connectTimeoutMillis, readTimeoutMillis, writeTimeoutMillis, authenticator, cache, certificatePinner, connectionPool, cookieJar, dns, hostnameVerifier, proxy, proxyAuthenticator, proxySelector, retryOnConnectionFailure, socketFactory, sslSocketFactory, x509TrustManager, certificateChainCleaner)
  fun connection() → Connection?
  fun connectTimeoutMillis() → Int
  fun withConnectTimeout(timeout, unit) → Interceptor.Chain
  fun readTimeoutMillis() → Int
  fun withReadTimeout(timeout, unit) → Interceptor.Chain
```

### okhttp/src/commonJvmAndroid/kotlin/okhttp3/internal/http/RealResponseBody.kt
```
class RealResponseBody
  fun contentLength() → Long
  fun contentType() → MediaType?
  fun source() → BufferedSource
```

### okhttp/src/commonJvmAndroid/kotlin/okhttp3/internal/http/RequestLine.kt
```
object RequestLine
  fun get(request, proxyType) → String
  fun requestPath(url) → String
```

### okhttp/src/commonJvmAndroid/kotlin/okhttp3/internal/http/RetryAndFollowUpInterceptor.kt
```
class RetryAndFollowUpInterceptor
  fun intercept(chain) → Response
```

### okhttp/src/commonJvmAndroid/kotlin/okhttp3/internal/http/StatusLine.kt
```
class StatusLine
  fun toString() → String
  fun get(response) → StatusLine
  fun parse(statusLine) → StatusLine
```

### okhttp/src/commonJvmAndroid/kotlin/okhttp3/internal/http1/HeadersReader.kt
```
class HeadersReader
  fun readLine() → String
  fun readHeaders() → Headers
```

### okhttp/src/commonJvmAndroid/kotlin/okhttp3/internal/http1/Http1ExchangeCodec.kt
```
class Http1ExchangeCodec
  fun createRequestBody(request, contentLength) → Sink
  fun cancel()
  fun writeRequestHeaders(request)
  fun reportedContentLength(response) → Long
  fun openResponseBodySource(response) → Source
  fun peekTrailers() → Headers?
  fun flushRequest()
  fun finishRequest()
```

### okhttp/src/commonJvmAndroid/kotlin/okhttp3/internal/http2/FlowControlListener.kt
```
interface FlowControlListener
  fun receivingStreamWindowChanged(streamId, windowCounter, bufferSize)
  fun receivingConnectionWindowChanged(windowCounter)
  fun receivingStreamWindowChanged(streamId, windowCounter, bufferSize)
  fun receivingConnectionWindowChanged(windowCounter)
```

### okhttp/src/commonJvmAndroid/kotlin/okhttp3/internal/http2/Header.kt
```
class Header
  fun toString() → String
```

### okhttp/src/commonJvmAndroid/kotlin/okhttp3/internal/http2/Hpack.kt
```
object Hpack
  fun getAndResetHeaderList() → List<Header>
  fun maxDynamicTableByteCount() → Int
```

### okhttp/src/commonJvmAndroid/kotlin/okhttp3/internal/http2/Http2.kt
```
object Http2
  fun frameLog(inbound, streamId, length, type, flags) → String
  fun frameLogWindowUpdate(inbound, streamId, length, windowSizeIncrement) → String
  fun formattedType(type) → String
  fun formatFlags(type, flags) → String
```

### okhttp/src/commonJvmAndroid/kotlin/okhttp3/internal/http2/Http2Connection.kt
```
class Http2Connection
  fun openStreamCount() → Int
  fun getStream(id) → Http2Stream?
  fun removeStream(streamId) → Http2Stream?
  fun updateConnectionFlowControl(read)
  fun pushStream(associatedStreamId, requestHeaders, out) → Http2Stream
  fun newStream(requestHeaders, out) → Http2Stream
```

### okhttp/src/commonJvmAndroid/kotlin/okhttp3/internal/http2/Http2ExchangeCodec.kt
```
class Http2ExchangeCodec
  fun createRequestBody(request, contentLength) → Sink
  fun writeRequestHeaders(request)
  fun flushRequest()
  fun finishRequest()
  fun readResponseHeaders(expectContinue) → Response.Builder?
  fun reportedContentLength(response) → Long
  fun openResponseBodySource(response) → Source
  fun peekTrailers() → Headers?
```

### okhttp/src/commonJvmAndroid/kotlin/okhttp3/internal/http2/Http2Reader.kt
```
class Http2Reader
  fun readConnectionPreface(handler)
  fun nextFrame(requireSettings, handler) → Boolean
```

### okhttp/src/commonJvmAndroid/kotlin/okhttp3/internal/http2/Http2Stream.kt
```
class Http2Stream
  fun takeHeaders(callerIsIdle) → Headers
  fun peekTrailers() → Headers?
  fun writeHeaders(responseHeaders, outFinished, flushHeaders)
  fun enqueueTrailers(trailers)
  fun readTimeout() → Timeout
  fun writeTimeout() → Timeout
  fun close(rstStatusCode, errorException)
  fun cancel()
```

### okhttp/src/commonJvmAndroid/kotlin/okhttp3/internal/http2/Http2Writer.kt
```
class Http2Writer
  fun connectionPreface()
  fun applyAndAckSettings(peerSettings)
  fun pushPromise(streamId, promisedStreamId, requestHeaders)
  fun flush()
  fun rstStream(streamId, errorCode)
  fun maxDataLength() → Int
  fun data(outFinished, streamId, source, byteCount)
  fun dataFrame(streamId, flags, buffer, byteCount)
```

### okhttp/src/commonJvmAndroid/kotlin/okhttp3/internal/http2/Huffman.kt
```
object Huffman
```

### okhttp/src/commonJvmAndroid/kotlin/okhttp3/internal/http2/PushObserver.kt
```
interface PushObserver
  fun onRequest(streamId, requestHeaders) → Boolean
  fun onHeaders(streamId, responseHeaders, last) → Boolean
  fun onData(streamId, source, byteCount, last) → Boolean
  fun onReset(streamId, errorCode)
  fun onRequest(streamId, requestHeaders) → Boolean
  fun onHeaders(streamId, responseHeaders, last) → Boolean
  fun onData(streamId, source, byteCount, last) → Boolean
  fun onReset(streamId, errorCode)
```

### okhttp/src/commonJvmAndroid/kotlin/okhttp3/internal/http2/Settings.kt
```
class Settings
  fun clear()
  fun isSet(id) → Boolean
  fun size() → Int
  fun getEnablePush(defaultValue) → Boolean
  fun getMaxConcurrentStreams() → Int
  fun getMaxFrameSize(defaultValue) → Int
  fun getMaxHeaderListSize(defaultValue) → Int
  fun merge(other)
```

### okhttp/src/commonJvmAndroid/kotlin/okhttp3/internal/http2/flowcontrol/WindowCounter.kt
```
class WindowCounter
  fun update(total, acknowledged)
  fun toString() → String
```

### okhttp/src/commonJvmAndroid/kotlin/okhttp3/internal/idn/IdnaMappingTable.kt
```
class IdnaMappingTable
  fun map(codePoint, sink) → Boolean
```

### okhttp/src/commonJvmAndroid/kotlin/okhttp3/internal/idn/Punycode.kt
```
object Punycode
  fun encode(string) → String?
  fun decode(string) → String?
```

### okhttp/src/commonJvmAndroid/kotlin/okhttp3/internal/internal.kt
```
fun parseCookie(currentTimeMillis, url, setCookie) → Cookie?
fun cookieToString(cookie, forObsoleteRfc2965) → String
fun addHeaderLenient(builder, line) → Headers.Builder
fun addHeaderLenient(builder, name, value) → Headers.Builder
fun cacheGet(cache, request) → Response?
fun applyConnectionSpec(connectionSpec, sslSocket, isFallback)
fun buildConnectionPool(connectionListener, taskRunner) → ConnectionPool
```

### okhttp/src/commonJvmAndroid/kotlin/okhttp3/internal/platform/Jdk9Platform.kt
```
class Jdk9Platform
  fun configureTlsExtensions(sslSocket, hostname, protocols)
  fun getSelectedProtocol(sslSocket) → String?
  fun trustManager(sslSocketFactory) → X509TrustManager?
  fun newSSLContext() → SSLContext
  fun buildIfSupported() → Jdk9Platform?
```

### okhttp/src/commonJvmAndroid/kotlin/okhttp3/internal/platform/Platform.kt
```
class Platform
  fun getPrefix()
  fun toString() → String
  fun get() → Platform
  fun resetForTests(platform)
  fun alpnProtocolNames(protocols)
```

### okhttp/src/commonJvmAndroid/kotlin/okhttp3/internal/proxy/NullProxySelector.kt
```
object NullProxySelector
  fun select(uri) → List<Proxy>
  fun connectFailed(uri, sa, ioe)
```

### okhttp/src/commonJvmAndroid/kotlin/okhttp3/internal/publicsuffix/BasePublicSuffixList.kt
```
class BasePublicSuffixList
  fun ensureLoaded()
```

### okhttp/src/commonJvmAndroid/kotlin/okhttp3/internal/publicsuffix/PublicSuffixDatabase.kt
```
class PublicSuffixDatabase
  fun getEffectiveTldPlusOne(domain) → String?
  fun get() → PublicSuffixDatabase
```

### okhttp/src/commonJvmAndroid/kotlin/okhttp3/internal/publicsuffix/PublicSuffixList.kt
```
interface PublicSuffixList
  fun ensureLoaded()
```

### okhttp/src/commonJvmAndroid/kotlin/okhttp3/internal/publicsuffix/ResourcePublicSuffixList.kt
```
class ResourcePublicSuffixList
  fun listSource() → Source
```

### okhttp/src/commonJvmAndroid/kotlin/okhttp3/internal/tls/BasicCertificateChainCleaner.kt
```
class BasicCertificateChainCleaner
  fun clean(chain, hostname) → List<Certificate>
  fun hashCode() → Int
  fun equals(other) → Boolean
```

### okhttp/src/commonJvmAndroid/kotlin/okhttp3/internal/tls/BasicTrustRootIndex.kt
```
class BasicTrustRootIndex
  fun findByIssuerAndSignature(cert) → X509Certificate?
  fun equals(other) → Boolean
  fun hashCode() → Int
```

### okhttp/src/commonJvmAndroid/kotlin/okhttp3/internal/tls/CertificateChainCleaner.kt
```
class CertificateChainCleaner
  fun get(trustManager) → CertificateChainCleaner
  fun get(vararg caCerts) → CertificateChainCleaner
```

### okhttp/src/commonJvmAndroid/kotlin/okhttp3/internal/tls/OkHostnameVerifier.kt
```
object OkHostnameVerifier
  fun verify(host, session) → Boolean
  fun verify(host, certificate) → Boolean
  fun allSubjectAltNames(certificate) → List<String>
```

### okhttp/src/commonJvmAndroid/kotlin/okhttp3/internal/ws/MessageDeflater.kt
```
class MessageDeflater
  fun deflate(buffer)
  fun close()
```

### okhttp/src/commonJvmAndroid/kotlin/okhttp3/internal/ws/MessageInflater.kt
```
class MessageInflater
  fun inflate(buffer)
  fun close()
```

### okhttp/src/commonJvmAndroid/kotlin/okhttp3/internal/ws/RealWebSocket.kt
```
class RealWebSocket
  fun request() → Request
  fun cancel()
  fun connect(client)
  fun onResponse(call, response)
  fun onFailure(call, e)
  fun checkUpgradeSuccess(response) → Socket
```

### okhttp/src/commonJvmAndroid/kotlin/okhttp3/internal/ws/WebSocketExtensions.kt
```
class WebSocketExtensions
  fun noContextTakeover(clientOriginated) → Boolean
  fun parse(responseHeaders) → WebSocketExtensions
```

### okhttp/src/commonJvmAndroid/kotlin/okhttp3/internal/ws/WebSocketProtocol.kt
```
object WebSocketProtocol
  fun toggleMask(cursor, key)
  fun closeCodeExceptionMessage(code) → String?
  fun validateCloseCode(code)
  fun acceptHeader(key) → String
```

### okhttp/src/commonJvmAndroid/kotlin/okhttp3/internal/ws/WebSocketReader.kt
```
class WebSocketReader
  fun onReadMessage(text)
  fun onReadMessage(bytes)
  fun onReadPing(payload)
  fun onReadPong(payload)
  fun onReadClose(code, reason)
  fun processNextFrame()
```

### okhttp/src/commonJvmAndroid/kotlin/okhttp3/internal/ws/WebSocketWriter.kt
```
class WebSocketWriter
  fun writePing(payload)
  fun writePong(payload)
  fun writeClose(code, reason)
  fun writeMessageFrame(formatOpcode, data)
  fun close()
```

### okhttp/src/commonTest/kotlin/okhttp3/CompressionInterceptorTest.kt
```
class CompressionInterceptorTest
  fun emptyDoesntChangeRequestOrResponse()
  fun gzipThroughCall()
```

### okhttp/src/commonTest/kotlin/okhttp3/OkHttpTest.kt
```
class OkHttpTest
  fun testVersion()
```

### okhttp/src/commonTest/kotlin/okhttp3/internal/IsProbablyUtf8Test.kt
```
class IsProbablyUtf8Test
  fun read(sink, byteCount) → Long
  fun close()
  fun timeout()
```

### okhttp/src/commonTest/kotlin/okhttp3/internal/publicsuffix/ConfiguredPublicSuffixDatabaseTest.kt
```
class ConfiguredPublicSuffixDatabaseTest
```

### okhttp/src/commonTest/kotlin/okhttp3/internal/publicsuffix/ConfiguredPublicSuffixList.kt
```
class ConfiguredPublicSuffixList
  fun ensureLoaded()
```

### okhttp/src/commonTest/kotlin/okhttp3/internal/publicsuffix/PublicSuffixDatabaseTest.kt
```
class PublicSuffixDatabaseTest
  fun setUp()
  fun allPublicSuffixes()
  fun publicSuffixExceptions()
  fun threadIsInterruptedOnFirstRead()
  fun secondReadFailsSameAsFirst()
  fun publicSuffixDotOrgTestCases()
```

### okhttp/src/jvmMain/kotlin/okhttp3/internal/graal/GraalSvm.kt
```
class TargetBouncyCastlePlatform
  fun findPlatform() → Platform
```

### okhttp/src/jvmMain/kotlin/okhttp3/internal/graal/OkHttpFeature.kt
```
class OkHttpFeature
  fun beforeAnalysis(access)
```

### okhttp/src/jvmMain/kotlin/okhttp3/internal/platform/BouncyCastlePlatform.kt
```
class BouncyCastlePlatform
  fun newSSLContext() → SSLContext
  fun platformTrustManager() → X509TrustManager
  fun trustManager(sslSocketFactory) → X509TrustManager
  fun configureTlsExtensions(sslSocket, hostname, protocols)
  fun getSelectedProtocol(sslSocket) → String?
  fun buildIfSupported() → BouncyCastlePlatform?
```

### okhttp/src/jvmMain/kotlin/okhttp3/internal/platform/ConscryptPlatform.kt
```
class ConscryptPlatform
  fun newSSLContext() → SSLContext
  fun platformTrustManager() → X509TrustManager
  fun verify(hostname, session) → Boolean
  fun verify(certs, hostname, session) → Boolean
  fun trustManager(sslSocketFactory) → X509TrustManager?
  fun configureTlsExtensions(sslSocket, hostname, protocols)
  fun getSelectedProtocol(sslSocket) → String?
  fun newSslSocketFactory(trustManager) → SSLSocketFactory
```

### okhttp/src/jvmMain/kotlin/okhttp3/internal/platform/Jdk8WithJettyBootPlatform.kt
```
class Jdk8WithJettyBootPlatform
  fun configureTlsExtensions(sslSocket, hostname, protocols)
  fun afterHandshake(sslSocket)
  fun getSelectedProtocol(sslSocket) → String?
  fun invoke(proxy, method, args) → Any?
  fun buildIfSupported() → Platform?
```

### okhttp/src/jvmMain/kotlin/okhttp3/internal/platform/OpenJSSEPlatform.kt
```
class OpenJSSEPlatform
  fun newSSLContext() → SSLContext
  fun platformTrustManager() → X509TrustManager
  fun trustManager(sslSocketFactory) → X509TrustManager
  fun configureTlsExtensions(sslSocket, hostname, protocols)
  fun getSelectedProtocol(sslSocket) → String?
  fun buildIfSupported() → OpenJSSEPlatform?
```

### okhttp/src/jvmTest/java/okhttp3/CallJavaTest.java
```
class CallJavaTest
  tagsSeededFromRequest() → void
  tagsCanBeComputed() → void
```

### okhttp/src/jvmTest/kotlin/okhttp3/AddressTest.kt
```
class AddressTest
```

### okhttp/src/jvmTest/kotlin/okhttp3/AutobahnTester.kt
```
class AutobahnTester
  fun run()
  fun onOpen(webSocket, response)
  fun onMessage(webSocket, bytes)
  fun onMessage(webSocket, text)
  fun onClosing(webSocket, code, reason)
  fun onFailure(webSocket, t, response)
  fun onMessage(webSocket, text)
  fun onClosing(webSocket, code, reason)
```

### okhttp/src/jvmTest/kotlin/okhttp3/BouncyCastleTest.kt
```
class BouncyCastleTest
  fun setUp()
  fun testMozilla()
```

### okhttp/src/jvmTest/kotlin/okhttp3/CacheControlJvmTest.kt
```
class CacheControlJvmTest
  fun completeBuilder()
  fun parseEmpty()
  fun parse()
  fun parseIgnoreCacheControlExtensions()
  fun parseCacheControlAndPragmaAreCombined()
```

### okhttp/src/jvmTest/kotlin/okhttp3/CacheControlTest.kt
```
class CacheControlTest
  fun emptyBuilderIsEmpty()
  fun completeBuilder()
```

### okhttp/src/jvmTest/kotlin/okhttp3/CacheCorruptionTest.kt
```
class CacheCorruptionTest
  fun setUp()
  fun tearDown()
  fun corruptedCipher()
  fun truncatedMetadataEntry()
```

### okhttp/src/jvmTest/kotlin/okhttp3/CacheTest.kt
```
class CacheTest
  fun setUp()
  fun tearDown()
  fun responseCachingByResponseCode()
  fun responseCachingWith1xxInformationalResponse()
```

### okhttp/src/jvmTest/kotlin/okhttp3/CallHandshakeTest.kt
```
class CallHandshakeTest
  fun setup()
  fun testDefaultHandshakeCipherSuiteOrderingTls12Restricted()
  fun testDefaultHandshakeCipherSuiteOrderingTls12Modern()
  fun testDefaultHandshakeCipherSuiteOrderingTls13Modern()
  fun testHandshakeCipherSuiteOrderingWhenReversed()
  fun clientOrderApplied()
```

### okhttp/src/jvmTest/kotlin/okhttp3/CallKotlinTest.kt
```
class CallKotlinTest
  fun legalToExecuteTwiceCloning()
  fun testMockWebserverRequest()
  fun testHeadAfterPut()
  fun contentType() → MediaType
  fun writeTo(sink)
  fun contentType() → MediaType
  fun writeTo(sink)
  fun staleConnectionNotReusedForNonIdempotentRequest()
```

### okhttp/src/jvmTest/kotlin/okhttp3/CallLimitsTest.kt
```
class CallLimitsTest
  fun largeStatusLine()
  fun largeResponseHeader()
  fun largeCompressedResponseHeader()
  fun largeResponseHeadersList()
```

### okhttp/src/jvmTest/kotlin/okhttp3/CallTagsTest.kt
```
class CallTagsTest
  fun tagsSeededFromRequest()
  fun tagsCanBeComputed()
  fun computedTagsAreNotRetainedInClone()
```

### okhttp/src/jvmTest/kotlin/okhttp3/CallTest.kt
```
class CallTest
  fun setUp()
  fun tearDown()
  fun get()
  fun buildRequestUsingHttpUrl()
  fun invalidScheme()
  fun invalidPort()
  fun getReturns500()
  fun get_HTTP_2()
```

### okhttp/src/jvmTest/kotlin/okhttp3/CertificateChainCleanerTest.kt
```
class CertificateChainCleanerTest
  fun equalsFromCertificate()
  fun equalsFromTrustManager()
  fun normalizeSingleSelfSignedCertificate()
  fun normalizeUnknownSelfSignedCertificate()
  fun orderedChainOfCertificatesWithRoot()
  fun orderedChainOfCertificatesWithoutRoot()
  fun unorderedChainOfCertificatesWithRoot()
  fun unorderedChainOfCertificatesWithoutRoot()
```

### okhttp/src/jvmTest/kotlin/okhttp3/CertificatePinnerKotlinTest.kt
```
class CertificatePinnerKotlinTest
  fun successfulCheckSha1Pin()
  fun wildcardHostnameShouldNotMatchThroughDot()
  fun doubleWildcardHostnameShouldMatchThroughDot()
  fun doubleWildcardHostnameShouldNotMatchSuffix()
  fun checkSubstringMatch()
```

### okhttp/src/jvmTest/kotlin/okhttp3/CertificatePinnerTest.kt
```
class CertificatePinnerTest
  fun malformedPin()
  fun malformedBase64()
  fun sameKeypairSamePin()
  fun successfulCheck()
  fun successfulMatchAcceptsAnyMatchingCertificateOld()
  fun successfulMatchAcceptsAnyMatchingCertificate()
  fun unsuccessfulCheck()
  fun multipleCertificatesForOneHostname()
```

### okhttp/src/jvmTest/kotlin/okhttp3/ChannelSocketFactory.kt
```
class ChannelSocketFactory
  fun createSocket() → Socket
  fun createSocket(host, port) → Socket
  fun createSocket(host, port, localHost, localPort) → Socket
  fun createSocket(host, port) → Socket
  fun createSocket(address, port, localAddress, localPort) → Socket
```

### okhttp/src/jvmTest/kotlin/okhttp3/CipherSuiteTest.kt
```
class CipherSuiteTest
  fun hashCode_usesIdentityHashCode_legacyCase()
  fun hashCode_usesIdentityHashCode_regularCase()
  fun instancesAreInterned()
  fun instancesAreInterned_survivesGarbageCollection()
  fun equals()
  fun forJavaName_acceptsArbitraryStrings()
  fun javaName_examples()
  fun javaName_equalsToString()
```

### okhttp/src/jvmTest/kotlin/okhttp3/CommonRequestBodyTest.kt
```
class CommonRequestBodyTest
  fun correctContentType()
```

### okhttp/src/jvmTest/kotlin/okhttp3/ConnectionCoalescingTest.kt
```
class ConnectionCoalescingTest
  fun setUp()
  fun commonThenAlternative()
  fun alternativeThenCommon()
  fun staleCoalescedConnection()
  fun coalescedConnectionDestroyedAfterAcquire()
```

### okhttp/src/jvmTest/kotlin/okhttp3/ConnectionListenerTest.kt
```
class ConnectionListenerTest
  fun setUp()
  fun successfulCallEventSequence()
  fun failedCallEventSequence()
  fun secondCallEventSequence()
  fun successfulEmptyH2CallEventSequence()
  fun multipleDnsLookupsForSingleCall()
class ConnectionListenerLegacyTest
```

### okhttp/src/jvmTest/kotlin/okhttp3/ConnectionReuseTest.kt
```
class ConnectionReuseTest
  fun connectionsAreReused()
  fun connectionsAreReusedForPosts()
  fun connectionsAreReusedWithHttp2()
  fun connectionsAreNotReusedWithRequestConnectionClose()
  fun connectionsAreNotReusedWithResponseConnectionClose()
  fun connectionsAreNotReusedWithUnknownLengthResponseBody()
  fun connectionsAreNotReusedIfPoolIsSizeZero()
  fun connectionsReusedWithRedirectEvenIfPoolIsSizeZero()
```

### okhttp/src/jvmTest/kotlin/okhttp3/ConnectionSpecTest.kt
```
class ConnectionSpecTest
  fun noTlsVersions()
  fun noCipherSuites()
  fun cleartextBuilder()
  fun tlsBuilder_explicitCiphers()
  fun tlsBuilder_defaultCiphers()
  fun tls_defaultCiphers_noFallbackIndicator()
  fun tls_defaultCiphers_withFallbackIndicator()
```

### okhttp/src/jvmTest/kotlin/okhttp3/ConscryptTest.kt
```
class ConscryptTest
  fun testTrustManager()
  fun testMozilla()
  fun testGoogle()
  fun testBuildIfSupported()
  fun testVersion()
```

### okhttp/src/jvmTest/kotlin/okhttp3/CookieTest.kt
```
class CookieTest
```

### okhttp/src/jvmTest/kotlin/okhttp3/CookiesTest.kt
```
class CookiesTest
  fun testNetscapeResponse()
  fun testRfc2109Response()
  fun testQuotedAttributeValues()
  fun testSendingCookiesFromStore()
```

### okhttp/src/jvmTest/kotlin/okhttp3/CorrettoTest.kt
```
class CorrettoTest
  fun testMozilla()
  fun testGoogle()
  fun testIfSupported()
```

### okhttp/src/jvmTest/kotlin/okhttp3/DispatcherCleanupTest.kt
```
class DispatcherCleanupTest
  fun testFinish()
  fun onFailure(call, e)
  fun onResponse(call, response)
```

### okhttp/src/jvmTest/kotlin/okhttp3/DispatcherTest.kt
```
class DispatcherTest
  fun setUp()
  fun maxRequestsZero()
  fun maxPerHostZero()
  fun enqueuedJobsRunImmediately()
  fun maxRequestsEnforced()
  fun maxPerHostEnforced()
  fun maxPerHostNotEnforcedForWebSockets()
  fun increasingMaxRequestsPromotesJobsImmediately()
```

### okhttp/src/jvmTest/kotlin/okhttp3/DuplexTest.kt
```
class DuplexTest
  fun setUp()
  fun tearDown()
  fun http1DoesntSupportDuplex()
  fun trueDuplexClientWritesFirst()
  fun trueDuplexServerWritesFirst()
  fun clientReadsHeadersDataTrailers()
```

### okhttp/src/jvmTest/kotlin/okhttp3/EventListenerTest.kt
```
class EventListenerTest
  fun setUp()
  fun tearDown()
  fun successfulCallEventSequence()
  fun successfulCallEventSequenceForIpAddress()
  fun successfulCallEventSequenceForEnqueue()
  fun onFailure(call, e)
  fun onResponse(call, response)
```

### okhttp/src/jvmTest/kotlin/okhttp3/FakeRoutePlanner.kt
```
class FakeRoutePlanner
  fun addPlan() → FakePlan
  fun isCanceled()
  fun plan() → FakePlan
  fun hasNext(failedConnection) → Boolean
  fun sameHostAndPort(url) → Boolean
  fun close()
  fun createRetry() → FakePlan
  fun createConnectTcpNextPlan() → FakePlan
```

### okhttp/src/jvmTest/kotlin/okhttp3/FallbackTestClientSocketFactory.kt
```
class FallbackTestClientSocketFactory
  fun configureSocket(sslSocket) → SSLSocket
  fun setEnabledCipherSuites(suites)
```

### okhttp/src/jvmTest/kotlin/okhttp3/FastFallbackTest.kt
```
class FastFallbackTest
  fun setUp()
  fun tearDown()
  fun callIpv6FirstEvenWhenIpv4IpIsListedFirst()
  fun callIpv6WhenBothServersAreReachable()
  fun reachesIpv4WhenIpv6IsDown()
  fun reachesIpv6WhenIpv4IsDown()
```

### okhttp/src/jvmTest/kotlin/okhttp3/FormBodyTest.kt
```
class FormBodyTest
  fun urlEncoding()
  fun addEncoded()
  fun encodedPair()
  fun encodeMultiplePairs()
  fun buildEmptyForm()
  fun characterEncoding()
```

### okhttp/src/jvmTest/kotlin/okhttp3/HandshakeTest.kt
```
class HandshakeTest
  fun createFromParts()
  fun createFromSslSession()
  fun sslWithNullNullNull()
  fun tlsWithNullNullNull()
  fun getProtocol()
  fun getCipherSuite()
  fun getPeerCertificates()
  fun getLocalCertificates()
```

### okhttp/src/jvmTest/kotlin/okhttp3/HeadersChallengesTest.kt
```
class HeadersChallengesTest
```

### okhttp/src/jvmTest/kotlin/okhttp3/HeadersJvmTest.kt
```
class HeadersJvmTest
```

### okhttp/src/jvmTest/kotlin/okhttp3/HeadersKotlinTest.kt
```
class HeadersKotlinTest
```

### okhttp/src/jvmTest/kotlin/okhttp3/HeadersRequestTest.kt
```
class HeadersRequestTest
```

### okhttp/src/jvmTest/kotlin/okhttp3/HeadersTest.kt
```
class HeadersTest
```

### okhttp/src/jvmTest/kotlin/okhttp3/HttpUrlJvmTest.kt
```
class HttpUrlJvmTest
  fun hostContainsOnlyStrippedCharacters()
  fun hostToUriStripsCharacters()
  fun hostToUriStripsCharacters2()
  fun fragmentNonAsciiThatOffendsJavaNetUri()
  fun toUriWithControlCharacters()
  fun toUriWithSpaceCharacters()
  fun toUriWithNonHexPercentEscape()
  fun toUriWithTruncatedPercentEscape()
```

### okhttp/src/jvmTest/kotlin/okhttp3/HttpUrlTest.kt
```
class HttpUrlTest
  fun parseTrimsAsciiWhitespace()
  fun parseHostAsciiNonPrintable()
  fun parseDoesNotTrimOtherWhitespaceCharacters()
  fun newBuilderResolve()
  fun redactedUrl()
  fun resolveNoScheme()
  fun resolveUnsupportedScheme()
  fun resolveSchemeLikePath()
```

### okhttp/src/jvmTest/kotlin/okhttp3/InsecureForHostTest.kt
```
class InsecureForHostTest
  fun setup()
```

### okhttp/src/jvmTest/kotlin/okhttp3/InterceptorOverridesTest.kt
```
class InterceptorOverridesTest
  fun testOverrideInApplicationInterceptor(override, OverrideParam.Cache, OverrideParam.CertificatePinner, OverrideParam.ConnectTimeout, OverrideParam.ConnectionPool, OverrideParam.CookieJar, OverrideParam.Dns, OverrideParam.HostnameVerifier, OverrideParam.Proxy, OverrideParam.ProxyAuthenticator, OverrideParam.ProxySelector, OverrideParam.ReadTimeout, OverrideParam.RetryOnConnectionFailure, OverrideParam.SocketFactory, OverrideParam.SslSocketFactory, OverrideParam.WriteTimeout, OverrideParam.X509TrustManager)
  fun testOverrideInNetworkInterceptor(override, OverrideParam.Cache, OverrideParam.CertificatePinner, OverrideParam.ConnectTimeout, OverrideParam.ConnectionPool, OverrideParam.CookieJar, OverrideParam.Dns, OverrideParam.HostnameVerifier, OverrideParam.Proxy, OverrideParam.ProxyAuthenticator, OverrideParam.ProxySelector, OverrideParam.ReadTimeout, OverrideParam.RetryOnConnectionFailure, OverrideParam.SocketFactory, OverrideParam.SslSocketFactory, OverrideParam.WriteTimeout, OverrideParam.X509TrustManager)
```

### okhttp/src/jvmTest/kotlin/okhttp3/InterceptorTest.kt
```
class InterceptorTest
  fun applicationInterceptorsCanShortCircuitResponses()
  fun networkInterceptorsCannotShortCircuitResponses()
  fun networkInterceptorsCannotCallProceedMultipleTimes()
  fun networkInterceptorsCannotChangeServerAddress()
  fun networkInterceptorsHaveConnectionAccess()
```

### okhttp/src/jvmTest/kotlin/okhttp3/JSSETest.kt
```
class JSSETest
  fun setUp()
  fun testTlsv13Works()
  fun testSupportedProtocols()
  fun testFacebook()
  fun connectionAcquired(call, connection)
```

### okhttp/src/jvmTest/kotlin/okhttp3/KotlinDeprecationErrorTest.kt
```
class KotlinDeprecationErrorTest
  fun tearDown()
  fun address()
  fun cache()
  fun cacheControl()
  fun challenge()
  fun cipherSuite()
  fun connectionSpec()
  fun cookie()
```

### okhttp/src/jvmTest/kotlin/okhttp3/KotlinSourceModernTest.kt
```
class KotlinSourceModernTest
  fun disabled()
  fun tearDown()
  fun address()
  fun authenticator()
  fun cache()
  fun cacheControl()
  fun cacheControlBuilder()
  fun call()
```

### okhttp/src/jvmTest/kotlin/okhttp3/LoomTest.kt
```
class LoomTest
  fun setUp()
  fun testRequest()
  fun testIfSupported()
```

### okhttp/src/jvmTest/kotlin/okhttp3/MediaTypeGetTest.kt
```
class MediaTypeGetTest
  fun parse(string) → MediaType
  fun assertInvalid(string, exceptionMessage)
```

### okhttp/src/jvmTest/kotlin/okhttp3/MediaTypeJvmTest.kt
```
class MediaTypeJvmTest
```

### okhttp/src/jvmTest/kotlin/okhttp3/MediaTypeTest.kt
```
class MediaTypeTest
```

### okhttp/src/jvmTest/kotlin/okhttp3/MultipartBodyTest.kt
```
class MultipartBodyTest
  fun onePartRequired()
  fun singlePart()
  fun threeParts()
  fun fieldAndTwoFiles()
```

### okhttp/src/jvmTest/kotlin/okhttp3/MultipartReaderTest.kt
```
class MultipartReaderTest
```

### okhttp/src/jvmTest/kotlin/okhttp3/OkHttpClientTest.kt
```
class OkHttpClientTest
```

### okhttp/src/jvmTest/kotlin/okhttp3/OpenJSSETest.kt
```
class OpenJSSETest
  fun setUp()
  fun testTlsv13Works()
  fun testSupportedProtocols()
  fun testMozilla()
  fun testBuildIfSupported()
```

### okhttp/src/jvmTest/kotlin/okhttp3/ProtocolTest.kt
```
class ProtocolTest
  fun testGetKnown()
  fun testGetUnknown()
  fun testToString()
```

### okhttp/src/jvmTest/kotlin/okhttp3/PublicInternalApiTest.kt
```
class PublicInternalApiTest
  fun permitsRequestBody()
  fun requiresRequestBody()
  fun hasBody()
```

### okhttp/src/jvmTest/kotlin/okhttp3/RecordedResponse.kt
```
class RecordedResponse
  fun assertRequestUrl(url)
  fun assertRequestMethod(method)
  fun assertRequestHeader(name, vararg values)
  fun assertCode(expectedCode)
  fun assertSuccessful()
  fun assertNotSuccessful()
  fun assertHeader(name, vararg values)
  fun assertHeaders(headers)
```

### okhttp/src/jvmTest/kotlin/okhttp3/RecordingCallback.kt
```
class RecordingCallback
  fun onFailure(call, e)
  fun onResponse(call, response)
  fun await(url) → RecordedResponse
```

### okhttp/src/jvmTest/kotlin/okhttp3/RecordingExecutor.kt
```
class RecordingExecutor
  fun execute(command)
  fun assertJobs(vararg expectedUrls)
  fun finishJob(url)
  fun shutdown()
  fun shutdownNow() → List<Runnable>
  fun isShutdown() → Boolean
  fun isTerminated() → Boolean
  fun awaitTermination(timeout, unit) → Boolean
```

### okhttp/src/jvmTest/kotlin/okhttp3/RequestBodyTest.kt
```
class RequestBodyTest
  fun setup(@TempDir tempDir)
  fun testFileDescriptor()
  fun testFileDescriptorRead()
  fun testFileDescriptorDefaultMediaType()
  fun testFileDescriptorMediaType()
  fun testFileDescriptorReadTwice()
  fun testFileDescriptorAfterClose()
  fun testPathRead()
```

### okhttp/src/jvmTest/kotlin/okhttp3/RequestCommonTest.kt
```
class RequestCommonTest
  fun constructorNormal()
  fun constructorNoBodyNoMethod()
  fun constructorNoMethod()
  fun constructorNoBody()
  fun newBuilderUrlResetsUrl()
  fun cacheControl()
  fun emptyCacheControlClearsAllCacheControlHeaders()
  fun headerAcceptsPermittedCharacters()
```

### okhttp/src/jvmTest/kotlin/okhttp3/RequestTest.kt
```
class RequestTest
  fun constructor()
  fun constructorNoBodyNoMethod()
  fun constructorNoMethod()
  fun constructorNoBody()
  fun string()
  fun stringWithDefaultCharsetAdded()
  fun stringWithNonDefaultCharsetSpecified()
  fun byteArray()
```

### okhttp/src/jvmTest/kotlin/okhttp3/ResponseBodyJvmTest.kt
```
class ResponseBodyJvmTest
  fun stringEmpty()
  fun stringLooksLikeBomButTooShort()
  fun stringDefaultsToUtf8()
  fun stringExplicitCharset()
  fun stringBomOverridesExplicitCharset()
  fun stringBomUtf8()
  fun stringBomUtf16Be()
  fun stringBomUtf16Le()
```

### okhttp/src/jvmTest/kotlin/okhttp3/ResponseBodyTest.kt
```
class ResponseBodyTest
  fun sourceEmpty()
  fun sourceClosesUnderlyingSource()
  fun contentType() → MediaType?
  fun contentLength() → Long
  fun source() → BufferedSource
  fun close()
  fun throwingUnderlyingSourceClosesQuietly()
  fun contentType() → MediaType?
class ForwardingSource
  fun read(sink, byteCount) → Long
  fun timeout()
  fun close()
```

### okhttp/src/jvmTest/kotlin/okhttp3/ResponseCommonTest.kt
```
class ResponseCommonTest
  fun close()
  fun read(sink, byteCount) → Long
  fun timeout() → Timeout
```

### okhttp/src/jvmTest/kotlin/okhttp3/ResponseJvmTest.kt
```
class ResponseJvmTest
  fun testEmptyByDefaultIfTrailersNotSet()
  fun worksIfTrailersSet()
  fun get()
  fun close()
  fun read(sink, byteCount) → Long
  fun timeout() → Timeout
```

### okhttp/src/jvmTest/kotlin/okhttp3/RouteFailureTest.kt
```
class RouteFailureTest
  fun setUp()
  fun http2OneBadHostOneGoodNoRetryOnConnectionFailure()
  fun http2OneBadHostOneGoodRetryOnConnectionFailure()
  fun http2OneBadHostOneGoodNoRetryOnConnectionFailureFastFallback()
```

### okhttp/src/jvmTest/kotlin/okhttp3/ServerTruncatesRequestTest.kt
```
class ServerTruncatesRequestTest
  fun setUp()
  fun serverTruncatesRequestOnLongPostHttp1()
  fun serverTruncatesRequestOnLongPostHttp2()
  fun serverTruncatesRequestHttp2OnDuplexRequest()
  fun serverTruncatesRequestButTrailersCanStillBeReadHttp1()
  fun serverTruncatesRequestButTrailersCanStillBeReadHttp2()
```

### okhttp/src/jvmTest/kotlin/okhttp3/SessionReuseTest.kt
```
class SessionReuseTest
  fun setUp()
  fun testSessionReuse(tlsVersion, "TLSv1.3")
  fun configureSocket(sslSocket) → SSLSocket
  fun connectionAcquired(call, connection)
```

### okhttp/src/jvmTest/kotlin/okhttp3/SocketChannelTest.kt
```
class SocketChannelTest
  fun setUp()
  fun testHttp(socketMode, Standard)
  fun testHttps(provider, protocol, HTTP_2)
  fun configureSocket(sslSocket) → SSLSocket
  fun matches(serverName) → Boolean
class SocketMode
  fun toString() → String
object Standard
  fun toString() → String
class TlsInstance
  fun toString() → String
```

### okhttp/src/jvmTest/kotlin/okhttp3/SocksProxy.kt
```
class SocksProxy
  fun play()
  fun proxy() → Proxy
  fun connectionCount() → Int
  fun shutdown()
```

### okhttp/src/jvmTest/kotlin/okhttp3/SocksProxyTest.kt
```
class SocksProxyTest
  fun setUp()
  fun tearDown()
  fun proxy()
  fun proxySelector()
  fun select(uri)
  fun connectFailed(uri, socketAddress, e)
  fun checkRemoteDNSResolve()
```

### okhttp/src/jvmTest/kotlin/okhttp3/TestLogHandler.kt
```
class TestLogHandler
  fun publish(logRecord)
  fun flush()
  fun close()
  fun beforeEach(context)
  fun afterEach(context)
  fun apply(base, description) → Statement
  fun evaluate()
  fun takeAll() → List<String>
```

### okhttp/src/jvmTest/kotlin/okhttp3/TestTls13Request.kt
```
fun main(vararg args)
```

### okhttp/src/jvmTest/kotlin/okhttp3/TrailersTest.kt
```
class TrailersTest
  fun trailersHttp1()
  fun trailersHttp2()
  fun trailersEmptyResponseBodyHttp1()
  fun trailersEmptyResponseBodyHttp2()
  fun trailersWithoutReadingFullResponseBodyHttp1()
  fun trailersWithoutReadingFullResponseBodyHttp2()
  fun trailersAndCacheHttp1()
  fun trailersAndCacheHttp2()
```

### okhttp/src/jvmTest/kotlin/okhttp3/URLConnectionTest.kt
```
class URLConnectionTest
  fun setUp()
  fun tearDown()
  fun requestHeaders()
  fun getRequestPropertyReturnsLastValue()
  fun responseHeaders()
  fun serverSendsInvalidStatusLine()
  fun serverSendsInvalidCodeTooLarge()
  fun serverSendsInvalidCodeNotANumber()
```

### okhttp/src/jvmTest/kotlin/okhttp3/UrlComponentEncodingTester.kt
```
class UrlComponentEncodingTester
  fun override(encoding, vararg codePoints)
  fun nonPrintableAscii(encoding)
  fun nonAscii(encoding)
  fun test(component)
```

### okhttp/src/jvmTest/kotlin/okhttp3/UrlComponentEncodingTesterJvm.kt
```
fun urlComponentEncodingTesterJvmPlatform(component) → UrlComponentEncodingTeste
```

### okhttp/src/jvmTest/kotlin/okhttp3/WebPlatformToAsciiData.kt
```
class WebPlatformToAsciiData
  fun toString()
  fun load() → List<WebPlatformToAsciiDa
```

### okhttp/src/jvmTest/kotlin/okhttp3/WebPlatformToAsciiTest.kt
```
class WebPlatformToAsciiTest
  fun test()
```

### okhttp/src/jvmTest/kotlin/okhttp3/WebPlatformUrlTest.kt
```
class WebPlatformUrlTest
  fun arguments()
  fun httpUrl(testData)
```

### okhttp/src/jvmTest/kotlin/okhttp3/WebPlatformUrlTestData.kt
```
class WebPlatformUrlTestData
  fun expectParseFailure()
  fun toString() → String
  fun load(source) → List<WebPlatformUrlTestDa
```

### okhttp/src/jvmTest/kotlin/okhttp3/WholeOperationTimeoutTest.kt
```
class WholeOperationTimeoutTest
  fun defaultConfigIsNoTimeout()
  fun configureClientDefault()
  fun timeoutWritingRequest()
  fun timeoutWritingRequestWithEnqueue()
  fun onFailure(call, e)
  fun onResponse(call, response)
  fun timeoutProcessing()
  fun timeoutProcessingWithEnqueue()
```

### okhttp/src/jvmTest/kotlin/okhttp3/internal/DoubleInetAddressDns.kt
```
class DoubleInetAddressDns
  fun lookup(hostname) → List<InetAddress>
```

### okhttp/src/jvmTest/kotlin/okhttp3/internal/HostnamesTest.kt
```
class HostnamesTest
  fun canonicalizeInetAddressNotMapped()
  fun canonicalizeInetAddressMapped()
  fun canonicalizeInetAddressIPv6RepresentationOfCompatibleIPV4()
  fun canonicalizeInetAddressIPv6RepresentationOfMappedIPV4()
  fun inet4AddressToAscii()
  fun testToCanonicalHost()
```

### okhttp/src/jvmTest/kotlin/okhttp3/internal/RecordingAuthenticator.kt
```
class RecordingAuthenticator
  fun getPasswordAuthentication() → PasswordAuthentication?
```

### okhttp/src/jvmTest/kotlin/okhttp3/internal/TagsTest.kt
```
class TagsTest
  fun emptyTags()
  fun singleElement()
  fun multipleElements()
  fun replaceFirstElement()
  fun replaceMiddleElement()
  fun replaceLastElement()
  fun removeFirstElement()
  fun removeMiddleElement()
```

### okhttp/src/jvmTest/kotlin/okhttp3/internal/UtilTest.kt
```
class UtilTest
  fun socketIsHealthy()
  fun testDurationTimeUnit()
  fun testDurationDuration()
```

### okhttp/src/jvmTest/kotlin/okhttp3/internal/authenticator/JavaNetAuthenticatorTest.kt
```
class JavaNetAuthenticatorTest
  fun setup()
  fun tearDown()
  fun testBasicAuth()
  fun noSupportForNonBasicAuth()
```

### okhttp/src/jvmTest/kotlin/okhttp3/internal/cache/DiskLruCacheTest.kt
```
class DiskLruCacheTest
  fun setUp()
  fun emptyCache()
  fun recoverFromInitializationFailure()
  fun validateKey()
```

### okhttp/src/jvmTest/kotlin/okhttp3/internal/concurrent/TaskLoggerTest.kt
```
class TaskLoggerTest
```

### okhttp/src/jvmTest/kotlin/okhttp3/internal/concurrent/TaskRunnerRealBackendTest.kt
```
class TaskRunnerRealBackendTest
```

### okhttp/src/jvmTest/kotlin/okhttp3/internal/concurrent/TaskRunnerTest.kt
```
class TaskRunnerTest
  fun tearDown()
  fun runOnce() → Long
  fun runOnce() → Long
```

### okhttp/src/jvmTest/kotlin/okhttp3/internal/connection/ConnectionPoolTest.kt
```
class ConnectionPoolTest
```

### okhttp/src/jvmTest/kotlin/okhttp3/internal/connection/FastFallbackExchangeFinderTest.kt
```
class FastFallbackExchangeFinderTest
  fun tearDown()
  fun takeConnectedConnection()
  fun takeConnectingConnection()
  fun firstPlanConnectedBeforeSecondPlan()
  fun secondPlanAlreadyConnected()
  fun secondPlanConnectedBeforeFirstPlan()
  fun thirdPlanAlreadyConnected()
```

### okhttp/src/jvmTest/kotlin/okhttp3/internal/connection/InetAddressOrderTest.kt
```
class InetAddressOrderTest
```

### okhttp/src/jvmTest/kotlin/okhttp3/internal/connection/RetryConnectionTest.kt
```
class RetryConnectionTest
```

### okhttp/src/jvmTest/kotlin/okhttp3/internal/connection/RouteSelectorTest.kt
```
class RouteSelectorTest
```

### okhttp/src/jvmTest/kotlin/okhttp3/internal/http/CancelTest.kt
```
class CancelTest
  fun setUp()
  fun configureServerSocket(serverSocket) → ServerSocket
  fun configureSocket(socket) → Socket
  fun cancelWritingRequestBody()
  fun contentType() → MediaType?
  fun writeTo(sink)
  fun cancelReadingResponseBody()
  fun cancelAndFollowup()
class CancelModelParamProvider
  fun arguments()
```

### okhttp/src/jvmTest/kotlin/okhttp3/internal/http/ExternalHttp2Example.kt
```
object ExternalHttp2Example
  fun main(args)
```

### okhttp/src/jvmTest/kotlin/okhttp3/internal/http/HttpDateTest.kt
```
class HttpDateTest
  fun setUp()
  fun tearDown()
  fun parseStandardFormats()
  fun format()
  fun parseNonStandardStrings()
```

### okhttp/src/jvmTest/kotlin/okhttp3/internal/http/HttpUpgradesTest.kt
```
class HttpUpgradesTest
  fun executeAndCheckUpgrade(request)
  fun upgrade()
  fun upgradeWithEmptyRequestBody()
  fun upgradeWithNonEmptyRequestBody()
  fun upgradeHttps()
  fun upgradeRefusedByServer()
  fun upgradeForbiddenOnHttp2()
  fun upgradesOnReusedConnection()
```

### okhttp/src/jvmTest/kotlin/okhttp3/internal/http/SocketFailureTest.kt
```
class SocketFailureTest
  fun connectionAcquired(call, connection)
  fun requestHeadersStart(call)
  fun socketFailureOnLargeRequestHeaders()
```

### okhttp/src/jvmTest/kotlin/okhttp3/internal/http/StatusLineTest.kt
```
class StatusLineTest
  fun parse()
  fun emptyMessage()
  fun emptyMessageAndNoLeadingSpace()
  fun shoutcast()
  fun missingProtocol()
  fun protocolVersions()
  fun nonThreeDigitCode()
  fun truncated()
```

### okhttp/src/jvmTest/kotlin/okhttp3/internal/http/ThreadInterruptTest.kt
```
class ThreadInterruptTest
  fun setUp()
  fun configureServerSocket(serverSocket) → ServerSocket
  fun configureSocket(socket) → Socket
  fun tearDown()
  fun interruptWritingRequestBody()
  fun contentType()
  fun writeTo(sink)
  fun interruptReadingResponseBody()
```

### okhttp/src/jvmTest/kotlin/okhttp3/internal/http2/BaseTestHandler.kt
```
class BaseTestHandler
  fun data(inFinished, streamId, source, length)
  fun headers(inFinished, streamId, associatedStreamId, headerBlock)
  fun rstStream(streamId, errorCode)
  fun settings(clearPrevious, settings)
  fun ackSettings()
  fun ping(ack, payload1, payload2)
  fun goAway(lastGoodStreamId, errorCode, debugData)
  fun windowUpdate(streamId, windowSizeIncrement)
```

### okhttp/src/jvmTest/kotlin/okhttp3/internal/http2/FrameLogTest.kt
```
class FrameLogTest
  fun exampleStream()
  fun windowUpdateFrames()
  fun flagOverlapOn0x1()
  fun flagOverlapOn0x4()
  fun flagOverlapOn0x20()
  fun allFormattedFlagsWithValidBits()
```

### okhttp/src/jvmTest/kotlin/okhttp3/internal/http2/HpackTest.kt
```
class HpackTest
  fun reset()
  fun largeHeaderValue()
  fun tooLargeToHPackIsStillEmitted()
  fun writerEviction()
  fun readerEviction()
  fun dynamicallyGrowsBeyond64Entries()
```

### okhttp/src/jvmTest/kotlin/okhttp3/internal/http2/Http2ConnectionTest.kt
```
class Http2ConnectionTest
```

### okhttp/src/jvmTest/kotlin/okhttp3/internal/http2/Http2Test.kt
```
class Http2Test
  fun headers(inFinished, streamId, associatedStreamId, headerBlock)
  fun priority(streamId, streamDependency, weight, exclusive)
  fun headers(inFinished, streamId, associatedStreamId, headerBlock)
  fun headers(inFinished, streamId, associatedStreamId, headerBlock)
```

### okhttp/src/jvmTest/kotlin/okhttp3/internal/http2/HttpOverHttp2Test.kt
```
class HttpOverHttp2Test
  fun setUp()
  fun get()
  fun get204Response()
  fun head()
  fun emptyResponse()
```

### okhttp/src/jvmTest/kotlin/okhttp3/internal/http2/HuffmanTest.kt
```
class HuffmanTest
  fun roundTripForRequestAndResponse()
```

### okhttp/src/jvmTest/kotlin/okhttp3/internal/http2/MockHttp2Peer.kt
```
class MockHttp2Peer
  fun setClient(client)
  fun acceptFrame()
  fun maxOutboundDataLength() → Int
  fun frameCount() → Int
  fun sendFrame() → Http2Writer
  fun truncateLastFrame(length) → Http2Writer
  fun takeFrame() → InFrame
  fun play()
```

### okhttp/src/jvmTest/kotlin/okhttp3/internal/http2/SettingsTest.kt
```
class SettingsTest
  fun unsetField()
  fun setFields()
  fun merge()
```

### okhttp/src/jvmTest/kotlin/okhttp3/internal/idn/IdnaMappingTableTest.kt
```
class IdnaMappingTableTest
  fun setUp()
```

### okhttp/src/jvmTest/kotlin/okhttp3/internal/idn/PunycodeTest.kt
```
class PunycodeTest
```

### okhttp/src/jvmTest/kotlin/okhttp3/internal/io/FaultyFileSystem.kt
```
class FaultyFileSystem
  fun setFaultyWrite(file, faulty)
  fun setFaultyDelete(file, faulty)
  fun setFaultyRename(file, faulty)
  fun atomicMove(source, target)
  fun delete(path, mustExist)
  fun deleteRecursively(fileOrDirectory, mustExist)
  fun appendingSink(file, mustExist) → Sink
  fun sink(file, mustCreate) → Sink
```

### okhttp/src/jvmTest/kotlin/okhttp3/internal/platform/Jdk8WithJettyBootPlatformTest.kt
```
class Jdk8WithJettyBootPlatformTest
  fun testBuildsWithJettyBoot()
  fun testNotBuildWithOther()
```

### okhttp/src/jvmTest/kotlin/okhttp3/internal/platform/Jdk9PlatformTest.kt
```
class Jdk9PlatformTest
  fun buildsWhenJdk9()
  fun buildsWhenJdk8()
  fun testToStringIsClassname()
  fun selectedProtocolIsNullWhenSslSocketThrowsExceptionForApplicationProtocol()
  fun getApplicationProtocol() → String
```

### okhttp/src/jvmTest/kotlin/okhttp3/internal/platform/PlatformTest.kt
```
class PlatformTest
  fun alwaysBuilds()
  fun defaultPrefix()
  fun testToStringIsClassname()
  fun testNotAndroid()
```

### okhttp/src/jvmTest/kotlin/okhttp3/internal/publicsuffix/PublicSuffixListGenerator.kt
```
class PublicSuffixListGenerator
  suspend fun import()
  fun writeOut(sink)
suspend fun main()
```

### okhttp/src/jvmTest/kotlin/okhttp3/internal/tls/CertificatePinnerChainValidationTest.kt
```
class CertificatePinnerChainValidationTest
  fun setup()
  fun pinRootNotPresentInChain()
  fun pinIntermediatePresentInChain()
```

### okhttp/src/jvmTest/kotlin/okhttp3/internal/tls/ClientAuthTest.kt
```
class ClientAuthTest
  fun setUp()
  fun clientAuthForWants()
  fun clientAuthForNeeds()
  fun clientAuthSkippedForNone()
```

### okhttp/src/jvmTest/kotlin/okhttp3/internal/tls/HostnameVerifierTest.kt
```
class HostnameVerifierTest
```

### okhttp/src/jvmTest/kotlin/okhttp3/internal/ws/MessageDeflaterInflaterTest.kt
```
class MessageDeflaterInflaterTest
```

### okhttp/src/jvmTest/kotlin/okhttp3/internal/ws/RealWebSocketTest.kt
```
class RealWebSocketTest
  fun setUp()
  fun tearDown()
  fun close()
  fun clientCloseThenMethodsReturnFalse()
  fun clientCloseWith0Fails()
  fun afterSocketClosedPingFailsWebSocket()
  fun socketClosedDuringMessageKillsWebSocket()
  fun serverCloseThenWritingPingSucceeds()
```

### okhttp/src/jvmTest/kotlin/okhttp3/internal/ws/WebSocketExtensionsTest.kt
```
class WebSocketExtensionsTest
  fun emptyHeader()
  fun noExtensionHeader()
  fun emptyExtension()
  fun unknownExtension()
  fun perMessageDeflate()
  fun emptyParameters()
  fun repeatedPerMessageDeflate()
  fun multiplePerMessageDeflateHeaders()
```

### okhttp/src/jvmTest/kotlin/okhttp3/internal/ws/WebSocketHttpTest.kt
```
class WebSocketHttpTest
  fun setUp()
  fun tearDown()
  fun textMessage()
  fun binaryMessage()
  fun serverMessage()
  fun throwingOnOpenFailsImmediately()
  fun onOpen(webSocket, response) → Unit
  fun throwingOnFailLogs()
```

### okhttp/src/jvmTest/kotlin/okhttp3/internal/ws/WebSocketReaderTest.kt
```
class WebSocketReaderTest
```

### okhttp/src/jvmTest/kotlin/okhttp3/internal/ws/WebSocketRecorder.kt
```
class WebSocketRecorder
  fun setNextEventDelegate(delegate)
  fun onOpen(webSocket, response)
  fun onMessage(webSocket, bytes)
  fun onMessage(webSocket, text)
  fun onClosing(webSocket, code, reason)
  fun onClosed(webSocket, code, reason)
  fun onFailure(webSocket, t, response)
  fun assertTextMessage(payload)
```

### okhttp/src/jvmTest/kotlin/okhttp3/internal/ws/WebSocketWriterTest.kt
```
class WebSocketWriterTest
```

## okhttp-brotli

### okhttp-brotli/src/main/kotlin/okhttp3/brotli/Brotli.kt
```
object Brotli
  fun decompress(compressedSource) → Source
```

### okhttp-brotli/src/test/java/okhttp3/brotli/BrotliInterceptorTest.kt
```
class BrotliInterceptorTest
  fun testUncompressBrotli()
```

### okhttp-brotli/src/test/java/okhttp3/brotli/BrotliTestMain.kt
```
fun main()
```

## okhttp-coroutines

### okhttp-coroutines/src/test/kotlin/okhttp3/coroutines/ExecuteAsyncTest.kt
```
class ExecuteAsyncTest
  suspend fun suspendCall()
  fun timeoutCall()
  fun cancelledCall()
  fun failedCall()
  fun responseClosedIfCoroutineCanceled()
  fun contentType()
  fun contentLength()
  fun source()
```

## okhttp-dnsoverhttps

### okhttp-dnsoverhttps/src/main/kotlin/okhttp3/dnsoverhttps/BootstrapDns.kt
```
class BootstrapDns
  fun lookup(hostname) → List<InetAddress>
```

### okhttp-dnsoverhttps/src/main/kotlin/okhttp3/dnsoverhttps/DnsOverHttps.kt
```
class DnsOverHttps
  fun lookup(hostname) → List<InetAddress>
  fun onFailure(call, e)
  fun onResponse(call, response)
```

### okhttp-dnsoverhttps/src/main/kotlin/okhttp3/dnsoverhttps/DnsRecordCodec.kt
```
object DnsRecordCodec
  fun encodeQuery(host, type) → ByteString
  fun decodeAnswers(hostname, byteString) → List<InetAddress>
```

### okhttp-dnsoverhttps/src/test/java/okhttp3/dnsoverhttps/DnsOverHttpsTest.kt
```
class DnsOverHttpsTest
  fun setUp()
  fun getOne()
  fun getIpv6()
  fun failure()
  fun failOnExcessiveResponse()
  fun failOnBadResponse()
```

### okhttp-dnsoverhttps/src/test/java/okhttp3/dnsoverhttps/DnsRecordCodecTest.kt
```
class DnsRecordCodecTest
  fun testGoogleDotComEncoding()
  fun testGoogleDotComEncodingWithIPv6()
  fun testGoogleDotComDecodingFromCloudflare()
  fun testGoogleDotComDecodingFromGoogle()
  fun testGoogleDotComDecodingFromGoogleIPv6()
  fun testGoogleDotComDecodingNxdomainFailure()
```

### okhttp-dnsoverhttps/src/test/java/okhttp3/dnsoverhttps/DohProviders.kt
```
object DohProviders
  fun buildCleanBrowsing(bootstrapClient) → DnsOverHttps
  fun providers(client, http2Only, workingOnly, getOnly) → List<DnsOverHttps>
```

### okhttp-dnsoverhttps/src/test/java/okhttp3/dnsoverhttps/TestDohMain.kt
```
fun main()
```

## okhttp-hpacktests

### okhttp-hpacktests/src/test/java/okhttp3/internal/http2/HpackDecodeInteropTest.kt
```
class HpackDecodeInteropTest
  fun testGoodDecoderInterop(story)
  fun arguments() → List<Any>
```

### okhttp-hpacktests/src/test/java/okhttp3/internal/http2/HpackDecodeTestBase.kt
```
class HpackDecodeTestBase
```

### okhttp-hpacktests/src/test/java/okhttp3/internal/http2/HpackRoundTripTest.kt
```
class HpackRoundTripTest
  fun arguments() → List<Any>
  fun testRoundTrip(story)
```

### okhttp-hpacktests/src/test/java/okhttp3/internal/http2/hpackjson/Case.kt
```
class Case
```

### okhttp-hpacktests/src/test/java/okhttp3/internal/http2/hpackjson/HpackJsonUtil.kt
```
object HpackJsonUtil
  fun storiesForCurrentDraft() → Array<String>
  fun readStories(testFolderName) → List<Story>
```

### okhttp-hpacktests/src/test/java/okhttp3/internal/http2/hpackjson/Story.kt
```
class Story
  fun toString()
```

## okhttp-idna-mapping-table

### okhttp-idna-mapping-table/src/main/kotlin/okhttp3/internal/idn/GenerateIdnaMappingTableCode.kt
```
fun main(vararg args)
fun loadIdnaMappingTableData() → IdnaMappingTableData
fun generateMappingTableFile(data) → FileSpec
```

### okhttp-idna-mapping-table/src/main/kotlin/okhttp3/internal/idn/MappedRange.kt
```
interface MappedRange
```

### okhttp-idna-mapping-table/src/main/kotlin/okhttp3/internal/idn/MappingTables.kt
```
fun buildIdnaMappingTableData(table) → IdnaMappingTableData
fun inlineDeltaOrNull(mapping) → MappedRange.InlineDelta?
fun sections(mappings) → Map<Int, List<MappedRange
fun mergeAdjacentDeltaMappedRanges(ranges) → MutableList<MappedRange>
fun withoutSectionSpans(mappings) → List<Mapping>
fun mergeAdjacentRanges(mappings) → List<Mapping>
fun canonicalizeType(type) → Int
```

### okhttp-idna-mapping-table/src/main/kotlin/okhttp3/internal/idn/SimpleIdnaMappingTable.kt
```
class SimpleIdnaMappingTable
  fun map(codePoint, sink) → Boolean
class Mapping
```

### okhttp-idna-mapping-table/src/test/kotlin/okhttp3/internal/idn/MappingTablesTest.kt
```
class MappingTablesTest
```

## okhttp-java-net-cookiejar

### okhttp-java-net-cookiejar/src/main/kotlin/okhttp3/java/net/cookiejar/JavaNetCookieJar.kt
```
class JavaNetCookieJar
  fun saveFromResponse(url, cookies)
  fun loadForRequest(url) → List<Cookie>
```

## okhttp-logging-interceptor

### okhttp-logging-interceptor/src/main/kotlin/okhttp3/logging/HttpLoggingInterceptor.kt
```
class HttpLoggingInterceptor
  fun log(message)
  fun log(message)
  fun redactHeader(name)
  fun redactQueryParams(vararg name)
  fun setLevel(level)
  fun getLevel() → Level
  fun intercept(chain) → Response
```

### okhttp-logging-interceptor/src/main/kotlin/okhttp3/logging/LoggingEventListener.kt
```
class LoggingEventListener
  fun callStart(call)
  fun dispatcherQueueStart(call, dispatcher)
  fun dispatcherQueueEnd(call, dispatcher)
  fun proxySelectStart(call, url)
  fun proxySelectEnd(call, url, proxies)
  fun dnsStart(call, domainName)
  fun dnsEnd(call, domainName, inetAddressList)
  fun connectStart(call, inetSocketAddress, proxy)
```

### okhttp-logging-interceptor/src/test/java/okhttp3/logging/HttpLoggingInterceptorTest.kt
```
class HttpLoggingInterceptorTest
  fun setUp()
  fun levelGetter()
  fun setLevelShouldReturnSameInstanceOfInterceptor()
  fun none()
  fun basicGet()
  fun basicPost()
  fun basicResponseBody()
  fun basicChunkedResponseBody()
```

### okhttp-logging-interceptor/src/test/java/okhttp3/logging/LoggingEventListenerTest.kt
```
class LoggingEventListenerTest
  fun setUp()
  fun get()
  fun post()
  fun secureGet()
```

## okhttp-osgi-tests

### okhttp-osgi-tests/src/test/kotlin/okhttp3/osgi/OsgiTest.kt
```
class OsgiTest
  fun setUp()
  fun testMainModuleWithSiblings()
```

## okhttp-sse

### okhttp-sse/src/main/kotlin/okhttp3/sse/EventSource.kt
```
interface EventSource
  fun request() → Request
  fun cancel()
  fun newEventSource(request, listener) → EventSource
```

### okhttp-sse/src/main/kotlin/okhttp3/sse/EventSourceListener.kt
```
class EventSourceListener
```

### okhttp-sse/src/main/kotlin/okhttp3/sse/EventSources.kt
```
object EventSources
  fun createFactory(client)
  fun createFactory(callFactory) → EventSource.Factory
  fun processResponse(response, listener)
```

### okhttp-sse/src/main/kotlin/okhttp3/sse/internal/RealEventSource.kt
```
class RealEventSource
  fun connect(callFactory)
  fun onResponse(call, response)
  fun processResponse(response)
  fun onFailure(call, e)
  fun request() → Request
  fun cancel()
  fun onEvent(id, type, data)
  fun onRetryChange(timeMs)
```

### okhttp-sse/src/main/kotlin/okhttp3/sse/internal/ServerSentEventReader.kt
```
class ServerSentEventReader
  fun onEvent(id, type, data)
  fun onRetryChange(timeMs)
  fun processNextEvent() → Boolean
```

### okhttp-sse/src/test/java/okhttp3/sse/internal/EventSourceHttpTest.kt
```
class EventSourceHttpTest
  fun after()
  fun event()
  fun cancelInEventShortCircuits()
  fun badContentType()
  fun badResponseCode()
  fun fullCallTimeoutDoesNotApplyOnceConnected()
  fun fullCallTimeoutAppliesToSetup()
  fun retainsAccept()
```

### okhttp-sse/src/test/java/okhttp3/sse/internal/EventSourceRecorder.kt
```
class EventSourceRecorder
  fun enqueueCancel()
  fun onOpen(eventSource, response)
  fun onEvent(eventSource, id, type, data)
  fun onClosed(eventSource)
  fun onFailure(eventSource, t, response)
  fun assertExhausted()
  fun assertEvent(id, type, data)
  fun assertOpen() → EventSource
```

### okhttp-sse/src/test/java/okhttp3/sse/internal/EventSourcesHttpTest.kt
```
class EventSourcesHttpTest
  fun after()
  fun processResponse()
  fun cancelShortCircuits()
  fun failureWith401IsReadable()
```

### okhttp-sse/src/test/java/okhttp3/sse/internal/ServerSentEventIteratorTest.kt
```
class ServerSentEventIteratorTest
  fun after()
  fun multiline()
  fun multilineCr()
  fun multilineCrLf()
  fun eventType()
  fun commentsIgnored()
  fun idCleared()
  fun nakedFieldNames()
```

## okhttp-testing-support

### okhttp-testing-support/src/main/kotlin/okhttp3/CallEvent.kt
```
class CallEvent
  fun closes(event) → Boolean
  fun closes(event) → Boolean
  fun closes(event) → Boolean
  fun closes(event) → Boolean
  fun closes(event) → Boolean
  fun closes(event) → Boolean
  fun closes(event) → Boolean
  fun closes(event) → Boolean
```

### okhttp-testing-support/src/main/kotlin/okhttp3/ClientRuleEventListener.kt
```
class ClientRuleEventListener
  fun create(call) → EventListener
  fun callStart(call)
  fun dispatcherQueueStart(call, dispatcher)
  fun dispatcherQueueEnd(call, dispatcher)
  fun proxySelectStart(call, url)
  fun proxySelectEnd(call, url, proxies)
  fun dnsStart(call, domainName)
  fun dnsEnd(call, domainName, inetAddressList)
```

### okhttp-testing-support/src/main/kotlin/okhttp3/ConnectionEvent.kt
```
class ConnectionEvent
  fun closes(event) → Boolean
  fun closes(event) → Boolean
  fun closes(event) → Boolean
```

### okhttp-testing-support/src/main/kotlin/okhttp3/DelegatingSSLSession.kt
```
class DelegatingSSLSession
  fun getId() → ByteArray
  fun getSessionContext() → SSLSessionContext
  fun getCreationTime() → Long
  fun getLastAccessedTime() → Long
  fun invalidate()
  fun isValid() → Boolean
  fun putValue(s, o)
  fun getValue(s) → Any
```

### okhttp-testing-support/src/main/kotlin/okhttp3/DelegatingSSLSocket.kt
```
class DelegatingSSLSocket
  fun shutdownInput()
  fun shutdownOutput()
  fun getSupportedCipherSuites() → Array<String>
  fun getEnabledCipherSuites() → Array<String>
  fun setEnabledCipherSuites(suites)
  fun getSupportedProtocols() → Array<String>
  fun getEnabledProtocols() → Array<String>
  fun setEnabledProtocols(protocols)
```

### okhttp-testing-support/src/main/kotlin/okhttp3/DelegatingSSLSocketFactory.kt
```
class DelegatingSSLSocketFactory
  fun createSocket() → SSLSocket
  fun createSocket(host, port) → SSLSocket
  fun createSocket(host, port, localAddress, localPort) → SSLSocket
  fun createSocket(host, port) → SSLSocket
  fun createSocket(host, port, localAddress, localPort) → SSLSocket
  fun getDefaultCipherSuites() → Array<String>
  fun getSupportedCipherSuites() → Array<String>
  fun createSocket(socket, host, port, autoClose) → SSLSocket
```

### okhttp-testing-support/src/main/kotlin/okhttp3/DelegatingServerSocketFactory.kt
```
class DelegatingServerSocketFactory
  fun createServerSocket() → ServerSocket
  fun createServerSocket(port) → ServerSocket
  fun createServerSocket(port, backlog) → ServerSocket
  fun createServerSocket(port, backlog, ifAddress) → ServerSocket
```

### okhttp-testing-support/src/main/kotlin/okhttp3/DelegatingSocketFactory.kt
```
class DelegatingSocketFactory
  fun createSocket() → Socket
  fun createSocket(host, port) → Socket
  fun createSocket(host, port, localAddress, localPort) → Socket
  fun createSocket(host, port) → Socket
  fun createSocket(host, port, localAddress, localPort) → Socket
```

### okhttp-testing-support/src/main/kotlin/okhttp3/EventListenerAdapter.kt
```
class EventListenerAdapter
  fun dispatcherQueueStart(call, dispatcher)
  fun dispatcherQueueEnd(call, dispatcher)
  fun proxySelectStart(call, url)
  fun proxySelectEnd(call, url, proxies)
  fun dnsStart(call, domainName)
  fun dnsEnd(call, domainName, inetAddressList)
  fun connectStart(call, inetSocketAddress, proxy)
  fun secureConnectStart(call)
```

### okhttp-testing-support/src/main/kotlin/okhttp3/EventListenerRelay.kt
```
class EventListenerRelay
```

### okhttp-testing-support/src/main/kotlin/okhttp3/EventRecorder.kt
```
class EventRecorder
  fun forbidLock(lock)
  fun takeEvent(eventClass, elapsedMs) → CallEvent
  fun recordedEventTypes()
  fun clearAllEvents()
  fun logEvent(e)
```

### okhttp-testing-support/src/main/kotlin/okhttp3/FailingCall.kt
```
class FailingCall
  fun request() → Request
  fun execute() → Response
  fun enqueue(responseCallback) → Unit
  fun cancel() → Unit
  fun isExecuted() → Boolean
  fun isCanceled() → Boolean
  fun timeout() → Timeout
  fun addEventListener(eventListener)
```

### okhttp-testing-support/src/main/kotlin/okhttp3/FakeDns.kt
```
class FakeDns
  fun clear(hostname) → FakeDns
  fun lookup(hostname, index) → InetAddress
  fun lookup(hostname) → List<InetAddress>
  fun assertRequests(vararg expectedHosts)
  fun allocate(count) → List<InetAddress>
  fun allocateIpv6(count) → List<InetAddress>
```

### okhttp-testing-support/src/main/kotlin/okhttp3/FakeProxySelector.kt
```
class FakeProxySelector
  fun addProxy(proxy) → FakeProxySelector
  fun select(uri) → List<Proxy>
  fun connectFailed(uri, sa, ioe)
```

### okhttp-testing-support/src/main/kotlin/okhttp3/FakeSSLSession.kt
```
class FakeSSLSession
  fun getApplicationBufferSize() → Int
  fun getCipherSuite() → String
  fun getCreationTime() → Long
  fun getId() → ByteArray
  fun getLastAccessedTime() → Long
  fun getLocalCertificates() → Array<Certificate>
  fun getLocalPrincipal() → Principal
  fun getPacketBufferSize() → Int
```

### okhttp-testing-support/src/main/kotlin/okhttp3/ForwardingRequestBody.kt
```
class ForwardingRequestBody
  fun delegate() → RequestBody
  fun contentType() → MediaType?
  fun contentLength() → Long
  fun writeTo(sink)
  fun isDuplex() → Boolean
  fun toString() → String
```

### okhttp-testing-support/src/main/kotlin/okhttp3/ForwardingResponseBody.kt
```
class ForwardingResponseBody
  fun delegate() → ResponseBody
  fun contentType() → MediaType?
  fun contentLength() → Long
  fun source() → BufferedSource
  fun toString() → String
```

### okhttp-testing-support/src/main/kotlin/okhttp3/JsseDebugLogging.kt
```
object JsseDebugLogging
  fun toString() → String
  fun enableJsseDebugLogging(debugHandler)
  fun publish(record)
  fun flush()
  fun close()
```

### okhttp-testing-support/src/main/kotlin/okhttp3/OkHttpClientTestRule.kt
```
class OkHttpClientTestRule
  fun publish(record)
  fun flush()
  fun close()
  fun wrap(eventListener)
  fun wrap(eventRecorder)
  fun wrap(eventListenerFactory)
  fun newClient() → OkHttpClient
```

### okhttp-testing-support/src/main/kotlin/okhttp3/OkHttpDebugLogging.kt
```
object OkHttpDebugLogging
  fun enableHttp2()
  fun enableTaskRunner()
  fun logHandler()
  fun format(record)
  fun enable(loggerClass, handler)
  fun enable(loggerClass)
```

### okhttp-testing-support/src/main/kotlin/okhttp3/RecordingConnectionListener.kt
```
class RecordingConnectionListener
  fun forbidLock(lock)
  fun takeEvent(eventClass, elapsedMs) → ConnectionEvent
  fun recordedEventTypes()
  fun clearAllEvents()
  fun connectStart(route, call)
  fun connectFailed(route, call, failure)
  fun connectEnd(connection, route, call)
  fun connectionClosed(connection)
```

### okhttp-testing-support/src/main/kotlin/okhttp3/RecordingCookieJar.kt
```
class RecordingCookieJar
  fun enqueueRequestCookies(vararg cookies)
  fun takeResponseCookies() → List<Cookie>
  fun assertResponseCookies(vararg cookies)
  fun saveFromResponse(url, cookies)
  fun loadForRequest(url) → List<Cookie>
```

### okhttp-testing-support/src/main/kotlin/okhttp3/RecordingHostnameVerifier.kt
```
class RecordingHostnameVerifier
```

### okhttp-testing-support/src/main/kotlin/okhttp3/SimpleProvider.kt
```
class SimpleProvider
  fun provideArguments(context)
```

### okhttp-testing-support/src/main/kotlin/okhttp3/SpecificHostSocketFactory.kt
```
class SpecificHostSocketFactory
  fun createSocket() → Socket
  fun connect(endpoint, timeout)
```

### okhttp-testing-support/src/main/kotlin/okhttp3/TestUtilCommon.kt
```
fun String(vararg codePoints) → String
```

### okhttp-testing-support/src/main/kotlin/okhttp3/TestUtilJvm.kt
```
object TestUtil
  fun headerEntries(vararg elements) → List<Header>
  fun repeat(c, count) → String
  fun fragmentBuffer(buffer) → Buffer
  fun awaitGarbageCollection()
  fun assumeNetwork()
  fun assumeNotWindows()
  fun threadFactory(name) → ThreadFactory
  fun newThread(runnable) → Thread
fun getEnv(name)
```

### okhttp-testing-support/src/main/kotlin/okhttp3/TestValueFactory.kt
```
class TestValueFactory
  fun newConnection(pool, route, idleAtNanos, taskRunner) → RealConnection
  fun newConnectionPool(taskRunner, maxIdleConnections) → RealConnectionPool
  fun newAddress(uriHost, uriPort, proxy, proxySelector) → Address
  fun newHttpsAddress(uriHost, uriPort, proxy, proxySelector, sslSocketFactory, hostnameVerifier) → Address
  fun newRoute(address)
  fun newChain(call) → RealInterceptorChain
  fun newRoutePlanner(client, address)
```

### okhttp-testing-support/src/main/kotlin/okhttp3/UppercaseRequestInterceptor.kt
```
class UppercaseRequestInterceptor
  fun intercept(chain) → Response
  fun writeTo(sink)
  fun write(source, byteCount)
```

### okhttp-testing-support/src/main/kotlin/okhttp3/UppercaseResponseInterceptor.kt
```
class UppercaseResponseInterceptor
  fun intercept(chain) → Response
  fun source() → BufferedSource
  fun read(sink, byteCount) → Long
```

### okhttp-testing-support/src/main/kotlin/okhttp3/internal/RecordingOkAuthenticator.kt
```
class RecordingOkAuthenticator
  fun onlyResponse()
  fun onlyRoute()
  fun authenticate(route, response) → Request?
```

### okhttp-testing-support/src/main/kotlin/okhttp3/internal/concurrent/TaskFaker.kt
```
class TaskFaker
  fun execute(taskRunner, runnable)
  fun nanoTime()
  fun coordinatorNotify(taskRunner)
  fun start()
  fun coordinatorWait(taskRunner, nanos)
  fun runTasks()
  fun advanceUntil(newTime)
  fun assertNoMoreTasks()
```

### okhttp-testing-support/src/main/kotlin/okhttp3/internal/duplex/AsyncRequestBody.kt
```
class AsyncRequestBody
  fun contentType() → MediaType?
  fun writeTo(sink)
  fun isDuplex() → Boolean
  fun takeSink() → BufferedSink
  fun assertNoMoreSinks()
```

### okhttp-testing-support/src/main/kotlin/okhttp3/internal/duplex/MockSocketHandler.kt
```
class MockSocketHandler
  fun receiveRequest(expected)
  fun exhaustRequest()
  fun cancelStream()
  fun requestIOException()
  fun sendResponse(s, responseSent)
  fun exhaustResponse()
  fun sleep(duration, unit)
  fun handle(socket)
```

### okhttp-testing-support/src/main/kotlin/okhttp3/internal/http/RecordingProxySelector.kt
```
class RecordingProxySelector
  fun select(uri) → List<Proxy>
  fun assertRequests(vararg expectedUris)
  fun connectFailed(uri, sa, ioe)
  fun toString()
```

### okhttp-testing-support/src/main/kotlin/okhttp3/okio/LoggingFilesystem.kt
```
class LoggingFilesystem
  fun log(line)
  fun appendingSink(file, mustExist) → Sink
  fun atomicMove(source, target)
  fun createDirectory(dir, mustCreate)
  fun delete(path, mustExist)
  fun sink(file, mustCreate) → Sink
  fun source(file) → Source
```

### okhttp-testing-support/src/main/kotlin/okhttp3/testing/PlatformRule.kt
```
class PlatformRule
  fun beforeEach(context)
  fun afterEach(context)
  fun interceptTestMethod(invocation, invocationContext, extensionContext)
  fun setupPlatform()
  fun resetPlatform()
  fun expectFailureOnConscryptPlatform()
  fun expectFailureOnCorrettoPlatform()
  fun expectFailureOnOpenJSSEPlatform()
```

### okhttp-testing-support/src/main/kotlin/okhttp3/testing/PlatformVersion.kt
```
object PlatformVersion
  fun getJvmSpecVersion() → String
```

### okhttp-testing-support/src/test/kotlin/okhttp3/OkHttpClientTestRuleTest.kt
```
class OkHttpClientTestRuleTest
  fun run() → Unit
```

### okhttp-testing-support/src/test/kotlin/okhttp3/testing/PlatformRuleTest.kt
```
class PlatformRuleTest
  fun testMode()
  fun testGreenCase()
  fun testGreenCaseFailingOnLater()
  fun failureCase()
```

## okhttp-tls

### okhttp-tls/src/main/kotlin/okhttp3/tls/HandshakeCertificates.kt
```
class HandshakeCertificates
  fun keyManager() → X509KeyManager
  fun trustManager() → X509TrustManager
  fun sslSocketFactory() → SSLSocketFactory
  fun sslContext() → SSLContext
  fun heldCertificate(heldCertificate, vararg intermediates)
  fun addTrustedCertificate(certificate)
  fun addPlatformTrustedCertificates()
  fun addInsecureHost(hostname)
```

### okhttp-tls/src/main/kotlin/okhttp3/tls/HeldCertificate.kt
```
class HeldCertificate
  fun certificate() → X509Certificate
  fun keyPair() → KeyPair
  fun certificatePem() → String
  fun privateKeyPkcs8Pem() → String
  fun privateKeyPkcs1Pem() → String
  fun validityInterval(notBefore, notAfter)
  fun duration(duration, unit)
  fun addSubjectAlternativeName(altName)
```

### okhttp-tls/src/main/kotlin/okhttp3/tls/internal/InsecureAndroidTrustManager.kt
```
class InsecureAndroidTrustManager
  fun checkServerTrusted(chain, authType, host) → List<Certificate>
  fun getAcceptedIssuers() → Array<X509Certificate>
  fun checkClientTrusted(chain, authType)
  fun checkServerTrusted(chain, authType)
```

### okhttp-tls/src/main/kotlin/okhttp3/tls/internal/InsecureExtendedTrustManager.kt
```
class InsecureExtendedTrustManager
  fun getAcceptedIssuers() → Array<X509Certificate>
  fun checkServerTrusted(chain, authType, socket)
  fun checkServerTrusted(chain, authType, engine)
  fun checkServerTrusted(chain, authType)
  fun checkClientTrusted(chain, authType)
  fun checkClientTrusted(chain, authType, engine)
  fun checkClientTrusted(chain, authType, socket)
```

### okhttp-tls/src/main/kotlin/okhttp3/tls/internal/TlsUtil.kt
```
object TlsUtil
  fun localhost() → HandshakeCertificates
  fun newTrustManager(keyStoreType, trustedCertificates, insecureHosts) → X509TrustManager
  fun newKeyManager(keyStoreType, heldCertificate, vararg intermediates) → X509KeyManager
```

### okhttp-tls/src/main/kotlin/okhttp3/tls/internal/der/Adapters.kt
```
object Adapters
  fun decode(reader) → Boolean
  fun encode(writer, value)
  fun decode(reader) → Long
  fun encode(writer, value)
  fun decode(reader) → BigInteger
  fun encode(writer, value)
  fun decode(reader) → BitString
  fun encode(writer, value)
```

### okhttp-tls/src/main/kotlin/okhttp3/tls/internal/der/AnyValue.kt
```
class AnyValue
  fun hashCode() → Int
```

### okhttp-tls/src/main/kotlin/okhttp3/tls/internal/der/BasicDerAdapter.kt
```
class BasicDerAdapter
  fun matches(header) → Boolean
  fun fromDer(reader) → T
  fun toDer(writer, value)
  fun withTag(tagClass, tag) → BasicDerAdapter<T>
  fun optional(defaultValue) → BasicDerAdapter<T>
  fun asTypeHint() → BasicDerAdapter<T>
  fun hashCode() → Int
  fun toString() → String
```

### okhttp-tls/src/main/kotlin/okhttp3/tls/internal/der/BitString.kt
```
class BitString
  fun hashCode() → Int
```

### okhttp-tls/src/main/kotlin/okhttp3/tls/internal/der/Certificate.kt
```
class Certificate
  fun checkSignature(issuer) → Boolean
  fun toX509Certificate() → X509Certificate
class TbsCertificate
  fun hashCode() → Int
class AlgorithmIdentifier
  fun hashCode() → Int
class SubjectPublicKeyInfo
  fun hashCode() → Int
```

### okhttp-tls/src/main/kotlin/okhttp3/tls/internal/der/CertificateAdapters.kt
```
object CertificateAdapters
  fun matches(header) → Boolean
  fun fromDer(reader) → Long
  fun toDer(writer, value)
```

### okhttp-tls/src/main/kotlin/okhttp3/tls/internal/der/DerAdapter.kt
```
interface DerAdapter
  fun matches(header) → Boolean
  fun fromDer(reader) → T
  fun fromDer(byteString) → T
  fun toDer(writer, value)
  fun toDer(value) → ByteString
  fun withExplicitBox(tagClass, tag, forceConstructed) → BasicDerAdapter<T>
  fun decode(reader) → T
  fun encode(writer, value)
```

### okhttp-tls/src/main/kotlin/okhttp3/tls/internal/der/DerHeader.kt
```
class DerHeader
  fun hashCode() → Int
  fun toString() → String
```

### okhttp-tls/src/main/kotlin/okhttp3/tls/internal/der/DerReader.kt
```
class DerReader
  fun hasNext() → Boolean
  fun peekHeader() → DerHeader?
  fun readHeader() → DerHeader
  fun readBoolean() → Boolean
  fun readBigInteger() → BigInteger
```

### okhttp-tls/src/main/kotlin/okhttp3/tls/internal/der/DerWriter.kt
```
class DerWriter
  fun write(name, tagClass, tag, block)
  fun writeBoolean(b)
  fun writeBigInteger(value)
  fun writeLong(v)
  fun writeBitString(bitString)
  fun writeOctetString(byteString)
  fun writeUtf8(value)
  fun writeObjectIdentifier(s)
```

### okhttp-tls/src/main/kotlin/okhttp3/tls/internal/der/ObjectIdentifiers.kt
```
object ObjectIdentifiers
```

### okhttp-tls/src/test/java/okhttp3/tls/CertificatesJavaTest.java
```
class CertificatesJavaTest
  testRoundtrip() → void
```

### okhttp-tls/src/test/java/okhttp3/tls/CertificatesTest.kt
```
class CertificatesTest
```

### okhttp-tls/src/test/java/okhttp3/tls/HandshakeCertificatesTest.kt
```
class HandshakeCertificatesTest
```

### okhttp-tls/src/test/java/okhttp3/tls/HeldCertificateTest.kt
```
class HeldCertificateTest
  fun defaultCertificate()
  fun customInterval()
  fun customDuration()
  fun subjectAlternativeNames()
  fun commonName()
  fun organizationalUnit()
  fun pems()
```

### okhttp-tls/src/test/java/okhttp3/tls/internal/der/DerCertificatesTest.kt
```
class DerCertificatesTest
```

### okhttp-tls/src/test/java/okhttp3/tls/internal/der/DerTest.kt
```
class DerTest
```

## okhttp-urlconnection

### okhttp-urlconnection/src/main/kotlin/okhttp3/JavaNetAuthenticator.kt
```
class JavaNetAuthenticator
  fun authenticate(route, response) → Request?
```

### okhttp-urlconnection/src/main/kotlin/okhttp3/JavaNetCookieJar.kt
```
class JavaNetCookieJar
```

## okhttp-zstd

### okhttp-zstd/src/main/kotlin/okhttp3/zstd/Zstd.kt
```
object Zstd
  fun decompress(compressedSource) → Source
```

### okhttp-zstd/src/test/java/okhttp3/zstd/ZstdInterceptorJavaTest.java
```
class ZstdInterceptorJavaTest
  testConstructor() → void
```

### okhttp-zstd/src/test/java/okhttp3/zstd/ZstdInterceptorTest.kt
```
class ZstdInterceptorTest
  fun testDecompressZstd()
```

### okhttp-zstd/src/test/java/okhttp3/zstd/ZstdTestMain.kt
```
fun main()
```

## regression-test

### regression-test/src/androidTest/java/okhttp/regression/IssueReproductionTest.java
```
class IssueReproductionTest
```

### regression-test/src/androidTest/java/okhttp/regression/LetsEncryptTest.java
```
class LetsEncryptTest
```

### regression-test/src/androidTest/java/okhttp/regression/compare/AndroidHttpEngineTest.kt
```
class AndroidHttpEngineTest
  fun tearDown()
  fun get()
  fun onRedirectReceived(request, info, newLocationUrl)
  fun onResponseStarted(request, info)
  fun onReadCompleted(request, info, byteBuffer)
  fun onSucceeded(request, info)
  fun onFailed(request, info, error)
```

### regression-test/src/androidTest/java/okhttp/regression/compare/ApacheHttpClientHttp2Test.kt
```
class ApacheHttpClientHttp2Test
  fun testHttp2()
object LoggingCallback
  fun completed(response)
  fun failed(ex)
  fun cancelled()
```

### regression-test/src/androidTest/java/okhttp/regression/compare/ApacheHttpClientTest.kt
```
class ApacheHttpClientTest
```

### regression-test/src/androidTest/java/okhttp/regression/compare/OkHttpClientTest.java
```
class OkHttpClientTest
```
