

## Auto-generated signatures
<!-- Updated by gen-context.js -->
<!-- Generated by SigMap gen-context.js v3.3.2 -->
<!-- Updated: 2026-04-14T06:21:21.824Z -->
<!-- Do not edit below — regenerate with: node gen-context.js -->

# Code signatures

<!-- Generated by SigMap gen-context.js v3.3.2 -->
<!-- DO NOT EDIT below the marker line — run gen-context.js to regenerate -->

# Code signatures

## todos
```
okhttp/src/jvmTest/kotlin/okhttp3/internal/http2/Http2Test.kt:703  # TODO: real max is based on settings between 16384 and 16777215
okhttp/src/jvmTest/kotlin/okhttp3/internal/http2/HttpOverHttp2Test.kt:166  # TODO: reenable after https://github.com/square/okhttp/issues/8206
okhttp-dnsoverhttps/src/test/java/okhttp3/dnsoverhttps/DnsOverHttpsTest.kt:176  # TODO: GET preferred order - with tests to confirm this
okhttp-testing-support/src/main/kotlin/okhttp3/internal/concurrent/TaskFaker.kt:362  # TODO: report compiler bug
okhttp-tls/src/main/kotlin/okhttp3/tls/internal/der/DerReader.kt:308  # TODO: (jwilson): detect overflow.
regression-test/src/androidTest/java/okhttp/regression/compare/ApacheHttpClientTest.kt:47  # TODO: enable ALPN later
```

## okhttp

### okhttp/src/jvmTest/kotlin/okhttp3/internal/http/ThreadInterruptTest.kt
```
class ThreadInterruptTest
  fun setUp()
  fun configureServerSocket(serverSocket) → ServerSocket
  fun configureSocket(socket) → Socket
  fun tearDown()
  fun interruptWritingRequestBody()
  fun contentType()
  fun writeTo(sink)
  fun interruptReadingResponseBody()
```

### okhttp/src/jvmTest/kotlin/okhttp3/internal/http2/BaseTestHandler.kt
```
class BaseTestHandler
  fun data(inFinished, streamId, source, length)
  fun headers(inFinished, streamId, associatedStreamId, headerBlock)
  fun rstStream(streamId, errorCode)
  fun settings(clearPrevious, settings)
  fun ackSettings()
  fun ping(ack, payload1, payload2)
  fun goAway(lastGoodStreamId, errorCode, debugData)
  fun windowUpdate(streamId, windowSizeIncrement)
```

### okhttp/src/jvmTest/kotlin/okhttp3/internal/http2/FrameLogTest.kt
```
class FrameLogTest
  fun exampleStream()
  fun windowUpdateFrames()
  fun flagOverlapOn0x1()
  fun flagOverlapOn0x4()
  fun flagOverlapOn0x20()
  fun allFormattedFlagsWithValidBits()
```

### okhttp/src/jvmTest/kotlin/okhttp3/internal/http2/HpackTest.kt
```
class HpackTest
  fun reset()
  fun largeHeaderValue()
  fun tooLargeToHPackIsStillEmitted()
  fun writerEviction()
  fun readerEviction()
  fun dynamicallyGrowsBeyond64Entries()
```

### okhttp/src/jvmTest/kotlin/okhttp3/internal/http2/Http2ConnectionTest.kt
```
class Http2ConnectionTest
```

### okhttp/src/jvmTest/kotlin/okhttp3/internal/http2/Http2Test.kt
```
class Http2Test
  fun headers(inFinished, streamId, associatedStreamId, headerBlock)
  fun priority(streamId, streamDependency, weight, exclusive)
  fun headers(inFinished, streamId, associatedStreamId, headerBlock)
  fun headers(inFinished, streamId, associatedStreamId, headerBlock)
```

### okhttp/src/jvmTest/kotlin/okhttp3/internal/http2/HttpOverHttp2Test.kt
```
class HttpOverHttp2Test
  fun setUp()
  fun get()
  fun get204Response()
  fun head()
  fun emptyResponse()
```

### okhttp/src/jvmTest/kotlin/okhttp3/internal/http2/HuffmanTest.kt
```
class HuffmanTest
  fun roundTripForRequestAndResponse()
```

### okhttp/src/jvmTest/kotlin/okhttp3/internal/http2/MockHttp2Peer.kt
```
class MockHttp2Peer
  fun setClient(client)
  fun acceptFrame()
  fun maxOutboundDataLength() → Int
  fun frameCount() → Int
  fun sendFrame() → Http2Writer
  fun truncateLastFrame(length) → Http2Writer
  fun takeFrame() → InFrame
  fun play()
```

### okhttp/src/jvmTest/kotlin/okhttp3/internal/http2/SettingsTest.kt
```
class SettingsTest
  fun unsetField()
  fun setFields()
  fun merge()
```

### okhttp/src/jvmTest/kotlin/okhttp3/internal/idn/IdnaMappingTableTest.kt
```
class IdnaMappingTableTest
  fun setUp()
```

### okhttp/src/jvmTest/kotlin/okhttp3/internal/idn/PunycodeTest.kt
```
class PunycodeTest
```

### okhttp/src/jvmTest/kotlin/okhttp3/internal/io/FaultyFileSystem.kt
```
class FaultyFileSystem
  fun setFaultyWrite(file, faulty)
  fun setFaultyDelete(file, faulty)
  fun setFaultyRename(file, faulty)
  fun atomicMove(source, target)
  fun delete(path, mustExist)
  fun deleteRecursively(fileOrDirectory, mustExist)
  fun appendingSink(file, mustExist) → Sink
  fun sink(file, mustCreate) → Sink
```

### okhttp/src/jvmTest/kotlin/okhttp3/internal/platform/Jdk8WithJettyBootPlatformTest.kt
```
class Jdk8WithJettyBootPlatformTest
  fun testBuildsWithJettyBoot()
  fun testNotBuildWithOther()
```

### okhttp/src/jvmTest/kotlin/okhttp3/internal/platform/Jdk9PlatformTest.kt
```
class Jdk9PlatformTest
  fun buildsWhenJdk9()
  fun buildsWhenJdk8()
  fun testToStringIsClassname()
  fun selectedProtocolIsNullWhenSslSocketThrowsExceptionForApplicationProtocol()
  fun getApplicationProtocol() → String
```

### okhttp/src/jvmTest/kotlin/okhttp3/internal/platform/PlatformTest.kt
```
class PlatformTest
  fun alwaysBuilds()
  fun defaultPrefix()
  fun testToStringIsClassname()
  fun testNotAndroid()
```

### okhttp/src/jvmTest/kotlin/okhttp3/internal/publicsuffix/PublicSuffixListGenerator.kt
```
class PublicSuffixListGenerator
  suspend fun import()
  fun writeOut(sink)
suspend fun main()
```

### okhttp/src/jvmTest/kotlin/okhttp3/internal/tls/CertificatePinnerChainValidationTest.kt
```
class CertificatePinnerChainValidationTest
  fun setup()
  fun pinRootNotPresentInChain()
  fun pinIntermediatePresentInChain()
```

### okhttp/src/jvmTest/kotlin/okhttp3/internal/tls/ClientAuthTest.kt
```
class ClientAuthTest
  fun setUp()
  fun clientAuthForWants()
  fun clientAuthForNeeds()
  fun clientAuthSkippedForNone()
```

### okhttp/src/jvmTest/kotlin/okhttp3/internal/tls/HostnameVerifierTest.kt
```
class HostnameVerifierTest
```

### okhttp/src/jvmTest/kotlin/okhttp3/internal/ws/MessageDeflaterInflaterTest.kt
```
class MessageDeflaterInflaterTest
```

### okhttp/src/jvmTest/kotlin/okhttp3/internal/ws/RealWebSocketTest.kt
```
class RealWebSocketTest
  fun setUp()
  fun tearDown()
  fun close()
  fun clientCloseThenMethodsReturnFalse()
  fun clientCloseWith0Fails()
  fun afterSocketClosedPingFailsWebSocket()
  fun socketClosedDuringMessageKillsWebSocket()
  fun serverCloseThenWritingPingSucceeds()
```

### okhttp/src/jvmTest/kotlin/okhttp3/internal/ws/WebSocketExtensionsTest.kt
```
class WebSocketExtensionsTest
  fun emptyHeader()
  fun noExtensionHeader()
  fun emptyExtension()
  fun unknownExtension()
  fun perMessageDeflate()
  fun emptyParameters()
  fun repeatedPerMessageDeflate()
  fun multiplePerMessageDeflateHeaders()
```

### okhttp/src/jvmTest/kotlin/okhttp3/internal/ws/WebSocketHttpTest.kt
```
class WebSocketHttpTest
  fun setUp()
  fun tearDown()
  fun textMessage()
  fun binaryMessage()
  fun serverMessage()
  fun throwingOnOpenFailsImmediately()
  fun onOpen(webSocket, response) → Unit
  fun throwingOnFailLogs()
```

### okhttp/src/jvmTest/kotlin/okhttp3/internal/ws/WebSocketReaderTest.kt
```
class WebSocketReaderTest
```

### okhttp/src/jvmTest/kotlin/okhttp3/internal/ws/WebSocketRecorder.kt
```
class WebSocketRecorder
  fun setNextEventDelegate(delegate)
  fun onOpen(webSocket, response)
  fun onMessage(webSocket, bytes)
  fun onMessage(webSocket, text)
  fun onClosing(webSocket, code, reason)
  fun onClosed(webSocket, code, reason)
  fun onFailure(webSocket, t, response)
  fun assertTextMessage(payload)
```

### okhttp/src/jvmTest/kotlin/okhttp3/internal/ws/WebSocketWriterTest.kt
```
class WebSocketWriterTest
```

## okhttp-brotli

### okhttp-brotli/src/main/kotlin/okhttp3/brotli/Brotli.kt
```
object Brotli
  fun decompress(compressedSource) → Source
```

### okhttp-brotli/src/test/java/okhttp3/brotli/BrotliInterceptorTest.kt
```
class BrotliInterceptorTest
  fun testUncompressBrotli()
```

### okhttp-brotli/src/test/java/okhttp3/brotli/BrotliTestMain.kt
```
fun main()
```

## okhttp-coroutines

### okhttp-coroutines/src/test/kotlin/okhttp3/coroutines/ExecuteAsyncTest.kt
```
class ExecuteAsyncTest
  suspend fun suspendCall()
  fun timeoutCall()
  fun cancelledCall()
  fun failedCall()
  fun responseClosedIfCoroutineCanceled()
  fun contentType()
  fun contentLength()
  fun source()
```

## okhttp-dnsoverhttps

### okhttp-dnsoverhttps/src/main/kotlin/okhttp3/dnsoverhttps/BootstrapDns.kt
```
class BootstrapDns
  fun lookup(hostname) → List<InetAddress>
```

### okhttp-dnsoverhttps/src/main/kotlin/okhttp3/dnsoverhttps/DnsOverHttps.kt
```
class DnsOverHttps
  fun lookup(hostname) → List<InetAddress>
  fun onFailure(call, e)
  fun onResponse(call, response)
```

### okhttp-dnsoverhttps/src/main/kotlin/okhttp3/dnsoverhttps/DnsRecordCodec.kt
```
object DnsRecordCodec
  fun encodeQuery(host, type) → ByteString
  fun decodeAnswers(hostname, byteString) → List<InetAddress>
```

### okhttp-dnsoverhttps/src/test/java/okhttp3/dnsoverhttps/DnsOverHttpsTest.kt
```
class DnsOverHttpsTest
  fun setUp()
  fun getOne()
  fun getIpv6()
  fun failure()
  fun failOnExcessiveResponse()
  fun failOnBadResponse()
```

### okhttp-dnsoverhttps/src/test/java/okhttp3/dnsoverhttps/DnsRecordCodecTest.kt
```
class DnsRecordCodecTest
  fun testGoogleDotComEncoding()
  fun testGoogleDotComEncodingWithIPv6()
  fun testGoogleDotComDecodingFromCloudflare()
  fun testGoogleDotComDecodingFromGoogle()
  fun testGoogleDotComDecodingFromGoogleIPv6()
  fun testGoogleDotComDecodingNxdomainFailure()
```

### okhttp-dnsoverhttps/src/test/java/okhttp3/dnsoverhttps/DohProviders.kt
```
object DohProviders
  fun buildCleanBrowsing(bootstrapClient) → DnsOverHttps
  fun providers(client, http2Only, workingOnly, getOnly) → List<DnsOverHttps>
```

### okhttp-dnsoverhttps/src/test/java/okhttp3/dnsoverhttps/TestDohMain.kt
```
fun main()
```

## okhttp-hpacktests

### okhttp-hpacktests/src/test/java/okhttp3/internal/http2/HpackDecodeInteropTest.kt
```
class HpackDecodeInteropTest
  fun testGoodDecoderInterop(story)
  fun arguments() → List<Any>
```

### okhttp-hpacktests/src/test/java/okhttp3/internal/http2/HpackDecodeTestBase.kt
```
class HpackDecodeTestBase
```

### okhttp-hpacktests/src/test/java/okhttp3/internal/http2/HpackRoundTripTest.kt
```
class HpackRoundTripTest
  fun arguments() → List<Any>
  fun testRoundTrip(story)
```

### okhttp-hpacktests/src/test/java/okhttp3/internal/http2/hpackjson/Case.kt
```
class Case
```

### okhttp-hpacktests/src/test/java/okhttp3/internal/http2/hpackjson/HpackJsonUtil.kt
```
object HpackJsonUtil
  fun storiesForCurrentDraft() → Array<String>
  fun readStories(testFolderName) → List<Story>
```

### okhttp-hpacktests/src/test/java/okhttp3/internal/http2/hpackjson/Story.kt
```
class Story
  fun toString()
```

## okhttp-idna-mapping-table

### okhttp-idna-mapping-table/src/main/kotlin/okhttp3/internal/idn/GenerateIdnaMappingTableCode.kt
```
fun main(vararg args)
fun loadIdnaMappingTableData() → IdnaMappingTableData
fun generateMappingTableFile(data) → FileSpec
```

### okhttp-idna-mapping-table/src/main/kotlin/okhttp3/internal/idn/MappedRange.kt
```
interface MappedRange
```

### okhttp-idna-mapping-table/src/main/kotlin/okhttp3/internal/idn/MappingTables.kt
```
fun buildIdnaMappingTableData(table) → IdnaMappingTableData
fun inlineDeltaOrNull(mapping) → MappedRange.InlineDelta?
fun sections(mappings) → Map<Int, List<MappedRange
fun mergeAdjacentDeltaMappedRanges(ranges) → MutableList<MappedRange>
fun withoutSectionSpans(mappings) → List<Mapping>
fun mergeAdjacentRanges(mappings) → List<Mapping>
fun canonicalizeType(type) → Int
```

### okhttp-idna-mapping-table/src/main/kotlin/okhttp3/internal/idn/SimpleIdnaMappingTable.kt
```
class SimpleIdnaMappingTable
  fun map(codePoint, sink) → Boolean
class Mapping
```

### okhttp-idna-mapping-table/src/test/kotlin/okhttp3/internal/idn/MappingTablesTest.kt
```
class MappingTablesTest
```

## okhttp-java-net-cookiejar

### okhttp-java-net-cookiejar/src/main/kotlin/okhttp3/java/net/cookiejar/JavaNetCookieJar.kt
```
class JavaNetCookieJar
  fun saveFromResponse(url, cookies)
  fun loadForRequest(url) → List<Cookie>
```

## okhttp-logging-interceptor

### okhttp-logging-interceptor/src/main/kotlin/okhttp3/logging/HttpLoggingInterceptor.kt
```
class HttpLoggingInterceptor
  fun log(message)
  fun log(message)
  fun redactHeader(name)
  fun redactQueryParams(vararg name)
  fun setLevel(level)
  fun getLevel() → Level
  fun intercept(chain) → Response
```

### okhttp-logging-interceptor/src/main/kotlin/okhttp3/logging/LoggingEventListener.kt
```
class LoggingEventListener
  fun callStart(call)
  fun dispatcherQueueStart(call, dispatcher)
  fun dispatcherQueueEnd(call, dispatcher)
  fun proxySelectStart(call, url)
  fun proxySelectEnd(call, url, proxies)
  fun dnsStart(call, domainName)
  fun dnsEnd(call, domainName, inetAddressList)
  fun connectStart(call, inetSocketAddress, proxy)
```

### okhttp-logging-interceptor/src/test/java/okhttp3/logging/HttpLoggingInterceptorTest.kt
```
class HttpLoggingInterceptorTest
  fun setUp()
  fun levelGetter()
  fun setLevelShouldReturnSameInstanceOfInterceptor()
  fun none()
  fun basicGet()
  fun basicPost()
  fun basicResponseBody()
  fun basicChunkedResponseBody()
```

### okhttp-logging-interceptor/src/test/java/okhttp3/logging/LoggingEventListenerTest.kt
```
class LoggingEventListenerTest
  fun setUp()
  fun get()
  fun post()
  fun secureGet()
```

## okhttp-osgi-tests

### okhttp-osgi-tests/src/test/kotlin/okhttp3/osgi/OsgiTest.kt
```
class OsgiTest
  fun setUp()
  fun testMainModuleWithSiblings()
```

## okhttp-sse

### okhttp-sse/src/main/kotlin/okhttp3/sse/EventSource.kt
```
interface EventSource
  fun request() → Request
  fun cancel()
  fun newEventSource(request, listener) → EventSource
```

### okhttp-sse/src/main/kotlin/okhttp3/sse/EventSourceListener.kt
```
class EventSourceListener
```

### okhttp-sse/src/main/kotlin/okhttp3/sse/EventSources.kt
```
object EventSources
  fun createFactory(client)
  fun createFactory(callFactory) → EventSource.Factory
  fun processResponse(response, listener)
```

### okhttp-sse/src/main/kotlin/okhttp3/sse/internal/RealEventSource.kt
```
class RealEventSource
  fun connect(callFactory)
  fun onResponse(call, response)
  fun processResponse(response)
  fun onFailure(call, e)
  fun request() → Request
  fun cancel()
  fun onEvent(id, type, data)
  fun onRetryChange(timeMs)
```

### okhttp-sse/src/main/kotlin/okhttp3/sse/internal/ServerSentEventReader.kt
```
class ServerSentEventReader
  fun onEvent(id, type, data)
  fun onRetryChange(timeMs)
  fun processNextEvent() → Boolean
```

### okhttp-sse/src/test/java/okhttp3/sse/internal/EventSourceHttpTest.kt
```
class EventSourceHttpTest
  fun after()
  fun event()
  fun cancelInEventShortCircuits()
  fun badContentType()
  fun badResponseCode()
  fun fullCallTimeoutDoesNotApplyOnceConnected()
  fun fullCallTimeoutAppliesToSetup()
  fun retainsAccept()
```

### okhttp-sse/src/test/java/okhttp3/sse/internal/EventSourceRecorder.kt
```
class EventSourceRecorder
  fun enqueueCancel()
  fun onOpen(eventSource, response)
  fun onEvent(eventSource, id, type, data)
  fun onClosed(eventSource)
  fun onFailure(eventSource, t, response)
  fun assertExhausted()
  fun assertEvent(id, type, data)
  fun assertOpen() → EventSource
```

### okhttp-sse/src/test/java/okhttp3/sse/internal/EventSourcesHttpTest.kt
```
class EventSourcesHttpTest
  fun after()
  fun processResponse()
  fun cancelShortCircuits()
  fun failureWith401IsReadable()
```

### okhttp-sse/src/test/java/okhttp3/sse/internal/ServerSentEventIteratorTest.kt
```
class ServerSentEventIteratorTest
  fun after()
  fun multiline()
  fun multilineCr()
  fun multilineCrLf()
  fun eventType()
  fun commentsIgnored()
  fun idCleared()
  fun nakedFieldNames()
```

## okhttp-testing-support

### okhttp-testing-support/src/main/kotlin/okhttp3/CallEvent.kt
```
class CallEvent
  fun closes(event) → Boolean
  fun closes(event) → Boolean
  fun closes(event) → Boolean
  fun closes(event) → Boolean
  fun closes(event) → Boolean
  fun closes(event) → Boolean
  fun closes(event) → Boolean
  fun closes(event) → Boolean
```

### okhttp-testing-support/src/main/kotlin/okhttp3/ClientRuleEventListener.kt
```
class ClientRuleEventListener
  fun create(call) → EventListener
  fun callStart(call)
  fun dispatcherQueueStart(call, dispatcher)
  fun dispatcherQueueEnd(call, dispatcher)
  fun proxySelectStart(call, url)
  fun proxySelectEnd(call, url, proxies)
  fun dnsStart(call, domainName)
  fun dnsEnd(call, domainName, inetAddressList)
```

### okhttp-testing-support/src/main/kotlin/okhttp3/ConnectionEvent.kt
```
class ConnectionEvent
  fun closes(event) → Boolean
  fun closes(event) → Boolean
  fun closes(event) → Boolean
```

### okhttp-testing-support/src/main/kotlin/okhttp3/DelegatingSSLSession.kt
```
class DelegatingSSLSession
  fun getId() → ByteArray
  fun getSessionContext() → SSLSessionContext
  fun getCreationTime() → Long
  fun getLastAccessedTime() → Long
  fun invalidate()
  fun isValid() → Boolean
  fun putValue(s, o)
  fun getValue(s) → Any
```

### okhttp-testing-support/src/main/kotlin/okhttp3/DelegatingSSLSocket.kt
```
class DelegatingSSLSocket
  fun shutdownInput()
  fun shutdownOutput()
  fun getSupportedCipherSuites() → Array<String>
  fun getEnabledCipherSuites() → Array<String>
  fun setEnabledCipherSuites(suites)
  fun getSupportedProtocols() → Array<String>
  fun getEnabledProtocols() → Array<String>
  fun setEnabledProtocols(protocols)
```

### okhttp-testing-support/src/main/kotlin/okhttp3/DelegatingSSLSocketFactory.kt
```
class DelegatingSSLSocketFactory
  fun createSocket() → SSLSocket
  fun createSocket(host, port) → SSLSocket
  fun createSocket(host, port, localAddress, localPort) → SSLSocket
  fun createSocket(host, port) → SSLSocket
  fun createSocket(host, port, localAddress, localPort) → SSLSocket
  fun getDefaultCipherSuites() → Array<String>
  fun getSupportedCipherSuites() → Array<String>
  fun createSocket(socket, host, port, autoClose) → SSLSocket
```

### okhttp-testing-support/src/main/kotlin/okhttp3/DelegatingServerSocketFactory.kt
```
class DelegatingServerSocketFactory
  fun createServerSocket() → ServerSocket
  fun createServerSocket(port) → ServerSocket
  fun createServerSocket(port, backlog) → ServerSocket
  fun createServerSocket(port, backlog, ifAddress) → ServerSocket
```

### okhttp-testing-support/src/main/kotlin/okhttp3/DelegatingSocketFactory.kt
```
class DelegatingSocketFactory
  fun createSocket() → Socket
  fun createSocket(host, port) → Socket
  fun createSocket(host, port, localAddress, localPort) → Socket
  fun createSocket(host, port) → Socket
  fun createSocket(host, port, localAddress, localPort) → Socket
```

### okhttp-testing-support/src/main/kotlin/okhttp3/EventListenerAdapter.kt
```
class EventListenerAdapter
  fun dispatcherQueueStart(call, dispatcher)
  fun dispatcherQueueEnd(call, dispatcher)
  fun proxySelectStart(call, url)
  fun proxySelectEnd(call, url, proxies)
  fun dnsStart(call, domainName)
  fun dnsEnd(call, domainName, inetAddressList)
  fun connectStart(call, inetSocketAddress, proxy)
  fun secureConnectStart(call)
```

### okhttp-testing-support/src/main/kotlin/okhttp3/EventListenerRelay.kt
```
class EventListenerRelay
```

### okhttp-testing-support/src/main/kotlin/okhttp3/EventRecorder.kt
```
class EventRecorder
  fun forbidLock(lock)
  fun takeEvent(eventClass, elapsedMs) → CallEvent
  fun recordedEventTypes()
  fun clearAllEvents()
  fun logEvent(e)
```

### okhttp-testing-support/src/main/kotlin/okhttp3/FailingCall.kt
```
class FailingCall
  fun request() → Request
  fun execute() → Response
  fun enqueue(responseCallback) → Unit
  fun cancel() → Unit
  fun isExecuted() → Boolean
  fun isCanceled() → Boolean
  fun timeout() → Timeout
  fun addEventListener(eventListener)
```

### okhttp-testing-support/src/main/kotlin/okhttp3/FakeDns.kt
```
class FakeDns
  fun clear(hostname) → FakeDns
  fun lookup(hostname, index) → InetAddress
  fun lookup(hostname) → List<InetAddress>
  fun assertRequests(vararg expectedHosts)
  fun allocate(count) → List<InetAddress>
  fun allocateIpv6(count) → List<InetAddress>
```

### okhttp-testing-support/src/main/kotlin/okhttp3/FakeProxySelector.kt
```
class FakeProxySelector
  fun addProxy(proxy) → FakeProxySelector
  fun select(uri) → List<Proxy>
  fun connectFailed(uri, sa, ioe)
```

### okhttp-testing-support/src/main/kotlin/okhttp3/FakeSSLSession.kt
```
class FakeSSLSession
  fun getApplicationBufferSize() → Int
  fun getCipherSuite() → String
  fun getCreationTime() → Long
  fun getId() → ByteArray
  fun getLastAccessedTime() → Long
  fun getLocalCertificates() → Array<Certificate>
  fun getLocalPrincipal() → Principal
  fun getPacketBufferSize() → Int
```

### okhttp-testing-support/src/main/kotlin/okhttp3/ForwardingRequestBody.kt
```
class ForwardingRequestBody
  fun delegate() → RequestBody
  fun contentType() → MediaType?
  fun contentLength() → Long
  fun writeTo(sink)
  fun isDuplex() → Boolean
  fun toString() → String
```

### okhttp-testing-support/src/main/kotlin/okhttp3/ForwardingResponseBody.kt
```
class ForwardingResponseBody
  fun delegate() → ResponseBody
  fun contentType() → MediaType?
  fun contentLength() → Long
  fun source() → BufferedSource
  fun toString() → String
```

### okhttp-testing-support/src/main/kotlin/okhttp3/JsseDebugLogging.kt
```
object JsseDebugLogging
  fun toString() → String
  fun enableJsseDebugLogging(debugHandler)
  fun publish(record)
  fun flush()
  fun close()
```

### okhttp-testing-support/src/main/kotlin/okhttp3/OkHttpClientTestRule.kt
```
class OkHttpClientTestRule
  fun publish(record)
  fun flush()
  fun close()
  fun wrap(eventListener)
  fun wrap(eventRecorder)
  fun wrap(eventListenerFactory)
  fun newClient() → OkHttpClient
```

### okhttp-testing-support/src/main/kotlin/okhttp3/OkHttpDebugLogging.kt
```
object OkHttpDebugLogging
  fun enableHttp2()
  fun enableTaskRunner()
  fun logHandler()
  fun format(record)
  fun enable(loggerClass, handler)
  fun enable(loggerClass)
```

### okhttp-testing-support/src/main/kotlin/okhttp3/RecordingConnectionListener.kt
```
class RecordingConnectionListener
  fun forbidLock(lock)
  fun takeEvent(eventClass, elapsedMs) → ConnectionEvent
  fun recordedEventTypes()
  fun clearAllEvents()
  fun connectStart(route, call)
  fun connectFailed(route, call, failure)
  fun connectEnd(connection, route, call)
  fun connectionClosed(connection)
```

### okhttp-testing-support/src/main/kotlin/okhttp3/RecordingCookieJar.kt
```
class RecordingCookieJar
  fun enqueueRequestCookies(vararg cookies)
  fun takeResponseCookies() → List<Cookie>
  fun assertResponseCookies(vararg cookies)
  fun saveFromResponse(url, cookies)
  fun loadForRequest(url) → List<Cookie>
```

### okhttp-testing-support/src/main/kotlin/okhttp3/RecordingHostnameVerifier.kt
```
class RecordingHostnameVerifier
```

### okhttp-testing-support/src/main/kotlin/okhttp3/SimpleProvider.kt
```
class SimpleProvider
  fun provideArguments(context)
```

### okhttp-testing-support/src/main/kotlin/okhttp3/SpecificHostSocketFactory.kt
```
class SpecificHostSocketFactory
  fun createSocket() → Socket
  fun connect(endpoint, timeout)
```

### okhttp-testing-support/src/main/kotlin/okhttp3/TestUtilCommon.kt
```
fun String(vararg codePoints) → String
```

### okhttp-testing-support/src/main/kotlin/okhttp3/TestUtilJvm.kt
```
object TestUtil
  fun headerEntries(vararg elements) → List<Header>
  fun repeat(c, count) → String
  fun fragmentBuffer(buffer) → Buffer
  fun awaitGarbageCollection()
  fun assumeNetwork()
  fun assumeNotWindows()
  fun threadFactory(name) → ThreadFactory
  fun newThread(runnable) → Thread
fun getEnv(name)
```

### okhttp-testing-support/src/main/kotlin/okhttp3/TestValueFactory.kt
```
class TestValueFactory
  fun newConnection(pool, route, idleAtNanos, taskRunner) → RealConnection
  fun newConnectionPool(taskRunner, maxIdleConnections) → RealConnectionPool
  fun newAddress(uriHost, uriPort, proxy, proxySelector) → Address
  fun newHttpsAddress(uriHost, uriPort, proxy, proxySelector, sslSocketFactory, hostnameVerifier) → Address
  fun newRoute(address)
  fun newChain(call) → RealInterceptorChain
  fun newRoutePlanner(client, address)
```

### okhttp-testing-support/src/main/kotlin/okhttp3/UppercaseRequestInterceptor.kt
```
class UppercaseRequestInterceptor
  fun intercept(chain) → Response
  fun writeTo(sink)
  fun write(source, byteCount)
```

### okhttp-testing-support/src/main/kotlin/okhttp3/UppercaseResponseInterceptor.kt
```
class UppercaseResponseInterceptor
  fun intercept(chain) → Response
  fun source() → BufferedSource
  fun read(sink, byteCount) → Long
```

### okhttp-testing-support/src/main/kotlin/okhttp3/internal/RecordingOkAuthenticator.kt
```
class RecordingOkAuthenticator
  fun onlyResponse()
  fun onlyRoute()
  fun authenticate(route, response) → Request?
```

### okhttp-testing-support/src/main/kotlin/okhttp3/internal/concurrent/TaskFaker.kt
```
class TaskFaker
  fun execute(taskRunner, runnable)
  fun nanoTime()
  fun coordinatorNotify(taskRunner)
  fun start()
  fun coordinatorWait(taskRunner, nanos)
  fun runTasks()
  fun advanceUntil(newTime)
  fun assertNoMoreTasks()
```

### okhttp-testing-support/src/main/kotlin/okhttp3/internal/duplex/AsyncRequestBody.kt
```
class AsyncRequestBody
  fun contentType() → MediaType?
  fun writeTo(sink)
  fun isDuplex() → Boolean
  fun takeSink() → BufferedSink
  fun assertNoMoreSinks()
```

### okhttp-testing-support/src/main/kotlin/okhttp3/internal/duplex/MockSocketHandler.kt
```
class MockSocketHandler
  fun receiveRequest(expected)
  fun exhaustRequest()
  fun cancelStream()
  fun requestIOException()
  fun sendResponse(s, responseSent)
  fun exhaustResponse()
  fun sleep(duration, unit)
  fun handle(socket)
```

### okhttp-testing-support/src/main/kotlin/okhttp3/internal/http/RecordingProxySelector.kt
```
class RecordingProxySelector
  fun select(uri) → List<Proxy>
  fun assertRequests(vararg expectedUris)
  fun connectFailed(uri, sa, ioe)
  fun toString()
```

### okhttp-testing-support/src/main/kotlin/okhttp3/okio/LoggingFilesystem.kt
```
class LoggingFilesystem
  fun log(line)
  fun appendingSink(file, mustExist) → Sink
  fun atomicMove(source, target)
  fun createDirectory(dir, mustCreate)
  fun delete(path, mustExist)
  fun sink(file, mustCreate) → Sink
  fun source(file) → Source
```

### okhttp-testing-support/src/main/kotlin/okhttp3/testing/PlatformRule.kt
```
class PlatformRule
  fun beforeEach(context)
  fun afterEach(context)
  fun interceptTestMethod(invocation, invocationContext, extensionContext)
  fun setupPlatform()
  fun resetPlatform()
  fun expectFailureOnConscryptPlatform()
  fun expectFailureOnCorrettoPlatform()
  fun expectFailureOnOpenJSSEPlatform()
```

### okhttp-testing-support/src/main/kotlin/okhttp3/testing/PlatformVersion.kt
```
object PlatformVersion
  fun getJvmSpecVersion() → String
```

### okhttp-testing-support/src/test/kotlin/okhttp3/OkHttpClientTestRuleTest.kt
```
class OkHttpClientTestRuleTest
  fun run() → Unit
```

### okhttp-testing-support/src/test/kotlin/okhttp3/testing/PlatformRuleTest.kt
```
class PlatformRuleTest
  fun testMode()
  fun testGreenCase()
  fun testGreenCaseFailingOnLater()
  fun failureCase()
```

## okhttp-tls

### okhttp-tls/src/main/kotlin/okhttp3/tls/HandshakeCertificates.kt
```
class HandshakeCertificates
  fun keyManager() → X509KeyManager
  fun trustManager() → X509TrustManager
  fun sslSocketFactory() → SSLSocketFactory
  fun sslContext() → SSLContext
  fun heldCertificate(heldCertificate, vararg intermediates)
  fun addTrustedCertificate(certificate)
  fun addPlatformTrustedCertificates()
  fun addInsecureHost(hostname)
```

### okhttp-tls/src/main/kotlin/okhttp3/tls/HeldCertificate.kt
```
class HeldCertificate
  fun certificate() → X509Certificate
  fun keyPair() → KeyPair
  fun certificatePem() → String
  fun privateKeyPkcs8Pem() → String
  fun privateKeyPkcs1Pem() → String
  fun validityInterval(notBefore, notAfter)
  fun duration(duration, unit)
  fun addSubjectAlternativeName(altName)
```

### okhttp-tls/src/main/kotlin/okhttp3/tls/internal/InsecureAndroidTrustManager.kt
```
class InsecureAndroidTrustManager
  fun checkServerTrusted(chain, authType, host) → List<Certificate>
  fun getAcceptedIssuers() → Array<X509Certificate>
  fun checkClientTrusted(chain, authType)
  fun checkServerTrusted(chain, authType)
```

### okhttp-tls/src/main/kotlin/okhttp3/tls/internal/InsecureExtendedTrustManager.kt
```
class InsecureExtendedTrustManager
  fun getAcceptedIssuers() → Array<X509Certificate>
  fun checkServerTrusted(chain, authType, socket)
  fun checkServerTrusted(chain, authType, engine)
  fun checkServerTrusted(chain, authType)
  fun checkClientTrusted(chain, authType)
  fun checkClientTrusted(chain, authType, engine)
  fun checkClientTrusted(chain, authType, socket)
```

### okhttp-tls/src/main/kotlin/okhttp3/tls/internal/TlsUtil.kt
```
object TlsUtil
  fun localhost() → HandshakeCertificates
  fun newTrustManager(keyStoreType, trustedCertificates, insecureHosts) → X509TrustManager
  fun newKeyManager(keyStoreType, heldCertificate, vararg intermediates) → X509KeyManager
```

### okhttp-tls/src/main/kotlin/okhttp3/tls/internal/der/Adapters.kt
```
object Adapters
  fun decode(reader) → Boolean
  fun encode(writer, value)
  fun decode(reader) → Long
  fun encode(writer, value)
  fun decode(reader) → BigInteger
  fun encode(writer, value)
  fun decode(reader) → BitString
  fun encode(writer, value)
```

### okhttp-tls/src/main/kotlin/okhttp3/tls/internal/der/AnyValue.kt
```
class AnyValue
  fun hashCode() → Int
```

### okhttp-tls/src/main/kotlin/okhttp3/tls/internal/der/BasicDerAdapter.kt
```
class BasicDerAdapter
  fun matches(header) → Boolean
  fun fromDer(reader) → T
  fun toDer(writer, value)
  fun withTag(tagClass, tag) → BasicDerAdapter<T>
  fun optional(defaultValue) → BasicDerAdapter<T>
  fun asTypeHint() → BasicDerAdapter<T>
  fun hashCode() → Int
  fun toString() → String
```

### okhttp-tls/src/main/kotlin/okhttp3/tls/internal/der/BitString.kt
```
class BitString
  fun hashCode() → Int
```

### okhttp-tls/src/main/kotlin/okhttp3/tls/internal/der/Certificate.kt
```
class Certificate
  fun checkSignature(issuer) → Boolean
  fun toX509Certificate() → X509Certificate
class TbsCertificate
  fun hashCode() → Int
class AlgorithmIdentifier
  fun hashCode() → Int
class SubjectPublicKeyInfo
  fun hashCode() → Int
```

### okhttp-tls/src/main/kotlin/okhttp3/tls/internal/der/CertificateAdapters.kt
```
object CertificateAdapters
  fun matches(header) → Boolean
  fun fromDer(reader) → Long
  fun toDer(writer, value)
```

### okhttp-tls/src/main/kotlin/okhttp3/tls/internal/der/DerAdapter.kt
```
interface DerAdapter
  fun matches(header) → Boolean
  fun fromDer(reader) → T
  fun fromDer(byteString) → T
  fun toDer(writer, value)
  fun toDer(value) → ByteString
  fun withExplicitBox(tagClass, tag, forceConstructed) → BasicDerAdapter<T>
  fun decode(reader) → T
  fun encode(writer, value)
```

### okhttp-tls/src/main/kotlin/okhttp3/tls/internal/der/DerHeader.kt
```
class DerHeader
  fun hashCode() → Int
  fun toString() → String
```

### okhttp-tls/src/main/kotlin/okhttp3/tls/internal/der/DerReader.kt
```
class DerReader
  fun hasNext() → Boolean
  fun peekHeader() → DerHeader?
  fun readHeader() → DerHeader
  fun readBoolean() → Boolean
  fun readBigInteger() → BigInteger
```

### okhttp-tls/src/main/kotlin/okhttp3/tls/internal/der/DerWriter.kt
```
class DerWriter
  fun write(name, tagClass, tag, block)
  fun writeBoolean(b)
  fun writeBigInteger(value)
  fun writeLong(v)
  fun writeBitString(bitString)
  fun writeOctetString(byteString)
  fun writeUtf8(value)
  fun writeObjectIdentifier(s)
```

### okhttp-tls/src/main/kotlin/okhttp3/tls/internal/der/ObjectIdentifiers.kt
```
object ObjectIdentifiers
```

### okhttp-tls/src/test/java/okhttp3/tls/CertificatesJavaTest.java
```
class CertificatesJavaTest
  testRoundtrip() → void
```

### okhttp-tls/src/test/java/okhttp3/tls/CertificatesTest.kt
```
class CertificatesTest
```

### okhttp-tls/src/test/java/okhttp3/tls/HandshakeCertificatesTest.kt
```
class HandshakeCertificatesTest
```

### okhttp-tls/src/test/java/okhttp3/tls/HeldCertificateTest.kt
```
class HeldCertificateTest
  fun defaultCertificate()
  fun customInterval()
  fun customDuration()
  fun subjectAlternativeNames()
  fun commonName()
  fun organizationalUnit()
  fun pems()
```

### okhttp-tls/src/test/java/okhttp3/tls/internal/der/DerCertificatesTest.kt
```
class DerCertificatesTest
```

### okhttp-tls/src/test/java/okhttp3/tls/internal/der/DerTest.kt
```
class DerTest
```

## okhttp-urlconnection

### okhttp-urlconnection/src/main/kotlin/okhttp3/JavaNetAuthenticator.kt
```
class JavaNetAuthenticator
  fun authenticate(route, response) → Request?
```

### okhttp-urlconnection/src/main/kotlin/okhttp3/JavaNetCookieJar.kt
```
class JavaNetCookieJar
```

## okhttp-zstd

### okhttp-zstd/src/main/kotlin/okhttp3/zstd/Zstd.kt
```
object Zstd
  fun decompress(compressedSource) → Source
```

### okhttp-zstd/src/test/java/okhttp3/zstd/ZstdInterceptorJavaTest.java
```
class ZstdInterceptorJavaTest
  testConstructor() → void
```

### okhttp-zstd/src/test/java/okhttp3/zstd/ZstdInterceptorTest.kt
```
class ZstdInterceptorTest
  fun testDecompressZstd()
```

### okhttp-zstd/src/test/java/okhttp3/zstd/ZstdTestMain.kt
```
fun main()
```

## regression-test

### regression-test/src/androidTest/java/okhttp/regression/IssueReproductionTest.java
```
class IssueReproductionTest
```

### regression-test/src/androidTest/java/okhttp/regression/LetsEncryptTest.java
```
class LetsEncryptTest
```

### regression-test/src/androidTest/java/okhttp/regression/compare/AndroidHttpEngineTest.kt
```
class AndroidHttpEngineTest
  fun tearDown()
  fun get()
  fun onRedirectReceived(request, info, newLocationUrl)
  fun onResponseStarted(request, info)
  fun onReadCompleted(request, info, byteBuffer)
  fun onSucceeded(request, info)
  fun onFailed(request, info, error)
```

### regression-test/src/androidTest/java/okhttp/regression/compare/ApacheHttpClientHttp2Test.kt
```
class ApacheHttpClientHttp2Test
  fun testHttp2()
object LoggingCallback
  fun completed(response)
  fun failed(ex)
  fun cancelled()
```

### regression-test/src/androidTest/java/okhttp/regression/compare/ApacheHttpClientTest.kt
```
class ApacheHttpClientTest
```

### regression-test/src/androidTest/java/okhttp/regression/compare/OkHttpClientTest.java
```
class OkHttpClientTest
```
