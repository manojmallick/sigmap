<!-- Generated by SigMap gen-context.js v3.3.2 -->
<!-- DO NOT EDIT below the marker line — run gen-context.js to regenerate -->

# Code signatures

## mockwebserver-deprecated

### mockwebserver-deprecated/src/main/kotlin/okhttp3/mockwebserver/Dispatcher.kt
```
class Dispatcher
```

### mockwebserver-deprecated/src/main/kotlin/okhttp3/mockwebserver/MockResponse.kt
```
class MockResponse
  fun getStatus() → String
  fun setStatus(status)
  fun setResponseCode(code) → MockResponse
  fun clearHeaders()
  fun addHeader(header)
  fun addHeader(name, value)
  fun addHeaderLenient(name, value)
  fun setHeader(name, value)
```

### mockwebserver-deprecated/src/main/kotlin/okhttp3/mockwebserver/MockWebServer.kt
```
class MockWebServer
  fun getPort() → Int
  fun toProxyAddress() → Proxy
  fun setServerSocketFactory(serverSocketFactory)
  fun url(path) → HttpUrl
  fun setBodyLimit(bodyLimit)
  fun setProtocolNegotiationEnabled(protocolNegotiationEnabled)
  fun setProtocols(protocols)
  fun protocols() → List<Protocol>
```

### mockwebserver-deprecated/src/main/kotlin/okhttp3/mockwebserver/PushPromise.kt
```
class PushPromise
  fun method() → String
  fun path() → String
  fun headers() → Headers
  fun response() → MockResponse
```

### mockwebserver-deprecated/src/main/kotlin/okhttp3/mockwebserver/QueueDispatcher.kt
```
class QueueDispatcher
  fun dispatch(request) → MockResponse
  fun peek() → MockResponse
  fun enqueueResponse(response)
  fun shutdown()
  fun setFailFast(failFast)
  fun setFailFast(failFastResponse)
```

### mockwebserver-deprecated/src/main/kotlin/okhttp3/mockwebserver/RecordedRequest.kt
```
class RecordedRequest
  fun getUtf8Body() → String
  fun getHeader(name) → String?
  fun toString() → String
```

### mockwebserver-deprecated/src/test/java/okhttp3/mockwebserver/KotlinSourceModernTest.kt
```
class KotlinSourceModernTest
  fun dispatcherFromMockWebServer()
  fun dispatch(request) → MockResponse
  fun peek() → MockResponse
  fun shutdown()
  fun mockResponse()
  fun mockWebServer()
```

### mockwebserver-deprecated/src/test/java/okhttp3/mockwebserver/MockWebServerTest.kt
```
class MockWebServerTest
  fun setUp()
  fun tearDown()
  fun defaultMockResponse()
  fun setResponseMockReason()
  fun setStatusControlsWholeStatusLine()
  fun setBodyAdjustsHeaders()
  fun mockResponseAddHeader()
  fun mockResponseSetHeader()
```
