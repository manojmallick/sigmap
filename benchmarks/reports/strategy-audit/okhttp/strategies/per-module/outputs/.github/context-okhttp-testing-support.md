<!-- Generated by SigMap gen-context.js v3.3.2 -->
<!-- DO NOT EDIT below the marker line — run gen-context.js to regenerate -->

# Code signatures

## todos
```
okhttp-testing-support/src/main/kotlin/okhttp3/internal/concurrent/TaskFaker.kt:362  # TODO: report compiler bug
```

## okhttp-testing-support

### okhttp-testing-support/src/main/kotlin/okhttp3/RecordingConnectionListener.kt
```
class RecordingConnectionListener
  fun forbidLock(lock)
  fun takeEvent(eventClass, elapsedMs) → ConnectionEvent
  fun recordedEventTypes()
  fun clearAllEvents()
  fun connectStart(route, call)
  fun connectFailed(route, call, failure)
  fun connectEnd(connection, route, call)
  fun connectionClosed(connection)
```

### okhttp-testing-support/src/main/kotlin/okhttp3/RecordingCookieJar.kt
```
class RecordingCookieJar
  fun enqueueRequestCookies(vararg cookies)
  fun takeResponseCookies() → List<Cookie>
  fun assertResponseCookies(vararg cookies)
  fun saveFromResponse(url, cookies)
  fun loadForRequest(url) → List<Cookie>
```

### okhttp-testing-support/src/main/kotlin/okhttp3/RecordingHostnameVerifier.kt
```
class RecordingHostnameVerifier
```

### okhttp-testing-support/src/main/kotlin/okhttp3/SimpleProvider.kt
```
class SimpleProvider
  fun provideArguments(context)
```

### okhttp-testing-support/src/main/kotlin/okhttp3/SpecificHostSocketFactory.kt
```
class SpecificHostSocketFactory
  fun createSocket() → Socket
  fun connect(endpoint, timeout)
```

### okhttp-testing-support/src/main/kotlin/okhttp3/TestUtilCommon.kt
```
fun String(vararg codePoints) → String
```

### okhttp-testing-support/src/main/kotlin/okhttp3/TestUtilJvm.kt
```
object TestUtil
  fun headerEntries(vararg elements) → List<Header>
  fun repeat(c, count) → String
  fun fragmentBuffer(buffer) → Buffer
  fun awaitGarbageCollection()
  fun assumeNetwork()
  fun assumeNotWindows()
  fun threadFactory(name) → ThreadFactory
  fun newThread(runnable) → Thread
fun getEnv(name)
```

### okhttp-testing-support/src/main/kotlin/okhttp3/TestValueFactory.kt
```
class TestValueFactory
  fun newConnection(pool, route, idleAtNanos, taskRunner) → RealConnection
  fun newConnectionPool(taskRunner, maxIdleConnections) → RealConnectionPool
  fun newAddress(uriHost, uriPort, proxy, proxySelector) → Address
  fun newHttpsAddress(uriHost, uriPort, proxy, proxySelector, sslSocketFactory, hostnameVerifier) → Address
  fun newRoute(address)
  fun newChain(call) → RealInterceptorChain
  fun newRoutePlanner(client, address)
```

### okhttp-testing-support/src/main/kotlin/okhttp3/UppercaseRequestInterceptor.kt
```
class UppercaseRequestInterceptor
  fun intercept(chain) → Response
  fun writeTo(sink)
  fun write(source, byteCount)
```

### okhttp-testing-support/src/main/kotlin/okhttp3/UppercaseResponseInterceptor.kt
```
class UppercaseResponseInterceptor
  fun intercept(chain) → Response
  fun source() → BufferedSource
  fun read(sink, byteCount) → Long
```

### okhttp-testing-support/src/main/kotlin/okhttp3/internal/RecordingOkAuthenticator.kt
```
class RecordingOkAuthenticator
  fun onlyResponse()
  fun onlyRoute()
  fun authenticate(route, response) → Request?
```

### okhttp-testing-support/src/main/kotlin/okhttp3/internal/concurrent/TaskFaker.kt
```
class TaskFaker
  fun execute(taskRunner, runnable)
  fun nanoTime()
  fun coordinatorNotify(taskRunner)
  fun start()
  fun coordinatorWait(taskRunner, nanos)
  fun runTasks()
  fun advanceUntil(newTime)
  fun assertNoMoreTasks()
```

### okhttp-testing-support/src/main/kotlin/okhttp3/internal/duplex/AsyncRequestBody.kt
```
class AsyncRequestBody
  fun contentType() → MediaType?
  fun writeTo(sink)
  fun isDuplex() → Boolean
  fun takeSink() → BufferedSink
  fun assertNoMoreSinks()
```

### okhttp-testing-support/src/main/kotlin/okhttp3/internal/duplex/MockSocketHandler.kt
```
class MockSocketHandler
  fun receiveRequest(expected)
  fun exhaustRequest()
  fun cancelStream()
  fun requestIOException()
  fun sendResponse(s, responseSent)
  fun exhaustResponse()
  fun sleep(duration, unit)
  fun handle(socket)
```

### okhttp-testing-support/src/main/kotlin/okhttp3/internal/http/RecordingProxySelector.kt
```
class RecordingProxySelector
  fun select(uri) → List<Proxy>
  fun assertRequests(vararg expectedUris)
  fun connectFailed(uri, sa, ioe)
  fun toString()
```

### okhttp-testing-support/src/main/kotlin/okhttp3/okio/LoggingFilesystem.kt
```
class LoggingFilesystem
  fun log(line)
  fun appendingSink(file, mustExist) → Sink
  fun atomicMove(source, target)
  fun createDirectory(dir, mustCreate)
  fun delete(path, mustExist)
  fun sink(file, mustCreate) → Sink
  fun source(file) → Source
```

### okhttp-testing-support/src/main/kotlin/okhttp3/testing/PlatformRule.kt
```
class PlatformRule
  fun beforeEach(context)
  fun afterEach(context)
  fun interceptTestMethod(invocation, invocationContext, extensionContext)
  fun setupPlatform()
  fun resetPlatform()
  fun expectFailureOnConscryptPlatform()
  fun expectFailureOnCorrettoPlatform()
  fun expectFailureOnOpenJSSEPlatform()
```

### okhttp-testing-support/src/main/kotlin/okhttp3/testing/PlatformVersion.kt
```
object PlatformVersion
  fun getJvmSpecVersion() → String
```

### okhttp-testing-support/src/test/kotlin/okhttp3/OkHttpClientTestRuleTest.kt
```
class OkHttpClientTestRuleTest
  fun run() → Unit
```

### okhttp-testing-support/src/test/kotlin/okhttp3/testing/PlatformRuleTest.kt
```
class PlatformRuleTest
  fun testMode()
  fun testGreenCase()
  fun testGreenCaseFailingOnLater()
  fun failureCase()
```
