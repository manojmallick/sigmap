

## Auto-generated signatures
<!-- Updated by gen-context.js -->
<!-- Generated by SigMap gen-context.js v3.3.2 -->
<!-- Updated: 2026-04-14T06:20:51.995Z -->
<!-- Do not edit below — regenerate with: node gen-context.js -->

# Code signatures

<!-- Generated by SigMap gen-context.js v3.3.2 -->
<!-- DO NOT EDIT below the marker line — run gen-context.js to regenerate -->

# Code signatures

## deps
```
absl/copts/generate_copts.py ← copts
```

## todos
```
absl/base/attributes.h:452  # TODO: (b/176172494): Use ABSL_HAVE_CPP_ATTRIBUTE(nodiscard) when all code is
absl/base/dynamic_annotations.h:61  # TODO: (rogeeff): Remove after the backward compatibility period.
absl/base/dynamic_annotations.h:313  # TODO: (delesley) -- The exclusive lock here ignores writes as well, but
absl/base/internal/exception_safety_testing.h:43  # TODO: (b/500018833): Update the namespace as appropriate.
absl/base/internal/raw_logging.cc:81  # TODO: (gfalcon): We want raw-logging to work on as many platforms as possibl
absl/base/internal/raw_logging.cc:195  # TODO: (gfalcon): When string_view no longer depends on base, change this
absl/base/internal/raw_logging.h:151  # TODO: (gfalcon): Come up with a better name for this method.
absl/base/internal/raw_logging.h:193  # TODO: (gfalcon): When string_view no longer depends on base, change this
absl/base/internal/sysinfo.cc:420  # TODO: (dcross): Akaros anticipates moving the thread ID to the uthread
absl/container/btree_benchmark.cc:229  # TODO: (b/207389011): consider adding benchmarks that remove different fracti
absl/container/btree_benchmark.cc:385  # TODO: (ezb): we could add a GenerateNext version of generator that could red
absl/container/btree_test.cc:3198  # TODO: (ezb): fix insert_multi to allow this to compile.
absl/container/btree_test.cc:3220  # TODO: (ezb): fix insert_multi to allow this to compile.
absl/container/inlined_vector_exception_safety_test.cc:142  # TODO: (johnsoncj): Add `testing::NoThrowGuarantee` to the framework
absl/container/inlined_vector_test.cc:72  # TODO: (bsamwel): Add support for movable-only types.
absl/container/inlined_vector_test.cc:312  # TODO: (absl-team): Using unique_ptr here is technically correct, but
absl/container/inlined_vector_test.cc:341  # TODO: (absl-team): Using unique_ptr here is technically correct, but
absl/container/inlined_vector_test.cc:367  # TODO: (absl-team): Using unique_ptr here is technically correct, but
absl/container/inlined_vector_test.cc:1480  # TODO: (bsamwel): Test with an input iterator source.
absl/container/inlined_vector_test.cc:1579  # TODO: (bsamwel): Test with an input iterator.
```

## absl

### absl/abseil.podspec.gen.py
```
def get_elem_value(elem, name)  # Returns the value of XML element with the given name
def normalize_paths(paths)  # Returns the list of normalized path
def parse_rule(elem, package)  # Returns a rule from bazel XML rule
def read_build(package)  # Runs bazel query on given package file and returns all cc ru
def collect_rules(root_path)  # Collects and returns all rules from root path recursively
def relevant_rule(rule)  # Returns true if a given rule is relevant when generating a p
def get_spec_var(depth)  # Returns the name of variable for spec with given depth
def get_spec_name(label)  # Converts the label of bazel rule to the name of podspec
def write_podspec(f, rules, args)  # Writes a podspec from given rules and args
def build_rule_directory(rules)  # Builds a tree-style rule directory from given rules
def write_podspec_map(f, cur_map, depth)  # Writes podspec from rule map recursively
def write_podspec_rule(f, rule, depth)  # Writes podspec from given rule
def write_indented_list(f, leading, values)  # Writes leading values in an indented style
def generate(args)  # Generates a podspec file from all BUILD files under absl dir
def main()
```

### absl/algorithm/algorithm.h
```
linear_search(InputIterator first, InputIterator last, const EqualityComparable& value) → ABSL_INTERNAL_CONSTEXPR_SINCE_
```

### absl/algorithm/algorithm_test.cc
```
class LinearSearchTest
Is3(int n) → static bool
Is4(int n) → static bool
```

### absl/algorithm/container.h
```
c_begin(C& c) → ABSL_INTERNAL_CONSTEXPR_SINCE_
c_end(C& c) → ABSL_INTERNAL_CONSTEXPR_SINCE_
c_linear_search(const C& c, EqualityComparable&& value) → ABSL_INTERNAL_CONSTEXPR_SINCE_
c_distance(const C& c) → container_algorithm_internal::
c_all_of(const C& c, Pred&& pred) → ABSL_INTERNAL_CONSTEXPR_SINCE_
c_any_of(const C& c, Pred&& pred) → ABSL_INTERNAL_CONSTEXPR_SINCE_
c_none_of(const C& c, Pred&& pred) → ABSL_INTERNAL_CONSTEXPR_SINCE_
c_for_each(C&& c, Function&& f) → ABSL_INTERNAL_CONSTEXPR_SINCE_
c_find(C& c, T&& value) → container_algorithm_internal::
c_contains(const Sequence& sequence, T&& value) → ABSL_INTERNAL_CONSTEXPR_SINCE_
c_find_if(C& c, Pred&& pred) → container_algorithm_internal::
c_find_if_not(C& c, Pred&& pred) → container_algorithm_internal::
c_find_end(Sequence1& sequence, Sequence2& subsequence) → container_algorithm_internal::
c_find_end(Sequence1& sequence, Sequence2& subsequence, BinaryPredicate&& pred) → container_algorithm_internal::
c_find_first_of(C1& container, const C2& options) → container_algorithm_internal::
c_find_first_of(C1& container, const C2& options, BinaryPredicate&& pred) → container_algorithm_internal::
c_adjacent_find(Sequence& sequence) → container_algorithm_internal::
c_adjacent_find(Sequence& sequence, BinaryPredicate&& pred) → container_algorithm_internal::
c_count(const C& c, T&& value) → container_algorithm_internal::
c_count_if(const C& c, Pred&& pred) → container_algorithm_internal::
c_mismatch(C1& c1, C2& c2)
c_mismatch(C1& c1, C2& c2, BinaryPredicate pred)
c_equal(const C1& c1, const C2& c2) → ABSL_INTERNAL_CONSTEXPR_SINCE_
c_equal(const C1& c1, const C2& c2, BinaryPredicate&& pred) → ABSL_INTERNAL_CONSTEXPR_SINCE_
c_is_permutation(const C1& c1, const C2& c2) → ABSL_INTERNAL_CONSTEXPR_SINCE_
```

### absl/algorithm/container_test.cc
```
class NonMutatingTest
struct AccumulateCalls
class NumericMutatingTest
class SortingTest
struct Element
class SetOperationsTest
Predicate(int value) → bool
BinPredicate(int v1, int v2) → bool
Equals(int v1, int v2) → bool
IsOdd(int x) → bool
EXPECT_FALSE(absl::c_all_of(v, [](int x)
EXPECT_TRUE(absl::c_all_of(v, [](int x)
EXPECT_TRUE(absl::c_any_of(v, [](int x)
EXPECT_FALSE(absl::c_any_of(v, [](int x)
EXPECT_FALSE(absl::c_none_of(v, [](int x)
EXPECT_TRUE(absl::c_none_of(v, [](int x)
EXPECT_THAT(actual, Truly([](const std::vector<int>& c)
static_assert(!absl::c_all_of(kArray, [](int x)
static_assert(absl::c_all_of(kArray, [](int x)
static_assert(absl::c_any_of(kArray, [](int x)
static_assert(!absl::c_any_of(kArray, [](int x)
static_assert(!absl::c_none_of(kArray, [](int x)
static_assert(absl::c_none_of(kArray, [](int x)
static_assert(absl::c_find_if(kArray, [](int x)
static_assert(absl::c_find_if(kArray, [](int x)
```

### absl/base/attributes.h
```
struct AbslInternal_YouForgotToExplicitlyInitializeAField
T() → operator
for(;;)
```

### absl/base/bit_cast_test.cc
```
struct marshall
TestMarshall(const T values[], int num_values) → void
for(int i = 0; i < num_values; ++i)
TestIntegral(const T values[], int num_values) → void
for(int i = 0; i < num_values; ++i)
```

### absl/base/c_header_test.c
```
main() → int
```

### absl/base/call_once.h
```
class once_flag
class SchedulingHelper
if(mode_ == base_internal::SCHEDULE_KERNEL_ONLY)
if(mode_ == base_internal::SCHEDULE_KERNEL_ONLY)
CallOnceImpl(std::atomic<uint32_t>* absl_nonnull control, base_internal::SchedulingMode scheduling_mode, Callable&& fn, Args&&... args) → void
if(old_control != kOnceInit && old_control != kOnceRunning && old_control != kOnceWaiter && old_control != kOnceDone)
if(old_control == base_internal::kOnceWaiter)
ControlWord(once_flag* absl_nonnull flag) → inline std::atomic<uint32_t>* 
LowLevelCallOnce(absl::once_flag* absl_nonnull flag, Callable&& fn, Args&&... args) → void
call_once(absl::once_flag& flag, Callable&& fn, Args&&... args) → void
```

### absl/base/call_once_test.cc
```
WaitAndIncrement() → void
ThreadBody() → void
for(int i = 0; i < 10; ++i)
for(std::thread& thread : threads)
```

### absl/base/casts.cc
```
DemangleCppString(const char* mangled) → std::string
if(status == 0 && demangled != nullptr)
BadDownCastCrash(const char* source_type, const char* target_type) → void
```

### absl/base/casts.h
```
implicit_cast(absl::type_identity_t<To> to) → To>
implicit_cast(absl::type_identity_t<To> to ABSL_ATTRIBUTE_LIFETIME_BOUND) → To>
bit_cast(const Source& source) → inline constexpr Dest
bit_cast(const Source& source) → inline Dest
ValidateDownCast(From* f ABSL_ATTRIBUTE_UNUSED) → inline void
down_cast(From* f) → inline To
constexpr(!std::is_same<std::remove_cv_t<std::remove_pointer_t<To>>, std::remove_cv_t<From>>::value) → if
down_cast(From& f) → inline To
constexpr(!std::is_same<std::remove_cv_t<std::remove_reference_t<To>>, std::remove_cv_t<From>>::value) → if
```

### absl/base/casts_test.cc
```
struct BaseForImplicitCast
class BaseForDownCast
class DerivedForDownCast
class Derived2ForDownCast
```

### absl/base/dynamic_annotations.h
```
AnnotateUnprotectedRead(const volatile T& x) → inline T
HwasanTagPointer(T* ptr, uintptr_t tag) → T*
HwasanTagPointer(T* ptr, uintptr_t) → T*
```

### absl/base/exception_safety_testing_test.cc
```
struct NullaryTestValidator
struct UnaryTestValidator
struct ExampleStruct
struct NonNegative
struct FailsBasicGuarantee
struct FollowsBasicGuarantee
struct BasicGuaranteeWithExtraContracts
struct FollowsStrongGuarantee
struct HasReset
struct NonCopyable
struct NonEqualityComparable
struct ExhaustivenessTester
struct LeaksIfCtorThrows
struct Tracked
ExpectNoThrow(const F& f) → void
ExpectNoThrow([]()
ExpectNoThrow([]()
TestOp(const F& f) → void
TestOp([]()
TestOp([&]()
TestOp([&]()
TestOp([&]()
TestOp([&]()
TestOp([&]()
TestOp([&]()
```

### absl/base/fast_type_id.h
```
struct FastTypeTag
AbslHashValue(H h, FastTypeIdType x) → friend H
FastTypeId() → constexpr FastTypeIdType
```

### absl/base/fast_type_id_test.cc
```
struct Base
struct PDerived
for(size_t j = 0; j < i; ++j)
for(size_t j = 0; j < i; ++j)
```

### absl/base/internal/atomic_hook.h
```
default_fn_(default_fn) → :
Store(FnPtr fn) → void
Load() → FnPtr
DummyFunction(Args...) → static ReturnType
DoLoad() → FnPtr
DoStore(FnPtr fn) → bool
DoLoad() → FnPtr
if(value == kUninitialized)
DoStore(FnPtr fn) → bool
```

### absl/base/internal/atomic_hook_test.cc
```
TestHook(int x) → void
OverrideFunc() → void
OverrideInstaller()
```

### absl/base/internal/atomic_hook_test_helper.cc
```
DefaultFunc() → void
RegisterFunc(VoidF f) → void
```

### absl/base/internal/cmake_thread_test.cc
```
main() → int
```

### absl/base/internal/cycleclock.cc
```
if(fn == nullptr)
```

### absl/base/internal/cycleclock.h
```
class CycleClock
  Now() → int64_t
  Frequency() → double
  LoadCycleClockSource() → CycleClockSourceFunc
class CycleClockSource
  Register(CycleClockSourceFunc source) → void
if(fn == nullptr)
```

### absl/base/internal/direct_mmap.h
```
if(pagesize == 0)
if(offset < 0 || offset % pagesize != 0)
DirectMunmap(void* start, size_t length) → inline int
DirectMmap(void* start, size_t length, int prot, int flags, int fd, off_t offset) → inline void*
DirectMunmap(void* start, size_t length) → inline int
```

### absl/base/internal/endian.h
```
gbswap_64(uint64_t x) → constexpr uint64_t
gbswap_32(uint32_t x) → constexpr uint32_t
gbswap_16(uint16_t x) → constexpr uint16_t
ghtons(uint16_t x) → inline uint16_t
ghtonl(uint32_t x) → inline uint32_t
ghtonll(uint64_t x) → inline uint64_t
ghtons(uint16_t x) → inline uint16_t
ghtonl(uint32_t x) → inline uint32_t
ghtonll(uint64_t x) → inline uint64_t
gntohs(uint16_t x) → inline uint16_t
gntohl(uint32_t x) → inline uint32_t
gntohll(uint64_t x) → inline uint64_t
FromHost16(uint16_t x) → inline uint16_t
ToHost16(uint16_t x) → inline uint16_t
FromHost32(uint32_t x) → inline uint32_t
ToHost32(uint32_t x) → inline uint32_t
FromHost64(uint64_t x) → inline uint64_t
ToHost64(uint64_t x) → inline uint64_t
IsLittleEndian() → inline constexpr bool
FromHost16(uint16_t x) → inline uint16_t
ToHost16(uint16_t x) → inline uint16_t
FromHost32(uint32_t x) → inline uint32_t
ToHost32(uint32_t x) → inline uint32_t
FromHost64(uint64_t x) → inline uint64_t
ToHost64(uint64_t x) → inline uint64_t
```

### absl/base/internal/endian_test.cc
```
GenerateAllUint16Values() → std::vector<uint16_t>
GenerateRandomIntegers(size_t num_values_to_test) → std::vector<T>
for(size_t i = 0; i < num_values_to_test; ++i)
ManualByteSwap(char* bytes, int length) → void
for(int i = 0; i < length / 2; ++i)
UnalignedLoad(const char* p) → inline T
GBSwapHelper(const std::vector<T>& host_values_to_test, const ByteSwapper& byte_swapper) → static void
Swap16(char* bytes) → void
Swap32(char* bytes) → void
Swap64(char* bytes) → void
for(int i = 0; i < 100; ++i)
```

### absl/base/internal/errno_saver.h
```
class ErrnoSaver
```

### absl/base/internal/errno_saver_test.cc
```
struct ErrnoPrinter
```

### absl/base/internal/exception_safety_testing.cc
```
MakeExceptionSafetyTester() → exceptions_internal::Exception
MaybeThrow(absl::string_view msg, bool throw_bad_alloc) → void
if(countdown-- == 0)
GetSpecString(TypeSpec spec) → std::string
GetSpecString(AllocSpec spec) → std::string
```

### absl/base/internal/exception_safety_testing.h
```
struct NoThrowTag
struct StrongGuaranteeTagType
class TestException
struct TrackedAddress
class ConstructorTracker
  assert(current_tracker_instance_ == nullptr)
  assert(current_tracker_instance_ == this)
class TrackedObject
class ThrowingBool
class ThrowingValue
  ThrowingValue(dummy_ + other.dummy_, nothrow_ctor) → return
  ThrowingValue(dummy_, nothrow_ctor) → return
  ThrowingValue(dummy_ - other.dummy_, nothrow_ctor) → return
  ThrowingValue(-dummy_, nothrow_ctor) → return
  ThrowingValue(dummy_ * other.dummy_, nothrow_ctor) → return
  ThrowingValue(dummy_ / other.dummy_, nothrow_ctor) → return
  ThrowingValue(dummy_ % other.dummy_, nothrow_ctor) → return
  ThrowingValue(dummy_ << shift, nothrow_ctor) → return
class ThrowingAllocator
  ReadStateAndMaybeThrow(ABSL_PRETTY_FUNCTION)
  allocate(n) → return
  ReadState()
  ReadStateAndMaybeThrow(ABSL_PRETTY_FUNCTION)
  ReadState()
  ReadStateAndMaybeThrow(ABSL_PRETTY_FUNCTION)
```

### absl/base/internal/hardening.h
```
HardeningAssertInBounds(size_t index, size_t size) → inline void
```

### absl/base/internal/hide_ptr.h
```
HideMask() → constexpr uintptr_t
HidePtr(T* ptr) → inline uintptr_t
UnhidePtr(uintptr_t hidden) → inline T*
```

### absl/base/internal/iterator_traits.h
```
struct IteratorCategory
```

### absl/base/internal/iterator_traits_test_helper.h
```
class Cpp20ForwardZipIterator
  tmp(*this) → Cpp20ForwardZipIterator
```

### absl/base/internal/low_level_alloc.cc
```
struct AllocList
IntLog2(size_t size, size_t base) → static int
for(size_t i = size; i > base; i >>= 1)
Random(uint32_t *state) → static int
LLA_SkiplistLevels(size_t size, size_t base, uint32_t *random) → static int
for(int level = head->levels - 1; level >= 0; level--)
LLA_SkiplistInsert(AllocList *head, AllocList *e, AllocList **prev) → static void
for(; head->levels < e->levels; head->levels++)
for(int i = 0; i != e->levels; i++)
LLA_SkiplistDelete(AllocList *head, AllocList *e, AllocList **prev) → static void
for(int i = 0; i != e->levels && prev[i]->next[i] == e; i++)
while(head->levels > 0 && head->next[head->levels - 1] == nullptr)
InitSigSafeArena() → void
CreateGlobalArenas() → void
arena_(arena) → :
if(mask_valid_)
if(err != 0)
Magic(uintptr_t magic, AllocList::Header *ptr) → inline static uintptr_t
GetPageSize() → size_t
RoundedUpBlockSize() → size_t
random(0)
if(arena->allocation_count != 0)
while(arena->freelist.next[0] != nullptr)
if(munmap_result != 0)
CheckedAdd(uintptr_t a, uintptr_t b) → static inline uintptr_t
```

### absl/base/internal/low_level_alloc.h
```
class LowLevelAlloc
  ABSL_ATTRIBUTE_SECTION(malloc_hook)
  DeleteArena(Arena *arena) → bool
  LowLevelAlloc() → private:
```

### absl/base/internal/low_level_alloc_test.cc
```
struct BlockDesc
CheckBlockDesc(const BlockDesc &d) → static void
for(int i = 0; i != d.len; i++)
RandomizeBlockDesc(BlockDesc *d) → static void
for(int i = 0; i != d->len; i++)
Test(bool use_new_arena, bool call_malloc_hook, int n) → static void
if(use_new_arena)
for(int i = 0; i != n; i++)
if(i != 0 && i % 10000 == 0)
if(use_new_arena)
BeforeMain()
main(int argc, char *argv[]) → int
MAIN_THREAD_EM_ASM({ if (ENVIRONMENT_IS_WEB)
if(typeof TEST_FINISH === 'function')
```

### absl/base/internal/low_level_scheduling.h
```
class SchedulingGuard
  ReschedulingIsAllowed() → bool
  DisableRescheduling() → bool
  EnableRescheduling(bool disable_result) → void
  ScopedEnable() → public:
ScopedDisable()
if(identity != nullptr)
if(identity != nullptr)
if(!disable_result)
if(identity != nullptr)
if(scheduling_disabled_depth_ != 0)
if(scheduling_disabled_depth_ == 0)
```

### absl/base/internal/nullability_traits.h
```
struct IsNullabilityCompatibleType
struct IsNullabilityCompatibleType
```

### absl/base/internal/nullability_traits_test.cc
```
struct NotSmartPtr
```

### absl/base/internal/poison.cc
```
GetPageSize() → size_t
InitializePoisonedPointerInternal() → void*
```

### absl/base/internal/poison.h
```
GetBadPointerInternal() → inline void*
get_poisoned_pointer() → inline void*
```

### absl/base/internal/raw_logging.cc
```
VADoRawLog(char** buf, int* size, const char* format, va_list ap) → bool
if(n < 0 || n > *size)
DoRawLog(char** buf, int* size, const char* format, ...) → bool
DefaultLogFilterAndPrefix(absl::LogSeverity, const char* file, int line, char** buf, int* buf_size) → bool
RawLogVA(absl::LogSeverity severity, const char* file, int line, const char* format, va_list ap) → void
if(enabled)
if(no_chop)
if(severity == absl::LogSeverity::kFatal)
DefaultInternalLog(absl::LogSeverity severity, const char* file, int line, const std::string& message) → void
AsyncSignalSafeWriteError(const char* s, size_t len) → void
if(s[len - 1] == '\n')
if(len >= kLogBufSize)
RawLog(absl::LogSeverity severity, const char* file, int line, const char* format, ...) → void
RawLoggingFullySupported() → bool
RegisterLogFilterAndPrefixHook(LogFilterAndPrefixHook func) → void
RegisterAbortHook(AbortHook func) → void
RegisterInternalLogFunction(InternalLogFunction func) → void
```

### absl/base/internal/raw_logging.h
```
Basename(const char* fname, int offset) → constexpr const char*
```

### absl/base/internal/scoped_set_env.cc
```
SetEnvVar(const char* name, const char* value) → void
if(value == nullptr)
if(get_res == 0)
if(val == nullptr)
```

### absl/base/internal/scoped_set_env.h
```
class ScopedSetEnv
  ScopedSetEnv(const char* var_name, const char* new_value) → public:
```

### absl/base/internal/scoped_set_env_test.cc
```
GetEnvVar(const char* name) → std::string
if(get_res == 0)
if(val == nullptr)
```

### absl/base/internal/spinlock.cc
```
if(clamped == 0)
if(clamped == kSpinLockSleeper)
```

### absl/base/internal/spinlock.h
```
SpinLock() → :
SpinLock(mode) → :
GetAdaptiveSpinCount() → static int
SetAdaptiveSpinCount(int count) → static void
IsCooperative(SchedulingMode scheduling_mode) → static constexpr bool
RegisterWithTsan() → constexpr void
IsCooperative() → bool
TryLockImpl() → inline bool
SpinLockHolder(*l) → :
```

### absl/base/internal/spinlock_benchmark.cc
```
BM_TryLock(benchmark::State& state) → static void
for(auto _ : state)
BM_SpinLock(benchmark::State& state) → static void
for(auto _ : state)
```

### absl/base/internal/spinlock_wait.cc
```
SpinLockWait(std::atomic<uint32_t> *w, int n, const SpinLockWaitTransition trans[], base_internal::SchedulingMode scheduling_mode) → uint32_t
for(;;)
for(i = 0; i != n && v != trans[i].from; i++)
if(i == n)
SpinLockSuggestedDelayNS(int loop) → int
if(loop < 0 || loop > 32)
```

### absl/base/internal/spinlock_wait.h
```
struct SpinLockWaitTransition
```

### absl/base/internal/strerror.cc
```
StrErrorAdaptor(int errnum, char* buf, size_t buflen) → const char*
StrErrorInternal(int errnum) → std::string
if(*str == '\0')
StrError(int errnum) → std::string
```

### absl/base/internal/strerror_benchmark.cc
```
BM_AbslStrError(benchmark::State& state) → void
for(auto _ : state)
```

### absl/base/internal/strerror_test.cc
```
for(int i = 0; i < kNumCodes; ++i)
for(int i = 0; i < kNumCodes; ++i)
for(int i = 0; i < kNumThreads; ++i)
for(auto& thread : threads)
```

### absl/base/internal/sysinfo.cc
```
class UnscaledCycleClockWrapperForInitializeFrequency
struct TimeTscPair
Win32CountSetBits(ULONG_PTR bitMask) → DWORD
for(DWORD bitSetCount = 0; ; ++bitSetCount)
Win32NumCPUs() → int
if(success)
switch(ptr->Relationship)
GetNumCPUs() → static int
GetNominalCPUFrequency() → static double
GetNominalCPUFrequency() → static double
ReadLongFromFile(const char *file, long *value) → static bool
if(fd != -1)
if(len <= 0)
ReadMonotonicClockNanos() → static int64_t
if(rc != 0)
Now() → static int64_t
GetTimeTscPair() → static TimeTscPair
for(int i = 0; i < 10; ++i)
if(latency < best_latency)
MeasureTscFrequencyWithSleep(int sleep_nanoseconds) → static double
MeasureTscFrequency() → static double
for(int i = 0; i < 8; ++i)
if(measurement * 0.99 < last_measurement && last_measurement < measurement * 1.01)
GetNominalCPUFrequency() → static double
NumCPUs() → int
```

### absl/base/internal/sysinfo_test.cc
```
for(int i = 0; i < 10; ++i)
for(int j = 0; j < kNumThreads; ++j)
for(auto& thread : threads)
```

### absl/base/internal/thread_identity.cc
```
AllocateThreadIdentityKey(ThreadIdentityReclaimerFunction reclaimer) → void
SetCurrentThreadIdentity(ThreadIdentity* identity, ThreadIdentityReclaimerFunction reclaimer) → void
CurrentThreadIdentityIfPresent() → ThreadIdentity*
ClearCurrentThreadIdentity() → void
CurrentThreadIdentityIfPresent() → ThreadIdentity*
if(!initialized)
```

### absl/base/internal/thread_identity.h
```
struct PerThreadSynch
struct ThreadIdentity
thread_identity() → ThreadIdentity*
association_lock() → inline SpinLock*
CurrentThreadIdentityIfPresent() → inline ThreadIdentity*
```

### absl/base/internal/thread_identity_benchmark.cc
```
BM_SafeCurrentThreadIdentity(benchmark::State& state) → void
for(auto _ : state)
BM_UnsafeCurrentThreadIdentity(benchmark::State& state) → void
for(auto _ : state)
```

### absl/base/internal/thread_identity_test.cc
```
TestThreadIdentityCurrent(const void* assert_no_identity) → static void
if(assert_no_identity == kCheckNoIdentity)
for(int iter = 0; iter < kNumLoops; iter++)
for(int i = 0; i < kNumThreads; ++i)
for(auto& thread : threads)
for(int iter = 0; iter < kNumLoops; ++iter)
for(int thread = 0; thread < kNumThreads; ++thread)
for(int l = 0; l < kNumLockLoops; ++l)
for(int m = 0; m < kNumMutexes; ++m)
for(auto& thread : threads)
```

### absl/base/internal/tracing.h
```
TraceWait(const void* object, ObjectKind kind) → inline void
TraceContinue(const void* object, ObjectKind kind) → inline void
TraceSignal(const void* object, ObjectKind kind) → inline void
TraceObserved(const void* object, ObjectKind kind) → inline void
```

### absl/base/internal/tracing_strong_test.cc
```
if(tls_records != nullptr)
if(tls_records != nullptr)
if(tls_records != nullptr)
if(tls_records != nullptr)
```

### absl/base/internal/unaligned_access.h
```
UnalignedLoad16(const void* absl_nonnull p) → inline uint16_t
UnalignedLoad32(const void* absl_nonnull p) → inline uint32_t
UnalignedLoad64(const void* absl_nonnull p) → inline uint64_t
UnalignedStore16(void* absl_nonnull p, uint16_t v) → inline void
UnalignedStore32(void* absl_nonnull p, uint32_t v) → inline void
UnalignedStore64(void* absl_nonnull p, uint64_t v) → inline void
```

### absl/base/internal/unique_small_name_test.cc
```
struct VeryLongStructName
  VeryLongStructName(int i) → explicit
DoNotOptimize(const T& var) → void
VeryLongFreeFunctionName() → int
```

### absl/base/internal/unscaledcycleclock.h
```
class UnscaledCycleClock
  Now() → int64_t
  Frequency() → double
```

### absl/base/log_severity.cc
```
switch(s)
switch(s)
```

### absl/base/log_severity.h
```
LogSeverityName(absl::LogSeverity s) → constexpr const char*
switch(s)
NormalizeLogSeverity(absl::LogSeverity s) → constexpr absl::LogSeverity
NormalizeLogSeverity(int s) → constexpr absl::LogSeverity
op1(absl::T lhs, absl::LogSeverity rhs) → constexpr bool operator
op2(absl::LogSeverity lhs, absl::T rhs) → constexpr bool operator
```

### absl/base/log_severity_test.cc
```
StreamHelper(T value) → std::string
```

### absl/base/no_destructor.h
```
class NoDestructor
get() → T* absl_nonnull
get() → const T* absl_nonnull
get() → const T* absl_nonnull
get() → T* absl_nonnull
PlacementImpl(Args&&... args) → explicit
get() → const T* absl_nonnull
get() → T* absl_nonnull
```

### absl/base/no_destructor_benchmark.cc
```
class BM_Blob
BM_Blob(int val) → public:
Verify(int val) → void
NoDestrBlobFunc() → const BM_Blob&
OnHeapBlobFunc() → const BM_Blob&
BM_Direct(benchmark::State& state) → void
for(auto s : state)
BM_NoDestr(benchmark::State& state) → void
for(auto s : state)
BM_OnHeap(benchmark::State& state) → void
for(auto s : state)
BlobFunc(BM_Type t, int i) → FuncType
if(i == n)
switch(t)
switch(t)
RandIdx(int i) → int
BM_Many(benchmark::State& state) → void
for(int i = 0; i < kNumObjects; ++i)
if(t == kDirect)
for(auto s : state)
for(auto s : state)
BM_DirectMany(benchmark::State& state) → void
BM_NoDestrMany(benchmark::State& state) → void
BM_OnHeapMany(benchmark::State& state) → void
```

### absl/base/no_destructor_test.cc
```
struct Blob
struct TypeWithDeletedDestructor
Blob(std::initializer_list<int> xs)
Str0() → std::string*
Str1() → const char*
Str2() → const std::string&
Str2Copy() → const std::string&
Array() → const MyArray&
Vector() → const MyVector&
Int() → const int&
```

### absl/base/nullability_default_nonnull_test.cc
```
FuncWithDefaultNonnullArg(int*) → void
FuncWithDeducedDefaultNonnullArg(T*) → void
FuncWithDefaultNonnullReturn() → int*
```

### absl/base/nullability_test.cc
```
funcWithNonnullArg(int* absl_nonnull) → void
funcWithDeducedNonnullArg(T* absl_nonnull) → void
funcWithNonnullReturn() → int* absl_nonnull
```

### absl/base/optimization_test.cc
```
class ImplicitlyConvertibleToBool
class ExplicitlyConvertibleToBool
bool() → operator
bool() → explicit operator
```

### absl/base/prefetch.h
```
PrefetchToLocalCache(const void* addr) → ABSL_ATTRIBUTE_ALWAYS_INLINE i
PrefetchToLocalCacheNta(const void* addr) → ABSL_ATTRIBUTE_ALWAYS_INLINE i
PrefetchToLocalCacheForWrite(const void* addr) → ABSL_ATTRIBUTE_ALWAYS_INLINE i
PrefetchToLocalCache(const void* addr) → ABSL_ATTRIBUTE_ALWAYS_INLINE i
PrefetchToLocalCacheNta(const void* addr) → ABSL_ATTRIBUTE_ALWAYS_INLINE i
PrefetchToLocalCacheForWrite(const void* addr) → ABSL_ATTRIBUTE_ALWAYS_INLINE i
PrefetchToLocalCache(const void* addr) → ABSL_ATTRIBUTE_ALWAYS_INLINE i
PrefetchToLocalCacheNta(const void* addr) → ABSL_ATTRIBUTE_ALWAYS_INLINE i
PrefetchToLocalCacheForWrite(const void* addr) → ABSL_ATTRIBUTE_ALWAYS_INLINE i
```

### absl/base/spinlock_test_common.cc
```
struct SpinLockTest
EncodeWaitCycles(int64_t wait_start_time, int64_t wait_end_time) → static uint32_t
DecodeWaitCycles(uint32_t lock_value) → static int64_t
IsCooperative(const SpinLock& l) → static bool
Hash32(uint32_t a, uint32_t c) → static uint32_t
TestFunction(uint32_t thread_salt, SpinLock* spinlock) → static void
for(int i = 0; i < kIters; i++)
for(size_t j = 0; j < kArrayLength; j++)
ThreadedTest(SpinLock* spinlock) → static void
for(uint32_t i = 0; i < kNumThreads; ++i)
for(auto& thread : threads)
for(size_t i = 1; i < kArrayLength; i++)
for(int i = 0; i < 100; i++)
NotifyThenLock(Notification* locked, SpinLock* spinlock, BlockingCounter* b) → static void
LockThenWait(Notification* locked, SpinLock* spinlock, BlockingCounter* b) → static void
DeadlockTest(SpinLock* spinlock, int num_spinners) → static void
for(int i = 0; i < num_spinners; ++i)
for(auto& thread : threads)
```

### absl/base/throw_delegate.cc
```
ThrowStdLogicError(const std::string& what_arg) → void
ThrowStdLogicError(const char* what_arg) → void
ThrowStdInvalidArgument(const std::string& what_arg) → void
ThrowStdInvalidArgument(const char* what_arg) → void
ThrowStdDomainError(const std::string& what_arg) → void
ThrowStdDomainError(const char* what_arg) → void
ThrowStdLengthError(const std::string& what_arg) → void
ThrowStdLengthError(const char* what_arg) → void
ThrowStdOutOfRange(const std::string& what_arg) → void
ThrowStdOutOfRange(const char* what_arg) → void
ThrowStdRuntimeError(const std::string& what_arg) → void
ThrowStdRuntimeError(const char* what_arg) → void
ThrowStdRangeError(const std::string& what_arg) → void
ThrowStdRangeError(const char* what_arg) → void
ThrowStdOverflowError(const std::string& what_arg) → void
ThrowStdOverflowError(const char* what_arg) → void
ThrowStdUnderflowError(const std::string& what_arg) → void
ThrowStdUnderflowError(const char* what_arg) → void
ThrowStdBadFunctionCall() → void
ThrowStdBadAlloc() → void
ThrowStdBadArrayNewLength() → void
```

### absl/cleanup/cleanup_test.cc
```
struct IdentityFactory
class FunctorClass
  ASSERT_TRUE(callback_)
  callback_()
struct FunctorClassFactory
struct StdFunctionFactory
struct CleanupTest
struct DestructionCounter
IsSame() → constexpr bool
AsCallback(Callback callback) → static Callback
AsCallback(Callback callback) → static FunctorClass
FnPtrFunction() → void
```

### absl/cleanup/internal/cleanup.h
```
struct Tag
class Storage
WasDeduced() → constexpr bool
ReturnsVoid() → constexpr bool
Storage(Callback callback) → explicit
Storage(Storage&& other)
GetCallbackBuffer() → void*
GetCallback() → Callback&
IsCallbackEngaged() → bool
DestroyCallback() → void
```

### absl/container/btree_benchmark.cc
```
struct EraseIfPred
struct BigType
  Copy(other)
struct BigTypePtr
GenerateValues(int n) → std::vector<V>
BM_InsertImpl(benchmark::State& state, bool sorted) → void
if(sorted)
for(int j = i; j < i + batch_size; j++)
for(int j = i; j < i + batch_size; j++)
BM_Insert(benchmark::State& state) → void
BM_InsertSorted(benchmark::State& state) → void
BM_InsertSmall(benchmark::State& state) → void
for(int i = 0; i < kSize; ++i)
BM_LookupImpl(benchmark::State& state, bool sorted) → void
if(sorted)
BM_Lookup(benchmark::State& state) → void
BM_FullLookup(benchmark::State& state) → void
BM_Erase(benchmark::State& state) → void
for(int j = i; j < i + batch_size; j++)
for(int j = i; j < i + batch_size; j++)
BM_EraseRange(benchmark::State& state) → void
for(int j = 0; j < batch_size; j++)
BM_EraseIf(benchmark::State& state) → void
BM_QueueAddRem(benchmark::State& state) → void
for(size_t i = 0; i < half; ++i)
```

### absl/container/btree_map.h
```
struct btree_map_defaults
btree_map() → public:
swap(btree_map<K, V, C, A> &x, btree_map<K, V, C, A> &y) → void
btree_multimap() → public:
swap(btree_multimap<K, V, C, A> &x, btree_multimap<K, V, C, A> &y) → void
```

### absl/container/btree_set.h
```
struct btree_set_defaults
struct set_slot_policy
btree_set() → public:
swap(btree_set<K, C, A> &x, btree_set<K, C, A> &y) → void
btree_multiset() → public:
swap(btree_multiset<K, C, A> &x, btree_multiset<K, C, A> &y) → void
construct(Alloc *alloc, slot_type *slot, Args &&...args) → static void
construct(Alloc *alloc, slot_type *slot, slot_type *other) → static void
construct(Alloc *alloc, slot_type *slot, const slot_type *other) → static void
destroy(Alloc *alloc, slot_type *slot) → static void
```

### absl/container/btree_test.cc
```
class base_checker
  CheckPairEquals(*tree_iter, *checker_iter)
  CheckPairEquals(*tree_iter, *checker_iter)
  lower_bound(key)
  upper_bound(key)
  equal_range(key)
  contains(key)
  count(key)
  iter_check(tree_res.first, checker_res.first)
struct CompareIntToString
struct NonTransparentCompare
class StringLike
struct SubstringLess
class BtreeNodePeer
class BtreeMapTest
struct MovableOnlyInstanceThreeWayCompare
struct NoDefaultCtor
struct InsertMultiHintData
struct InsertMultiHintDataKeyCompare
struct IntCompareToCmp
struct KeyCompareToWeakOrdering
struct KeyCompareToStrongOrdering
struct ConstructorCounted
struct ConstructorCountedCompare
struct MultiKey
```

### absl/container/btree_test.h
```
struct remove_pair_const
struct KeyOfValue
struct Generator
  assert(i <= maxval)
  K(i) → return
GenerateDigits(char buf[16], unsigned val, unsigned maxval) → inline char*
while(maxval > 0)
GenerateNumbersWithSeed(int n, int maxval, int seed) → inline std::vector<int>
GenerateValuesWithSeed(int n, int maxval, int seed) → std::vector<V>
for(int i = 0; i < n; i++)
```

### absl/container/chunked_queue.h
```
class chunked_queue
  static_assert(kBlockSizeMin > 0, "Min block size cannot be zero") → private:
  resize(count)
  assign(count, value)
Incr() → void
IncrBy(size_t n) → void
while(ptr + n > limit)
iterator_common(it) → :
alloc_and_size_(alloc) → :
alloc_and_size_(alloc) → :
alloc_and_size_(alloc) → :
alloc_and_size_(alloc) → :
alloc_and_size_(alloc) → :
for(const_reference item : other)
if(this == &other)
empty() → bool
size() → size_t
resize(size_type new_size, const T& value) → void
for(size_t i = 0; i < to_add; ++i)
shrink_to_fit() → void
assign(Iter first, Iter last) → void
for(; first != last; ++first)
assign(size_type count, const T& value) → void
for(size_type i = 0; i < count; ++i)
assign(std::initializer_list<T> il) → void
```

### absl/container/chunked_queue_benchmark.cc
```
class forward_list_queue
class Element
emplace_back(Args&&... args) → void
push_back(const T& value) → void
begin() → iterator
end() → iterator
front() → T&
front() → const T&
pop_front() → void
empty() → bool
clear() → void
int() → operator
MakeQueue(int64_t num_elements) → Q
for(int64_t i = 0; i < num_elements; i++)
CustomArgs(benchmark::internal::Benchmark* b) → void
BM_construct(benchmark::State& state) → void
for(auto s : state)
BM_destroy(benchmark::State& state) → void
for(auto s : state)
BM_push_back(benchmark::State& state) → void
for(auto s : state)
BM_pop_front(benchmark::State& state) → void
for(auto s : state)
BM_clear(benchmark::State& state) → void
for(auto s : state)
```

### absl/container/chunked_queue_test.cc
```
struct TestEntry
struct MyType
BlockSizeRounding() → void
for(size_t n = 1; n < 100; ++n)
for(int i = 0; i < 10000; i++)
for(int i = 0; i < 10000; i++)
for(int i = 0; i < 130; ++i)
for(int j = 0; j < i; ++j)
for(int i = 0; i < 130; ++i)
for(int j = i; j < 130; ++j)
for(int i = 0; i < 130; ++i)
for(int i = 0; i < 130; ++i)
for(int i = 0; i < 100; i++)
for(int j = 0; j < i; j++)
for(int j = 0; j < i; j++)
if(i % 10 == 0)
for(int j = 0; j < i; j++)
for(int i = 0; i < 10; ++i)
ABSL_HARDENING_ASSERT([&hardened]()
if(!hardened)
```

### absl/container/fixed_array.h
```
NoexceptCopyable() → static constexpr bool
NoexceptMovable() → static constexpr bool
DefaultConstructorIsNonTrivial() → static constexpr bool
storage_(n, a) → :
storage_(n, a) → :
size() → size_type
max_size() → constexpr size_type
empty() → bool
memsize() → size_t
fill(const value_type& val) → void
AbslHashValue(H h, const FixedArray& v) → friend H
AsValueType(pointer ptr) → static pointer
AsValueType(StorageElementWrapper<value_type>* ptr) → static pointer
data() → StorageElement*
RedzoneBegin() → void*
RedzoneEnd() → void*
data() → StorageElement*
AnnotateConstruct(size_type) → void
AnnotateDestruct(size_type) → void
size() → size_type
begin() → StorageElement*
end() → StorageElement*
alloc() → allocator_type&
alloc() → const allocator_type&
UsingInlinedStorage(size_type n) → static bool
```

### absl/container/fixed_array_benchmark.cc
```
class SimpleClass
BM_FixedArray(benchmark::State& state) → void
for(auto _ : state)
```

### absl/container/fixed_array_exception_safety_test.cc
```
ReadMemory(FixedArrT* fixed_arr) → testing::AssertionResult
for(const auto& thrower : *fixed_arr)
```

### absl/container/fixed_array_test.cc
```
class ConstructionTester
  assert(self_ptr_ == this)
class ThreeInts
struct PickyDelete
  delete(p) → ::operator
IsOnStack(const ArrayType& a) → static bool
CheckConstructed() → void
set(int value) → void
get() → int
ThreeInts() → public:
for(int i = 0; i < 5; ++i)
for(int i = 0; i < 15; ++i)
for(int i = 0; i < 10; ++i)
for(int i = 1; i < 10; ++i)
TestArray(int n) → static void
for(int i = 0; i < n; i++)
for(int i = 0; i < n; i++)
for(int i = 0; i < n; i++)
for(int i = 0; i < n; i++)
for(int i = 0; i < n; i++)
TestArrayOfArrays(int n) → static void
for(int i = 0; i < n; i++)
for(int j = 0; j < elements_per_inner_array; j++)
for(int i = 0; i < n; i++)
for(int j = 0; j < elements_per_inner_array; j++)
```

### absl/container/flat_hash_map.h
```
struct FlatHashMapPolicy
flat_hash_map() → public:
c_for_each_fast(const flat_hash_map<K, V, H, E, A>& c, Function&& f) → std::decay_t<Function>
c_for_each_fast(flat_hash_map<K, V, H, E, A>& c, Function&& f) → std::decay_t<Function>
c_for_each_fast(flat_hash_map<K, V, H, E, A>&& c, Function&& f) → std::decay_t<Function>
construct(Allocator* alloc, slot_type* slot, Args&&... args) → static void
destroy(Allocator* alloc, slot_type* slot) → static auto
transfer(Allocator* alloc, slot_type* new_slot, slot_type* old_slot) → static auto
apply(F&& f, Args&&... args)
get_hash_slot_fn() → static constexpr HashSlotFn
space_used(const slot_type*) → static size_t
value(std::pair<const K, V>* kv) → static V&
value(const std::pair<const K, V>* kv) → static const V&
```

### absl/container/flat_hash_map_test.cc
```
struct BeforeMain
struct balast
struct LazyInt
struct Hash
struct Eq
struct InconsistentHashEqType
BeforeMain()
NonRelocatable(NonRelocatable&&)
for(int i = 0; i < 100; ++i)
for(int j = 0; j < 100; ++j)
size_t() → explicit operator
FirstIsEven(std::pair<const int, int> p) → bool
EXPECT_EQ(erase_if(s, [](std::pair<const int, int>)
EXPECT_EQ(erase_if(s, [](std::pair<const int, int>)
EXPECT_EQ(erase_if(s, [](std::pair<const int, int> kvp)
for(int i = 0; i < 100; ++i)
for(int i = 0; i < 100; ++i)
for(auto& p : expected)
for(size_t trial = 0; trial < 20; ++trial)
for(size_t initial = 3; initial < 100; ++initial)
for(size_t i = 0; i < initial; ++i)
AbslHashValue(H h, InconsistentHashEqType t) → friend H
for(int i = 100; i < 20000; ++i)
```

### absl/container/flat_hash_set.h
```
struct FlatHashSetPolicy
flat_hash_set() → public:
c_for_each_fast(const flat_hash_set<T, H, E, A>& c, Function&& f) → std::decay_t<Function>
c_for_each_fast(flat_hash_set<T, H, E, A>& c, Function&& f) → std::decay_t<Function>
c_for_each_fast(flat_hash_set<T, H, E, A>&& c, Function&& f) → std::decay_t<Function>
construct(Allocator* alloc, slot_type* slot, Args&&... args) → static void
destroy(Allocator* alloc, slot_type* slot) → static auto
element(slot_type* slot) → static T&
apply(F&& f, Args&&... args)
space_used(const T*) → static size_t
get_hash_slot_fn() → static constexpr HashSlotFn
```

### absl/container/flat_hash_set_test.cc
```
struct BeforeMain
class PoisonSoo
  SanitizerUnpoisonObject(&data_)
  SanitizerPoisonObject(&data_)
struct HashEqInvalidOnMove
  CHECK(!moved)
  CHECK(!moved)
class MoveOnlyInt
BeforeMain()
IsEven(int k) → bool
EXPECT_EQ(erase_if(s, [](int)
EXPECT_EQ(erase_if(s, [](int)
EXPECT_EQ(erase_if(s, [](int k)
for(int i = 0; i < 100; ++i)
AbslHashValue(H h, const PoisonSoo& pi) → friend H
HashEqInvalidOnMove(HashEqInvalidOnMove&& rhs)
AbslHashValue(H h, const MoveOnlyInt& m) → friend H
```

### absl/container/inlined_vector.h
```
storage_(allocator) → :
storage_(allocator) → :
storage_(allocator) → :
storage_(allocator) → :
storage_(allocator) → :
storage_(allocator) → :
if(absl::is_trivially_relocatable<value_type>::value && std::is_same<A, std::allocator<value_type>>::value)
storage_(allocator) → :
if(absl::is_trivially_relocatable<value_type>::value && std::is_same<A, std::allocator<value_type>>::value)
get_allocator() → allocator_type
assign(size_type n, const_reference v) → void
assign(std::initializer_list<value_type> list) → void
assign(ForwardIterator first, ForwardIterator last) → void
assign(InputIterator first, InputIterator last) → void
resize(size_type n) → void
resize(size_type n, const_reference v) → void
push_back(const_reference v) → void
push_back(value_type&& v) → void
reserve(size_type n) → void
shrink_to_fit() → void
swap(InlinedVector& other) → void
MoveAssignment(MemcpyPolicy, InlinedVector&& other) → void
DestroyExistingAndAdopt(InlinedVector&& other) → void
MoveAssignment(ElementwiseAssignPolicy, InlinedVector&& other) → void
MoveAssignment(ElementwiseConstructPolicy, InlinedVector&& other) → void
```

### absl/container/inlined_vector_benchmark.cc
```
struct Buffer
struct LargeCopyableOnly
struct LargeCopyableSwappable
  swap(*this, o)
  swap(a.d, b.d)
struct LargeCopyableMovable
struct LargeCopyableMovableSwappable
  swap(*this, o)
  swap(a.d, b.d)
struct TrivialType
class NontrivialType
BM_InlinedVectorFill(benchmark::State& state) → void
for(auto _ : state)
for(int i = 0; i < len; ++i)
BM_InlinedVectorFillRange(benchmark::State& state) → void
for(auto _ : state)
BM_StdVectorFill(benchmark::State& state) → void
for(auto _ : state)
for(int i = 0; i < len; ++i)
StringRepresentedInline(std::string s) → bool
GetNonShortStringOptimizationSize() → int
for(int i = 24; i <= 192; i *= 2)
BM_InlinedVectorFillString(benchmark::State& state) → void
for(auto _ : state)
for(int i = 0; i < len; i++)
```

### absl/container/inlined_vector_exception_safety_test.cc
```
class TestParams
GetSizeAt(size_t i) → constexpr static size_t
InlinedVectorInvariants(VecT* vec) → bool
NoThrowGuarantee(VecT*) → bool
if(!absl::allocator_is_nothrow<allocator_type>::value)
EXPECT_TRUE(tester.Test([](VecT* vec)
EXPECT_TRUE(tester.Test([](VecT* vec)
EXPECT_TRUE(tester.Test([](VecT* vec)
EXPECT_TRUE(tester.Test([](VecT* vec)
EXPECT_TRUE(tester.Test([](VecT* vec)
EXPECT_TRUE(tester.Test([](VecT* vec)
EXPECT_TRUE(tester.Test([](VecT* vec)
EXPECT_TRUE(tester.Test([](VecT* vec)
EXPECT_TRUE(tester.Test([](VecT* vec)
EXPECT_TRUE(tester.Test([](VecT* vec)
EXPECT_TRUE(tester.Test([](VecT* vec)
EXPECT_TRUE(tester.Test([](VecT* vec)
EXPECT_TRUE(tester.Test([](VecT* vec)
EXPECT_TRUE(tester.Test([](VecT* vec)
EXPECT_TRUE(tester.Test([](VecT* vec)
EXPECT_TRUE(tester.Test([](VecT* vec)
EXPECT_TRUE(tester.Test([](VecT* vec)
EXPECT_TRUE(tester.Test([](VecT* vec)
EXPECT_TRUE(tester.Test([](VecT* vec)
EXPECT_TRUE(tester.Test([](VecT* vec)
```

### absl/container/inlined_vector_test.cc
```
class InstanceTest
class RefCounted
  Ref()
  Unref()
  swap(a.value_, b.value_)
  swap(a.count_, b.count_)
  swap(*this, v)
  CHECK_NE(count_, nullptr)
  CHECK_GE(*count_, 0)
class Dynamic
struct NoDefaultCtor
struct NoCopy
struct NoAssign
struct MoveOnly
class NotTriviallyDestructible
class MoveConstructibleOnlyInstance
class NonSwappableInstance
struct MySpan
swap(RefCounted& a, RefCounted& b) → friend void
Ref() → void
Unref() → void
Fill(Container* v, size_t len, int offset = 0) → static void
for(size_t i = 0; i < len; i++)
Fill(size_t len, int offset = 0) → static IntVec
for(size_t len = 0; len < 20; len++)
```

### absl/container/internal/btree.h
```
struct StringBtreeDefaultLess
struct StringBtreeDefaultGreater
struct BtreeTestOnlyCheckedCompareOptOutBase
struct key_compare_adapter
  assert(rhs_comp_lhs < 0 && "lhs_comp_rhs > 0 -> rhs_comp_lhs < 0")
  assert(rhs_comp_lhs == 0 && "lhs_comp_rhs == 0 -> rhs_comp_lhs == 0")
  assert(rhs_comp_lhs > 0 && "lhs_comp_rhs < 0 -> rhs_comp_lhs > 0")
class map_value_compare
  comp(lhs.first, rhs.first) → return
struct upper_bound_adapter
struct SearchResult
class btree_node
  NodeTargetSlots(0, kTargetNodeSize)
  leaf_layout_type(slot_count, 0) → return
class btree_iterator_generation_info_enabled
  ABSL_INTERNAL_LOG(FATAL, "Attempting to use an invalidated iterator. The corresponding b-tree " "container has been mutated since this iterator was constructed.")
class btree_iterator_generation_info_disabled
class btree_iterator
  Equals(other) → return
  Equals(other) → return
  distance_slow(other) → return
  assert_valid_generation(node_)
  increment_n_slow(n) → return
  assert_valid_generation(node_)
  decrement_n_slow(n) → return
```

### absl/container/internal/btree_container.h
```
class btree_container
tree_(comp, alloc) → :
tree_(other.tree_, alloc) → :
count(const key_arg<K> &key) → size_type
contains(const key_arg<K> &key) → bool
erase(const key_arg<K> &key) → size_type
extract(iterator position) → node_type
extract(const_iterator position) → node_type
clear() → ABSL_ATTRIBUTE_REINITIALIZES v
swap(btree_container &other) → void
verify() → void
size() → size_type
max_size() → size_type
empty() → bool
get_allocator() → allocator_type
key_comp() → key_compare
value_comp() → value_compare
AbslHashValue(State h, const btree_container &b) → friend State
for(const auto &v : b)
btree_set_container()
super_type(comp, alloc) → :
insert(InputIterator b, InputIterator e) → void
insert(std::initializer_list<init_type> init) → void
if(res.second)
extract(const key_arg<K> &key) → node_type
```

### absl/container/internal/chunked_queue.h
```
class ChunkedQueueBlock
  ABSL_ASSERT(size_hint >= size_t{1})
  block_alloc(*alloc) → ChunkedQueueBlockAllocator
  block_alloc(*alloc) → ChunkedQueueBlockAllocator
  delete(ptr) → ::operator
  byte_alloc(*alloc) → ByteAllocator
  byte_alloc(*alloc) → ByteAllocator
New(size_t size_hint, Allocator* alloc) → static ChunkedQueueBlock*
Delete(ChunkedQueueBlock* ptr, Allocator* alloc) → static void
constexpr(std::is_same_v<ByteAllocator, std::allocator<char>>) → if
next() → ChunkedQueueBlock*
set_next(ChunkedQueueBlock* next) → void
start() → T*
limit() → T*
size() → size_t
block_size_from_bytes(size_t bytes) → static constexpr size_t
AllocSize(size_t min_element_count) → static constexpr size_t
start_offset() → static constexpr ptrdiff_t
alignment() → static constexpr size_t
elements_in_bytes(size_t bytes) → static constexpr size_t
constexpr(std::is_same_v<ByteAllocator, std::allocator<char>>) → if
```

### absl/container/internal/common.h
```
struct IfRRef
struct KeyArg
class node_handle_base
  destroy()
  reset()
struct CommonAccess
struct InsertReturnType
struct TypeList
struct ApplyWithoutDefaultSuffix
get_allocator() → allocator_type
alloc_(a) → :
alloc_(a) → :
destroy() → void
reset() → void
slot() → slot_type*
alloc() → allocator_type*
node_handle() → constexpr
value() → value_type&
node_handle() → constexpr
mapped() → mapped_type&
Destroy(Node* node) → static void
Reset(Node* node) → static void
Transfer(Args&&... args) → static T
Construct(Args&&... args) → static T
```

### absl/container/internal/common_policy_traits.h
```
struct common_policy_traits
  transfer_impl(alloc, new_slot, old_slot, Rank2{})
  destroy(alloc, old_slot)
construct(Alloc* alloc, slot_type* slot, Args&&... args) → static void
destroy(Alloc* alloc, slot_type* slot) → static auto
transfer(Alloc* alloc, slot_type* new_slot, slot_type* old_slot) → static void
transfer_uses_memcpy() → static constexpr bool
destroy_is_trivial() → static constexpr bool
transfer_impl(Alloc*, slot_type* new_slot, slot_type* old_slot, Rank1) → static std::true_type
transfer_impl(Alloc* alloc, slot_type* new_slot, slot_type* old_slot, Rank0) → static void
```

### absl/container/internal/common_policy_traits_test.cc
```
struct PolicyWithoutOptionalOps
functions() → static PolicyFunctions*
construct(void* a, Slot* b, Slot c) → static void
destroy(void* a, Slot* b) → static void
element(Slot* b) → static Slot&
transfer_fn() → static TransferFunctions*
transfer(void* a, Slot* b, Slot* c) → static void
transfer(void*, Slot*, Slot*) → static std::true_type
destroy(void*, Slot*) → static std::true_type
Test()
```

### absl/container/internal/compressed_tuple.h
```
struct Storage
ShouldUseBase() → constexpr bool
ShouldAnyUseBase() → constexpr bool
```

### absl/container/internal/compressed_tuple_test.cc
```
struct Empty
struct NotEmpty
struct TwoValues
struct IncompleteType
AsLValue(T&& t) → constexpr T&
value() → constexpr int
value() → constexpr int
f() → int
```

### absl/container/internal/container_memory.h
```
struct WithConstructedImplF
struct OffsetOf
struct IsLayoutCompatible
struct map_slot_policy
  emplace(slot)
  emplace(slot)
  emplace(slot)
  type() → absl::is_trivially_relocatable
  emplace(new_slot)
  destroy(alloc, old_slot)
struct HashElement
struct HashKey
struct EqualElement
Allocate(Alloc* alloc, size_t n) → void*
IsDestructionTrivial() → constexpr auto
Deallocate(Alloc* alloc, void* p, size_t n) → void
ConstructFromTupleImpl(Alloc* alloc, T* ptr, Tuple&& t, absl::index_sequence<I...>) → void
ConstructFromTuple(Alloc* alloc, T* ptr, Tuple&& t) → void
SanitizerPoisonMemoryRegion(const void* m, size_t s) → inline void
SanitizerUnpoisonMemoryRegion(const void* m, size_t s) → inline void
SanitizerPoisonObject(const T* object) → inline void
SanitizerUnpoisonObject(const T* object) → inline void
LayoutCompatible() → static constexpr bool
map_slot_type()
emplace(slot_type* slot) → static void
```

### absl/container/internal/container_memory_test.cc
```
struct TypeCountingAllocator
class Fixture
TypeCountingAllocator(const TypeCountingAllocator<U>&) → template <typename U>
allocate(size_t n, const void* = nullptr) → T*
deallocate(T* p, std::size_t n) → void
Fixture() → public:
ptr() → std::string*
alloc() → Alloc*
DecomposeValueImpl(char, F&& f, Arg&& arg) → const char*
DecomposePairImpl(char, F&& f, Args&&... args) → const char*
Slots()
for(int i = 0; i < 99; ++i)
NonRelocatable(NonRelocatable&&)
hash_with_seed(size_t v, size_t seed) → size_t
```

### absl/container/internal/hash_function_defaults.h
```
struct HashEq
struct StringHash
struct StringEq
struct StringHashEq
struct BasicStringHash
struct BasicStringEq
struct BasicStringHashEq
struct AbslContainerEq
struct AbslContainerHash
hash_with_seed(absl::string_view v, size_t seed) → size_t
hash_with_seed(const absl::Cord& v, size_t seed) → size_t
ToPtr(const T* ptr) → static const T*
ToPtr(const std::unique_ptr<U, D>& ptr) → static const T*
ToPtr(const std::shared_ptr<U>& ptr) → static const T*
```

### absl/container/internal/hash_function_defaults_test.cc
```
struct NoDeleter
struct StringLikeTest
struct TypeWithAbslContainerHash
struct TypeWithAbslContainerHashAndEq
struct Hashable
for(MyEnum e : {MyEnum::A, MyEnum::B, MyEnum::C})
NoOpReleaser(absl::string_view data, void* arg) → void
for(int i = 0; i < 1000; ++i)
for(size_t s = 0; s < number_of_segments; ++s)
HashableBy(Hash h) → static constexpr bool
Hash(const T& v) → size_t
```

### absl/container/internal/hash_generator_testing.h
```
struct UniqueGenerator
for(;;)
```

### absl/container/internal/hash_policy_testing.h
```
struct WithId
struct NonStandardLayout
id() → size_t
next_id() → static size_t
NonStandardLayout()
AbslHashValue(H h, const NonStandardLayout& v) → friend H
id() → size_t
```

### absl/container/internal/hash_policy_traits.h
```
Impl(Key&& k, int) → static key_type&
Impl(Key&& k, char) → static Key
space_used(const slot_type* slot) → static size_t
get_hash_slot_fn() → static constexpr HashSlotFn
soo_enabled() → static constexpr bool
hash_slot_fn_non_type_erased(const void* hash_fn, void* slot, size_t seed) → static size_t
soo_enabled_impl(Rank0) → static constexpr bool
```

### absl/container/internal/hash_policy_traits_test.cc
```
struct PolicyWithoutOptionalOps
struct Hash
struct PolicyNoHashFn
  fn(v) → return
apply(int v) → static int
get_hash_slot_fn() → static constexpr HashSlotFn
Test()
element(Slot* slot) → static Slot&
apply(const Fn& fn, int v) → static size_t
get_hash_slot_fn() → static constexpr HashSlotFn
get_hash_slot_fn() → static constexpr HashSlotFn
```

### absl/container/internal/hashtable_control_bytes.h
```
class NonIterableBitMask
struct GroupSse2Impl
struct GroupAArch64Impl
  NonIterableBitMaskType(mask) → return
  BitMaskType(mask) → return
  BitMaskType(mask) → return
  NonIterableBitMaskType(mask) → return
  NonIterableBitMaskType(mask) → return
struct GroupPortableImpl
TrailingZeros(T x) → uint32_t
bool() → explicit operator
LowestBitSet() → uint32_t
TrailingZeros() → uint32_t
LeadingZeros() → uint32_t
if(Shift == 3 && !NullifyBitsOnIteration)
if(Shift == 3 && NullifyBitsOnIteration)
begin() → BitMask
end() → BitMask
IsEmpty(ctrl_t c) → inline bool
IsFull(ctrl_t c) → inline bool
IsDeleted(ctrl_t c) → inline bool
IsEmptyOrDeleted(ctrl_t c) → inline bool
if(std::is_unsigned<char>::value)
GroupSse2Impl(const ctrl_t* pos) → explicit
Match(h2_t hash) → BitMaskType
```

### absl/container/internal/hashtable_control_bytes_test.cc
```
class GroupTest
CtrlT(int i) → ctrl_t
for(int i = 0; i < 100; ++i)
if(GroupType::kWidth == 16)
if(GroupType::kWidth == 16)
if(GroupType::kWidth == 16)
if(GroupType::kWidth == 16)
if(GroupType::kWidth == 16)
if(GroupType::kWidth == 16)
```

### absl/container/internal/hashtable_debug.h
```
struct HashtableDebugProbeSummary
GetHashtableDebugNumProbes(const C& c, const typename C::key_type& key) → size_t
GetHashtableDebugNumProbesHistogram(const C& container) → std::vector<size_t>
GetHashtableDebugProbeSummary(const C& container) → HashtableDebugProbeSummary
AllocatedByteSize(const C& c) → size_t
```

### absl/container/internal/hashtable_debug_hooks.h
```
struct HashtableDebugAccess
GetKey(const typename T::key_type& key, char) → const typename T::key_type&
GetNumProbes(const Container& c, const typename Container::key_type& key) → static size_t
```

### absl/container/internal/hashtablez_sampler.cc
```
TriggerHashtablezConfigListener() → void
GlobalHashtablezSampler() → HashtablezSampler&
ShouldForceSampling() → static bool
if(state == kUninitialized)
ForcedTrySample(size_t inline_element_size, size_t key_size, size_t value_size, uint16_t soo_capacity) → HashtablezInfoHandle
TestOnlyRefreshSamplingStateForCurrentThread() → void
ForcedTrySample(size_t, size_t, size_t, uint16_t) → HashtablezInfoHandle
TestOnlyRefreshSamplingStateForCurrentThread() → void
SampleSlow(SamplingState& next_sample, size_t inline_element_size, size_t key_size, size_t value_size, uint16_t soo_capacity) → HashtablezInfo*
if(first)
UnsampleSlow(HashtablezInfo* info) → void
RecordRehashSlow(HashtablezInfo* info, size_t total_probe_length) → void
RecordReservationSlow(HashtablezInfo* info, size_t target_capacity) → void
RecordClearedReservationSlow(HashtablezInfo* info) → void
RecordStorageChangedSlow(HashtablezInfo* info, size_t size, size_t capacity) → void
if(size == 0)
RecordInsertMissSlow(HashtablezInfo* info, size_t hash, size_t distance_from_desired) → void
RecordEraseSlow(HashtablezInfo* info) → void
SetHashtablezConfigListener(HashtablezConfigListener l) → void
IsHashtablezEnabled() → bool
SetHashtablezEnabled(bool enabled) → void
SetHashtablezEnabledInternal(bool enabled) → void
GetHashtablezSampleParameter() → int32_t
SetHashtablezSampleParameter(int32_t rate) → void
SetHashtablezSampleParameterInternal(int32_t rate) → void
```

### absl/container/internal/hashtablez_sampler.h
```
struct SamplingState
class HashtablezInfoHandle
  UnsampleSlow(info_)
  RecordStorageChangedSlow(info_, size, capacity)
  RecordRehashSlow(info_, total_probe_length)
  RecordReservationSlow(info_, target_capacity)
  RecordClearedReservationSlow(info_)
  RecordInsertMissSlow(info_, hash, distance_from_desired)
  RecordEraseSlow(info_)
class HashtablezInfoHandle
Unregister() → void
IsSampled() → inline bool
RecordStorageChanged(size_t size, size_t capacity) → inline void
RecordRehash(size_t total_probe_length) → inline void
RecordReservation(size_t target_capacity) → inline void
RecordClearedReservation() → inline void
RecordInsertMiss(size_t hash, size_t distance_from_desired) → inline void
RecordErase() → inline void
swap(HashtablezInfoHandle& lhs, HashtablezInfoHandle& rhs) → friend inline void
HashtablezInfoHandle(std::nullptr_t) → explicit
Unregister() → inline void
IsSampled() → inline bool
RecordStorageChanged(size_t , size_t) → inline void
RecordRehash(size_t) → inline void
RecordReservation(size_t) → inline void
```

### absl/container/internal/hashtablez_sampler_test.cc
```
class HashtablezInfoHandlePeer
class HashtablezInfoHandlePeer
GetInfo(HashtablezInfoHandle* h) → static HashtablezInfo*
GetInfo(HashtablezInfoHandle*) → static HashtablezInfo*
GetSizes(HashtablezSampler* s) → std::vector<size_t>
EXPECT_EQ(s->Iterate([&](const HashtablezInfo& info)
Register(HashtablezSampler* s, size_t size) → HashtablezInfo*
for(int i = 0; i < 1000; ++i)
for(int i = 0; i < 1000; ++i)
for(int i = 0; i < 1000000; ++i)
EXPECT_EQ(sampler.Iterate([&](const HashtablezInfo& h)
if(&h == info)
EXPECT_EQ(sampler.Iterate([&](const HashtablezInfo& h)
if(&h == info)
for(size_t i = 0; i < 3; ++i)
for(int i = 0; i < 10; ++i)
EXPECT_EQ(sampler.Iterate([&](const HashtablezInfo& info)
```

### absl/container/internal/heterogeneous_lookup_testing.h
```
class ExpensiveType
class CheapType
struct HeterogeneousHash
struct HeterogeneousEqual
ExpensiveType() → explicit operator
value() → int
```

### absl/container/internal/inlined_vector.h
```
struct Allocation
struct MallocAdapter
struct StorageView
class IteratorValueAdapter
class CopyValueAdapter
class DefaultValueAdapter
class AllocationTransaction
  Reset()
  GetCapacity()
class ConstructionTransaction
  GetSize()
class Storage
  DeallocateIfAllocated()
  DestroyContents()
  InitFrom(const Storage& other) → ABSL_ATTRIBUTE_NOINLINE void
  Initialize(ValueAdapter values, SizeType<A> new_size) → void
  Assign(ValueAdapter values, SizeType<A> new_size) → void
  Resize(ValueAdapter values, SizeType<A> new_size) → void
DestroyElements(A& allocator, Pointer<A> destroy_first, SizeType<A> destroy_size) → static void
for(SizeType<A> i = destroy_size; i != 0;)
DestroyElements(A& allocator, Pointer<A> destroy_first, SizeType<A> destroy_size) → static void
Allocate(A& allocator, SizeType<A> requested_capacity) → static Allocation<A>
Deallocate(A& allocator, Pointer<A> pointer, SizeType<A> capacity) → static void
ConstructElements(absl::type_identity_t<A>& allocator, Pointer<A> construct_first, ValueAdapter& values, SizeType<A> construct_size) → void
for(SizeType<A> i = 0; i < construct_size; ++i)
```

### absl/container/internal/layout.h
```
struct NotAligned
Find(Needle, Needle, Ts...) → constexpr size_t
Find(Needle, T, Ts...) → constexpr size_t
IsPow2(size_t n) → constexpr bool
Align(size_t n, size_t m) → constexpr size_t
Min(size_t a, size_t b) → constexpr size_t
Max(size_t a) → constexpr size_t
Max(size_t a, size_t b, Ts... rest) → constexpr size_t
TypeName() → std::string
ElementIndex() → static constexpr size_t
Alignment() → static constexpr size_t
Offset() → constexpr size_t
constexpr(N == 0) → if
Offset() → constexpr size_t
Size() → constexpr size_t
constexpr(N < NumStaticSizes) → if
Size() → constexpr size_t
Pointers(Char* p) → auto
Slices([[maybe_unused]] Char* p) → auto
AllocSize() → constexpr size_t
PoisonPadding(const Char* p) → void
constexpr(N == 0) → if
if(ElementAlignment<N - 1>::value % ElementAlignment<N>::value)
DebugString() → std::string
for(size_t i = 0; i != NumOffsets - 1; ++i)
```

### absl/container/internal/layout_benchmark.cc
```
MyAlign(size_t n, size_t m) → constexpr size_t
BM_OffsetConstantHeadroom(benchmark::State& state) → void
for(auto _ : state)
BM_OffsetConstantStatic(benchmark::State& state) → void
for(auto _ : state)
BM_OffsetConstant(benchmark::State& state) → void
for(auto _ : state)
BM_OffsetConstantIndirect(benchmark::State& state) → void
for(auto _ : state)
BM_OffsetPartialHeadroom(benchmark::State& state) → void
for(auto _ : state)
BM_OffsetPartialStatic(benchmark::State& state) → void
for(auto _ : state)
BM_OffsetPartial(benchmark::State& state) → void
for(auto _ : state)
BM_OffsetVariableHeadroom(benchmark::State& state) → void
for(auto _ : state)
BM_OffsetVariable(benchmark::State& state) → void
for(auto _ : state)
BM_AllocSizeHeadroom(benchmark::State& state) → void
for(auto _ : state)
BM_AllocSizeStatic(benchmark::State& state) → void
for(auto _ : state)
BM_AllocSize(benchmark::State& state) → void
for(auto _ : state)
```

### absl/container/internal/layout_test.cc
```
class TupleMatcher
  MatchAndExplainImpl(p, absl::make_index_sequence<std::tuple_size<Tuple>::value>{}) → return
struct Region
class CompactString
  layout(1, size + 1) → const L
class StaticCompactString
  layout(size + 1) → const SL
Distance(const void* from, const void* to) → size_t
Type(Actual val) → Expected
Name() → static std::string
SameType() → void
MatchAndExplain(const Tuple& p, testing::MatchResultListener*) → bool
DescribeTo(::std::ostream* os) → void
DescribeNegationTo(::std::ostream* os) → void
MatchAndExplainImpl(const Tuple& p, absl::index_sequence<Is...>) → bool
ExpectRegionPoisoned(const unsigned char* p, size_t n, bool poisoned) → void
for(size_t i = 0; i != n; ++i)
for(const Region& r : reg)
CompactString(const char* s = "") → public:
size() → size_t
c_str() → const char*
StaticCompactString(const char* s = "") → public:
size() → size_t
c_str() → const char*
```

### absl/container/internal/node_slot_policy.h
```
struct node_slot_policy
  static_assert(std::is_lvalue_reference<Reference>::value, "")
construct(Alloc* alloc, slot_type* slot, Args&&... args) → static void
destroy(Alloc* alloc, slot_type* slot) → static void
transfer(Alloc*, slot_type* new_slot, slot_type* old_slot) → static std::true_type
space_used(const slot_type* slot) → static size_t
element(slot_type* slot) → static Reference
```

### absl/container/internal/node_slot_policy_test.cc
```
new_element(Alloc* alloc, int value) → static int*
delete_element(Alloc* alloc, int* elem) → static void
```

### absl/container/internal/raw_hash_map.h
```
struct InstantiateRawHashMap
raw_hash_map()
if(res.second)
if(res.second)
```

### absl/container/internal/raw_hash_set.cc
```
struct BackingArrayPtrs
class ProbedItemEncoder
  ProcessEncodeWithOverflow(item) → return
  ABSL_SWISSTABLE_ASSERT(pos_ < end_)
  DecodeAndInsertImpl(common, policy, buffer_, pos_, old_slots) → return
  DecodeAndInsertToTableOverflow(common, policy, old_slots)
  ABSL_SWISSTABLE_ASSERT(pos_ >= end_)
  ABSL_SWISSTABLE_ASSERT(local_buffer_full_ && "must not be called when local buffer is not full")
  ProcessProbedMarkedElements(common, policy, control_, old_slots, marked_elements_starting_position_)
ZeroCtrlT() → constexpr ctrl_t
ValidateMaxSize([[maybe_unused]] size_t size, [[maybe_unused]] size_t key_size, [[maybe_unused]] size_t slot_size) → void
ValidateMaxCapacity(size_t capacity, size_t key_size, size_t slot_size) → void
RandomSeed() → inline size_t
ShouldRehashForBugDetection(size_t capacity) → bool
SingleGroupTableH1(size_t hash, PerTableSeed seed) → size_t
Resize1To3NewOffset(size_t hash, PerTableSeed seed) → size_t
SlotAddress(void* slot_array, size_t slot, size_t slot_size) → inline void*
NextSlot(void* slot, size_t slot_size, size_t i = 1) → inline void*
PrevSlot(void* slot, size_t slot_size) → inline void*
EmptyGeneration() → GenerationType*
should_rehash_for_bug_detection_on_insert(size_t capacity) → bool CommonFieldsGenerationInf
probe_till_first_non_full_group(const ctrl_t* ctrl, probe_seq<Group::kWidth>& seq, [[maybe_unused]] size_t capacity) → inline Group::NonIterableBitMa
while(true)
if(mask)
find_first_non_full_from_h1(const ctrl_t* ctrl, size_t h1, size_t capacity) → FindInfo
```

### absl/container/internal/raw_hash_set.h
```
class probe_seq
struct RequireUsableKey
struct soo_tag_t
struct full_soo_tag_t
struct non_soo_tag_t
struct uninitialized_tag_t
struct no_seed_empty_tag_t
class PerTableSeed
class HashtableSize
  NextSeed() → uint16_t
class CommonFieldsGenerationInfoEnabled
  should_rehash_for_bug_detection_on_insert(size_t capacity) → bool
  should_rehash_for_bug_detection_on_move(size_t capacity) → bool
  increment_generation()
class CommonFieldsGenerationInfoDisabled
class HashSetIteratorGenerationInfoEnabled
class HashSetIteratorGenerationInfoDisabled
class GrowthInfo
class RawHashSetLayout
struct HeapPtrs
class CommonFields
  ABSL_SWISSTABLE_IGNORE_UNINITIALIZED_RETURN(ctrl)
  AssertInSooMode()
  AssertInSooMode()
struct FindInfo
```

### absl/container/internal/raw_hash_set_allocator_test.cc
```
struct AllocState
class CheckedAlloc
  track_alloc(ptr)
  track_dealloc(ptr)
  swap(id_, that.id_)
  swap(state_, that.state_)
struct Identity
struct Policy
  destroy(alloc, old_slot)
struct PropagateTest
class PAlloc
CheckedAlloc()
select_on_container_copy_construction() → CheckedAlloc
allocate(size_t n) → T*
deallocate(T* ptr, size_t n) → void
num_allocs() → size_t
swap(CheckedAlloc& that) → void
swap(CheckedAlloc& a, CheckedAlloc& b) → friend void
track_alloc(void* ptr) → void
track_dealloc(void* ptr) → void
construct(allocator_type* alloc, slot_type* slot, Args&&... args) → static void
destroy(allocator_type* alloc, slot_type* slot) → static void
transfer(allocator_type* alloc, slot_type* new_slot, slot_type* old_slot) → static void
element(slot_type* slot) → static slot_type&
get_hash_slot_fn() → static constexpr HashSlotFn
```

### absl/container/internal/raw_hash_set_benchmark.cc
```
struct RawHashSetTestOnlyAccess
struct IntPolicy
class StringPolicy
  destroy(alloc, old_slot)
struct string_generator
  printable_ascii(0x20, 0x7E) → std::uniform_int_distribution<
construct(void*, int64_t* slot, int64_t v) → static void
destroy(void*, int64_t*) → static void
transfer(void*, int64_t* new_slot, int64_t* old_slot) → static void
element(slot_type* slot) → static int64_t&
get_hash_slot_fn() → static constexpr HashSlotFn
construct(allocator_type* alloc, slot_type* slot, Args... args) → static void
destroy(allocator_type* alloc, slot_type* slot) → static void
transfer(allocator_type* alloc, slot_type* new_slot, slot_type* old_slot) → static void
get_hash_slot_fn() → static constexpr HashSlotFn
StringTable()
IntTable()
BM_CacheInSteadyState(benchmark::State& state) → void
for(int i = 0; i != 90; ++i)
while(true)
if(x.second)
CacheInSteadyStateArgs(::benchmark::Benchmark* bm) → void
BM_EraseEmplace(benchmark::State& state) → void
for(int64_t i = 0; i < size; ++i)
for(int64_t i = 0; i < size; ++i)
```

### absl/container/internal/raw_hash_set_probe_benchmark.cc
```
struct Policy
class RandomizedAllocator
struct DefaultHash
struct LoadSizes
struct Ratios
struct String
struct Sequential
struct AlmostSequential
  current()
  current() → return
struct Uniform
struct Gaussian
struct Zipf
struct Random
struct Result
output() → OutputStyle
construct(allocator_type* alloc, slot_type* slot, const Arg& arg) → static void
destroy(allocator_type* alloc, slot_type* slot) → static void
element(slot_type* slot) → static slot_type&
get_hash_slot_fn() → static constexpr auto
GlobalBitGen() → absl::BitGen&
RandomizedAllocator(RandomizedAllocator<U>) → template <typename U>
allocate(size_t n) → static T*
deallocate(T* p, size_t n) → static void
GetPointers(size_t n) → static std::vector<T*>&
```

### absl/container/internal/raw_hash_set_resize_impl.h
```
struct ProbedItemImpl
  static_assert(kMaxNewBits + kMaxOldBits + kH2Bits == kTotalBits)
```

### absl/container/internal/raw_hash_set_resize_impl_test.cc
```
class EncoderTest
for(uint8_t h2 = 0; h2 < 128; ++h2)
for(uint8_t h2 = 0; h2 < 128; ++h2)
```

### absl/container/internal/raw_hash_set_test.cc
```
struct RawHashSetTestOnlyAccess
struct ValuePolicy
  destroy(alloc, old_slot)
class SizedValue
class AlignedValue
class StringPolicy
  destroy(alloc, old_slot)
struct BadFastHash
class SooTest
class SmallTableResizeTest
struct DecomposeType
struct DecomposeHash
struct DecomposeEq
struct DecomposePolicy
struct Modulo1000Hash
struct ProbeStats
  MaxRatio() → return
struct ExpectedStats
class RawHashSamplerTest
class SanitizerTest
class AlignOneTest
class InstanceTrackerTest
struct InstanceTrackerHash
struct CountedHash
class SooTable
```

### absl/container/internal/test_allocator.h
```
class CountingAllocator
allocate(size_type n, typename AllocatorTraits::const_void_pointer hint = nullptr) → pointer
if(bytes_used_ != nullptr)
deallocate(pointer p, size_type n) → void
if(bytes_used_ != nullptr)
construct(U* p, Args&&... args) → void
if(instance_count_ != nullptr)
destroy(U* p) → void
if(instance_count_ != nullptr)
Base(other.bytes_used_, other.instance_count_) → :
Base(other.bytes_used_, other.instance_count_) → :
Base(other.bytes_used_, other.instance_count_) → :
MinimumAlignmentAlloc(const MinimumAlignmentAlloc<U>&) → explicit
allocate(size_t n) → T*
deallocate(T* ptr, size_t n) → void
IsAssertEnabled() → inline bool
assert([&]()
TestCopyAssignAllocPropagation() → void
TestMoveAssignAllocPropagation() → void
TestSwapAllocPropagation() → void
TestAllocPropagation() → void
```

### absl/container/internal/test_instance_tracker.h
```
class BaseCountedInstance
  swap(lhs.value_, rhs.value_)
  swap(lhs.is_live_, rhs.is_live_)
class InstanceTracker
  ResetCopiesMovesSwaps()
class CopyableOnlyInstance
class CopyableMovableInstance
class MovableOnlyInstance
compare(const BaseCountedInstance& x) → absl::weak_ordering
value() → int
SwapImpl(BaseCountedInstance& lhs, BaseCountedInstance& rhs) → static void
start_live_instances_(BaseCountedInstance::num_live_instances_)
instances() → int
live_instances() → int
moves() → int
copies() → int
swaps() → int
comparisons() → int
ResetCopiesMovesSwaps() → void
swap(CopyableOnlyInstance& lhs, CopyableOnlyInstance& rhs) → friend void
supports_move() → static bool
swap(CopyableMovableInstance& lhs, CopyableMovableInstance& rhs) → friend void
supports_move() → static bool
swap(MovableOnlyInstance& lhs, MovableOnlyInstance& rhs) → friend void
supports_move() → static bool
```

### absl/container/internal/tracked.h
```
class Tracked
Tracked() → public:
num_copies_(that.num_copies_)
val() → const T&
num_copies() → size_t
num_moves() → size_t
```

### absl/container/internal/unordered_map_constructor_test.h
```
class ConstructorTest
BucketCountAllocTest() → void
BucketCountHashAllocTest() → void
AllocTest() → void
InputIteratorBucketAllocTest() → void
InputIteratorBucketHashAllocTest() → void
CopyConstructorAllocTest() → void
MoveConstructorAllocTest() → void
InitializerListBucketAllocTest() → void
InitializerListBucketHashAllocTest() → void
```

### absl/container/internal/unordered_map_lookup_test.h
```
class LookupTest
for(const auto& p : values)
for(const auto& p : values)
for(const auto& p : values)
for(const auto& p : values)
for(const auto& p : values)
```

### absl/container/internal/unordered_map_members_test.h
```
class MembersTest
UseType() → void
```

### absl/container/internal/unordered_map_modifiers_test.h
```
class ModifiersTest
struct EraseFirst
class UniquePtrModifiersTest
  static_assert(is_unique_ptr<typename UnordMap::mapped_type>::value, "UniquePtrModifiersTyest may only be called with a " "std::unique_ptr value type.")
UniquePtrModifiersTest() → protected:
```

### absl/container/internal/unordered_set_constructor_test.h
```
class ConstructorTest
BucketCountAllocTest() → void
BucketCountHashAllocTest() → void
AllocTest() → void
InputIteratorBucketAllocTest() → void
InputIteratorBucketHashAllocTest() → void
CopyConstructorAllocTest() → void
MoveConstructorAllocTest() → void
InitializerListBucketAllocTest() → void
InitializerListBucketHashAllocTest() → void
```

### absl/container/internal/unordered_set_lookup_test.h
```
class LookupTest
for(const auto& v : values)
for(const auto& v : values)
for(const auto& v : values)
```

### absl/container/internal/unordered_set_members_test.h
```
class MembersTest
UseType() → void
```

### absl/container/internal/unordered_set_modifiers_test.h
```
class ModifiersTest
struct EraseFirst
```

### absl/container/linked_hash_map.h
```
class linked_hash_map
ToKey(const K& k) → static const K&
ToKey(typename ListType::const_iterator it) → static const key_type&
ToKey(typename ListType::iterator it) → static const key_type&
get_allocator() → allocator_type
key() → const key_type&
mapped() → mapped_type&
linked_hash_map()
list_(alloc)
linked_hash_map(bucket_count, hash, eq, alloc) → :
if(this != &other)
if(this != &other)
size() → size_type
empty() → bool
begin() → iterator
end() → iterator
begin() → const_iterator
end() → const_iterator
cbegin() → const_iterator
cend() → const_iterator
rbegin() → reverse_iterator
rend() → reverse_iterator
rbegin() → const_reverse_iterator
rend() → const_reverse_iterator
crbegin() → const_reverse_iterator
```

### absl/container/linked_hash_map_benchmark.cc
```
struct BenchmarkData
for(int i = 0; i < size; ++i)
for(auto& set : sets)
for(const auto& str : data.sample)
for(auto& set : sets)
for(const auto& str : data.sample)
BM_TryEmplaceShortStrings_Hit(benchmark::State& state) → void
BenchmarkTryEmplaceStrings(state, [](int i)
BM_TryEmplaceLongStrings_Hit(benchmark::State& state) → void
BenchmarkTryEmplaceStrings(state, [](int i)
BM_TryEmplaceShortStrings_Miss(benchmark::State& state) → void
BenchmarkTryEmplaceStrings(state, [](int i)
BM_TryEmplaceLongStrings_Miss(benchmark::State& state) → void
BenchmarkTryEmplaceStrings(state, [](int i)
BM_InsertOrAssignShortStrings_Hit(benchmark::State& state) → void
BenchmarkInsertOrAssignStrings(state, [](int i)
BM_InsertOrAssignLongStrings_Hit(benchmark::State& state) → void
BenchmarkInsertOrAssignStrings(state, [](int i)
BM_InsertOrAssignShortStrings_Miss(benchmark::State& state) → void
BenchmarkInsertOrAssignStrings(state, [](int i)
BM_InsertOrAssignLongStrings_Miss(benchmark::State& state) → void
BenchmarkInsertOrAssignStrings(state, [](int i)
```

### absl/container/linked_hash_map_test.cc
```
struct NoCopy
class CopyableMovableType
struct CountedHash
for(const auto& kv : data)
for(const auto& kv : data)
AbslHashValue(H h, const CopyableMovableType& c) → friend H
for(int i : {2, 9, 4})
for(int i : {1, 2, 8, 9})
for(int i : {3, 4, 5, 6, 7})
for(int i = kFirstKey; i < kFirstKey + 100; ++i)
BuildHasDebugModeRehashes() → constexpr bool
```

### absl/container/linked_hash_set.h
```
class linked_hash_set
ToKey(const K& k) → static const K&
ToKey(typename ListType::const_iterator it) → static const key_type&
ToKey(typename ListType::iterator it) → static const key_type&
get_allocator() → allocator_type
value() → value_type&
linked_hash_set()
list_(alloc)
linked_hash_set(bucket_count, hash, eq, alloc) → :
if(this != &other)
if(this != &other)
size() → size_type
empty() → bool
begin() → iterator
end() → iterator
begin() → const_iterator
end() → const_iterator
cbegin() → const_iterator
cend() → const_iterator
rbegin() → reverse_iterator
rend() → reverse_iterator
rbegin() → const_reverse_iterator
rend() → const_reverse_iterator
crbegin() → const_reverse_iterator
crend() → const_reverse_iterator
```

### absl/container/linked_hash_set_benchmark.cc
```
for(auto& set : sets)
for(const auto& str : sample)
BM_InsertShortStrings_Hit(benchmark::State& state) → void
BenchmarkInsertStrings(state, [](int i)
BM_InsertLongStrings_Hit(benchmark::State& state) → void
BenchmarkInsertStrings(state, [](int i)
BM_InsertShortStrings_Miss(benchmark::State& state) → void
BenchmarkInsertStrings(state, [](int i)
BM_InsertLongStrings_Miss(benchmark::State& state) → void
BenchmarkInsertStrings(state, [](int i)
```

### absl/container/linked_hash_set_test.cc
```
struct IntUniquePtrHash
struct IntUniquePtrEq
struct CountedHash
for(const std::unique_ptr<int>& elt : s)
for(const int x : data)
for(const std::unique_ptr<int>& elt : s)
for(int i : {1, 7, 3, 6, 10, 2, 9, 4, 16})
for(int i : {1, 10})
for(int i : {2, 9, 4, 16})
for(int i : {1, 2, 8, 9, 81})
for(int i : {3, 4, 5, 25, 36, 7})
for(int i : {1, 7, 2, 9, 3, 29})
for(int i = kFirstKey; i < kFirstKey + 100; ++i)
BuildHasDebugModeRehashes() → constexpr bool
```

### absl/container/node_hash_map.h
```
node_hash_map() → public:
c_for_each_fast(const node_hash_map<K, V, H, E, A>& c, Function&& f) → std::decay_t<Function>
c_for_each_fast(node_hash_map<K, V, H, E, A>& c, Function&& f) → std::decay_t<Function>
c_for_each_fast(node_hash_map<K, V, H, E, A>&& c, Function&& f) → std::decay_t<Function>
new_element(Allocator* alloc, Args&&... args) → static value_type*
delete_element(Allocator* alloc, value_type* pair) → static void
apply(F&& f, Args&&... args)
element_space_used(const value_type*) → static size_t
value(value_type* elem) → static Value&
value(const value_type* elem) → static const Value&
get_hash_slot_fn() → static constexpr HashSlotFn
```

### absl/container/node_hash_map_test.cc
```
struct NonMovableKey
struct NonMovableKeyHash
struct NonMovableKeyEq
FirstIsEven(std::pair<const int, int> p) → bool
EXPECT_EQ(erase_if(s, [](std::pair<const int, int>)
EXPECT_EQ(erase_if(s, [](std::pair<const int, int>)
EXPECT_EQ(erase_if(s, [](std::pair<const int, int> kvp)
for(int i = 0; i < 100; ++i)
for(int i = 0; i < 100; ++i)
for(auto& p : expected)
```

### absl/container/node_hash_set.h
```
node_hash_set() → public:
c_for_each_fast(const node_hash_set<T, H, E, A>& c, Function&& f) → std::decay_t<Function>
c_for_each_fast(node_hash_set<T, H, E, A>& c, Function&& f) → std::decay_t<Function>
c_for_each_fast(node_hash_set<T, H, E, A>&& c, Function&& f) → std::decay_t<Function>
new_element(Allocator* alloc, Args&&... args) → static T*
delete_element(Allocator* alloc, T* elem) → static void
apply(F&& f, Args&&... args)
element_space_used(const T*) → static size_t
get_hash_slot_fn() → static constexpr HashSlotFn
```

### absl/container/node_hash_set_test.cc
```
IsEven(int k) → bool
EXPECT_EQ(erase_if(s, [](int)
EXPECT_EQ(erase_if(s, [](int)
EXPECT_EQ(erase_if(s, [](int k)
for(int i = 0; i < 100; ++i)
```

### absl/container/sample_element_size_test.cc
```
struct bigstruct
TestInlineElementSize(HashtablezSampler& sampler, std::unordered_set<const HashtablezInfo*>& preexisting_info, std::vector<Table>& tables, const std::vector<typename Table::value_type>& values, size_t expected_element_size) → void
for(int i = 0; i < 10; ++i)
EXPECT_EQ(sampler.Iterate([&](const HashtablezInfo& info)
AbslHashValue(H h, const bigstruct& c) → friend H
EXPECT_EQ(sampler.Iterate([&](const HashtablezInfo& info)
```

### absl/copts/copts.py
```
def GccStyleFilterAndCombine(default_flags, test_flags)  # Merges default_flags and test_flags for GCC and LLVM
```

### absl/copts/generate_copts.py
```
class CMakeStyle(object)
  def separator()
  def list_introducer(name)
  def list_closer()
  def docstring()
  def filename()
class StarlarkStyle(object)
  def separator()
  def list_introducer(name)
  def list_closer()
  def docstring()
  def filename()
def file_header_lines()
def flatten(*lists)
def relative_filename(filename)
def copt_list(name, arg_list, style)  # Copt file generation
def generate_copt_file(style)  # Creates a generated copt file using the given style object
def main(argv)
```

### absl/crc/crc32c.cc
```
CrcEngine() → const crc_internal::CRC*
UnextendCrc32cByZeroes(crc32c_t initial_crc, size_t length) → crc32c_t
ExtendCrc32cInternal(crc32c_t initial_crc, absl::string_view buf_to_add) → crc32c_t
ExtendCrc32cByZeroes(crc32c_t initial_crc, size_t length) → crc32c_t
ConcatCrc32c(crc32c_t lhs_crc, crc32c_t rhs_crc, size_t rhs_len) → crc32c_t
RemoveCrc32cPrefix(crc32c_t crc_a, crc32c_t crc_ab, size_t length_b) → crc32c_t
MemcpyCrc32c(void* dest, const void* src, size_t count, crc32c_t initial_crc) → crc32c_t
RemoveCrc32cSuffix(crc32c_t full_string_crc, crc32c_t suffix_crc, size_t suffix_len) → crc32c_t
```

### absl/crc/crc32c.h
```
uint32_t() → explicit operator
AbslStringify(Sink& sink, crc32c_t crc) → friend void
ExtendCrc32c(crc32c_t initial_crc, absl::string_view buf_to_add) → inline crc32c_t
ComputeCrc32c(absl::string_view buf) → inline crc32c_t
```

### absl/crc/crc32c_benchmark.cc
```
TestString(size_t len) → std::string
for(size_t i = 0; i < len; ++i)
BM_Calculate(benchmark::State& state) → void
for(auto s : state)
BM_Extend(benchmark::State& state) → void
for(auto s : state)
BM_ExtendCacheMiss(benchmark::State& state) → void
for(auto s : state)
for(int i = 0; i < total; i += len * 2)
BM_ExtendByZeroes(benchmark::State& state) → void
for(auto s : state)
BM_UnextendByZeroes(benchmark::State& state) → void
for(auto s : state)
BM_Concat(benchmark::State& state) → void
for(auto s : state)
BM_Memcpy(benchmark::State& state) → void
for(auto s : state)
BM_RemoveSuffix(benchmark::State& state) → void
for(auto s : state)
```

### absl/crc/crc32c_test.cc
```
TestString(size_t len) → std::string
for(size_t i = 0; i < len; ++i)
for(const size_t extend_by : kExtendByValues)
for(const auto &test_case : kTestCases)
for(auto seed_crc : {absl::crc32c_t{0}, absl::crc32c_t{0xc99465aa}})
for(const size_t size_1 : kExtendByValues)
for(const size_t size_2 : kUnextendByValues)
for(const size_t size : kSizes)
for(size_t bytes : kBytesSize)
```

### absl/crc/internal/cpu_detect.cc
```
GetVendor() → Vendor
if(vendor == "GenuineIntel")
GetIntelCpuType() → CpuType
if(family == 0x0f)
if(family == 0x0f || family == 0x6)
switch(brand_id)
switch(family) → case 0:
switch(model_num) → case 6:
GetAmdCpuType() → CpuType
if(family == 0x0f)
switch(family)
switch(model_num) → case 0x17:
switch(model_num) → case 0x19:
switch(model_num) → case 0x1A:
GetCpuType() → CpuType
SupportsArmCRC32PMULL() → bool
GetCpuType() → CpuType
if(hwcaps & HWCAP_CPUID)
switch(implementer)
switch(part_number) → case 0x41:
switch(part_number) → case 0xc0:
SupportsArmCRC32PMULL() → bool
GetCpuType() → CpuType
ReadSysctlByName(const char* name) → static std::optional<T>
if(ret == -1)
```

### absl/crc/internal/crc.cc
```
ReverseBits(uint32_t bits) → uint32_t
PolyMultiply(uint32_t* val, uint32_t m, uint32_t poly) → void
for(uint32_t one = onebit; one != 0; one >>= 1)
if(m & 1)
for(int j = 0; j != word_size; j++)
for(int i = 128; i != 0; i >>= 1)
if(j == 0 && i == 128)
if(i == 128)
if(pred & 1)
for(int i = 2; i != 256; i <<= 1)
for(int i = 0; i < 3; ++i)
for(uint64_t inc_len = 1; inc_len != 0; inc_len <<= ZEROES_BASE_LG)
for(int a = 1; a != ZEROES_BASE; a++)
if(result == nullptr)
for(int i = 0; i != 256; i++)
for(size_t i = 0; i < size; ++i)
for(size_t b = 0; b < 4; ++b)
for(int i = 0; i < 256; ++i)
for(int i = 0; i < j; i++)
if(kNeedAlignedLoads)
if(x <= e)
while(p != x)
for(size_t i = 0; i < 4; ++i)
while(p != e)
if(length != 0)
```

### absl/crc/internal/crc.h
```
class CRC
  Extend(uint32_t* crc, const void* bytes, size_t length) → void
  ExtendByZeroes(uint32_t* crc, size_t length) → void
  UnextendByZeroes(uint32_t* crc, size_t length) → void
  Scramble(uint32_t* crc) → void
  Unscramble(uint32_t* crc) → void
  Crc32c() → CRC*
  CRC() → protected:
```

### absl/crc/internal/crc32_x86_arm_combined_simd.h
```
CRC32_u8(uint32_t crc, uint8_t v) → inline uint32_t
CRC32_u16(uint32_t crc, uint16_t v) → inline uint32_t
CRC32_u32(uint32_t crc, uint32_t v) → inline uint32_t
CRC32_u64(uint32_t crc, uint64_t v) → inline uint32_t
V128_Load(const V128* src) → inline V128
V128_LoadU(const V128* src) → inline V128
V128_Store(V128* dst, V128 data) → inline void
V128_PMulHi(const V128 l, const V128 r) → inline V128
V128_PMulLow(const V128 l, const V128 r) → inline V128
V128_PMul01(const V128 l, const V128 r) → inline V128
V128_PMul10(const V128 l, const V128 r) → inline V128
V128_Xor(const V128 l, const V128 r) → inline V128
V128_From64WithZeroFill(const uint64_t r) → inline V128
V128_Extract32(const V128 l) → inline int
V128_Extract64(const V128 l) → inline uint64_t
V128_Low64(const V128 l) → inline int64_t
V128_Add64(const V128 l, const V128 r) → inline V128
CRC32_u8(uint32_t crc, uint8_t v) → inline uint32_t
CRC32_u16(uint32_t crc, uint16_t v) → inline uint32_t
CRC32_u32(uint32_t crc, uint32_t v) → inline uint32_t
CRC32_u64(uint32_t crc, uint64_t v) → inline uint32_t
V128_Load(const V128* src) → inline V128
V128_LoadU(const V128* src) → inline V128
V128_Store(V128* dst, V128 data) → inline void
V128_PMulHi(const V128 l, const V128 r) → inline V128
```

### absl/crc/internal/crc32c_inline.h
```
ExtendCrc32cInline(uint32_t* crc, const char* p, size_t n) → inline bool
if(n & 1)
if(n & 2)
if(n & 4)
while(n)
```

### absl/crc/internal/crc_cord_state.cc
```
refcounted_rep_(other.refcounted_rep_) → :
refcounted_rep_(other.refcounted_rep_) → :
if(this != &other)
if(this != &other)
for(auto& prefix_crc : r->prefix_crc)
for(auto& prefix_crc : rep->prefix_crc)
```

### absl/crc/internal/crc_cord_state.h
```
class CrcCordState
  CrcCordState() → public:
  Unref(refcounted_rep_)
  Checksum() → absl::crc32c_t
  Normalize() → void
  NormalizedPrefixCrcAtNthChunk(size_t n) → PrefixCrc
  Poison() → void
  RefSharedEmptyRep() → RefcountedRep*
  assert(r != nullptr)
rep() → const Rep&
mutable_rep() → Rep*
IsNormalized() → bool
NumChunks() → size_t
Ref(RefcountedRep* r) → static void
Unref(RefcountedRep* r) → static void
```

### absl/crc/internal/crc_internal.h
```
class CRCImpl
  NewInternal() → CRCImpl*
  FillWordTable(uint32_t poly, uint32_t last, int word_size, Uint32By256* t) → void
  FillZeroesTable(uint32_t poly, Uint32By256* t) → int
  InitTables() → void
class CRC32
  Extend(uint32_t* crc, const void* bytes, size_t length) → void
  ExtendByZeroes(uint32_t* crc, size_t length) → void
  Scramble(uint32_t* crc) → void
  Unscramble(uint32_t* crc) → void
  UnextendByZeroes(uint32_t* crc, size_t length) → void
  InitTables() → void
  ExtendByZeroesImpl(uint32_t* crc, size_t length, const uint32_t zeroes_table[256], const uint32_t poly_table[256]) → void
RoundUp(const uint8_t* p) → const uint8_t*
```

### absl/crc/internal/crc_memcpy.h
```
class CrcMemcpyEngine
  Compute(void* __restrict dst, const void* __restrict src, std::size_t length, crc32c_t initial_crc) → crc32c_t
class CrcMemcpy
  GetTestEngine(int vector, int integer) → std::unique_ptr<CrcMemcpyEngin
  GetArchSpecificEngines() → ArchSpecificEngines
class FallbackCrcMemcpyEngine
  Compute(void* __restrict dst, const void* __restrict src, std::size_t length, crc32c_t initial_crc) → crc32c_t
class CrcNonTemporalMemcpyEngine
  Compute(void* __restrict dst, const void* __restrict src, std::size_t length, crc32c_t initial_crc) → crc32c_t
class CrcNonTemporalMemcpyAVXEngine
  Compute(void* __restrict dst, const void* __restrict src, std::size_t length, crc32c_t initial_crc) → crc32c_t
CrcAndCopy(void* __restrict dst, const void* __restrict src, std::size_t length, crc32c_t initial_crc = crc32c_t{0}, bool non_temporal = false) → static crc32c_t
Crc32CAndCopy(void* __restrict dst, const void* __restrict src, std::size_t length, crc32c_t initial_crc = crc32c_t{0}, bool non_temporal = false) → inline crc32c_t
```

### absl/crc/internal/crc_memcpy_fallback.cc
```
for(; offset + kBlockSize < length; offset += kBlockSize)
if(offset < length)
```

### absl/crc/internal/crc_memcpy_test.cc
```
class CrcMemcpyTest
struct TestParams
CrcMemcpyTest() → protected:
EngineParamTestTemplate() → protected:
GetParam() → ParamsT
for(size_t source_alignment = 0; source_alignment < kAlignment; source_alignment++)
for(auto size : kTestSizes)
for(size_t i = 0; i < size; i++)
for(size_t dest_alignment = 0; dest_alignment < kAlignment; dest_alignment++)
for(auto size : kTestSizes)
for(size_t i = 0; i < size; i++)
```

### absl/crc/internal/crc_memcpy_x86_arm_combined.cc
```
class AcceleratedCrcMemcpyEngine
  Compute(void* __restrict dst, const void* __restrict src, std::size_t length, crc32c_t initial_crc) → crc32c_t
ShortCrcCopy(char* dst, const char* src, std::size_t length, crc32c_t crc) → inline crc32c_t
for(std::size_t i = 0; i < length; i++)
LargeTailCopy(crc32c_t* crcs, char** dst, const char** src, size_t region_size, size_t copy_rounds) → ABSL_ATTRIBUTE_NO_SANITIZE_UND
while(copy_rounds > 0)
for(size_t i = 0; i < vec_regions; i++)
for(size_t i = 0; i < int_regions; i++)
for(size_t j = 0; j < kIntLoadsPerVec; j++)
if(length < kCrcSmallSize)
if(bytes_from_last_aligned != 0)
for(size_t i = 1; i < kRegions; i++)
while(copy_rounds > kBlocksPerCacheLine)
for(size_t i = 0; i < kRegions; i++)
for(size_t i = 0; i < kBlocksPerCacheLine; i++)
for(size_t j = 0; j < vec_regions; j++)
for(size_t j = 0; j < int_regions; j++)
for(size_t k = 0; k < kIntLoadsPerVec; k++)
if(kRegions == 1)
for(size_t i = 0; i + 1 < kRegions; i++)
for(size_t i = 1; i + 1 < kRegions; i++)
switch(cpu_type)
switch(cpu_type)
if(vector == 3 && integer == 0)
```

### absl/crc/internal/crc_non_temporal_memcpy.cc
```
for(; offset + kBlockSize < length; offset += kBlockSize)
if(offset < length)
for(; offset + kBlockSize < length; offset += kBlockSize)
if(offset < length)
```

### absl/crc/internal/crc_x86_arm_combined.cc
```
class CRC32AcceleratedX86ARMCombined
  ExtendByZeroes(uint32_t* crc, size_t length) → void
  ComputeZeroConstant(size_t length) → uint32_t
class CRC32AcceleratedX86ARMCombinedMultipleStreamsBase
class CRC32AcceleratedX86ARMCombinedMultipleStreams
  static_assert(num_crc_streams >= 1 && num_crc_streams <= kMaxStreams, "Invalid number of crc streams")
  static_assert(num_pclmul_streams >= 0 && num_pclmul_streams <= kMaxStreams, "Invalid number of pclmul streams")
  ABSL_INTERNAL_STEP8BY3(l64, l641, l642, p, p1, p2)
  ABSL_INTERNAL_STEP8BY3(l64, l641, l642, p, p1, p2)
  ABSL_INTERNAL_STEP8BY3(l64, l641, l642, p, p1, p2)
  ABSL_INTERNAL_STEP8BY3(l64, l641, l642, p, p1, p2)
  ABSL_INTERNAL_STEP8BY3(l64, l641, l642, p, p1, p2)
  ABSL_INTERNAL_STEP8BY3(l64, l641, l642, p, p1, p2)
CRC32AcceleratedX86ARMCombined() → public:
MultiplyWithExtraX33(uint32_t a, uint32_t b) → uint32_t
while(length)
if(length >= size_t{1} << kNumDroppedBits)
Process64BytesPclmul(const uint8_t* p, V128* partialCRC) → ABSL_ATTRIBUTE_ALWAYS_INLINE v
FinalizePclmulStream(V128* partialCRC) → ABSL_ATTRIBUTE_ALWAYS_INLINE u
Process64BytesCRC(const uint8_t* p, uint64_t crc) → ABSL_ATTRIBUTE_ALWAYS_INLINE u
for(int i = 0; i < 8; i++)
Process64BytesCRC2Streams(const uint8_t* p0, const uint8_t* p1, uint64_t* crc) → ABSL_ATTRIBUTE_ALWAYS_INLINE v
for(int i = 0; i < 8; i++)
Process64BytesCRC3Streams(const uint8_t* p0, const uint8_t* p1, const uint8_t* p2, uint64_t* crc) → ABSL_ATTRIBUTE_ALWAYS_INLINE v
for(int i = 0; i < 8; i++)
```

### absl/crc/internal/gen_crc32c_consts.py
```
def poly_mul(a, b)  # Polynomial multiplication: a * b
def poly_div(a, b)  # Polynomial division: floor(a / b)
def poly_reduce(a, b)  # Polynomial reduction: a mod b
def poly_exp(a, b, g)  # Polynomial exponentiation: a^b mod g
def bitreflect(a, num_bits)  # Reflects the bits of the given integer
def print_crc32c_powers()  # Generates kCRC32CPowers[]
```

### absl/crc/internal/non_temporal_memcpy.h
```
if(len >= kCacheLineSize)
while(len >= kCacheLineSize)
if(len)
if(len >= kCacheLineSize)
while(len >= kCacheLineSize)
if(len)
```

### absl/crc/internal/non_temporal_memcpy_test.cc
```
struct TestParam
for(size_t i = 0; i < buf_size; i++)
```

### absl/debugging/failure_signal_handler.cc
```
struct FailureSignalData
struct WriterFnStruct
RaiseToDefaultHandler(int signo) → static void
RaiseToPreviousHandler(int signo) → static void
for(const auto& it : failure_signal_data)
if(it.signo == signo)
FailureSignalToString(int signo) → const char*
for(const auto& it : failure_signal_data)
if(it.signo == signo)
SetupAlternateStackOnce() → static bool
if(sigstk.ss_sp == MAP_FAILED)
if(sigstk.ss_sp == nullptr)
MaybeSetupAlternateStack() → static int
if(fsh_options.use_alternate_stack)
if(cpu != -1)
if(signal_string != nullptr && signal_string[0] != '\0')
WriterFnWrapper(const char* data, void* arg) → static void
PortableSleepForSeconds(int seconds) → static void
ImmediateAbortSignalHandler(int) → static void
GetCpuNumber() → static int
AbslFailureSignalHandler(int signo) → static void
AbslFailureSignalHandler(int signo, siginfo_t*, void* ucontext) → static void
if(this_tid != previous_failed_tid)
if(fsh_options.alarm_on_failure_secs > 0)
WriteFailureInfo(signo, ucontext, my_cpu, +[](const char* data)
```

### absl/debugging/failure_signal_handler.h
```
struct FailureSignalHandlerOptions
```

### absl/debugging/failure_signal_handler_test.cc
```
InstallHandlerAndRaise(int signo) → void
WriteToErrorFile(const char* msg) → void
if(msg != nullptr)
GetTmpDir() → std::string
for(const char* const var : kTmpEnvVars)
if(tmp_dir != nullptr)
InstallHandlerWithWriteToFileAndRaise(const char* file, int signo) → void
SignalParamToString(const ::testing::TestParamInfo<int>& info) → std::string
main(int argc, char** argv) → int
```

### absl/debugging/internal/address_is_readable.cc
```
AddressIsReadable(const void*) → bool
AddressIsReadable(const void *addr) → bool
```

### absl/debugging/internal/addresses.h
```
StripPointerMetadata(uintptr_t ptr) → inline uintptr_t
StripPointerMetadata(void* ptr) → inline uintptr_t
```

### absl/debugging/internal/bounded_utf8_length_sequence.h
```
class BoundedUtf8LengthSequence
  static_assert(max_elements > 0 && max_elements % 32 == 0, "max_elements must be a positive multiple of 32") → private:
InsertAndReturnSumOfPredecessors(uint32_t index, uint32_t utf8_length) → uint32_t
for(uint32_t j = word_index; j > 0; --j)
for(uint32_t j = max_elements/32 - 1; j > word_index; --j)
```

### absl/debugging/internal/bounded_utf8_length_sequence_test.cc
```
for(uint32_t i = 1; i < 31; ++i)
for(uint32_t i = 0; i < 32; ++i)
```

### absl/debugging/internal/decode_rust_punycode.cc
```
ConsumeOptionalAsciiPrefix(const char*& punycode_begin, const char* const punycode_end, char* const out_begin, char* const out_end, uint32_t& num_ascii_chars) → bool
for(int i = 0; i < punycode_end - punycode_begin; ++i)
if(c == '_')
DigitValue(char c) → int
ScanNextDelta(const char*& punycode_begin, const char* const punycode_end, uint32_t bias, uint32_t& i) → bool
for(uint32_t k = kBase; punycode_begin != punycode_end; k += kBase)
if(k <= bias + kTMin)
DecodeRustPunycode(DecodeRustPunycodeOptions options) → char* absl_nullable
while(punycode_begin != punycode_end)
if(total_utf8_bytes + utf8_for_code_point.length + 1 > out_size)
```

### absl/debugging/internal/decode_rust_punycode.h
```
struct DecodeRustPunycodeOptions
```

### absl/debugging/internal/decode_rust_punycode_test.cc
```
class DecodeRustPunycodeTest
  FillBufferWithNonzeroBytes()
  FillBufferWithNonzeroBytes()
  FillBufferWithNonzeroBytes()
FillBufferWithNonzeroBytes() → void
WithAmpleSpace() → DecodeRustPunycodeOptions
WithJustEnoughSpace() → DecodeRustPunycodeOptions
WithOneByteTooFew() → DecodeRustPunycodeOptions
PointsToTheNulAfter(const std::string& golden) → auto
ResultOf("preceding string body", [golden_size](const char* p)
```

### absl/debugging/internal/demangle.cc
```
class ComplexityGuard
UpdateHighWaterMark(State *state) → void
if(state->high_water_mark < state->parse_state.mangled_idx)
ReportHighWaterMark(State *state) → void
UpdateHighWaterMark(State *) → void
ReportHighWaterMark(State *) → void
IsTooComplex() → bool
if(state_->recursion_depth > kRecursionDepthLimit || state_->steps > kParseStepsLimit)
StrLen(const char *str) → static size_t
while(*str != '\0')
AtLeastNumCharsRemaining(const char *str, size_t n) → static bool
for(size_t i = 0; i < n; ++i)
if(str[i] == '\0')
StrPrefix(const char *str, const char *prefix) → static bool
while(str[i] != '\0' && prefix[i] != '\0' && str[i] == prefix[i])
InitState(State* state, const char* mangled, char* out, size_t out_size) → static void
ParseOneCharToken(State *state, const char one_char_token) → static bool
ParseTwoCharToken(State *state, const char *two_char_token) → static bool
ParseThreeCharToken(State *state, const char *three_char_token) → static bool
ParseLongToken(State *state, const char *long_token) → static bool
for(; long_token[i] != '\0'; ++i)
ParseCharClass(State *state, const char *char_class) → static bool
for(; *p != '\0'; ++p)
ParseDigit(State *state, int *digit) → static bool
if(digit != nullptr)
```

### absl/debugging/internal/demangle_rust.cc
```
class RustSymbolParser
  ABSL_DEMANGLER_RECURSE(path, kInstantiatingCrate)
  ABSL_DEMANGLER_RECURSE(path, kVendorSpecificSuffix)
  ABSL_DEMANGLER_RECURSE(impl_path, kInherentImplType)
  ABSL_DEMANGLER_RECURSE(type, kInherentImplEnding)
  ABSL_DEMANGLER_RECURSE(impl_path, kTraitImplType)
  ABSL_DEMANGLER_RECURSE(type, kTraitImplInfix)
  ABSL_DEMANGLER_RECURSE(path, kTraitImplEnding)
  ABSL_DEMANGLER_RECURSE(path, kImplPathEnding)
IsDigit(char c) → bool
IsLower(char c) → bool
IsUpper(char c) → bool
IsAlpha(char c) → bool
IsIdentifierChar(char c) → bool
IsLowerHexDigit(char c) → bool
BasicTypeName(char c) → const char*
switch(c)
for(; iter < kMaxReturns && recursion_depth_ > 0; ++iter)
switch(recursion_stack_[--recursion_depth_])
if(silence_depth_ == 0)
if(silence_depth_ == 0)
if(silence_depth_ == 0)
Peek() → char
Take() → char
for(; disambiguator > 0; disambiguator /= 10)
```

### absl/debugging/internal/demangle_rust_test.cc
```
ResultOfDemangling(const char* mangled, size_t buffer_size) → std::string
if(buffer[buffer_size] != kCanaryCharacter)
```

### absl/debugging/internal/demangle_test.cc
```
struct Base
struct Derived
DemangleSignalHandler(int signo) → static void
NestedMangledName(int depth) → static std::string
if(depth > 0)
TestOnInput(const char* input) → static void
for(int i = 0; i < depth; i++)
```

### absl/debugging/internal/elf_mem_image.cc
```
if(sym->st_shndx == SHN_UNDEF || sym->st_shndx >= SHN_LORESERVE)
while(version_definition->vd_ndx < index && version_definition->vd_next)
if(!base)
if(base_as_char[EI_MAG0] != ELFMAG0 || base_as_char[EI_MAG1] != ELFMAG1 || base_as_char[EI_MAG2] != ELFMAG2 || base_as_char[EI_MAG3] != ELFMAG3)
if(elf_class != kElfClass)
switch(base_as_char[EI_DATA])
for(int i = 0; i < ehdr_->e_phnum; ++i)
switch(program_header->p_type)
if(!~link_base_) → case PT_LOAD:
if(!~link_base_ || !dynamic_program_header)
for(; dynamic_entry->d_tag != DT_NULL; ++dynamic_entry)
switch(dynamic_entry->d_tag)
if(sysv_hash)
for(uint32_t i = nbuckets; i > 0;)
if(idx != 0)
while(chain[idx++] % 2 == 0)
for(const SymbolInfo& info : *this)
if(info_out)
for(const SymbolInfo& info : *this)
if(symbol_start <= address && address < symbol_end)
if(info_out)
if(symbol->st_shndx == SHN_UNDEF)
if(version_definition)
```

### absl/debugging/internal/elf_mem_image.h
```
class ElfMemImage
  SymbolIterator(const void *const image, uint32_t index) → private:
  Update(uint32_t incr) → void
  ElfMemImage(const void *base) → explicit
  Init(const void *base) → void
  GetNumSymbols() → uint32_t
  begin() → SymbolIterator
  end() → SymbolIterator
  LookupSymbol(const char *name, const char *version, int symbol_type, SymbolInfo *info_out) → bool
IsPresent() → bool
```

### absl/debugging/internal/examine_stack.cc
```
Allocate(size_t num_bytes) → void*
Deallocate(void* p, size_t size) → void
DumpPC(OutputWriter* writer, void* writer_arg, void* const pc, const char* const prefix) → void
DumpPCAndFrameSize(OutputWriter* writer, void* writer_arg, void* const pc, int framesize, const char* const prefix) → void
if(framesize <= 0)
DumpPCAndSymbol(OutputWriter* writer, void* writer_arg, void* const pc, const char* const prefix) → void
DumpPCAndFrameSizeAndSymbol(OutputWriter* writer, void* writer_arg, void* const pc, void* const symbolize_pc, int framesize, const char* const prefix) → void
if(framesize <= 0)
DebugStackTraceHookLegacyAdapter(void* const stack[], int depth, OutputWriter* writer, void* writer_arg) → void
RegisterDebugStackTraceHook(SymbolizeUrlEmitter hook) → void
GetDebugStackTraceHookLegacy() → SymbolizeUrlEmitterLegacy
if(debug_stack_trace_hook == nullptr)
GetDebugStackTraceHook() → SymbolizeUrlEmitter
GetProgramCounter(void* const vuc) → void*
if(vuc != nullptr)
if(vuc != nullptr)
DumpPCAndFrameSizesAndStackTrace(void* const pc, void* const stack[], int frame_sizes[], int depth, int min_dropped_frames, bool symbolize_stacktrace, OutputWriter* writer, void* writer_arg) → void
if(pc != nullptr)
if(symbolize_stacktrace)
for(int i = 0; i < depth; i++)
if(symbolize_stacktrace)
if(min_dropped_frames > 0)
DumpStackTrace(int min_dropped_frames, int max_num_frames, bool symbolize_stacktrace, OutputWriter* writer, void* writer_arg) → void
if(num_stack >= max_num_frames)
if(p != nullptr)
```

### absl/debugging/internal/stack_consumption.cc
```
EmptySignalHandler(int) → void
GetStackConsumption(const void* const altstack) → int
if(kStackGrowsDown)
for(int usage_count = kAlternateStackSize; usage_count > 0; --usage_count)
if(*begin != kAlternateStackFillValue)
if(signal_handler_stack_consumption != -1 && base_stack_consumption != -1)
```

### absl/debugging/internal/stack_consumption_test.cc
```
SimpleSignalHandler(int signo) → static void
if(signo == 0)
```

### absl/debugging/internal/symbolize.h
```
class SymbolDecoratorDeleter
class SymbolDecorator
  Decorate(const void* pc, ptrdiff_t relocation, char* symbol_buf, size_t symbol_buf_size, char* tmp_buf, size_t tmp_buf_size) → void
```

### absl/debugging/internal/utf8_for_code_point.cc
```
if(code_point <= kMax1ByteCodePoint)
if(code_point <= kMax2ByteCodePoint)
if(code_point <= kMax3ByteCodePoint)
```

### absl/debugging/internal/utf8_for_code_point.h
```
struct Utf8ForCodePoint
  Utf8ForCodePoint(uint64_t code_point) → explicit
ok() → bool
```

### absl/debugging/internal/vdso_support.cc
```
if(errno == 0)
if(fd == -1)
if(aux.a_type == AT_SYSINFO_EHDR)
GetCPU() → int
```

### absl/debugging/internal/vdso_support.h
```
class VDSOSupport
  VDSOSupport() → public:
  LookupSymbol(const char *name, const char *version, int symbol_type, SymbolInfo *info_out) → bool
  LookupSymbolByAddress(const void *address, SymbolInfo *info_out) → bool
  InitAndGetCPU(unsigned *cpu, void *cache, void *unused) → long
  GetCPUViaSyscall(unsigned *cpu, void *cache, void *unused) → long
  GetCPU(void) → friend int
IsPresent() → bool
begin() → SymbolIterator
end() → SymbolIterator
```

### absl/debugging/leak_check.cc
```
HaveLeakSanitizer() → bool
LeakCheckerIsActive() → bool
LeakCheckerIsActive() → bool
FindAndReportLeaks() → bool
DoIgnoreLeak(const void* ptr) → void
RegisterLivePointers(const void* ptr, size_t size) → void
UnRegisterLivePointers(const void* ptr, size_t size) → void
HaveLeakSanitizer() → bool
LeakCheckerIsActive() → bool
DoIgnoreLeak(const void*) → void
RegisterLivePointers(const void*, size_t) → void
UnRegisterLivePointers(const void*, size_t) → void
```

### absl/debugging/leak_check.h
```
class LeakCheckDisabler
  LeakCheckDisabler() → public:
IgnoreLeak(T* ptr) → T*
```

### absl/debugging/stacktrace.cc
```
Unwind(void** result, uintptr_t* frames, int* sizes, size_t max_depth, int skip_count, const void* uc, int* min_dropped_frames, bool unwind_with_fixup = true) → ABSL_ATTRIBUTE_ALWAYS_INLINE i
if(unwind_with_fixup)
if(g != nullptr)
if(frames != nullptr)
if(unwind_with_fixup)
GetStackFrames(void** result, int* sizes, int max_depth, int skip_count) → ABSL_ATTRIBUTE_NOINLINE ABSL_A
GetStackFramesWithContext(void** result, int* sizes, int max_depth, int skip_count, const void* uc, int* min_dropped_frames) → ABSL_ATTRIBUTE_NOINLINE ABSL_A
GetStackTrace(void** result, int max_depth, int skip_count) → ABSL_ATTRIBUTE_NOINLINE ABSL_A
GetStackTraceWithContext(void** result, int max_depth, int skip_count, const void* uc, int* min_dropped_frames) → ABSL_ATTRIBUTE_NOINLINE ABSL_A
SetStackUnwinder(Unwinder w) → void
DefaultStackUnwinder(void** pcs, int* sizes, int depth, int skip, const void* uc, int* min_dropped_frames) → int
if(sizes == nullptr)
if(uc == nullptr)
if(uc == nullptr)
```

### absl/debugging/stacktrace_benchmark.cc
```
func(benchmark::State& state, int x, int depth) → ABSL_ATTRIBUTE_NOINLINE void
if(x <= 0)
BM_GetStackTrace(benchmark::State& state) → void
restore_state([prev = g_enable_fixup]() → const Cleanup
for(auto s : state)
```

### absl/debugging/stacktrace_test.cc
```
struct StackTrace
Unwind(void* p) → ABSL_ATTRIBUTE_NOINLINE void
HugeFrame() → ABSL_ATTRIBUTE_NOINLINE void
FixupNoFixupEquivalenceNoInline() → ABSL_ATTRIBUTE_NOINLINE static
if(kSkipReason != nullptr)
if(!can_rely_on_frame_pointers)
restore_state([enable_fixup = g_enable_fixup, fixup_calls = g_fixup_calls, should_fixup_calls = g_should_fixup_calls]() → const Cleanup
if(kSkipReason != nullptr)
restore_state([enable_fixup = g_enable_fixup, fixup_calls = g_fixup_calls, should_fixup_calls = g_should_fixup_calls]() → const Cleanup
if(kSkipReason != nullptr)
restore_state([enable_fixup = g_enable_fixup, fixup_calls = g_fixup_calls, should_fixup_calls = g_should_fixup_calls]() → const Cleanup
SigUsr2Handler(int, siginfo_t*, void* uc) → void
if(support_is_expected)
if(support_is_expected)
SigUsr1Handler(int, siginfo_t*, void*) → void
RaiseSignal() → ABSL_ATTRIBUTE_NOINLINE void
TestNestedSignal() → ABSL_ATTRIBUTE_NOINLINE void
```

### absl/debugging/symbolize_test.cc
```
struct Foo
  func(int x) → void
nonstatic_func() → ABSL_SYMBOLIZE_TEST_NOINLINE v
static_func() → ABSL_SYMBOLIZE_TEST_NOINLINE s
regular_func() → int
if(found)
TestWithReturnAddress() → void ABSL_ATTRIBUTE_NOINLINE
SymbolizeSignalHandler(int signo) → static void
GetStackConsumptionUpperLimit() → static int
FilterElfHeader(struct dl_phdr_info *info, size_t size, void *data) → static int
for(int i = 0; i < info->dlpi_phnum; i++)
if(info->dlpi_name != nullptr && info->dlpi_name[0] != '\0')
if(volatile_bool)
for(const char *ptr : ptrs)
for(int i = 0; i < 2; i++)
for(size_t offset : offsets)
Factory(int) → static absl::debugging_interna
inline_func() → inline void *ABSL_ATTRIBUTE_AL
non_inline_func() → void *ABSL_ATTRIBUTE_NOINLINE
TestWithPCInsideNonInlineFunction() → void ABSL_ATTRIBUTE_NOINLINE
TestWithPCInsideInlineFunction() → void ABSL_ATTRIBUTE_NOINLINE
TestArmThumbOverlap() → void ABSL_ATTRIBUTE_NOINLINE
main(int argc, char **argv) → int
if(volatile_bool)
```

### absl/flags/commandlineflag.h
```
class CommandLineFlag
  Read(&u.value)
  Name() → absl::string_view
  Filename() → std::string
  Help() → std::string
  IsRetired() → bool
  DefaultValue() → std::string
  CurrentValue() → std::string
  ParseFrom(absl::string_view value, std::string* absl_nonnull error) → bool
IsOfType() → inline bool
TryGet() → std::optional<T>
U()
```

### absl/flags/commandlineflag_test.cc
```
class CommandLineFlagTest
  normalized(fname) → std::string
SetUpTestSuite() → static void
NormalizeFileName(absl::string_view fname) → static std::string
```

### absl/flags/flag.h
```
SetFlag(absl::Flag<T>* absl_nonnull flag, const T& v) → void
SetFlag(absl::Flag<T>* absl_nonnull flag, const V& v) → void
GetFlagReflectionHandle(const absl::Flag<T>& f) → const CommandLineFlag&
NonConst() → static std::string
Gen(void* absl_flag_default_loc) → static void
```

### absl/flags/flag_benchmark.cc
```
struct UDT
struct Accumulator
AbslParseFlag(absl::string_view src, OptionalInt* flag, std::string* error) → bool
AbslUnparseFlag(const OptionalInt& flag) → std::string
AbslParseFlag(absl::string_view src, OptionalString* flag, std::string* error) → bool
AbslUnparseFlag(const OptionalString& flag) → std::string
UDT(const UDT&)
AbslParseFlag(absl::string_view, UDT*, std::string*) → bool
AbslUnparseFlag(const UDT&) → std::string
for(auto _ : state)
Accumulate(typename Accumulator<T>::type& a, const T& f) → void
Accumulate(bool& a, bool f) → void
Accumulate(size_t& a, const std::string& f) → void
Accumulate(size_t& a, const std::vector<std::string>& f) → void
Accumulate(bool& a, const OptionalInt& f) → void
Accumulate(bool& a, const OptionalString& f) → void
Accumulate(bool& a, const UDT& f) → void
for(auto* flag_ptr : FlagPtrs_##T)
BM_ThreadedFindCommandLineFlag(benchmark::State& state) → void
for(auto* flag_ptr : FlagPtrs_bool)
```

### absl/flags/flag_test.cc
```
struct UDT
class FlagTest
  normalized(fname) → std::string
struct S1
struct S2
struct VerifyConsteval
struct NonTriviallyCopyableAggregate
struct CustomUDT
struct ConversionTestVal
struct NonDfltConstructible
struct SmallAlignUDT
struct NonTriviallyCopyableUDT
struct EnumWrapper
TestHelpMsg() → std::string
TestLiteralHelpMsg() → std::string
TestMakeDflt(void* dst) → void
TestCallback() → void
AbslParseFlag(absl::string_view, UDT*, std::string*) → bool
AbslUnparseFlag(const UDT&) → std::string
SetUpTestSuite() → static void
NormalizeFileName(absl::string_view fname) → static std::string
TestConstructionFor(const absl::Flag<T>& f1, absl::Flag<T>& f2) → bool
value(rhs.value) → :
AbslParseFlag(absl::string_view src, NonTriviallyCopyableAggregate* f, std::string* e) → bool
AbslUnparseFlag(const NonTriviallyCopyableAggregate& ntc) → std::string
```

### absl/flags/internal/commandlineflag.h
```
class FlagStateInterface
```

### absl/flags/internal/flag.cc
```
class MutexRelock
class FlagState
ShouldValidateFlagValue(FlagFastTypeId flag_type_id) → bool
FreelistMutex() → absl::Mutex&
AddToFreelist(void* p) → void
if(!s_freelist)
NumLeakedFlagValues() → uint64_t
counter_(counter)
if(is_candidate)
if(is_candidate)
if(def_kind == FlagDefaultKind::kGenFunc)
if(callback_ == nullptr)
switch(set_mode)
if(source == kCommandLine)
if(modified_)
if(!modified_)
```

### absl/flags/internal/flag.h
```
struct FixedCharArray
struct FlagHelpArg
struct FlagDefaultArg
struct EmptyBraces
struct FlagOneWordValue
class MaskedPointer
  IsUnprotectedReadCandidate() → bool
  HasBeenRead() → bool
  Set(FlagOpFn op, const void* src, bool is_candidate) → void
  MarkAsRead() → void
  ApplyMask(mask_t mask) → void
  CheckMask(mask_t mask) → bool
struct FlagMaskedPointerValue
struct FlagCallback
struct DynValueDeleter
  DynValueDeleter(FlagOpFn op_arg = nullptr) → explicit
class Flag
class FlagImplPeer
struct FlagRegistrarEmpty
class FlagRegistrar
Alloc(FlagOpFn op) → inline void*
Delete(FlagOpFn op, void* obj) → inline void
Copy(FlagOpFn op, const void* src, void* dst) → inline void
CopyConstruct(FlagOpFn op, const void* src, void* dst) → inline void
Clone(FlagOpFn op, const void* obj) → inline void*
```

### absl/flags/internal/path_util.h
```
Basename(absl::string_view filename) → inline absl::string_view
Package(absl::string_view filename) → inline absl::string_view
```

### absl/flags/internal/private_handle_accessor.h
```
class PrivateHandleAccessor
  TypeId(const CommandLineFlag& flag) → FlagFastTypeId
  SaveState(CommandLineFlag& flag) → std::unique_ptr<FlagStateInter
  IsSpecifiedOnCommandLine(const CommandLineFlag& flag) → bool
  ValidateInputValue(const CommandLineFlag& flag, absl::string_view value) → bool
  CheckDefaultValueParsingRoundtrip(const CommandLineFlag& flag) → void
  ParseFrom(CommandLineFlag& flag, absl::string_view value, flags_internal::FlagSettingMode set_mode, flags_internal::ValueSource source, std::string& error) → bool
  TypeName(const CommandLineFlag& flag) → absl::string_view
```

### absl/flags/internal/program_name.cc
```
ProgramNameMutex() → static absl::Mutex&
ProgramInvocationName() → std::string
ShortProgramInvocationName() → std::string
SetProgramInvocationName(absl::string_view prog_name_str) → void
if(!program_name)
```

### absl/flags/internal/registry.h
```
class RetiredFlag
Retire(const char* flag_name) → void
```

### absl/flags/internal/sequence_lock.h
```
class SequenceLock
  RelaxedCopyFromAtomic(dst, src, size)
  ABSL_PREDICT_TRUE(seq_before == seq_after) → return
  RelaxedCopyToAtomic(dst, src, size)
  assert(val != kUninitialized)
AlignUp(size_t x, size_t align) → inline constexpr size_t
MarkInitialized() → void
TryRead(void* dst, const std::atomic<uint64_t>* src, size_t size) → bool
Write(std::atomic<uint64_t>* dst, const void* src, size_t size) → void
ModificationCount() → int64_t
IncrementModificationCount() → void
RelaxedCopyFromAtomic(void* dst, const std::atomic<uint64_t>* src, size_t size) → static void
if(size > 0)
RelaxedCopyToAtomic(std::atomic<uint64_t>* dst, const void* src, size_t size) → static void
if(size > 0)
```

### absl/flags/internal/sequence_lock_test.cc
```
for(int i = 0; i < num_threads_; i++)
for(const auto& v : local_buf)
if(good)
MultiplicativeRange(int low, int high, int scale) → std::vector<int>
for(int current = low; current < high; current *= scale)
InterestingBufferSizes() → std::vector<int>
if(v > 1)
```

### absl/flags/internal/usage.cc
```
class XMLElement
class FlagHelpPrettyPrinter
  EndLine()
  EndLine()
  StartLine()
for(auto c : xml_elem.txt_)
switch(c)
IsValidXmlCharacter(unsigned char c) → static bool
first_line_(true)
Write(absl::string_view str, bool wrap_line = false) → void
if(wrap_line)
for(auto token : tokens)
if(token == "\n")
if(new_line)
StartLine() → void
if(first_line_)
EndLine() → void
FlagHelpHumanReadable(const CommandLineFlag& flag, std::ostream& out) → void
if(is_modified)
FlagsHelpImpl(std::ostream& out, PerFlagFilter filter_cb, HelpFormat format, absl::string_view program_usage_message) → void
if(format == HelpFormat::kHumanReadable)
for(auto& package : matching_flags)
if(format == HelpFormat::kHumanReadable)
for(auto& flags_in_file : package.second)
if(format == HelpFormat::kHumanReadable)
```

### absl/flags/internal/usage_test.cc
```
struct UDT
class UsageReportingTest
AbslParseFlag(absl::string_view, UDT*, std::string*) → static bool
AbslUnparseFlag(const UDT&) → static std::string
NormalizeFileName(absl::string_view fname) → static std::string
if(absl_pos != absl::string_view::npos)
UsageReportingTest() → protected:
main(int argc, char* argv[]) → int
```

### absl/flags/marshalling.cc
```
AbslParseFlag(absl::string_view text, bool* dst, std::string*) → bool
NumericBase(absl::string_view text) → static int
ParseFlagImpl(absl::string_view text, IntType& dst) → inline bool
AbslParseFlag(absl::string_view text, short* dst, std::string*) → bool
AbslParseFlag(absl::string_view text, unsigned short* dst, std::string*) → bool
AbslParseFlag(absl::string_view text, int* dst, std::string*) → bool
AbslParseFlag(absl::string_view text, unsigned int* dst, std::string*) → bool
AbslParseFlag(absl::string_view text, long* dst, std::string*) → bool
AbslParseFlag(absl::string_view text, unsigned long* dst, std::string*) → bool
AbslParseFlag(absl::string_view text, long long* dst, std::string*) → bool
AbslParseFlag(absl::string_view text, unsigned long long* dst, std::string*) → bool
AbslParseFlag(absl::string_view text, absl::int128* dst, std::string*) → bool
AbslParseFlag(absl::string_view text, absl::uint128* dst, std::string*) → bool
AbslParseFlag(absl::string_view text, float* dst, std::string*) → bool
AbslParseFlag(absl::string_view text, double* dst, std::string*) → bool
AbslParseFlag(absl::string_view text, std::string* dst, std::string*) → bool
AbslParseFlag(absl::string_view text, std::vector<std::string>* dst, std::string*) → bool
Unparse(bool v) → std::string
Unparse(short v) → std::string
Unparse(unsigned short v) → std::string
Unparse(int v) → std::string
Unparse(unsigned int v) → std::string
Unparse(long v) → std::string
Unparse(unsigned long v) → std::string
Unparse(long long v) → std::string
```

### absl/flags/marshalling.h
```
AbslParseFlag(absl::string_view text, std::optional<T>* f, std::string* err) → bool
InvokeParseFlag(absl::string_view input, T* dst, std::string* err) → bool
AbslUnparseFlag(const std::optional<T>& f) → std::string
Unparse(const T& v) → std::string
ParseFlag(absl::string_view input, T* dst, std::string* error) → inline bool
UnparseFlag(const T& v) → inline std::string
```

### absl/flags/marshalling_test.cc
```
TestRoundtrip(T v) → void
```

### absl/flags/parse.cc
```
struct SpecifiedFlagsCompare
class ArgsList
  ReadFromFlagfile(const std::string& flag_file_name) → bool
struct BestHints
ProcessingChecksMutex() → absl::Mutex&
SpecifiedFlagsMutex() → absl::Mutex&
if(absl::flags_internal::flagfile_needs_processing)
if(absl::flags_internal::fromenv_needs_processing)
if(absl::flags_internal::tryfromenv_needs_processing)
Size() → size_t
FrontIndex() → size_t
Front() → absl::string_view
PopFront() → void
if(!flag_file)
if(stripped[0] == '-')
if(stripped == "--")
GetEnvVar(const char* var_name, std::string& var_value) → bool
if(get_res == 0)
if(val == nullptr)
if(equal_sign_pos != absl::string_view::npos)
CheckDefaultValuesParsingRoundtrip() → void
ReadFlagfiles(const std::vector<std::string>& flagfiles, std::vector<ArgsList>& input_args) → bool
ReadFlagsFromEnv(const std::vector<std::string>& flag_names, std::vector<ArgsList>& input_args, bool fail_on_absent_in_env) → bool
for(const auto& flag_name : flag_names)
if(flag_name == "fromenv" || flag_name == "tryfromenv")
```

### absl/flags/parse.h
```
struct UnrecognizedFlag
```

### absl/flags/parse_test.cc
```
struct UDT
struct FlagfileData
class ParseTest
AbslParseFlag(absl::string_view in, UDT* udt, std::string* err) → bool
if(in == "A")
if(in == "AAA")
AbslUnparseFlag(const UDT& udt) → std::string
GetTestTmpDirEnvVar(const char* const env_var_name) → std::string
if(val == nullptr)
GetTestTempDir() → const std::string&
if(len < MAX_PATH && len != 0)
GetFlagfileFlag(const std::vector<FlagfileData>& ffd, std::string& flagfile_flag) → const char*
for(const auto& flagfile_data : ffd)
for(auto line : flagfile_data.file_lines)
```

### absl/flags/reflection.cc
```
class FlagRegistry
  RegisterFlag(CommandLineFlag& flag, const char* filename) → void
  FindFlag(absl::string_view name) → CommandLineFlag*
  GlobalRegistry() → FlagRegistry&
  FinalizeRegistry() → friend void
  FlagRegistry(const FlagRegistry&)
class FlagRegistryLock
class FlagSaverImpl
if(ins.second == false)
RegisterCommandLineFlag(CommandLineFlag& flag, const char* filename) → bool
FinalizeRegistry() → void
for(const auto& f : registry.flags_)
OnAccess() → void
Retire(const char* name, FlagFastTypeId type_id, unsigned char* buf) → void
SaveFromRegistry() → void
for(const auto& flag_state : backup_registry_)
FindCommandLineFlag(absl::string_view name) → CommandLineFlag*
```

### absl/flags/reflection.h
```
class FlagSaver
  FlagSaver() → public:
```

### absl/flags/reflection_test.cc
```
class ReflectionTest
struct CustomUDT
for(auto f : all_flags_1)
for(auto f : all_flags_2)
AbslParseFlag(absl::string_view in, CustomUDT* f, std::string*) → bool
AbslUnparseFlag(const CustomUDT& f) → std::string
```

### absl/flags/usage.cc
```
SetProgramUsageMessage(absl::string_view new_usage_message) → void
if(flags_internal::program_usage_message != nullptr)
ProgramUsageMessage() → absl::string_view
```

### absl/flags/usage_config.cc
```
ContainsHelpshortFlags(absl::string_view filename) → bool
ContainsHelppackageFlags(absl::string_view filename) → bool
VersionString() → std::string
NormalizeFilename(absl::string_view filename) → std::string
CustomUsageConfigMutex() → absl::Mutex&
GetUsageConfig() → FlagsUsageConfig
ReportUsageError(absl::string_view msg, bool is_fatal) → void
if(is_fatal)
SetFlagsUsageConfig(FlagsUsageConfig usage_config) → void
```

### absl/flags/usage_config.h
```
struct FlagsUsageConfig
```

### absl/flags/usage_config_test.cc
```
class FlagsUsageConfigTest
TstContainsHelpshortFlags(absl::string_view f) → bool
TstContainsHelppackageFlags(absl::string_view f) → bool
TstContainsHelpFlags(absl::string_view f) → bool
TstVersionString() → std::string
TstNormalizeFilename(absl::string_view filename) → std::string
TstReportUsageMessage(absl::string_view msg) → void
```

### absl/functional/any_invocable_test.cc
```
struct _
struct Wrapper
struct QualifiersForThisImpl
  static_assert(std::is_object<This>::value, "")
struct Int
struct TestParams
class AnyInvTestBasic
class AnyInvTestCombinatoric
class AnyInvTestMovable
class AnyInvTestNoexceptFalse
class AnyInvTestNoexceptTrue
class AnyInvTestNonRvalue
class AnyInvTestRvalue
noexcept(CallExceptionSpec == NothrowCall::yes)
noexcept(CallExceptionSpec == NothrowCall::yes)
Base(state_init, tail) → :
Base(state_init, tail) → :
Base(state_init, tail) → :
ToUnaryThisParam(T&& fun) → static UnaryThisParamType
ToThisParam(T&& fun) → static ThisParamType
ToVoidThisParam(T&& fun) → static VoidThisParamType
```

### absl/functional/bind_front_test.cc
```
struct Functor
struct ArgDescribe
struct NonCopyableFunctor
struct Struct
struct NonCopyable
struct FalseCopyable
struct ManglingCall
CharAt(const char* s, size_t index) → char
GetNonCopyableValue(const NonCopyable& n) → const std::string&
GetNonCopyableValueByWrapper(std::reference_wrapper<NonCopyable> n) → const std::string&
Sink(std::unique_ptr<int> p) → int
Factory(int n) → std::unique_ptr<int>
FalseCopyable()
GetMember(FalseCopyable<std::unique_ptr<int>> x) → int
Plus(int a, int b) → int
```

### absl/functional/function_ref.h
```
invoker_(&absl::functional_internal::InvokeObject<F&, R, Args...>) → :
invoker_(&absl::functional_internal::InvokeFunction<F*, R, Args...>) → :
Base(arg, obj) → :
```

### absl/functional/function_ref_test.cc
```
struct Class
Func() → int
CFunc() → int
Function() → int
Dereference(const T* v) → T
Copy(const T& v) → T
get_i() → int
```

### absl/functional/function_type_benchmark.cc
```
struct TrivialFunctor
struct LargeFunctor
struct FunctorWithTrivialArgs
struct FunctorWithNonTrivialArgs
FreeFunction() → void
CallFunction(Function f, Args&&... args) → void ABSL_ATTRIBUTE_NOINLINE
ConstructAndCallFunctionBenchmark(benchmark::State& state, const Callable& c, Args&&... args) → void
for(auto _ : state)
BM_TrivialStdFunction(benchmark::State& state) → void
BM_TrivialFunctionRef(benchmark::State& state) → void
BM_TrivialAnyInvocable(benchmark::State& state) → void
BM_LargeStdFunction(benchmark::State& state) → void
BM_LargeFunctionRef(benchmark::State& state) → void
BM_LargeAnyInvocable(benchmark::State& state) → void
BM_FunPtrStdFunction(benchmark::State& state) → void
BM_FunPtrFunctionRef(benchmark::State& state) → void
BM_FunPtrAnyInvocable(benchmark::State& state) → void
CallFunctionBenchmark(benchmark::State& state, const Callable& c, Args... args) → void
for(auto _ : state)
BM_TrivialArgsStdFunction(benchmark::State& state) → void
BM_TrivialArgsFunctionRef(benchmark::State& state) → void
BM_TrivialArgsAnyInvocable(benchmark::State& state) → void
BM_NonTrivialArgsStdFunction(benchmark::State& state) → void
BM_NonTrivialArgsFunctionRef(benchmark::State& state) → void
BM_NonTrivialArgsAnyInvocable(benchmark::State& state) → void
```

### absl/functional/internal/any_invocable.h
```
struct ForwardedParameter
struct TypedConversionConstruct
class Impl
class TrivialDeleter
  delete(target, size_) → ::operator
class TrivialDeleter
class CoreImpl
  Clear()
  manager_(FunctionToCall::dispose, &state_, &state_)
struct ConversionConstruct
struct UnwrapStdReferenceWrapperImpl
IsStoredLocally() → constexpr bool
InvokeR(F&& f, P&&... args) → ReturnType
constexpr(std::is_void_v<ReturnType>) → if
ObjectInLocalStorage(TypeErasedState* const state) → T&
switch(operation)
switch(operation)
switch(operation)
TrivialDeleter(std::size_t) → explicit
IsCompatibleConversion(void*, void*) → constexpr bool
IsCompatibleConversion(CoreImpl<NoExceptSrc, T...>*, CoreImpl<NoExceptDest, T...>*) → constexpr bool
CoreImpl(TypedConversionConstruct<QualDecayedTRef>, F&& f) → explicit
constexpr(std::is_pointer<DecayedT>::value || std::is_member_pointer<DecayedT>::value) → if
CoreImpl(absl::in_place_type_t<QualTRef>, Args&&... args) → explicit
HasValue() → bool
```

### absl/functional/internal/front_binder.h
```
class FrontBinder
Apply(Tuple&& bound, absl::index_sequence<Idx...>, Args&&... free) → R
```

### absl/functional/internal/function_ref.h
```
InvokeObject(VoidPtr ptr, typename ForwardT<Args>::type... args) → R
InvokeObject(VoidPtr ptr, typename ForwardT<Args>::type... args) → R
constexpr(std::is_member_function_pointer_v<Fun>) → if
InvokePtr(VoidPtr ptr, typename ForwardT<Args>::type... args) → R
constexpr(std::is_member_function_pointer_v<Fun>) → if
InvokeFunction(VoidPtr ptr, typename ForwardT<Args>::type... args) → R
InvokeFunction(VoidPtr, typename ForwardT<Args>::type... args) → R
AssertNonNull(const std::function<Sig>& f) → void
AssertNonNull(const AnyInvocable<Sig>& f) → void
AssertNonNull(const F&) → void
AssertNonNull(F C::* f) → void
```

### absl/hash/hash.h
```
HashOf(const Types&... values) → size_t
Create(T* state) → static HashState
combine_contiguous(HashState hash_state, const unsigned char* first, size_t size) → static HashState
combine_weakly_mixed_integer(HashState hash_state, hash_internal::WeaklyMixedInteger value) → static HashState
CombineContiguousImpl(void* p, const unsigned char* first, size_t size) → static void
CombineWeaklyMixedIntegerImpl(void* p, hash_internal::WeaklyMixedInteger value) → static void
combine_raw(HashState hash_state, uint64_t value) → static HashState
CombineRawImpl(void* p, uint64_t value) → static void
Init(T* state) → void
RunCombineUnordered(HashState state, CombinerT combiner) → static HashState
Init(HashState* state) → void
Real() → T&
```

### absl/hash/hash_benchmark.cc
```
class TypeErasedInterface
  HashValue(absl::HashState state) → void
struct TypeErasedAbslHash
struct FastUnorderedSet
struct LongCombine
struct PodPairInt64
struct PodRand
  static_assert(std::is_pod<T>::value, "")
struct StringRand
RunBenchmark(benchmark::State& state, T value) → void
for(auto _ : state)
AbslHashValue(H state, const TypeErasedInterface& wrapper) → friend H
FlatCord(size_t size) → absl::Cord
FragmentedCord(size_t size) → absl::Cord
while(size > chunk_size)
if(size > 0)
Vector(size_t count) → std::vector<T>
for(size_t v = 0; v < count; ++v)
FastUnorderedSet(size_t count) → explicit
for(size_t v = 0; v < count; ++v)
AbslHashValue(H h, const FastUnorderedSet& fus) → friend H
FlatHashSet(size_t count) → absl::flat_hash_set<T>
for(size_t v = 0; v < count; ++v)
AbslHashValue(H state, const LongCombine& v) → friend H
MakeLongTuple() → auto
```

### absl/hash/hash_instantiated_test.cc
```
class UnorderedSequence
class HashValueSequenceTest
class HashValueNestedSequenceTest
class HashValueAssociativeMapTest
class HashValueAssociativeMultimapTest
values_(begin, end) → :
size() → size_t
begin() → typename std::vector<T>::const
end() → typename std::vector<T>::const
AbslHashValue(H h, const UnorderedSequence& u) → friend H
```

### absl/hash/hash_test.cc
```
class HashValueIntTest
struct DummyDeleter
struct SmartPointerEq
struct WrapInTuple
struct Private
class PiecewiseHashTester
struct DummyFooBar
struct NoOp
struct EmptyCombine
struct CombineIterative
struct CombineVariadic
struct CustomHashType
class HashIntTest
struct StructWithPadding
struct ArraySlice
struct ConvertibleFromNoOp
struct IntAndString
struct ValueWithBoolConversion
struct TypeErasedHashStateUser
struct AutoReturnTypeUser
SpyHash(const T& value) → SpyHashState
for(size_t align = 1; align < kTotalSize / kNumValues; align < 8 ? align += 1 : align < 1024 ? align += 8 : align += 32)
for(size_t i = 0; i < kNumValues; ++i)
q() → void
vfa() → virtual void
```

### absl/hash/hash_testing.h
```
struct PrintVisitor
struct EqVisitor
  eq(*t, *u) → return
struct ExpandVisitor
struct TypeSet
struct ContainerAsVector
struct DefaultEquals
VerifyTypeImplementsAbslHashCorrectly(const Container& values, Eq equals) → testing::AssertionResult
ToString() → std::string
expand() → SpyHashState
for(const auto& value : values)
for(auto& eqclass : classes)
if(c == nullptr)
for(const auto& c : classes)
for(const Info& v : c)
for(const auto& c2 : classes)
Do(const Container& values) → static Out
DoImpl(const std::tuple<T...>& tuple, absl::index_sequence<I...>) → static Out
Do(const std::tuple<T...>& values) → static Out
Do(std::tuple<>) → static std::vector<VariantForT
VerifyTypeImplementsAbslHashCorrectly(const Container& values) → testing::AssertionResult
VerifyTypeImplementsAbslHashCorrectly(const Container& values, Eq equals) → testing::AssertionResult
VerifyTypeImplementsAbslHashCorrectly(std::initializer_list<T> values) → testing::AssertionResult
VerifyTypeImplementsAbslHashCorrectly(std::initializer_list<T> values, Eq equals) → testing::AssertionResult
```

### absl/hash/internal/city.cc
```
Fetch64(const char *p) → static uint64_t
Fetch32(const char *p) → static uint32_t
fmix(uint32_t h) → static uint32_t
Mur(uint32_t a, uint32_t h) → static uint32_t
Hash32Len13to24(const char *s, size_t len) → static uint32_t
Hash32Len0to4(const char *s, size_t len) → static uint32_t
for(size_t i = 0; i < len; i++)
Hash32Len5to12(const char *s, size_t len) → static uint32_t
CityHash32(const char *s, size_t len) → uint32_t
if(len <= 24)
ShiftMix(uint64_t val) → static uint64_t
HashLen16(uint64_t u, uint64_t v, uint64_t mul) → static uint64_t
HashLen16(uint64_t u, uint64_t v) → static uint64_t
HashLen0to16(const char *s, size_t len) → static uint64_t
if(len >= 8)
if(len >= 4)
if(len > 0)
HashLen17to32(const char *s, size_t len) → static uint64_t
HashLen33to64(const char *s, size_t len) → static uint64_t
CityHash64(const char *s, size_t len) → uint64_t
if(len <= 32)
if(len <= 16)
CityHash64WithSeed(const char *s, size_t len, uint64_t seed) → uint64_t
CityHash64WithSeeds(const char *s, size_t len, uint64_t seed0, uint64_t seed1) → uint64_t
```

### absl/hash/internal/city_test.cc
```
setup() → void
for(int i = 0; i < kDataSize; i++)
TestUnchanging(const uint64_t* expected, int offset, int len) → void
for(; i < kTestSize - 1; i++)
```

### absl/hash/internal/hash.cc
```
PrefetchFutureDataToLocalCache(const uint8_t* ptr) → void
Load128(const uint8_t* ptr) → inline Vector128
Set128(uint64_t a, uint64_t b) → inline Vector128
Add128(Vector128 a, Vector128 b) → inline Vector128
Sub128(Vector128 a, Vector128 b) → inline Vector128
Encrypt128(Vector128 data, Vector128 key) → inline Vector128
Decrypt128(Vector128 data, Vector128 key) → inline Vector128
MixA(Vector128 a, Vector128 state) → inline Vector128
MixB(Vector128 b, Vector128 state) → inline Vector128
MixC(Vector128 c, Vector128 state) → inline Vector128
MixD(Vector128 d, Vector128 state) → inline Vector128
ExtractLow64(Vector128 v) → inline uint64_t
ExtractHigh64(Vector128 v) → inline uint64_t
Mix4x16Vectors(Vector128 a, Vector128 b, Vector128 c, Vector128 d) → inline uint64_t
Load128(const uint8_t* ptr) → inline Vector128
Set128(uint64_t a, uint64_t b) → inline Vector128
Add128(Vector128 a, Vector128 b) → inline Vector128
Encrypt128(Vector128 data, Vector128 key) → inline Vector128
Decrypt128(Vector128 data, Vector128 key) → inline Vector128
MixA(Vector128 a, Vector128 state) → inline Vector128
MixB(Vector128 b, Vector128 state) → inline Vector128
MixC(Vector128 c, Vector128 state) → inline Vector128
MixD(Vector128 d, Vector128 state) → inline Vector128
ExtractLow64(Vector128 v) → inline uint64_t
ExtractHigh64(Vector128 v) → inline uint64_t
```

### absl/hash/internal/hash.h
```
class PiecewiseCombiner
  add_buffer(H state, const unsigned char* data, size_t size) → H
  finalize(H state) → H
class HashStateBase
  combine(H state, const T& value, const Ts&... values) → H
  combine_contiguous(H state, const T* data, size_t size) → H
  combine_unordered(H state, I begin, I end) → H
  cb(inner_state)
struct CombineRaw
struct HashWithSeed
struct VariantVisitor
struct HashSelect
  combine_contiguous(State hash_state, const unsigned char*, size_t) → State
  combine_raw(State state, uint64_t value) → State
  combine_weakly_mixed_integer(State hash_state, WeaklyMixedInteger value) → State
  Test(int) → std::true_type
  Test(char) → std::false_type
struct AggregateBarrier
struct PoisonedHash
struct HashImpl
PiecewiseChunkSize() → constexpr size_t
add_buffer(H state, const char* data, size_t size) → H
combine(H state) → static H
for(; begin != end; ++begin)
hash(const Hasher& hasher, const T& value, size_t seed) → size_t
```

### absl/hash/internal/hash_test.h
```
class TypeErasedValue
AbslHashValue(H hash_state, const TypeErasedValue& v) → friend H
HashValue(absl::HashState state) → void
```

### absl/hash/internal/low_level_hash_test.cc
```
if(skip)
for(size_t i = 0; i < kNumGoldenOutputs; ++i)
if(i % 3 == 2)
for(size_t i = 0; i < kNumGoldenOutputs; ++i)
```

### absl/hash/internal/print_hash_of.cc
```
main(int argc, char** argv) → int
```

### absl/hash/internal/spy_hash_state.h
```
struct OdrUse
struct RunOnStartup
SpyHashStateImpl(SpyHashStateImpl<U>&& other) → template <typename U>
combine(SpyHashStateImpl s, const A& a, const Args&... args) → static SpyHashStateImpl
combine(SpyHashStateImpl s) → static SpyHashStateImpl
if(direct_absl_hash_value_error_)
SetDirectAbslHashValueError() → static void
Compare(const SpyHashStateImpl& a, const SpyHashStateImpl& b) → static CompareResult
for(auto& s : hash_state.hash_representation_)
for(char c : s)
if(offset % 16 == 0)
if(offset % 2 == 0)
combine_contiguous(SpyHashStateImpl hash_state, const unsigned char* begin, size_t size) → static SpyHashStateImpl
if(size == 0)
while(size > large_chunk_stride)
if(size > 0)
combine_weakly_mixed_integer(SpyHashStateImpl hash_state, WeaklyMixedInteger value) → static SpyHashStateImpl
RunCombineUnordered(SpyHashStateImpl state, CombinerT combiner) → static SpyHashStateImpl
error() → std::optional<std::string>
if(moved_from_)
combine_raw(SpyHashStateImpl state, uint64_t value) → static SpyHashStateImpl
OdrUse() → constexpr
```

### absl/hash/internal/weakly_mixed_integer.h
```
struct WeaklyMixedInteger
```

### absl/log/die_if_null.cc
```
DieBecauseNull(const char* absl_nonnull file, int line, const char* absl_nonnull exprtext) → void
```

### absl/log/die_if_null.h
```
DieIfNull(const char* absl_nonnull file, int line, const char* absl_nonnull exprtext, T& t) → std::remove_reference_t<T>>::t
DieIfNull(const char* absl_nonnull file, int line, const char* absl_nonnull exprtext, T&& t) → std::remove_reference_t<T>>::t
```

### absl/log/flags.cc
```
SyncLoggingFlags() → void
RegisterSyncLoggingFlags() → bool
GetFromEnv(const char* varname, T dflt) → T
if(val != nullptr)
StderrThresholdDefault() → constexpr absl::LogSeverityAtL
if(last_colon == log_backtrace_at.npos)
```

### absl/log/flags_test.cc
```
class LogFlagsTest
DefaultStderrThreshold() → constexpr static absl::LogSeve
```

### absl/log/globals.cc
```
HashSiteForLogBacktraceAt(absl::string_view file, int line) → size_t
TriggerLoggingGlobalsListener() → void
RawSetMinLogLevel(absl::LogSeverityAtLeast severity) → void
RawSetStderrThreshold(absl::LogSeverityAtLeast severity) → void
RawEnableLogPrefix(bool on_off) → void
SetLoggingGlobalsListener(LoggingGlobalsListener l) → void
MinLogLevel() → absl::LogSeverityAtLeast
SetMinLogLevel(absl::LogSeverityAtLeast severity) → void
StderrThreshold() → absl::LogSeverityAtLeast
SetStderrThreshold(absl::LogSeverityAtLeast severity) → void
GetAndroidNativeTag() → const char*
SetAndroidNativeTag(const char* tag) → void
ShouldLogBacktraceAt(absl::string_view file, int line) → bool
SetLogBacktraceLocation(absl::string_view file, int line) → void
ClearLogBacktraceLocation() → void
ShouldPrependLogPrefix() → bool
EnableLogPrefix(bool on_off) → void
```

### absl/log/globals.h
```
SetStderrThreshold(absl::LogSeverity severity) → inline void
SetGlobalVLogLevel(int threshold) → inline int
SetVLogLevel(absl::string_view module_pattern, int threshold) → inline int
```

### absl/log/globals_test.cc
```
DefaultMinLogLevel() → constexpr static absl::LogSeve
DefaultStderrThreshold() → constexpr static absl::LogSeve
```

### absl/log/initialize.cc
```
InitializeLogImpl(absl::TimeZone time_zone) → void
InitializeLog() → void
```

### absl/log/internal/append_truncated.h
```
AppendTruncated(absl::string_view src, absl::Span<char> &dst) → inline size_t
AppendTruncated(std::wstring_view src, absl::Span<char> &dst) → inline size_t
for(const wchar_t wc : src)
AppendTruncated(char c, size_t n, absl::Span<char> &dst) → inline size_t
```

### absl/log/internal/check_op.cc
```
MakeCheckOpValueString(std::ostream& os, const char v) → void
if(v >= 32 && v <= 126)
MakeCheckOpValueString(std::ostream& os, const signed char v) → void
if(v >= 32 && v <= 126)
MakeCheckOpValueString(std::ostream& os, const unsigned char v) → void
if(v >= 32 && v <= 126)
MakeCheckOpValueString(std::ostream& os, const void* p) → void
if(p == nullptr)
if(equal == expected)
AbslFormatFlush(StringifySink* sink, absl::string_view text) → void
```

### absl/log/internal/check_op.h
```
struct UnprintableWrapper
class StringifySink
  StringifySink(std::ostream& os ABSL_ATTRIBUTE_LIFETIME_BOUND) → explicit
  Append(absl::string_view text) → void
  Append(size_t length, char ch) → void
  AbslFormatFlush(StringifySink* absl_nonnull sink, absl::string_view text) → friend void
class StringifyToStreamWrapper
  sink(os) → StringifySink
  AbslStringify(sink, wrapper.v_)
struct UnderlyingType
AsStatus(const absl::Status& s) → inline const absl::Status* abs
AsStatus(const absl::StatusOr<T>& s) → const absl::Status* absl_nonnu
ForVar1() → std::ostream&
MakeCheckOpValueString(std::ostream& os, const T& v) → inline void
UnprintableWrapper(const T&) → explicit
v_(v) → :
IsCharStarOrVoidStar() → constexpr bool
constexpr(std::is_reference_v<T>) → if
MakeCheckOpString(T1 v1, T2 v2, const char* absl_nonnull exprtext) → const char* absl_nonnull
constexpr(std::is_same_v<CheckOpStreamType<T1, T2>, UnprintableWrapper> && std::is_same_v<CheckOpStreamType<T2, T1>, UnprintableWrapper>) → if
GetReferenceableValue(const T& t) → inline constexpr const T&
GetReferenceableValue(char t) → inline constexpr char
GetReferenceableValue(unsigned char t) → inline constexpr unsigned char
GetReferenceableValue(signed char t) → inline constexpr signed char
GetReferenceableValue(short t) → inline constexpr short
```

### absl/log/internal/conditions.cc
```
LossyIncrement(std::atomic<uint32_t>* counter) → uint32_t
if(now_cycles > next_cycles)
```

### absl/log/internal/conditions.h
```
counter() → uint32_t
counter() → uint32_t
counter() → uint32_t
counter() → uint32_t
```

### absl/log/internal/container.h
```
struct LogBase
struct LogShortBase
struct LogMultilineBase
struct LogLegacyBase
struct LogShort
class LogShortUpToN
struct LogShortUpTo100
struct LogMultiline
class LogMultilineUpToN
struct LogMultilineUpTo100
struct LogLegacyUpTo100
struct LogLegacy
class RangeLogger
class EnumLogger
Log(std::ostream &out, const ElementT &element) → void
LogEllipsis(std::ostream &out) → void
LogOpening(std::ostream &out) → void
LogClosing(std::ostream &out) → void
LogFirstSeparator(std::ostream &out) → void
LogSeparator(std::ostream &out) → void
LogOpening(std::ostream &out) → void
LogClosing(std::ostream &out) → void
LogFirstSeparator(std::ostream &out) → void
LogSeparator(std::ostream &out) → void
LogOpening(std::ostream &out) → void
```

### absl/log/internal/container_test.cc
```
class ContainerLoggingTest
class UserDefFriend
class C
class LogEnumTest
  LogEnum(v) → return os <<
  LogEnum(v) → return os <<
stream() → std::ostream&
logged() → std::string
for(int i = 0; i < 1000; ++i)
if(i < 100)
if(i < max_elements)
AbslStringify(Sink& sink, const C& p) → friend void
```

### absl/log/internal/fnmatch.cc
```
FNMatch(absl::string_view pattern, absl::string_view str) → bool
while(true)
if(in_wildcard_match) → default:
if(end != fixed_portion.npos)
if(match == str.npos)
```

### absl/log/internal/fnmatch_benchmark.cc
```
BM_FNMatch(benchmark::State& state) → void
```

### absl/log/internal/globals.cc
```
IsInitialized() → bool
SetInitialized() → void
WriteToStderr(absl::string_view message, absl::LogSeverity severity) → void
if(severity >= absl::LogSeverity::kWarning)
SetTimeZone(absl::TimeZone tz) → void
TimeZone() → const absl::TimeZone*
ShouldSymbolizeLogStackTrace() → bool
EnableSymbolizeLogStackTrace(bool on_off) → void
MaxFramesInLogStackTrace() → int
SetMaxFramesInLogStackTrace(int max_num_frames) → void
ExitOnDFatal() → bool
SetExitOnDFatal(bool on_off) → void
SuppressSigabortTrace() → bool
SetSuppressSigabortTrace(bool on_off) → bool
```

### absl/log/internal/log_format.cc
```
PutLeadingWhitespace(T tid, char*& p) → inline std::enable_if_t<std::i
FormatBoundedFields(absl::LogSeverity severity, absl::Time timestamp, log_internal::Tid tid, absl::Span<char>& buf) → size_t
if(snprintf_result >= 0)
FormatLineNumber(int line, absl::Span<char>& buf) → size_t
FormatLogMessage(absl::LogSeverity severity, absl::CivilSecond civil_second, absl::Duration subsecond, log_internal::Tid tid, absl::string_view basename, int line, PrefixFormat format, absl::string_view message) → std::string
FormatLogPrefix(absl::LogSeverity severity, absl::Time timestamp, log_internal::Tid tid, absl::string_view basename, int line, PrefixFormat format, absl::Span<char>& buf) → size_t
```

### absl/log/internal/log_message.cc
```
PrintValue(absl::Span<char>& dst, absl::Span<const char> buf) → bool
ProtoSeverity(absl::LogSeverity severity, int verbose_level) → int32_t
switch(severity)
Basename(absl::string_view filepath) → absl::string_view
WriteToString(const char* data, void* str) → void
WriteToStream(const char* data, void* os) → void
encoded_remaining() → absl::Span<char>&
LogMessage(file, line, absl::LogSeverity::kInfo) → :
LogMessage(file, line, absl::LogSeverity::kWarning) → :
LogMessage(file, line, absl::LogSeverity::kError) → :
if(verbose_level == absl::LogEntry::kNoVerbosityLevel)
if(&__gcov_dump != nullptr)
if(v == nullptr)
if(data_->is_perror)
if(data_->first_fatal)
if(!data_->fail_quietly)
if(data_->fail_quietly)
LogMessage(file, line, absl::LogSeverity::kFatal) → :
LogMessage(file, line, absl::LogSeverity::kFatal) → :
LogMessage(file, line, absl::LogSeverity::kFatal) → :
LogMessage(file, line, absl::LogSeverity::kFatal) → :
LogMessage(file, line, absl::LogSeverity::kFatal) → :
LogMessageQuietlyFatal(file, line) → :
```

### absl/log/internal/log_message.h
```
class LogMessage
  AtLocation(absl::string_view file, int line) → LogMessage&
  NoPrefix() → LogMessage&
  WithVerbosity(int verbose_level) → LogMessage&
  WithTimestamp(absl::Time timestamp) → LogMessage&
  WithThreadID(absl::LogEntry::tid_t tid) → LogMessage&
  WithMetadataFrom(const absl::LogEntry& entry) → LogMessage&
  WithPerror() → LogMessage&
  ToSinkAlso(absl::LogSink* absl_nonnull sink) → LogMessage&
AtLocation(absl::SourceLocation loc) → LogMessage&
InternalStream() → LogMessage&
Append(size_t count, char ch) → void
Append(absl::string_view v) → void
AbslFormatFlush(StringifySink* absl_nonnull sink, absl::string_view v) → friend void
constexpr(absl::HasAbslStringify<T>::value) → if
```

### absl/log/internal/log_sink_set.cc
```
ThreadIsLoggingStatus() → bool&
if(pthread_key_create(&thread_is_logging_key, [](void* data)
AndroidLogLevel(const absl::LogEntry& entry) → static int
GlobalLogSinkSet() → public:
ABSL_LOCKS_EXCLUDED(guard_)
if(!extra_sinks_only)
for(absl::LogSink* sink : sinks_)
SendToSinks(const absl::LogEntry& entry, absl::Span<absl::LogSink*> sinks) → static void
for(absl::LogSink* sink : sinks)
GlobalSinks() → GlobalLogSinkSet&
ThreadIsLoggingToLogSink() → bool
LogToSinks(const absl::LogEntry& entry, absl::Span<absl::LogSink*> extra_sinks, bool extra_sinks_only) → void
AddLogSink(absl::LogSink* sink) → void
RemoveLogSink(absl::LogSink* sink) → void
FlushLogSinks() → void
```

### absl/log/internal/nullguard.h
```
Guard(const T& v) → static const T&
Guard(const char* v) → static const char*
Guard(const char* v) → static const char*
Guard(const signed char* v) → static const signed char*
Guard(const signed char* v) → static const signed char*
Guard(const unsigned char* v) → static const unsigned char*
Guard(const unsigned char* v) → static const unsigned char*
Guard(const std::nullptr_t&) → static const char*
```

### absl/log/internal/nullstream.h
```
class NullStream
AtLocation(absl::string_view, int) → NullStream&
AtLocation(SourceLocationType) → NullStream&
NoPrefix() → NullStream&
WithVerbosity(int) → NullStream&
WithTimestamp(TimeType) → NullStream&
WithThreadID(Tid) → NullStream&
WithMetadataFrom(const LogEntryType&) → NullStream&
WithPerror() → NullStream&
ToSinkAlso(LogSinkType*) → NullStream&
ToSinkOnly(LogSinkType*) → NullStream&
OutputToSink(LogSinkType*, bool) → NullStream&
InternalStream() → NullStream&
Flush() → void
fatal_(severity == absl::LogSeverity::kFatal) → :
if(fatal_)
```

### absl/log/internal/proto.cc
```
EncodeRawVarint(uint64_t value, size_t size, absl::Span<char> *buf) → void
for(size_t s = 0; s < size; s++)
EncodeVarint(uint64_t tag, uint64_t value, absl::Span<char> *buf) → bool
Encode64Bit(uint64_t tag, uint64_t value, absl::Span<char> *buf) → bool
Encode32Bit(uint64_t tag, uint32_t value, absl::Span<char> *buf) → bool
EncodeBytes(uint64_t tag, absl::Span<const char> value, absl::Span<char> *buf) → bool
EncodeBytesTruncate(uint64_t tag, absl::Span<const char> value, absl::Span<char> *buf) → bool
EncodeMessageLength(absl::Span<char> msg, const absl::Span<char> *buf) → void
DecodeVarint(absl::Span<const char> *buf) → uint64_t
Decode64Bit(absl::Span<const char> *buf) → uint64_t
Decode32Bit(absl::Span<const char> *buf) → uint32_t
switch(type_)
```

### absl/log/internal/proto.h
```
EncodeVarint(uint64_t tag, int64_t value, absl::Span<char> *buf) → inline bool
EncodeVarint(uint64_t tag, uint32_t value, absl::Span<char> *buf) → inline bool
EncodeVarint(uint64_t tag, int32_t value, absl::Span<char> *buf) → inline bool
EncodeVarintZigZag(uint64_t tag, int64_t value, absl::Span<char> *buf) → inline bool
Encode64Bit(uint64_t tag, int64_t value, absl::Span<char> *buf) → inline bool
Encode64Bit(uint64_t tag, uint32_t value, absl::Span<char> *buf) → inline bool
Encode64Bit(uint64_t tag, int32_t value, absl::Span<char> *buf) → inline bool
EncodeDouble(uint64_t tag, double value, absl::Span<char> *buf) → inline bool
Encode32Bit(uint64_t tag, int32_t value, absl::Span<char> *buf) → inline bool
EncodeFloat(uint64_t tag, float value, absl::Span<char> *buf) → inline bool
EncodeString(uint64_t tag, absl::string_view value, absl::Span<char> *buf) → inline bool
EncodeStringTruncate(uint64_t tag, absl::string_view value, absl::Span<char> *buf) → inline bool
VarintSize(uint64_t value) → constexpr size_t
MinVarintSize() → constexpr size_t
MaxVarintSize() → constexpr size_t
MaxVarintForSize(size_t size) → constexpr uint64_t
MakeTagType(uint64_t tag, WireType type) → constexpr uint64_t
BufferSizeFor(uint64_t tag, WireType type) → constexpr size_t
switch(type)
tag() → uint64_t
type() → WireType
double_value() → double
float_value() → float
int32_value() → int32_t
int64_value() → int64_t
```

### absl/log/internal/structured.h
```
str_(str) → :
AddToMessage(log_internal::LogMessage& m) → void
AddToMessage(LogMessage& m) → void
if(str_type == StructuredStringType::kLiteral)
AsStructuredValueImpl(T value ABSL_ATTRIBUTE_LIFETIME_BOUND, StructuredProtoField field ABSL_ATTRIBUTE_LIFETIME_BOUND, ValueFormatter value_formatter = [](T value) → constexpr
AddToMessage(LogMessage& m) → void
```

### absl/log/internal/structured_proto.cc
```
EncodeStructuredProtoField(StructuredProtoField field, absl::Span<char>& buf) → bool
```

### absl/log/internal/structured_proto.h
```
BufferSizeForStructuredProtoField(StructuredProtoField field) → inline size_t
```

### absl/log/internal/structured_proto_test.cc
```
struct StructuredProtoTestCase
```

### absl/log/internal/test_actions.h
```
DeathTestExpectedLogging() → inline WriteEntryToStderr
DeathTestUnexpectedLogging() → inline WriteEntryToStderr
```

### absl/log/internal/test_helpers.cc
```
LoggingEnabledAt(absl::LogSeverity severity) → bool
DiedOfFatal(int exit_status) → bool
DiedOfQFatal(int exit_status) → bool
```

### absl/log/internal/test_helpers.h
```
class LogTestEnvironment
  SetUp() → void
```

### absl/log/internal/test_matchers.cc
```
str_matcher_(str_matcher) → :
AsString(const Matcher<const std::string&>& str_matcher) → Matcher<absl::string_view>
SourceFilename(const Matcher<absl::string_view>& source_filename) → Matcher<const absl::LogEntry&>
SourceBasename(const Matcher<absl::string_view>& source_basename) → Matcher<const absl::LogEntry&>
SourceLine(const Matcher<int>& source_line) → Matcher<const absl::LogEntry&>
Prefix(const Matcher<bool>& prefix) → Matcher<const absl::LogEntry&>
LogSeverity(const Matcher<absl::LogSeverity>& log_severity) → Matcher<const absl::LogEntry&>
Timestamp(const Matcher<absl::Time>& timestamp) → Matcher<const absl::LogEntry&>
InMatchWindow() → Matcher<absl::Time>
Truly([](absl::Time arg)
ThreadID(const Matcher<absl::LogEntry::tid_t>& tid) → Matcher<const absl::LogEntry&>
TextMessageWithPrefixAndNewline(const Matcher<absl::string_view>& text_message_with_prefix_and_newline) → Matcher<const absl::LogEntry&>
TextMessageWithPrefix(const Matcher<absl::string_view>& text_message_with_prefix) → Matcher<const absl::LogEntry&>
TextMessage(const Matcher<absl::string_view>& text_message) → Matcher<const absl::LogEntry&>
TextPrefix(const Matcher<absl::string_view>& text_prefix) → Matcher<const absl::LogEntry&>
ResultOf([](const absl::LogEntry& entry) → return
RawEncodedMessage(const Matcher<absl::string_view>& raw_encoded_message) → Matcher<const absl::LogEntry&>
Verbosity(const Matcher<int>& verbosity) → Matcher<const absl::LogEntry&>
Stacktrace(const Matcher<absl::string_view>& stacktrace) → Matcher<const absl::LogEntry&>
MatchesOstream(const std::ostringstream& stream) → Matcher<absl::string_view>
DeathTestValidateExpectations() → Matcher<const std::string&>
```

### absl/log/internal/vlog_config.cc
```
ModuleIsPath(absl::string_view module_pattern) → bool
Basename(absl::string_view file) → absl::string_view
if(sep != file.npos)
vlog_level(vlog_level_arg)
GetUpdateSitesMutex() → absl::Mutex&
ABSL_PT_GUARDED_BY(mutex)
ABSL_EXCLUSIVE_LOCKS_REQUIRED(mutex)
VLogLevel(absl::string_view file, const std::vector<VModuleInfo>* infos, int current_global_v) → int
if(sep != stem_basename.npos)
for(const auto& info : *infos)
if(info.module_is_path)
ABSL_EXCLUSIVE_LOCKS_REQUIRED(mutex)
ABSL_EXCLUSIVE_LOCKS_REQUIRED(mutex)
while(n != nullptr)
if(n->file_ != last_file)
if(update_callbacks)
for(auto& cb : *update_callbacks)
for(const auto& it : glob_levels)
if(v == global_v)
SetVModuleListHeadForTestOnly(VLogSite* v) → VLogSite*
```

### absl/log/internal/vlog_config.h
```
IsEnabled(int level) → bool
```

### absl/log/internal/vlog_config_benchmark.cc
```
class SyntheticBinary
  static_assert(std::is_trivially_destructible<VLogSite>::value, "")
SyntheticBinary(const size_t num_tus, const size_t max_extra_static_data_bytes_per_tu, const size_t max_sites_per_tu, const int num_shuffles) → explicit
for(size_t i = 0; i < num_tus; i++)
for(size_t i = 0; i < num_tus; i++)
for(int shuffle_num = 0; shuffle_num < num_shuffles; shuffle_num++)
for(size_t i = 0; i < num_sites_; i++)
num_sites() → size_t
num_new_files() → size_t
num_extra_static_data_bytes() → size_t
BM_UpdateVModuleEmpty(benchmark::State& state) → void
for(auto s : state)
BM_UpdateVModuleShort(benchmark::State& state) → void
for(auto s : state)
BM_UpdateVModuleLong(benchmark::State& state) → void
for(auto s : state)
```

### absl/log/log_benchmark.cc
```
class NullLogSink
NullLogSink() → public:
BM_SuccessfulBinaryCheck(benchmark::State& state) → void
BM_SuccessfulUnaryCheck(benchmark::State& state) → static void
BM_DisabledLogOverhead(benchmark::State& state) → static void
for(auto _ : state)
BM_EnabledLogOverhead(benchmark::State& state) → static void
for(auto _ : state)
BM_VlogIsOnOverhead(benchmark::State& state) → static void
BM_VlogIsNotOnOverhead(benchmark::State& state) → static void
BM_LogEveryNOverhead(benchmark::State& state) → static void
```

### absl/log/log_entry.cc
```
PrintEscapedRangeTo(const absl::string_view str, const absl::string_view substr, std::ostream* os) → void
PrintTo(const LogEntry& entry, std::ostream* os) → void
```

### absl/log/log_entry.h
```
source_line() → int
prefix() → bool
log_severity() → absl::LogSeverity
verbosity() → int
timestamp() → absl::Time
tid() → tid_t
```

### absl/log/log_entry_test.cc
```
class LogEntryTestPeer
  str(sz, '\0') → std::string
FormatLogMessage() → std::string
FormatPrefixIntoSizedBuffer(size_t sz) → std::string
entry() → const absl::LogEntry&
if(std::is_signed<absl::LogEntry::tid_t>::value)
if(std::is_signed<absl::LogEntry::tid_t>::value)
```

### absl/log/log_format_test.cc
```
class CharLogFormatTest
class UnsignedIntLogFormatTest
class SignedIntLogFormatTest
class UnsignedEnumLogFormatTest
class SignedEnumLogFormatTest
class FloatingPointLogFormatTest
class VoidPtrLogFormatTest
class VolatilePtrLogFormatTest
class CharPtrLogFormatTest
class WideCharPtrLogFormatTest
class WideStringLogFormatTest
class CustomClass
class CustomClassNonCopyable
struct Point
struct PointWithAbslStringifiyAndOstream
struct PointStreamsNothing
struct PointMultipleAppend
class FromCustomClass
class StreamsNothing
struct PointPercentV
struct StringLike
ComparisonStream() → std::ostringstream
GetUtf8TestString() → absl::string_view
AbslStringify(Sink& sink, const Point& p) → friend void
AbslStringify(Sink& sink, const PointWithAbslStringifiyAndOstream& p) → friend void
```

### absl/log/log_macro_hygiene_test.cc
```
class Dummy
class string
class vector
class LogMessage
class LogMessageFatal
class LogMessageQuietlyFatal
class LogMessageVoidify
class LogSink
class NullStream
class NullStreamFatal
class UnbracedSwitchCompileTest
  LOG(INFO) → case 0:
Dummy() → public:
Log() → static void
switch(0)
```

### absl/log/log_modifier_methods_test.cc
```
TextMessageWithPrefix(Truly([](absl::string_view msg)
```

### absl/log/log_sink.h
```
class LogSink
  Send(const absl::LogEntry& entry) → void
Flush() → virtual void
```

### absl/log/log_sink_registry.h
```
AddLogSink(absl::LogSink* absl_nonnull sink) → inline void
RemoveLogSink(absl::LogSink* absl_nonnull sink) → inline void
FlushLogSinks() → inline void
```

### absl/log/log_sink_test.cc
```
class ReentrancyTest
ReentrantSendLogSink(severity, nullptr, LogMode::kNormal) → :
switch(mode_)
LogAndReturn(absl::LogSeverity severity, absl::string_view to_log, absl::string_view to_return) → static absl::string_view
```

### absl/log/log_streamer.h
```
stream_(absl::in_place, &buf_)
stream() → std::ostream&
LogInfoStreamer(absl::string_view file, int line) → inline LogStreamer
LogWarningStreamer(absl::string_view file, int line) → inline LogStreamer
LogErrorStreamer(absl::string_view file, int line) → inline LogStreamer
LogFatalStreamer(absl::string_view file, int line) → inline LogStreamer
LogDebugFatalStreamer(absl::string_view file, int line) → inline LogStreamer
```

### absl/log/log_streamer_test.cc
```
WriteToStream(absl::string_view data, std::ostream* os) → void
WriteToStreamRef(absl::string_view data, std::ostream& os) → void
```

### absl/log/scoped_mock_log.cc
```
if(default_exp == MockLogDefault::kIgnoreUnexpected)
```

### absl/log/scoped_mock_log_test.cc
```
TextMessageWithPrefix(Truly([](absl::string_view msg)
thread1([&barrier]() → std::thread
thread2([&barrier]() → std::thread
thread1([]() → std::thread
thread2([]() → std::thread
thread([&logging_started]() → std::thread
for(int i = 0; i < 100; ++i)
```

### absl/log/stripping_test.cc
```
class StrippingTest
  path(size, '\0') → std::string
Base64UnescapeOrDie(absl::string_view data) → std::string
while(true)
if(off != haystack.npos)
if(!fp)
if(!fp)
if(ret != -1)
if(ret != 0)
if(!fp)
while(true)
if(ret == 0)
FileHasSubstr(absl::string_view needle) → ::testing::Matcher<FILE*>
if(absl::LogSeverity::kInfo >= kAbslMinLogLevel)
if(absl::LogSeverity::kInfo >= kAbslMinLogLevel)
if(absl::LogSeverity::kFatal >= kAbslMinLogLevel)
if(kExpectPresent)
if(absl::LogSeverity::kFatal >= kAbslMinLogLevel)
if(kReallyDie)
if(absl::LogSeverity::kFatal >= kAbslMinLogLevel)
if(kReallyDie)
if(absl::LogSeverity::kFatal >= kAbslMinLogLevel)
if(kReallyDie)
if(absl::LogSeverity::kFatal >= kAbslMinLogLevel)
if(x)
```

### absl/log/structured.h
```
LogAsLiteral(absl::string_view s ABSL_ATTRIBUTE_LIFETIME_BOUND) → inline log_internal::AsLiteral
```

### absl/log/vlog_is_on_test.cc
```
class VLogIsOnTest
MaxLogVerbosity() → std::optional<int>
MinLogLevel() → std::optional<int>
ResetVLogLevels() → static void
```

### absl/memory/memory.h
```
struct MakeUniqueResult
struct ExtractOr
WrapUnique(T* ptr) → std::unique_ptr<T>
make_unique_for_overwrite() → typename memory_internal::Make
make_unique_for_overwrite(size_t n) → typename memory_internal::Make
RawPtr(std::nullptr_t) → inline std::nullptr_t
ShareUniquePtr(std::unique_ptr<T, D>&& ptr) → std::shared_ptr<T>
WeakenPtr(const std::shared_ptr<T>& ptr) → std::weak_ptr<T>
ConstructRange(Allocator& alloc, Iterator first, Iterator last, const Args&... args) → void
for(Iterator cur = first; cur != last; ++cur)
while(cur != first)
CopyRange(Allocator& alloc, Iterator destination, InputIterator first, InputIterator last) → void
while(cur != destination)
```

### absl/memory/memory_test.cc
```
class DestructorVerifier
struct InitializationVerifier
  memset(ret, kDefaultScalar, n)
  memset(ret, kDefaultArray, n)
struct ArrayWatch
class IntPointerNonConstDeref
DestructorVerifier() → public:
instance_count() → static int
new(size_t n) → static void* operator
allocs() → static std::vector<size_t>&
```

### absl/meta/internal/constexpr_testing.h
```
Tester(char) → constexpr std::false_type
HasConstexprEvaluation(F) → constexpr bool
```

### absl/meta/internal/requires.h
```
Requires(F) → constexpr bool
```

### absl/meta/internal/requires_test.cc
```
static_assert(absl::meta_internal::Requires<int>([](auto&&)
static_assert(absl::meta_internal::Requires<int, char>([](auto&&, auto&&)
foo() → int
```

### absl/meta/type_traits.h
```
struct VoidTImpl
struct is_detected_impl
struct remove_cvref
struct type_identity
struct AssertHashEnabledHelper
  GetReturnType(...) → NAT
  static_assert(IsHashable<Key>::value, "std::hash<Key> does not provide a call operator")
  static_assert(std::is_default_constructible<std::hash<Key>>::value, "std::hash<Key> must be default constructible when it is enabled")
  static_assert(std::is_copy_constructible<std::hash<Key>>::value, "std::hash<Key> must be copy constructible when it is enabled")
  static_assert(std::is_copy_assignable<std::hash<Key>>::value, "std::hash<Key> must be copy assignable when it is enabled")
  static_assert(std::is_same<ReturnType, NAT>::value || std::is_same<ReturnType, size_t>::value, "std::hash<Key> must return size_t")
  AssertHashEnabled() → friend void
Sink(...) → static void
DoIt() → static std::nullptr_t
AssertHashEnabled() → inline void
```

### absl/meta/type_traits_test.cc
```
struct simple_pair
struct Dummy
struct ReturnType
struct ConvertibleToReturnType
  ReturnType() → operator
struct StructA
struct StructB
struct StructC
struct TypeWithBarFunction
struct TypeWithBarFunctionAndConvertibleReturnType
struct TypeA
struct TypeB
struct TypeC
struct TypeD
struct Wrap
struct GetTypeT
struct GetTypeExtT
struct DeletedSwap
struct SpecialNoexceptSwap
SpecialNoexceptSwap(SpecialNoexceptSwap&&)
NoAssign(NoAssign&&)
NonNoexceptMoves(NonNoexceptMoves&&)
NoAssign(NoAssign&&)
S(S&&)
S(const S&)
```

### absl/numeric/bits.h
```
bit_ceil(T x)
```

### absl/numeric/bits_benchmark.cc
```
BM_bit_width(benchmark::State& state) → static void
for(size_t i = 0; i < count; ++i)
for(size_t i = 0; i < count; ++i)
BM_bit_width_nonzero(benchmark::State& state) → static void
for(size_t i = 0; i < count; ++i)
for(size_t i = 0; i < count; ++i)
```

### absl/numeric/bits_test.cc
```
class UnsignedIntegerTypesTest
class IntegerTypesTest
struct PopcountInput
for(int i = 0; i < kTrials; ++i)
for(int i = 0; i < kTrials; ++i)
for(int i = 0; i < kTrials; ++i)
for(int i = 0; i < kTrials; ++i)
for(int i = 0; i < 8; i++)
for(int i = 0; i < 16; i++)
for(int i = 0; i < 32; i++)
for(int i = 0; i < 64; i++)
for(int i = 0; i < 8; i++)
for(int i = 0; i < 16; i++)
for(int i = 0; i < 32; i++)
for(int i = 0; i < 64; i++)
GeneratePopcountInput(absl::BitGen& gen) → PopcountInput<T>
for(int i = 0; i < std::numeric_limits<T>::digits; i++)
if(coin)
for(int i = 0; i < kTrials; ++i)
for(int i = 0; i < kTrials; ++i)
for(int i = 0; i < kTrials; ++i)
for(int i = 0; i < kTrials; ++i)
IsBitCeilConstantExpression(int) → bool
IsBitCeilConstantExpression(char) → bool
for(int i = 0; i < 8; i++)
```

### absl/numeric/int128.cc
```
Fls128(uint128 n) → inline ABSL_ATTRIBUTE_ALWAYS_I
DivModImpl(uint128 dividend, uint128 divisor, uint128* quotient_ret, uint128* remainder_ret) → inline void
if(divisor > dividend)
if(divisor == dividend)
for(int i = 0; i <= shift; ++i)
if(dividend >= denominator)
MakeUint128FromFloat(T v) → uint128
MakeUint128FromFloat(long double v) → uint128
Uint128ToFormattedString(uint128 v, std::ios_base::fmtflags flags) → std::string
switch(flags & std::ios::basefield)
if(adjustfield == std::ios::left)
UnsignedAbsoluteValue(int128 v) → uint128
MakeInt128FromFloat(T v) → int128
if(print_as_decimal)
switch(flags & std::ios::adjustfield)
```

### absl/numeric/int128.h
```
class int128
  int128(int v) → constexpr
  int128(unsigned int v) → constexpr
  int128(long v) → constexpr
  int128(unsigned long v) → constexpr
  int128(long long v) → constexpr
  int128(unsigned long long v) → constexpr
  int128(uint128 v) → constexpr explicit
  int128(__int128 v) → constexpr
AbslHashValue(H h, uint128 v) → friend H
AbslStringify(Sink& sink, uint128 v) → friend void
Uint128Max() → constexpr uint128
lowest() → static constexpr absl::uint128
epsilon() → static constexpr absl::uint128
round_error() → static constexpr absl::uint128
infinity() → static constexpr absl::uint128
quiet_NaN() → static constexpr absl::uint128
signaling_NaN() → static constexpr absl::uint128
denorm_min() → static constexpr absl::uint128
AbslHashValue(H h, int128 v) → friend H
AbslStringify(Sink& sink, int128 v) → friend void
Int128Max() → constexpr int128
Int128Min() → constexpr int128
lowest() → static constexpr absl::int128
epsilon() → static constexpr absl::int128
```

### absl/numeric/int128_benchmark.cc
```
class UniformIntDistribution128
for(size_t i = 0; i < kSampleSize; ++i)
BM_DivideClass128UniformDivisor(benchmark::State& state) → void
for(const auto& pair : values)
BM_RemainderClass128UniformDivisor(benchmark::State& state) → void
for(const auto& pair : values)
for(size_t i = 0; i < kSampleSize; ++i)
BM_DivideClass128SmallDivisor(benchmark::State& state) → void
for(const auto& pair : values)
BM_RemainderClass128SmallDivisor(benchmark::State& state) → void
for(const auto& pair : values)
for(size_t i = 0; i < kSampleSize; ++i)
BM_MultiplyClass128(benchmark::State& state) → void
for(const auto& pair : values)
BM_AddClass128(benchmark::State& state) → void
for(const auto& pair : values)
for(size_t i = 0; i < kSampleSize; ++i)
BM_DivideIntrinsic128UniformDivisor(benchmark::State& state) → void
for(const auto& pair : values)
BM_RemainderIntrinsic128UniformDivisor(benchmark::State& state) → void
for(const auto& pair : values)
for(size_t i = 0; i < kSampleSize; ++i)
BM_DivideIntrinsic128SmallDivisor(benchmark::State& state) → void
for(const auto& pair : values)
BM_RemainderIntrinsic128SmallDivisor(benchmark::State& state) → void
```

### absl/numeric/int128_stream_test.cc
```
struct Uint128TestCase
struct Int128TestCase
StreamFormatToString(std::ios_base::fmtflags flags, std::streamsize width) → std::string
switch(flags & std::ios::basefield)
switch(flags & std::ios::adjustfield)
CheckUint128Case(const Uint128TestCase& test_case) → void
if(test_case.flags == kDec && test_case.width == 0)
CheckInt128Case(const Int128TestCase& test_case) → void
if(test_case.flags == kDec && test_case.width == 0)
GetInt128FormatCases() → std::vector<Int128TestCase>
GetUint128FormatCases() → std::vector<Uint128TestCase>
```

### absl/numeric/int128_test.cc
```
class Uint128IntegerTraitsTest
class Uint128FloatTraitsTest
class Int128IntegerTraitsTest
class Int128FloatTraitsTest
class Int128IntegerConversionTest
class Int128FloatConversionTest
for(int i = 0; i < 64; ++i)
for(int j = 0; j < 64; ++j)
for(int i = 0; i < kNumIters; ++i)
if(b == 0)
for(absl::int128 value : nonnegative_signed_values)
for(absl::int128 value : negative_values)
for(int i = 0; i < 64; ++i)
for(int i = 0; i < 63; ++i)
for(int i = 0; i < 110; ++i)
for(uint64_t value : values)
for(int i = 0; i <= 64; ++i)
for(absl::int128 value : large_values)
for(int i = 0; i < 127 - std::numeric_limits<TypeParam>::digits; ++i)
for(const HighLowPair& pair : values)
for(const TestCase& pair : cases)
for(int64_t value : values64)
for(int64_t value : values64)
for(int i = 0; i < 64; ++i)
for(int i = 0; i < 63; ++i)
```

### absl/numeric/internal/bits.h
```
if(r == 0)
if(r == 0)
CountLeadingZeroes32(uint32_t x) → ABSL_ATTRIBUTE_ALWAYS_INLINE A
if(x >> 16)
if(x >> 8)
if(x >> 4)
CountLeadingZeroes16(uint16_t x) → ABSL_ATTRIBUTE_ALWAYS_INLINE A
CountLeadingZeroes64(uint64_t x) → ABSL_ATTRIBUTE_ALWAYS_INLINE A
if(x >> 32)
if(x >> 16)
if(x >> 8)
if(x >> 4)
CountLeadingZeroes(T x) → ABSL_ATTRIBUTE_ALWAYS_INLINE A
CountTrailingZeroesNonzero32(uint32_t x) → ABSL_ATTRIBUTE_ALWAYS_INLINE A
CountTrailingZeroesNonzero64(uint64_t x) → ABSL_ATTRIBUTE_ALWAYS_INLINE A
CountTrailingZeroesNonzero16(uint16_t x) → ABSL_ATTRIBUTE_ALWAYS_INLINE A
BitCeilPromotionHelper(T x, T promotion)
BitCeilNonPowerOf2(T x)
```

### absl/numeric/internal/representation.h
```
IsDoubleDouble() → inline constexpr bool
```

### absl/profiling/hashtable.cc
```
MarshalHashtableProfile() → StatusOr<std::string>
DroppedHashtableSample() → static void
MarshalHashtableProfile(container_internal::HashtablezSampler& sampler, Time now) → StatusOr<std::string>
if(value == 0)
if(dropped > 0)
```

### absl/profiling/internal/exponential_biased.cc
```
for(int i = 0; i < 20; ++i)
```

### absl/profiling/internal/exponential_biased.h
```
class ExponentialBiased
  GetSkipCount(int64_t mean) → int64_t
  GetStride(int64_t mean) → int64_t
  NextRandom(uint64_t rnd) → uint64_t
  Initialize() → void
```

### absl/profiling/internal/exponential_biased_test.cc
```
AndersonDarlingInf(double z) → double
if(z < 2)
AndersonDarlingErrFix(int n, double x) → double
if(x > 0.8)
if(x < cutoff)
AndersonDarlingPValue(int n, double z) → double
AndersonDarlingStatistic(const std::vector<double>& random_sample) → double
for(size_t i = 0; i < n; i++)
AndersonDarlingTest(const std::vector<double>& random_sample) → double
for(int runs = 0; runs < 10; ++runs)
for(int i = 0; i < 10000000; ++i)
if(--stride == 0)
for(int i = 1; i <= 20; i++)
for(size_t i = 0; i < n; i++)
for(size_t i = 0; i < n; i++)
```

### absl/profiling/internal/periodic_sampler.h
```
class PeriodicSamplerBase
SetGlobalPeriod(int period) → static void
```

### absl/profiling/internal/periodic_sampler_benchmark.cc
```
BM_Sample(Sampler* sampler, benchmark::State& state) → void
for(auto _ : state)
BM_SampleMinunumInlined(Sampler* sampler, benchmark::State& state) → void
for(auto _ : state)
BM_PeriodicSampler_TinySample(benchmark::State& state) → void
BM_PeriodicSampler_ShortSample(benchmark::State& state) → void
BM_PeriodicSampler_LongSample(benchmark::State& state) → void
BM_PeriodicSampler_LongSampleMinunumInlined(benchmark::State& state) → void
BM_PeriodicSampler_Disabled(benchmark::State& state) → void
```

### absl/profiling/internal/periodic_sampler_test.cc
```
class MockPeriodicSampler
```

### absl/profiling/internal/profile_builder.cc
```
struct Varint
struct Tag
struct LengthDelimited
struct VarintField
for(int i = 0; i < info->dlpi_phnum; ++i)
if(info->dlpi_phdr[i].p_type == segment_type)
GetSoName(const dl_phdr_info* const info) → static const char*
if(pt_dynamic == nullptr)
for(; dyn->d_tag != DT_NULL; ++dyn)
if(dyn->d_tag == DT_SONAME)
if(dt_soname == nullptr)
GetBuildId(const dl_phdr_info* const info) → std::string
for(int i = 0; i < info->dlpi_phnum; ++i)
while(note < last)
if(nhdr->n_type == NT_GNU_BUILD_ID)
AbslStringify(Sink& sink, const Varint& v) → friend void
while(u >= 0x80)
AbslStringify(Sink& sink, const Tag& t) → friend void
AbslStringify(Sink& sink, const LengthDelimited& ld) → friend void
AbslStringify(Sink& sink, const VarintField& vf) → friend void
for(const void* addr : stack)
for(const auto& label : labels)
if(!inserted)
for(const auto& sample_type : sample_types_)
for(const auto& sample : samples_)
```

### absl/profiling/internal/profile_builder.h
```
struct ProfileProto
struct ValueTypeProto
struct SampleProto
struct LabelProto
struct MappingProto
struct LocationProto
class ProfileBuilder
  InternString(absl::string_view str) → StringId
  InternLocation(const void* address) → LocationId
  AddSample(int64_t value, absl::Span<const void* const> stack, absl::Span<const std::pair<StringId, int64_t>> labels) → void
  AddSampleType(StringId type, StringId unit) → void
  AddCurrentMappings() → void
  AddMapping(uintptr_t memory_start, uintptr_t memory_limit, uintptr_t file_offset, absl::string_view filename, absl::string_view build_id) → MappingId
  set_drop_frames_id(StringId drop_frames_id) → void
  set_comment_id(StringId comment_id) → void
HashtablezBarrier(const HashtablezBarrier<U>&) → template <typename U>
```

### absl/profiling/internal/sample_recorder.h
```
struct Sample
class SampleRecorder
  SampleRecorder() → public:
  Register(Targs&&... args) → T*
  Unregister(T* sample) → void
  SetDisposeCallback(DisposeCallback f) → DisposeCallback
  GetMaxSamples() → size_t
  SetMaxSamples(size_t max) → void
  PushNew(T* sample) → void
  PushDead(T* sample) → void
while(s != nullptr)
if(sample == nullptr)
while(s != nullptr)
if(s->dead == nullptr)
```

### absl/profiling/internal/sample_recorder_test.cc
```
PrepareForSampling(int64_t w) → void
GetSizes(SampleRecorder<Info>* s) → std::vector<size_t>
EXPECT_EQ(s->Iterate([&](const Info& info)
GetWeights(SampleRecorder<Info>* s) → std::vector<int64_t>
EXPECT_EQ(s->Iterate([&](const Info& info)
Register(SampleRecorder<Info>* s, int64_t weight, size_t size) → Info*
for(size_t i = 0; i < 3; ++i)
for(int i = 0; i < 10; ++i)
EXPECT_EQ(sampler.Iterate([&](const Info& info)
```

### absl/random/benchmarks.cc
```
class PrecompiledSeedSeq
class DefaultConstructorSeedSeq
PrecompiledSeedSeq(Iterator, Iterator) → template <typename Iterator>
PrecompiledSeedSeq(std::initializer_list<T>) → template <typename T>
generate(OutIterator begin, OutIterator end) → void
for(; begin != end; begin++)
size() → size_t
param(OutIterator out) → void
make_engine() → Engine
constexpr(use_default_initialization) → if
BM_Construct(benchmark::State& state) → void
for(auto _ : state)
BM_Direct(benchmark::State& state) → void
for(auto _ : state)
BM_Generate(benchmark::State& state) → void
BM_Shuffle(benchmark::State& state) → void
BM_ShuffleReuse(benchmark::State& state) → void
BM_Dist(benchmark::State& state, Args&&... args) → void
for(auto _ : state)
BM_Large(benchmark::State& state) → void
BM_Small(benchmark::State& state) → void
BM_Bernoulli(benchmark::State& state) → void
BM_Beta(benchmark::State& state) → void
BM_Gamma(benchmark::State& state) → void
BM_Poisson(benchmark::State& state) → void
```

### absl/random/bernoulli_distribution.h
```
class bernoulli_distribution
  assert(p >= 0.0 && p <= 1.0)
  Generate(double p, URBG& g) → bool
p() → double
reset() → void
param() → param_type
param(const param_type& param) → void
p() → double
while(true)
```

### absl/random/bernoulli_distribution_test.cc
```
for(size_t i = 0; i < trials; ++i)
for(int i = 0; i < 35; i++)
for(int i = 0; i < 12; i++)
```

### absl/random/beta_distribution.h
```
class beta_distribution
  assert(alpha >= 0)
  assert(beta >= 0)
  static_assert(std::is_floating_point<RealType>::value, "Class-template absl::beta_distribution<> must be " "parameterized using a floating-point type.")
if(alpha == 0 || beta == 0)
if(beta < alpha)
if(a_ <= 1)
alpha() → result_type
beta() → result_type
ThresholdForSmallA() → static ABSL_RANDOM_INTERNAL_LO
ThresholdForLargeA() → static ABSL_RANDOM_INTERNAL_LO
ThresholdPadding() → static constexpr result_type
param_(alpha, beta) → :
reset() → void
param() → param_type
param(const param_type& p) → void
alpha() → result_type
beta() → result_type
DegenerateCase(URBG& g, const param_type& p) → result_type
if(p.method_ == param_type::DEGENERATE_SMALL && p.alpha_ == p.beta_)
for(;;)
if(!std::is_same<float, result_type>::value)
if(z > 1)
if(z > 0)
if(z > 0)
```

### absl/random/beta_distribution_test.cc
```
class BetaDistributionInterfaceTest
class BetaDistributionModel
ShouldExerciseLongDoubleTests() → constexpr bool
for(TypeParam alpha : kValues)
for(TypeParam beta : kValues)
for(int i = 0; i < kCount; ++i)
for(TypeParam alpha : kSmallValues)
for(TypeParam beta : kSmallValues)
for(int i = 0; i < kCount; ++i)
if(x == 0.0)
if(alpha == beta)
for(TypeParam alpha : kSmallValues)
for(TypeParam beta : kLargeValues)
for(int i = 0; i < kCount; ++i)
for(TypeParam alpha : kLargeValues)
for(TypeParam beta : kSmallValues)
for(int i = 0; i < kCount; ++i)
for(int i = 0; i < kCount; ++i)
for(int i = 0; i < kCount; ++i)
Mean() → double
Variance() → double
Kurtosis() → double
for(size_t i = 0; i < samples; i++)
if(!pass)
for(; i < buckets; ++i)
```

### absl/random/bit_gen_ref.h
```
class BitGenRef
  mock_call_(t_erased_gen_ptr_, key_id, args_tuple, result) → return
generate_impl_fn_(ImplFn<URBG>)
generate_impl_fn_(ImplFn<URBG>)
ImplFn(uintptr_t ptr) → static result_type
MockCall(uintptr_t gen_ptr, FastTypeIdType key_id, void* args_tuple, void* result) → static bool
NotAMock(uintptr_t, FastTypeIdType, void*, void*) → static bool
InvokeMock(FastTypeIdType key_id, void* args_tuple, void* result) → inline bool
```

### absl/random/bit_gen_ref_test.cc
```
class ConstBitGen
class BitGenRefTest
struct MinStdRand
InvokeMock(FastTypeIdType, void*, void* result) → bool
FnTest(absl::BitGenRef gen_ref) → int
for(auto& v : output)
```

### absl/random/discrete_distribution.cc
```
for(double& item : *probabilities)
for(const double item : *probabilities)
if(v < 1.0)
if(r < 1.0)
for(auto i : over)
for(auto i : under)
```

### absl/random/discrete_distribution.h
```
class discrete_distribution
  init()
  init()
  assert(delta > 0)
  init()
  init() → void
  static_assert(std::is_integral<result_type>::value, "Class-template absl::discrete_distribution<> must be " "parameterized using an integral type.")
param_type()
p_(begin, end) → :
param_type(size_t nw, double xmin, double xmax, UnaryOperation fw) → explicit
if(nw > 0)
for(size_t i = 0; i < nw; ++i)
probabilities() → const std::vector<double>&
n() → size_t
param_(begin, end) → :
param_(weights) → :
reset() → void
param() → const param_type&
param(const param_type& p) → void
probabilities() → const std::vector<double>&
for(const auto& p : probabilities)
if(n > 0)
```

### absl/random/discrete_distribution_test.cc
```
class DiscreteDistributionTypeTest
for(size_t i = 0; i < kTrials; i++)
for(double x : weights)
for(double& x : weights)
if(chi_square > kThreshold)
for(auto& v : output)
for(auto& v : output)
```

### absl/random/distributions.h
```
Bernoulli(URBG&& urbg, double p) → bool
Beta(URBG&& urbg, RealType alpha, RealType beta) → RealType
Exponential(URBG&& urbg, RealType lambda = 1) → RealType
Gaussian(URBG&& urbg, RealType mean = 0, RealType stddev = 1) → RealType
LogUniform(URBG&& urbg, IntType lo, IntType hi, IntType base = 2) → IntType
Poisson(URBG&& urbg, double mean = 1.0) → IntType
```

### absl/random/distributions_test.cc
```
class RandomDistributionsTest
struct Invalid
CheckArgsInferType() → void
CheckArgsReturnExpectedType() → void
for(int i = 0; i < kSize; i++)
for(int i = 0; i < kSize; i++)
for(int i = 0; i < kSize; i++)
for(int i = 0; i < kSize; i++)
for(int i = 0; i < kSize; i++)
for(int i = 0; i < kSize; i++)
for(int i = 0; i < kSize; i++)
for(int i = 0; i < kSize; i++)
for(int i = 0; i < 100; ++i)
for(int i = 0; i < kSize; i++)
for(int i = 0; i < kSize; i++)
```

### absl/random/examples_test.cc
```
Use(T) → void
```

### absl/random/exponential_distribution.h
```
class exponential_distribution
  assert(lambda > 0)
  static_assert(std::is_floating_point<RealType>::value, "Class-template absl::exponential_distribution<> must be parameterized " "using a floating-point type.")
lambda() → result_type
reset() → void
param() → param_type
param(const param_type& p) → void
lambda() → result_type
```

### absl/random/exponential_distribution_test.cc
```
class ExponentialDistributionTypedTest
class ExponentialModel
  ABSL_ASSERT(p >= 0.0)
  ABSL_ASSERT(p < 1.0)
struct Param
for(const TypeParam lambda : kParams)
for(int i = 0; i < kCount; i++)
if(!std::is_same<TypeParam, long double>::value)
lambda() → double
mean() → double
variance() → double
stddev() → double
skew() → double
kurtosis() → double
CDF(double x) → double
InverseCDF(double p) → double
for(size_t i = 0; i < samples; i++)
if(!pass)
for(double p = kInc; p < 1.0; p += kInc)
for(int j = 0; j < kSamples; j++)
if(chi_square > threshold)
for(int i = 0; i < param.trials; i++)
for(int i = 0; i < kTrials; i++)
if(p_value < 0.005)
GenParams() → std::vector<Param>
```

### absl/random/gaussian_distribution.h
```
mean() → result_type
stddev() → result_type
param_(mean, stddev) → :
reset() → void
param() → param_type
param(const param_type& p) → void
mean() → result_type
stddev() → result_type
while(true)
if(i == 0)
```

### absl/random/gaussian_distribution_test.cc
```
class GaussianDistributionInterfaceTest
class GaussianModel
  ABSL_ASSERT(p >= 0.0)
  ABSL_ASSERT(p < 1.0)
struct Param
for(const auto mod : {0, 1, 2, 3})
for(const auto x : kParams)
for(const auto y : kParams)
for(int i = 0; i < kCount; i++)
if(!std::is_same<TypeParam, long double>::value)
mean() → double
variance() → double
stddev() → double
skew() → double
kurtosis() → double
InverseCDF(double p) → double
for(size_t i = 0; i < samples; i++)
if(!pass || jb > 9.21)
for(double p = kInc; p < 1.0; p += kInc)
for(int j = 0; j < kSamples; j++)
if(chi_square > threshold)
for(int i = 0; i < param.trials; i++)
for(int i = 0; i < kTrials; i++)
if(p_value < 0.0025)
GenParams() → std::vector<Param>
```

### absl/random/generators_test.cc
```
TestUniform(URBG* gen) → void
TestExponential(URBG* gen) → void
TestPoisson(URBG* gen) → void
TestBernoulli(URBG* gen) → void
TestZipf(URBG* gen) → void
TestGaussian(URBG* gen) → void
TestLogNormal(URBG* gen) → void
CompatibilityTest() → void
```

### absl/random/internal/chi_square.cc
```
fma(double x, double y, double z) → inline double
for(unsigned i = 2; i <= N; i++)
POZ(double z) → double
if(z == 0.0)
normal_survival(double z) → double
ChiSquareValue(int dof, double p) → double
if(dof < 1 || p_value > 1.0)
if(dof > kLargeDOF)
if(variance != 0)
ChiSquarePValue(double chi_square, int dof) → double
if(dof > kLargeDOF)
if(variance != 0)
if(z > 0)
if(dof <= 2)
if(a > kBigX)
while(z <= chi_square)
while(z <= chi_square)
```

### absl/random/internal/chi_square.h
```
ChiSquareWithExpected(Iterator begin, Iterator end, double expected) → double
for(auto it = begin; it != end; it++)
ChiSquare(Iterator it, Iterator end, Expected eit, Expected eend) → double
for(; it != end && eit != eend; ++it, ++eit)
if(*it > 0)
if(d != 0)
```

### absl/random/internal/chi_square_test.cc
```
for(const auto& spec : specs)
for(const auto& spec : specs)
for(const auto& spec : specs)
```

### absl/random/internal/distribution_caller.h
```
struct DistributionCaller
  static_assert(!std::is_pointer<URBG>::value, "You must pass a reference, not a pointer.")
  dist(*urbg) → return
Impl(std::false_type, URBG* urbg, Args&&... args) → static typename DistrT::result
Impl(std::true_type, URBG* urbg, Args&&... args) → static typename DistrT::result
Call(URBG* urbg, Args&&... args) → static typename DistrT::result
```

### absl/random/internal/distribution_test_util.cc
```
fma(double x, double y, double z) → inline double
ComputeDistributionMoments(absl::Span<const double> data_points) → DistributionMoments
for(double x : data_points)
for(double x : data_points)
InverseNormalSurvival(double x) → double
Near(absl::string_view msg, double actual, double expected, double bound) → bool
if(delta < bound)
beta(double p, double q) → double
erfinv(double x) → double
if(w < 6.250000)
BetaIncompleteImpl(const double x, const double p, const double q, const double beta) → double
for(;;)
if(temp < kErr && temp < kErr * result)
if(ns >= 0)
if(ns == 0)
BetaIncompleteInvImpl(const double p, const double q, const double beta, const double alpha) → double
if(alpha < 0.5)
if(p > 1. && q > 1.)
if(t <= 0)
if(t <= 1)
for(;;)
if(value < 0 || value > 1.0)
if(y * yprev <= 0)
for(;;)
if(adj_sq >= prev)
```

### absl/random/internal/distribution_test_util.h
```
struct DistributionMoments
```

### absl/random/internal/distribution_test_util_test.cc
```
for(const auto& data : kErfInvTable)
for(const auto& data : kBetaTable)
for(const auto& data : kBetaTable)
for(auto entry : cases)
```

### absl/random/internal/entropy_pool.cc
```
Init(absl::Span<const uint32_t> data) → void
if(next_ >= kState)
while(bytes > 0)
GetPoolID() → size_t
if(err)
if(err)
InitPoolURBG() → void
for(size_t i = 0; i < kPoolSize; i++)
GetPoolForCurrentThread() → RandenPoolEntry*
GetEntropyFromRandenPool(void* dest, size_t bytes) → void
```

### absl/random/internal/entropy_pool_test.cc
```
for(int i = 0; i < kNumThreads; i++)
for(const auto& v : data)
```

### absl/random/internal/explicit_seed_seq.h
```
class ExplicitSeedSeq
ExplicitSeedSeq(Iterator begin, Iterator end) → template <typename Iterator>
for(auto it = begin; it != end; it++)
size() → size_t
param(OutIterator out) → void
generate(OutIterator begin, OutIterator end) → void
for(size_t index = 0; begin != end; begin++)
```

### absl/random/internal/explicit_seed_seq_test.cc
```
ConformsToInterface() → bool
for(uint32_t& seed : outputs)
for(uint32_t& seed : seed_material)
for(size_t i = 0; i < kNumGenerated; i++)
for(size_t i = 0; i < kNumGenerated; i++)
for(size_t i = 0; i < kNumGenerated; i++)
for(uint32_t& entry : entropy)
for(uint32_t& entry : entropy)
```

### absl/random/internal/fast_uniform_bits.h
```
struct SimplifiedLoopTag
struct RejectionLoopTag
class FastUniformBits
  static_assert(IsUnsigned<UIntType>::value, "Class-template FastUniformBits<> must be parameterized using " "an unsigned type.") → private:
  Generate(URBG& g, SimplifiedLoopTag) → result_type
  Generate(URBG& g, RejectionLoopTag) → result_type
IsPowerOfTwoOrZero(UIntType n) → constexpr bool
RangeSize() → constexpr typename URBG::resul
IntegerLog2(UIntType n) → constexpr UIntType
NumBits() → constexpr size_t
MaskFromShift(size_t n) → constexpr UIntType
for(size_t n = 1; n < kIters; ++n)
for(size_t n = 0; n < kSmallIters; ++n)
for(size_t n = kSmallIters; n < kTotalIters; ++n)
```

### absl/random/internal/fast_uniform_bits_test.cc
```
class FastUniformBitsTypedTest
struct FakeUrbg
for(int i = 0; i < kIters; i++)
```

### absl/random/internal/fastmath.h
```
IntLog2Floor(uint64_t n) → inline int
IntLog2Ceil(uint64_t n) → inline int
StirlingLogFactorial(double n) → inline double
```

### absl/random/internal/fastmath_test.cc
```
for(int i = 0; i < 64; i++)
for(int j = i - 1; j > 0; --j)
for(int i = 0; i < 64; i++)
for(int j = i - 1; j > 0; --j)
for(int i = 2; i < 50; i++)
```

### absl/random/internal/gaussian_distribution_gentables.cc
```
class TableGenerator
  TableGenerator() → public:
  Print(std::ostream* os) → void
if(!std::numeric_limits<T>::is_exact)
for(size_t i = 0; i < N; ++i)
for(int i = 2; i < kC; i++)
main(int, char**) → int
```

### absl/random/internal/generate_real.h
```
struct GeneratePositiveTag
struct GenerateNegativeTag
struct GenerateSignedTag
GenerateRealFromBits(uint64_t bits, int exp_bias = 0) → inline RealType
if(std::is_same<SignedTag, GenerateSignedTag>::value)
if(std::is_same<uint_type, uint64_t>::value)
if(std::is_same<uint_type, uint32_t>::value)
if(IncludeZero)
```

### absl/random/internal/generate_real_test.cc
```
for(int i = 0; i < 100; ++i)
for(int i = 0; i < 64; ++i)
for(int j = 1; j < FLT_MANT_DIG && i - j >= 0; ++j)
for(int j = FLT_MANT_DIG; i - j >= 0; ++j)
for(int i = 0; i < 100; ++i)
for(int i = 0; i < 64; ++i)
for(int j = 1; j < DBL_MANT_DIG && i - j >= 0; ++j)
for(int j = DBL_MANT_DIG; i - j >= 0; ++j)
for(int i = 1; i < 100; ++i)
if(f >= last_f)
if(g >= last_g)
for(; x != 0 && limit > 0;)
if(clz < kDig)
for(; x > chk && limit > 0; x -= dec)
if(limit != 0)
```

### absl/random/internal/iostream_state_saver.h
```
class null_state_saver
class ostream_state_saver
struct stream_precision_helper
class istream_state_saver
null_state_saver(T&, flags_type)
make_ostream_state_saver(T& is, std::ios_base::fmtflags flags = std::ios_base::dec) → null_state_saver<T>>
make_istream_state_saver(T& is, std::ios_base::fmtflags flags = std::ios_base::dec) → null_state_saver<T>>
read(IStream& in) → inline absl::uint128
write(absl::uint128 val, OStream& out) → inline void
read(IStream& in) → inline __uint128_t
write(__uint128_t val, OStream& out) → inline void
read_floating_point(IStream& is) → inline FloatType
```

### absl/random/internal/iostream_state_saver_test.cc
```
for(const uint64_t u : kUintValues)
for(const float f : kValues)
for(const double d : kValues)
for(const long double dd : kValues)
```

### absl/random/internal/mock_helpers.h
```
struct NoOpValidator
class MockHelpers
Validate(ResultT, Args&&...) → static void
constexpr(RandomMockingAccess::HasInvokeMock<URBG>::value) → if
```

### absl/random/internal/mock_validators.h
```
class UniformDistributionValidator
  ValidateImpl(std::is_floating_point<NumType>{}, x, tag, lo, hi)
  dist(tag, lo, hi) → UniformDistributionWrapper<Num
  dist(tag, lo, hi) → UniformDistributionWrapper<Num
Validate(NumType x, TagType tag, NumType lo, NumType hi) → static void
Validate(NumType x, NumType lo, NumType hi) → static void
Validate(NumType) → static void
TagLbBound(IntervalClosedOpenTag) → static absl::string_view
TagLbBound(IntervalOpenOpenTag) → static absl::string_view
TagLbBound(IntervalClosedClosedTag) → static absl::string_view
TagLbBound(IntervalOpenClosedTag) → static absl::string_view
TagUbBound(IntervalClosedOpenTag) → static absl::string_view
TagUbBound(IntervalOpenOpenTag) → static absl::string_view
TagUbBound(IntervalClosedClosedTag) → static absl::string_view
TagUbBound(IntervalOpenClosedTag) → static absl::string_view
ValidateImpl(std::true_type , NumType x, TagType tag, NumType lo, NumType hi) → static void
ValidateImpl(std::false_type , NumType x, TagType tag, NumType lo, NumType hi) → static void
```

### absl/random/internal/nanobenchmark.cc
```
Cpuid(const uint32_t level, const uint32_t count, uint32_t* ABSL_RANDOM_INTERNAL_RESTRICT abcd) → void
for(int i = 0; i < 4; ++i)
BrandString() → std::string
if(abcd[0] < 0x80000004U)
for(int i = 0; i < 3; ++i)
NominalClockRate() → double
for(size_t i = 0; i < 3; ++i)
if(pos_prefix != std::string::npos)
if(pos_space != std::string::npos)
PreventElision(T&& output) → inline void
Start64() → inline uint64_t
Stop64() → inline uint64_t
Start32() → inline uint32_t
Stop32() → inline uint32_t
CountingSort(T* values, size_t num_values) → void
for(size_t i = 0; i < num_values; ++i)
for(const auto& value_count : unique)
MinRange(const T* const ABSL_RANDOM_INTERNAL_RESTRICT sorted, const size_t idx_begin, const size_t half_count) → size_t
for(size_t idx = idx_begin; idx < idx_begin + half_count; ++idx)
if(range < min_range)
ModeOfSorted(const T* const ABSL_RANDOM_INTERNAL_RESTRICT sorted, const size_t num_values) → T
while(half_count > 1)
if(half_count == 0)
Mode(T* values, const size_t num_values) → T
Median(T* values, const size_t num_values) → T
```

### absl/random/internal/nanobenchmark.h
```
struct Params
struct Result
CallClosure(const void* f, const FuncInput input) → static FuncOutput
```

### absl/random/internal/nanobenchmark_test.cc
```
Div(const void*, FuncInput in) → uint64_t
if(num_results == 0)
for(size_t i = 0; i < num_results; ++i)
RunAll(const int argc, char* argv[]) → void
if(argc == 2)
main(int argc, char* argv[]) → int
```

### absl/random/internal/nonsecure_base.h
```
class RandenPoolSeedSeq
  generate_impl(TagType{}, begin, end)
class NonsecureURBGBase
  URBG(seeder) → return
  URBG(salted_seq) → return
generate_impl(ContiguousTag, Contiguous begin, Contiguous end) → void
generate_impl(BufferTag, RandomAccessIterator begin, RandomAccessIterator end) → void
size() → size_t
param(OutIterator) → void
generate(RandomAccessIterator begin, RandomAccessIterator end) → void
if(begin != end)
discard(unsigned long long values) → void
ConstructURBG() → static URBG
ConstructURBG(SSeq&& seq) → static URBG
```

### absl/random/internal/nonsecure_base_test.cc
```
Use(const T&) → void
for(size_t i = 0; i < kNumSamples; i++)
for(uint32_t i = 0; i < 1000; i++)
for(uint32_t i = 0; i < 1000; i++)
for(int i = 0; i < kNumThreads; i++)
for(const auto& v : data)
```

### absl/random/internal/pcg_engine.h
```
class pcg_engine
  static_assert(std::is_same<typename Params::state_type, typename Mix::state_type>::value, "Class-template absl::pcg_engine must be parameterized by " "Params and Mix with identical state_type")
  static_assert(std::is_unsigned<typename Mix::result_type>::value, "Class-template absl::pcg_engine must be parameterized by " "an unsigned Mix::result_type")
  seed(seq)
  reseed(seq)
class pcg128_params
struct pcg_xsl_rr_128_64
class pcg64_params
struct pcg_xsh_rr_64_32
pcg_engine(uint64_t seed_value = 0) → explicit
pcg_engine(SeedSequence&& seq) → explicit
seed(uint64_t seed_value = 0) → void
seed(SeedSequence&& seq)
discard(uint64_t count) → void
lcg(state_type s) → static inline constexpr state_
advance(state_type s, uint64_t n) → inline state_type
while(n > 0)
if(n & 1)
reseed(SeedSequence& seq) → void
for(size_t i = 1; i < kBufferSize; i++)
multiplier() → static inline constexpr state_
increment() → static inline constexpr state_
multiplier() → static inline constexpr state_
increment() → static inline constexpr state_
```

### absl/random/internal/pcg_engine_test.cc
```
class PCGEngineTest
for(size_t i = 0; i < kNumOutputs; ++i)
for(size_t num_used = 0; num_used < kTwoBufferValues; ++num_used)
for(size_t i = 0; i < num_used; ++i)
for(size_t num_discard = 0; num_discard < kTwoBufferValues; ++num_discard)
for(size_t i = 0; i < num_discard; ++i)
for(size_t i = 0; i < kTwoBufferValues; ++i)
for(size_t discard = 0; discard < kTwoBufferValues; ++discard)
for(size_t i = 0; i < 64; ++i)
for(size_t i = 0; i < kNumGoldenOutputs; ++i)
if(i % 3 == 2)
for(const auto& elem : kGolden)
for(const auto& elem : kGolden)
for(size_t i = 0; i < kNumGoldenOutputs; ++i)
if(i % 3 == 2)
for(const auto& elem : kGolden)
for(const auto& elem : kGolden)
for(size_t i = 0; i < kNumGoldenOutputs; ++i)
if(i % 3 == 2)
for(const auto& elem : kGolden)
for(size_t i = 0; i < kNumGoldenOutputs; ++i)
if(i % 6 == 5)
for(const auto& elem : kGolden)
for(const auto& elem : kGolden)
for(size_t i = 0; i < kNumGoldenOutputs; ++i)
```

### absl/random/internal/randen.cc
```
struct RandenState
GetRandenState() → RandenState
```

### absl/random/internal/randen.h
```
class Randen
  Randen()
Generate(void* state) → inline void
if(has_crypto_)
Absorb(const void* seed, void* state) → inline void
if(has_crypto_)
```

### absl/random/internal/randen_benchmarks.cc
```
struct AbsorbFn
struct GenerateFn
struct Engine
  rng()
  rng() → return
bytes() → static constexpr size_t
for(size_t i = 0; i < num_iters; ++i)
GenerateFn()
bytes() → static constexpr size_t
for(size_t i = 0; i < num_iters; ++i)
bytes() → static constexpr size_t
for(size_t i = 0; i < num_iters - 1; ++i)
if(n == 0)
for(size_t i = 0; i < n; ++i)
RunAll(const int argc, char* argv[]) → void
if(argc == 2)
main(int argc, char* argv[]) → int
```

### absl/random/internal/randen_detect.cc
```
GetAuxval(uint32_t hwcap_type) → static uint32_t
GetAuxval(uint32_t hwcap_type) → static uint32_t
if(!libc_handle)
if(sym)
ReadSysctlByName(const char* name) → static std::optional<T>
if(ret == -1)
CPUSupportsRandenHwAes() → bool
```

### absl/random/internal/randen_engine.h
```
randen_engine(result_type seed_value) → explicit
randen_engine(SeedSequence&& seq) → explicit
if(next_ >= kStateSizeT)
seed(SeedSequence&& seq)
seed(result_type seed_value = 0) → void
reseed(SeedSequence& seq) → void
if(entropy_size < kBufferSize)
for(sequence_result_type& e : buffer)
while(dst > 7)
discard(uint64_t count) → void
while(count > 0)
for(auto* end = it + kStateSizeT; it < end; ++it)
for(auto& elem : state)
state() → result_type*
state() → const result_type*
```

### absl/random/internal/randen_engine_test.cc
```
class RandenEngineTypedTest
for(size_t i = 0; i < kNumOutputs; ++i)
for(size_t num_used = 0; num_used < kTwoBufferValues; ++num_used)
for(size_t i = 0; i < num_used; ++i)
for(size_t num_discard = 0; num_discard < kTwoBufferValues; ++num_discard)
for(size_t i = 0; i < num_discard; ++i)
for(size_t i = 0; i < kTwoBufferValues; ++i)
for(size_t discard = 0; discard < kTwoBufferValues; ++discard)
for(size_t i = 0; i < 64; ++i)
for(size_t i = 0; i < kNumGoldenOutputs; ++i)
if(i % 3 == 2)
for(const auto& elem : kGolden)
for(const auto& elem : kGolden)
for(size_t i = 0; i < kNumGoldenOutputs; ++i)
if(i % 3 == 2)
for(const auto& elem : kGolden)
for(const auto& elem : kGolden)
for(size_t i = 0; i < 2 * kNumGoldenOutputs; ++i)
if(i % 6 == 5)
for(const auto& elem : kGolden)
for(const auto& elem : kGolden)
for(size_t i = 0; i < 2 * kNumGoldenOutputs; ++i)
if(i % 6 == 5)
for(const auto& elem : kGolden)
for(const auto& elem : kGolden)
```

### absl/random/internal/randen_hwaes.cc
```
class Vector128
HasRandenHwAesImplementation() → bool
ReverseBytes(const Vector128& v) → inline ABSL_TARGET_CRYPTO Vect
Vector128Load(const void* from) → inline ABSL_TARGET_CRYPTO Vect
Vector128Store(const Vector128& v, void* to) → inline ABSL_TARGET_CRYPTO void
AesRound(const Vector128& state, const Vector128& round_key) → inline ABSL_TARGET_CRYPTO Vect
SwapEndian(absl::uint128* state) → inline ABSL_TARGET_CRYPTO void
for(uint32_t block = 0; block < RandenTraits::kFeistelBlocks; ++block)
Vector128Load(const void* from) → inline ABSL_TARGET_CRYPTO Vect
Vector128Store(const Vector128& v, void* to) → inline ABSL_TARGET_CRYPTO void
AesRound(const Vector128& state, const Vector128& round_key) → inline ABSL_TARGET_CRYPTO Vect
SwapEndian(void*) → inline ABSL_TARGET_CRYPTO void
data() → inline __m128i
Vector128Load(const void* from) → inline ABSL_TARGET_CRYPTO Vect
Vector128Store(const Vector128& v, void* to) → inline ABSL_TARGET_CRYPTO void
AesRound(const Vector128& state, const Vector128& round_key) → inline ABSL_TARGET_CRYPTO Vect
SwapEndian(void*) → inline ABSL_TARGET_CRYPTO void
BlockShuffle(absl::uint128* state) → inline ABSL_TARGET_CRYPTO void
FeistelRound(absl::uint128* state, const absl::uint128* ABSL_RANDOM_INTERNAL_RESTRICT keys) → inline ABSL_TARGET_CRYPTO cons
Permute(absl::uint128* state, const absl::uint128* ABSL_RANDOM_INTERNAL_RESTRICT keys) → inline ABSL_TARGET_CRYPTO void
for(size_t round = 0; round < RandenTraits::kFeistelRounds; ++round)
HasRandenHwAesImplementation() → bool
```

### absl/random/internal/randen_hwaes.h
```
class RandenHwAes
  Generate(const void* keys, void* state_void) → void
  Absorb(const void* seed_void, void* state_void) → void
  GetKeys() → const void*
```

### absl/random/internal/randen_hwaes_test.cc
```
main(int argc, char* argv[]) → int
if(!x || !y)
```

### absl/random/internal/randen_slow.cc
```
Vector128Load(const void* from) → inline ABSL_RANDOM_INTERNAL_AT
Vector128Store(const Vector128& v, void* to) → inline ABSL_RANDOM_INTERNAL_AT
AesRound(const Vector128& state, const Vector128& round_key) → inline ABSL_RANDOM_INTERNAL_AT
BlockShuffle(absl::uint128* state) → inline ABSL_RANDOM_INTERNAL_AT
for(size_t i = 0; i < RandenTraits::kFeistelBlocks; i++)
FeistelRound(absl::uint128* ABSL_RANDOM_INTERNAL_RESTRICT state, const absl::uint128* ABSL_RANDOM_INTERNAL_RESTRICT keys) → inline ABSL_RANDOM_INTERNAL_AT
for(size_t branch = 0; branch < RandenTraits::kFeistelBlocks; branch += 4)
Permute(absl::uint128* state, const absl::uint128* ABSL_RANDOM_INTERNAL_RESTRICT keys) → inline ABSL_RANDOM_INTERNAL_AT
for(size_t round = 0; round < RandenTraits::kFeistelRounds; ++round)
SwapEndian(absl::uint128* state) → inline ABSL_RANDOM_INTERNAL_AT
for(uint32_t block = 0; block < RandenTraits::kFeistelBlocks; ++block)
```

### absl/random/internal/randen_slow.h
```
class RandenSlow
  Generate(const void* keys, void* state_void) → void
  Absorb(const void* seed_void, void* state_void) → void
  GetKeys() → const void*
```

### absl/random/internal/randen_traits.h
```
struct RandenTraits
```

### absl/random/internal/salted_seed_seq.h
```
class SaltedSeedSeq
struct is_salted_seed_seq
generate(RandomAccessIterator begin, RandomAccessIterator end) → void
if(begin != end)
param(OutIterator out) → void
size() → size_t
generate_impl(ContiguousAndUint32Tag, Contiguous begin, Contiguous end, size_t n) → void
generate_impl(DefaultTag, RandomAccessIterator begin, RandomAccessIterator, size_t n) → void
MakeSaltedSeedSeq(SSeq&& seq) → SSeq
MakeSaltedSeedSeq(SSeq&& seq) → SaltedSeedSeq<typename std::de
```

### absl/random/internal/salted_seed_seq_test.cc
```
ConformsToInterface() → void
for(uint32_t& seed : seed_material)
if(salt_is_available)
```

### absl/random/internal/seed_material.cc
```
ReadSeedMaterialFromOSEntropyImpl(absl::Span<uint32_t> values) → bool
ReadSeedMaterialFromOSEntropyImpl(absl::Span<uint32_t> values) → bool
ReadSeedMaterialFromGetEntropy(absl::Span<uint32_t> values) → bool
while(buffer_size > 0)
if(result < 0)
ReadSeedMaterialFromDevURandom(absl::Span<uint32_t> values) → bool
if(dev_urandom < 0)
while(buffer_size > 0)
if(bytes_read == -1 && read_error == EINTR)
ReadSeedMaterialFromOSEntropyImpl(absl::Span<uint32_t> values) → bool
ReadSeedMaterialFromOSEntropy(absl::Span<uint32_t> values) → bool
MixIntoSeedMaterial(absl::Span<const uint32_t> sequence, absl::Span<uint32_t> seed_material) → void
for(const auto& seq_val : sequence)
for(auto& elem : seed_material)
GetSaltMaterial() → std::optional<uint32_t>
```

### absl/random/internal/seed_material.h
```
SeedBitsToBlocks(size_t seed_size) → constexpr size_t
for(uint32_t& seed_value : values)
```

### absl/random/internal/seed_material_test.cc
```
for(uint32_t seed : seed_material)
for(uint32_t v = 1; v != 0; v <<= 1)
for(uint32_t v = 1; v != 0; v <<= 1)
```

### absl/random/internal/sequence_urbg.h
```
class sequence_urbg
reset() → void
invocations() → size_t
```

### absl/random/internal/traits.h
```
class is_widening_convertible
struct U256
struct make_unsigned_bits
rank() → static constexpr int
BitWidth(T v) → int
```

### absl/random/internal/traits_test.cc
```
CheckWideningConvertsToSelf() → void
CheckWideningConvertsToSelf() → void
CheckNotWideningConvertibleWithSigned() → void
CheckNotWideningConvertibleWithSigned() → void
CheckWideningConvertsToLargerTypes() → void
CheckWideningConvertsToLargerTypes() → void
CheckWideningConvertsTo() → void
```

### absl/random/internal/uniform_helper.h
```
struct TagTypeCompare
uniform_lower_bound(Tag, IntType a, IntType) → IntType>
uniform_lower_bound(Tag, FloatType a, FloatType b) → FloatType>
uniform_lower_bound(Tag, NumType a, NumType) → NumType>
uniform_upper_bound(Tag, IntType, IntType b) → IntType>
uniform_upper_bound(Tag, FloatType, FloatType b) → FloatType>
uniform_upper_bound(Tag, IntType, IntType b) → IntType>
uniform_upper_bound(Tag, FloatType, FloatType b) → FloatType>
```

### absl/random/internal/uniform_helper_test.cc
```
class UniformHelperTest
struct Invalid
CheckArgsInferType() → void
```

### absl/random/internal/wide_multiply.h
```
struct wide_multiply
  static_assert(std::is_unsigned<UIntType>::value, "Class-template wide_multiply<> argument must be unsigned.")
multiply(input_type a, input_type b) → static result_type
hi(result_type r) → static input_type
lo(result_type r) → static input_type
MultiplyU128ToU256(uint128 a, uint128 b) → inline U256
multiply(input_type a, input_type b) → static result_type
hi(result_type r) → static input_type
lo(result_type r) → static input_type
```

### absl/random/internal/wide_multiply_test.cc
```
LeftShift(U256 v, int s) → U256
if(s == 0)
for(int i = 0; i < 64; ++i)
for(int i = 0; i < 64; ++i)
for(int j = 0; j < 64; ++j)
```

### absl/random/log_uniform_int_distribution.h
```
class log_uniform_int_distribution
  assert(max_ >= min_)
  assert(base_ > 1)
  static_assert(random_internal::IsIntegral<IntType>::value, "Class-template absl::log_uniform_int_distribution<> must be " "parameterized using an integral type.")
  Generate(URNG& g, const param_type& p) → unsigned_type
log_range_(0)
if(base_ == 2)
base() → result_type
log_range() → int
range() → unsigned_type
param_(min, max, base) → :
reset() → void
base() → result_type
param() → param_type
param(const param_type& p) → void
if(e == 0)
```

### absl/random/log_uniform_int_distribution_test.cc
```
class LogUniformIntDistributionTypeTest
for(int i = 0; i < kCount; i++)
for(size_t i = 0; i < trials; ++i)
if(chi_square > threshold)
for(int i = 0; i < kTrials; i++)
if(p_value < 0.005)
GenParams() → std::vector<log_uniform_i32::p
ParamName(const ::testing::TestParamInfo<log_uniform_i32::param_type>& info) → std::string
```

### absl/random/mocking_access.h
```
class RandomMockingAccess
InvokeMock(URBG* urbg, IdType key_id, void* args_tuple, void* result) → static inline bool
```

### absl/random/mocking_bit_gen.h
```
class MockingBitGen
  Apply(void* args_tuple, void* result) → void
if(!mock)
InvokeMock(FastTypeIdType key_id, void* args_tuple, void* result) → inline bool
```

### absl/random/mocking_bit_gen_test.cc
```
for(int i = 0; i < 10000; i++)
EXPECT_NONFATAL_FAILURE([]()
for(auto& r : results)
EXPECT_NONFATAL_FAILURE([]()
for(int i = 0; i < 100; i++)
EXPECT_NONFATAL_FAILURE([]()
EXPECT_NONFATAL_FAILURE([&]()
```

### absl/random/poisson_distribution.h
```
class poisson_distribution
  param_type(double mean = 1.0) → explicit
  static_assert(random_internal::IsIntegral<IntType>::value, "Class-template absl::poisson_distribution<> must be " "parameterized using an integral type.")
mean() → double
reset() → void
param() → param_type
param(const param_type& p) → void
mean() → double
if(mean_ < 10)
if(p.split_ != 0)
for(int split = p.split_; split > 0; --split)
for(;;)
if(lhs < rhs)
```

### absl/random/poisson_distribution_test.cc
```
class PoissonDistributionInterfaceTest
class PoissonModel
  InitCDF() → void
struct ZParam
for(const double m : kParams)
for(int i = 0; i < kCount; i++)
mean() → double
variance() → double
stddev() → double
skew() → double
kurtosis() → double
InverseCDF(double p) → CDF
LogCDF() → void
for(const auto c : cdf_)
if(e_neg_mean > 1e-10)
for(size_t i = 1; i < max_i; i++)
if(result < 1e-10 && result < last_result && cumulative > 0.999999)
if(result > 1e-7)
for(int j = 0; j < samples; j++)
if(!pass)
for(int i = 0; i < param.trials; i++)
GetZParams() → std::vector<ZParam>
ZParamName(const ::testing::TestParamInfo<ZParam>& info) → std::string
for(double p = inc; p <= 1.0; p += inc)
for(int j = 0; j < kSamples; j++)
```

### absl/random/seed_gen_exception.cc
```
ThrowSeedGenException() → void
```

### absl/random/seed_gen_exception.h
```
class SeedGenException
```

### absl/random/seed_sequences.cc
```
MakeSeedSeq() → SeedSeq
```

### absl/random/seed_sequences.h
```
CreateSeedSeqFrom(URBG* urbg) → SeedSeq
```

### absl/random/seed_sequences_test.cc
```
TestReproducibleVariateSequencesForNonsecureURBG() → void
for(auto& variate : variates)
for(auto& variate : variates)
```

### absl/random/uniform_int_distribution.h
```
class uniform_int_distribution
  assert(lo <= hi)
  static_assert(random_internal::IsIntegral<result_type>::value, "Class-template absl::uniform_int_distribution<> must be " "parameterized using an integral type.")
  Generate(URBG& g, unsigned_type R) → unsigned_type
a() → result_type
b() → result_type
range() → unsigned_type
param_(lo, hi) → :
reset() → void
a() → result_type
b() → result_type
param() → param_type
param(const param_type& params) → void
```

### absl/random/uniform_int_distribution_test.cc
```
class UniformIntDistributionTest
for(int i = 0; i < kCount; i++)
if(sample > sample_max)
if(sample < sample_min)
for(int i = 0; i < kSize; i++)
for(size_t i = 0; i < kTrials; i++)
if(chi_square > kThreshold)
for(const auto& a : counts)
for(auto& v : output)
for(auto& v : output)
for(auto& v : output)
```

### absl/random/uniform_real_distribution.h
```
class uniform_real_distribution
  assert(lo <= hi)
  static_assert(std::is_floating_point<RealType>::value, "Class-template absl::uniform_real_distribution<> must be " "parameterized using a floating-point type.")
a() → result_type
b() → result_type
param_(lo, hi) → :
reset() → void
a() → result_type
b() → result_type
param() → param_type
param(const param_type& params) → void
while(true)
```

### absl/random/uniform_real_distribution_test.cc
```
class UniformRealDistributionTest
for(int i = 0; i < kCount; i++)
if(sample > sample_max)
if(sample < sample_min)
if(!std::is_same<real_type, long double>::value)
for(int i = 0; i < kSize; i++)
for(size_t i = 0; i < kTrials; i++)
if(chi_square > kThreshold)
for(const auto& a : counts)
```

### absl/random/zipf_distribution.h
```
class zipf_distribution
  h(double x) → double
  hinv(double x) → double
  compute_s() → double
  pow_negative_q(double x) → double
  static_assert(random_internal::IsIntegral<IntType>::value, "Class-template absl::zipf_distribution<> must be " "parameterized using an integral type.")
k() → result_type
q() → double
v() → double
param_(k, q, v) → :
reset() → void
k() → result_type
q() → double
v() → double
param() → param_type
param(const param_type& p) → void
if(kd > kMax)
for(;;)
```

### absl/random/zipf_distribution_test.cc
```
class ZipfDistributionTypedTest
class ZipfModel
  assert(sum_hnq_ > 0)
for(int i = 0; i < kCount; i++)
mean() → double
PMF(size_t i) → double
CDF(size_t i) → double
for(const auto end = it; it != end; it++)
while(max > min + 1)
if(x > p)
Init() → void
for(size_t i = 0; i < k_; i++)
if(i > 1000 && hnq <= 1e-10)
for(double p = 0.01; p < 1.0; p += 0.01)
if(q < min_p)
if(last_cdf < 0.999)
if(q < min_p)
for(size_t i = 0; i < trials; i++)
for(auto& e : expected)
if(chi_square > threshold)
GenParams() → std::vector<zipf_u64::param_ty
ParamName(const ::testing::TestParamInfo<zipf_u64::param_type>& info) → std::string
for(const auto& instance : kInputs)
```

### absl/status/internal/status_internal.cc
```
FindPayloadIndexByUrl(const Payloads* payloads, absl::string_view type_url) → static std::optional<size_t>
if(payloads_ == nullptr)
if(with_payload)
for(const auto& payload : *larger_payloads)
for(const auto& other_payload : *smaller_payloads)
if(payload.type_url == other_payload.type_url)
if(payload.payload != other_payload.payload)
if(payloads_)
MapToLocalCode(int value) → absl::StatusCode
switch(code)
MakeCheckFailString(const absl::Status* absl_nonnull status, const char* absl_nonnull prefix) → const char* absl_nonnull
```

### absl/status/internal/status_internal.h
```
struct Payload
class StatusRep
  Unref() → void
  GetPayload(absl::string_view type_url) → std::optional<absl::Cord>
  SetPayload(absl::string_view type_url, absl::Cord payload) → void
  ErasePayload(absl::string_view type_url) → EraseResult
  GetSourceLocations() → absl::Span<const SourceLocatio
  AddSourceLocation(absl::SourceLocation loc) → void
  ToString(StatusToStringMode mode) → std::string
  CloneAndUnref() → StatusRep* absl_nonnull
code() → absl::StatusCode
message() → const std::string&
Ref() → void
```

### absl/status/internal/status_matchers.h
```
class IsOkAndHoldsMatcher
class StatusCode
class StatusIsMatcherCommonImpl
  DescribeTo(std::ostream* os) → void
  DescribeNegationTo(std::ostream* os) → void
  MatchAndExplain(const absl::Status& status, ::testing::MatchResultListener* result_listener) → bool
class StatusIsMatcher
class IsOkMatcher
GetStatus(const absl::Status& status) → ABSL_REFACTOR_INLINE inline co
GetStatus(const absl::StatusOr<T>& status) → ABSL_REFACTOR_INLINE const abs
code_(code) → :
int() → explicit operator
PrintTo(const StatusCode& code, std::ostream* os) → friend inline void
```

### absl/status/internal/statusor_internal.h
```
class Helper
  HandleInvalidStatusCtorArg(Status* absl_nonnull) → void
class Reference
class StatusOrData
  MakeValue(other.data_)
  MakeStatus()
  MakeStatus(other.status_)
  MakeStatus()
  MakeValue(other.data_)
  MakeStatus()
  MakeStatus(other.status_)
  MakeStatus()
struct OperatorBase
struct CopyCtorBase
struct MoveCtorBase
struct CopyAssignBase
struct MoveAssignBase
class StringifyRandom
PlacementNew(void* absl_nonnull p, Args&&... args) → void
T() → operator
get() → T
constexpr(std::is_reference_v<T>) → if
StatusOrData(const StatusOrData& other)
StatusOrData(const StatusOrData<U>& other) → explicit
StatusOrData(StatusOrData<U>&& other) → explicit
```

### absl/status/status.cc
```
StatusCodeToString(StatusCode code) → std::string
StatusCodeToStringView(StatusCode code) → absl::string_view
switch(code)
MakeErrorImpl(string_view message, SourceLocation loc) → Status
IsAborted(const Status& status) → bool
IsAlreadyExists(const Status& status) → bool
IsCancelled(const Status& status) → bool
IsDataLoss(const Status& status) → bool
IsDeadlineExceeded(const Status& status) → bool
IsFailedPrecondition(const Status& status) → bool
IsInternal(const Status& status) → bool
IsInvalidArgument(const Status& status) → bool
IsNotFound(const Status& status) → bool
IsOutOfRange(const Status& status) → bool
IsPermissionDenied(const Status& status) → bool
IsResourceExhausted(const Status& status) → bool
IsUnauthenticated(const Status& status) → bool
IsUnavailable(const Status& status) → bool
IsUnimplemented(const Status& status) → bool
IsUnknown(const Status& status) → bool
ErrnoToStatusCode(int error_number) → StatusCode
switch(error_number)
MessageForErrnoToStatus(int error_number, absl::string_view message) → std::string
ErrnoToStatus(int error_number, absl::string_view message, absl::SourceLocation loc) → Status
StatusMessageAsCStr(const Status& status) → const char* absl_nonnull
```

### absl/status/status.h
```
Status(base_status) → :
AbslStringify(Sink& sink, const Status& status) → friend void
GetSourceLocations() → absl::Span<const absl::SourceL
if(x.rep_ != old_rep)
if(x.rep_ != old_rep)
OkStatus() → inline Status
CancelledError() → inline Status
MakeError(string_view message, SourceLocation loc) → Status
AbortedError(absl::string_view message, absl::SourceLocation loc) → inline Status
AlreadyExistsError(absl::string_view message, absl::SourceLocation loc) → inline Status
CancelledError(absl::string_view message, absl::SourceLocation loc) → inline Status
DataLossError(absl::string_view message, absl::SourceLocation loc) → inline Status
DeadlineExceededError(absl::string_view message, absl::SourceLocation loc) → inline Status
FailedPreconditionError(absl::string_view message, absl::SourceLocation loc) → inline Status
InternalError(absl::string_view message, absl::SourceLocation loc) → inline Status
InvalidArgumentError(absl::string_view message, absl::SourceLocation loc) → inline Status
NotFoundError(absl::string_view message, absl::SourceLocation loc) → inline Status
OutOfRangeError(absl::string_view message, absl::SourceLocation loc) → inline Status
PermissionDeniedError(absl::string_view message, absl::SourceLocation loc) → inline Status
ResourceExhaustedError(absl::string_view message, absl::SourceLocation loc) → inline Status
UnauthenticatedError(absl::string_view message, absl::SourceLocation loc) → inline Status
UnavailableError(absl::string_view message, absl::SourceLocation loc) → inline Status
UnimplementedError(absl::string_view message, absl::SourceLocation loc) → inline Status
UnknownError(absl::string_view message, absl::SourceLocation loc) → inline Status
```

### absl/status/status_benchmark.cc
```
BM_CreateOk(benchmark::State& state) → void
for(auto _ : state)
BM_CreateBad(benchmark::State& state) → void
for(auto _ : state)
BM_AppendSourceLocation(benchmark::State& state) → void
for(auto _ : state)
```

### absl/status/status_matchers.h
```
IsOkAndHolds(InnerMatcherT&& inner_matcher) → status_internal::IsOkAndHoldsM
StatusIs(StatusCodeMatcherT&& code_matcher, StatusMessageMatcherT&& message_matcher) → status_internal::StatusIsMatch
StatusIs(StatusCodeMatcherT&& code_matcher) → status_internal::StatusIsMatch
IsOk() → inline status_internal::IsOkMa
```

### absl/status/status_matchers_test.cc
```
Explain(const MatcherType& m, const Value& x) → std::string
```

### absl/status/status_payload_printer.cc
```
SetStatusPayloadPrinter(StatusPayloadPrinter printer) → void
GetStatusPayloadPrinter() → StatusPayloadPrinter
```

### absl/status/status_test.cc
```
struct ErrorTest
for(const auto& test : kErrorTests)
for(const auto& other : kErrorTests)
if(other.code != test.code)
AllVisitedPayloads(const absl::Status& s) → PayloadsVec
while(true)
EraseAndReturn(const absl::Status& base) → absl::Status
if(i == j)
for(const absl::Status& status : {a, b})
for(const absl::Status& status : {a, b})
for(const absl::Status& status : {a, b})
for(const absl::Status& status : {a, b})
for(const absl::Status& status : {a, b})
for(absl::Status& s : status_ignores_source_location)
for(int i = 0; i < max_iter; ++i)
for(int i = 0; i < max_iter; ++i)
for(int i = 0; i < max_iter; ++i)
IsRvalueStatus(absl::Status&& s) → absl::Status&&
```

### absl/status/statusor.cc
```
status_(other.status_) → :
ThrowBadStatusOrAccess(absl::Status status) → void
```

### absl/status/statusor.h
```
class BadStatusOrAccess
  BadStatusOrAccess(absl::Status status) → explicit
  BadStatusOrAccess(const BadStatusOrAccess& other)
  status() → const absl::Status&
  InitWhat() → void
ok() → ABSL_MUST_USE_RESULT bool
GetSourceLocations() → absl::Span<const absl::SourceL
AbslStringify(Sink& sink, const StatusOr<T>& status_or) → void
```

### absl/status/statusor_benchmark.cc
```
BM_StatusOrInt_CtorStatus(benchmark::State& state) → void
for(auto _ : state)
BM_StatusOrInt_CtorStatusWithMessage(benchmark::State& state) → void
for(auto _ : state)
BM_StatusOrInt_CopyCtor_Error(benchmark::State& state) → void
for(auto _ : state)
BM_StatusOrInt_CopyCtor_Ok(benchmark::State& state) → void
for(auto _ : state)
BM_StatusOrInt_MoveCtor_Error(benchmark::State& state) → void
for(auto _ : state)
BM_StatusOrInt_MoveCtor_Ok(benchmark::State& state) → void
for(auto _ : state)
BM_StatusOrInt_CopyAssign_Error(benchmark::State& state) → void
for(auto _ : state)
BM_StatusOrInt_CopyAssign_Ok(benchmark::State& state) → void
for(auto _ : state)
BM_StatusOrInt_MoveAssign_Error(benchmark::State& state) → void
for(auto _ : state)
BM_StatusOrInt_MoveAssign_Ok(benchmark::State& state) → void
for(auto _ : state)
BM_StatusOrInt_OkMethod_Error(benchmark::State& state) → void
for(auto _ : state)
BM_StatusOrInt_OkMethod_Ok(benchmark::State& state) → void
for(auto _ : state)
BM_StatusOrInt_StatusMethod_Error(benchmark::State& state) → void
```

### absl/status/statusor_test.cc
```
struct CopyDetector
class Base1
class Base2
class CopyNoAssign
struct Foo
struct InPlaceHelper
struct A
struct ImplicitConstructibleFromA
struct ExplicitConstructibleFromA
struct ImplicitConstructibleFromBool
struct ConvertibleToBool
struct ExplicitConstructibleFromInt
struct Copyable
struct MoveOnly
struct NonMovable
struct FromConstructibleAssignableLvalue
struct FromConstructibleAssignableRvalue
struct FromImplicitConstructibleOnly
struct FromAssignableOnly
struct MockValue
struct MyType
struct StatusOrConversionBase
struct ConversionBase
struct StatusConversionBase
struct ConvertibleToAnyStatusOr
```

### absl/strings/ascii.cc
```
AsciiInAZRange(unsigned char c) → constexpr bool
AsciiInAZRangeNaive(unsigned char c) → constexpr bool
AsciiStrCaseFoldImpl(char* absl_nonnull dst, const char* absl_nullable src, size_t size) → constexpr void
for(size_t i = 0; i < size; ++i)
ABSL_INTERNAL_CONSTEXPR_SINCE_CXX17(Naive) → if
AsciiStrCaseFold(char* absl_nonnull dst, const char* absl_nullable src, size_t size) → constexpr void
AsciiStrToLower(char* absl_nonnull dst, const char* absl_nullable src, size_t n) → void
AsciiStrToUpper(char* absl_nonnull dst, const char* absl_nullable src, size_t n) → void
ValidateAsciiCasefold() → static constexpr size_t
for(unsigned int i = 0; i < num_chars; ++i)
for(size_t i = 0; i < num_chars; ++i)
if(uppered[i] != ch_upper || lowered[i] != ch_lower)
AsciiStrToLower(std::string* absl_nonnull s) → void
AsciiStrToUpper(std::string* absl_nonnull s) → void
RemoveExtraAsciiWhitespace(std::string* absl_nonnull str) → void
for(; input_it < input_end; ++input_it)
if(is_ws)
```

### absl/strings/ascii.h
```
ascii_isalpha(unsigned char c) → inline bool
ascii_isalnum(unsigned char c) → inline bool
ascii_isspace(unsigned char c) → inline bool
ascii_ispunct(unsigned char c) → inline bool
ascii_isblank(unsigned char c) → inline bool
ascii_iscntrl(unsigned char c) → inline bool
ascii_isxdigit(unsigned char c) → inline bool
ascii_isdigit(unsigned char c) → inline constexpr bool
ascii_isprint(unsigned char c) → inline constexpr bool
ascii_isgraph(unsigned char c) → inline constexpr bool
ascii_isupper(unsigned char c) → inline constexpr bool
ascii_islower(unsigned char c) → inline constexpr bool
ascii_isascii(unsigned char c) → inline constexpr bool
ascii_tolower(unsigned char c) → inline char
ascii_toupper(unsigned char c) → inline char
StripLeadingAsciiWhitespace(std::string* absl_nonnull str) → inline void
StripTrailingAsciiWhitespace(std::string* absl_nonnull str) → inline void
StripAsciiWhitespace(std::string* absl_nonnull str) → inline void
```

### absl/strings/ascii_benchmark.cc
```
AsciiBenchmark(benchmark::State& state, Function f) → void
for(auto _ : state)
BM_Ascii(benchmark::State& state) → void
BM_Ascii(benchmark::State& state) → void
BM_Ascii(benchmark::State& state) → void
Noop(unsigned char b) → inline char
BM_StrToLower(benchmark::State& state) → static void
for(auto _ : state)
BM_StrToUpper(benchmark::State& state) → static void
for(auto _ : state)
BM_StrToUpperFromRvalref(benchmark::State& state) → static void
for(auto _ : state)
BM_StrToLowerFromRvalref(benchmark::State& state) → static void
for(auto _ : state)
```

### absl/strings/ascii_test.cc
```
for(int i = 0; i < 256; i++)
for(int i = 0; i < 256; i++)
for(int i = 0; i < 256; i++)
for(int i = 0; i < 256; i++)
for(int i = 0; i < 256; i++)
for(int i = 0; i < 256; i++)
for(int i = 0; i < 256; i++)
for(int i = 0; i < 256; i++)
for(int i = 0; i < 256; i++)
for(int i = 0; i < 256; i++)
for(int i = 0; i < 256; i++)
for(int i = 0; i < 256; i++)
for(unsigned char c = 0; c < 128; c++)
for(int i = 128; i < 256; i++)
for(int i = 0; i < 256; i++)
for(int i = 0; i < 256; i++)
for(int i = 0; i < NUM_TESTS; i++)
```

### absl/strings/atod_manual_test.cc
```
ReadFileToString(const char* filename) → static std::optional<std::stri
if(!f)
if(n != size)
ProcessOneTestFile(const char* filename) → static bool
if(!contents)
for(int i = 0; i < 8; i++)
if(have32 != want32)
for(int i = 0; i < 16; i++)
if(have64 != want64)
main(int argc, char** argv) → int
if(argc < 2)
for(int i = 1; i < argc; i++)
```

### absl/strings/charconv.cc
```
struct CalculatedFloat
MakeNan(const char* absl_nonnull tagp) → static double
Make(mantissa_t mantissa, int exponent, bool sign) → static double
if(mantissa > kMantissaMask)
MakeNan(const char* absl_nonnull tagp) → static float
Make(mantissa_t mantissa, int exponent, bool sign) → static float
if(mantissa > kMantissaMask)
Power10Mantissa(int n) → uint64_t
Power10Exponent(int n) → int
Power10Overflow(int n) → bool
Power10Underflow(int n) → bool
Power10Exact(int n) → bool
BitWidth(uint128 value) → int
NormalizedShiftSize(int mantissa_width, int binary_exponent) → int
TruncateToBitWidth(int bit_width, uint128* absl_nonnull value) → int
HandleEdgeCase(const strings_internal::ParsedFloat& input, bool negative, FloatType* absl_nonnull value) → bool
if(input.type == strings_internal::FloatType::kNan)
if(input.subrange_begin == nullptr)
if(input.type == strings_internal::FloatType::kInfinity)
if(input.mantissa == 0)
EncodeResult(const CalculatedFloat& calculated, bool negative, absl::from_chars_result* absl_nonnull result, FloatType* absl_nonnull value) → void
if(calculated.exponent == kOverflow)
ShiftRightAndRound(uint128 value, int shift, bool input_exact, bool* absl_nonnull output_exact) → uint64_t
if(shift <= 0)
if(shift >= 128)
```

### absl/strings/charconv.h
```
struct from_chars_result
```

### absl/strings/charconv_benchmark.cc
```
BM_Strtod_Pi(benchmark::State& state) → void
for(auto s : state)
BM_Absl_Pi(benchmark::State& state) → void
for(auto s : state)
BM_Strtod_Pi_float(benchmark::State& state) → void
for(auto s : state)
BM_Absl_Pi_float(benchmark::State& state) → void
for(auto s : state)
BM_Strtod_HardLarge(benchmark::State& state) → void
for(auto s : state)
BM_Absl_HardLarge(benchmark::State& state) → void
for(auto s : state)
BM_Strtod_HardSmall(benchmark::State& state) → void
for(auto s : state)
BM_Absl_HardSmall(benchmark::State& state) → void
for(auto s : state)
BM_Strtod_HugeMantissa(benchmark::State& state) → void
for(auto s : state)
BM_Absl_HugeMantissa(benchmark::State& state) → void
for(auto s : state)
MakeHardCase(int length) → std::string
BM_Strtod_Big_And_Difficult(benchmark::State& state) → void
for(auto s : state)
BM_Absl_Big_And_Difficult(benchmark::State& state) → void
for(auto s : state)
```

### absl/strings/charconv_test.cc
```
TestDoubleParse(absl::string_view str, double expected_number) → void
TestFloatParse(absl::string_view str, float expected_number) → void
ToFloat(absl::string_view s) → float
ToDouble(absl::string_view s) → double
TestHalfwayValue(const std::string& mantissa, int exponent, FloatType expected_low, FloatType expected_high, FloatType expected_half) → void
for(int mantissa = 1000000; mantissa <= 9999999; mantissa += 501)
for(int exponent = -300; exponent < 300; ++exponent)
for(int mantissa = 1000000; mantissa <= 9999999; mantissa += 501)
for(int exponent = -43; exponent < 32; ++exponent)
Identical(Float a, Float b) → bool
for(std::string n_char_sequence : {"", "1", "2", "3", "fff", "FFF", "200000", "400000", "4000000000000", "8000000000000", "abc123", "legal_but_unexpected", "99999999999999999999999", "_"})
for(std::string n_char_sequence : {"", "1", "2", "3", "fff", "FFF", "200000", "400000", "4000000000000", "8000000000000", "abc123", "legal_but_unexpected", "99999999999999999999999", "_"})
NextStep(int step) → int
```

### absl/strings/charset.h
```
class CharSet
  CharSet(a.m_[0] & b.m_[0], a.m_[1] & b.m_[1], a.m_[2] & b.m_[2], a.m_[3] & b.m_[3]) → return
  CharSet(a.m_[0] | b.m_[0], a.m_[1] | b.m_[1], a.m_[2] | b.m_[2], a.m_[3] | b.m_[3]) → return
  CharSet(~a.m_[0], ~a.m_[1], ~a.m_[2], ~a.m_[3]) → return
for(char c : str)
contains(char c) → constexpr bool
empty() → constexpr bool
for(uint64_t c : m_)
Char(char x) → static constexpr CharSet
Range(char lo, char hi) → static constexpr CharSet
AsciiUppercase() → static constexpr CharSet
AsciiLowercase() → static constexpr CharSet
AsciiDigits() → static constexpr CharSet
AsciiAlphabet() → static constexpr CharSet
AsciiAlphanumerics() → static constexpr CharSet
AsciiHexDigits() → static constexpr CharSet
AsciiPrintable() → static constexpr CharSet
AsciiWhitespace() → static constexpr CharSet
AsciiPunctuation() → static constexpr CharSet
RangeForWord(char lo, char hi, uint64_t word) → static constexpr uint64_t
OpenRangeFromZeroForWord(uint64_t upper, uint64_t word) → static constexpr uint64_t
CharMaskForWord(char x, uint64_t word) → static constexpr uint64_t
SetChar(unsigned char c) → constexpr void
```

### absl/strings/charset_benchmark.cc
```
MakeBenchmarkMap() → absl::CharSet
for(uint32_t i = 0; i < 256; ++i)
BM_Contains(benchmark::State& state) → static void
for(auto _ : state)
```

### absl/strings/charset_test.cc
```
for(unsigned char ch = 1; ch != 0; ++ch)
Members(const absl::CharSet& m) → std::string
ClosedRangeString(unsigned char lo, unsigned char hi) → std::string
while(true)
for(size_t i = 0; i < 256; ++i)
for(int c = 0; c < 256; ++c)
```

### absl/strings/cord.cc
```
VerifyTree(CordRep* absl_nullable node) → static inline CordRep* absl_nu
CreateFlat(const char* absl_nonnull data, size_t length, size_t alloc_hint) → static CordRepFlat* absl_nonnu
NewBtree(const char* absl_nonnull data, size_t length, size_t alloc_hint) → static CordRep* absl_nonnull
if(length <= kMaxFlatLength)
NewTree(const char* absl_nullable data, size_t length, size_t alloc_hint) → static CordRep* absl_nullable
InitializeCordRepExternal(absl::string_view data, CordRepExternal* absl_nonnull rep) → void
CordRepFromString(std::string&& src) → static CordRep* absl_nonnull
ForceBtree(CordRep* rep) → static CordRepBtree* absl_nonn
PrepareAppendRegion(CordRep* absl_nonnull root, char* absl_nullable* absl_nonnull region, size_t* absl_nonnull size, size_t max_length) → static inline bool
if(in_use == capacity)
contents_(InlineData::kDefaultInit) → :
if(n <= InlineRep::kMaxInline)
if(length <= InlineRep::kMaxInline)
if(tree != nullptr)
if(root != nullptr)
if(src_size <= kMaxBytesToCopy)
if(src_tree == nullptr)
if(&src == this)
ExtractAppendBuffer(CordRep* absl_nonnull rep, size_t min_capacity) → static CordRep::ExtractResult
switch(rep->tag)
CreateAppendBuffer(InlineData& data, size_t block_size, size_t capacity) → static CordBuffer
if(tree != nullptr)
if(result.extracted != nullptr)
if(src_tree != nullptr)
if(tree == nullptr)
```

### absl/strings/cord.h
```
class Cord
  Cord(const Cord& src)
  Cord(absl::string_view src) → explicit
  Cord(T&& src) → explicit
  MakeCordFromExternal(absl::string_view data, Releaser&& releaser) → friend Cord
  Clear() → ABSL_ATTRIBUTE_REINITIALIZES v
  Append(const Cord& src) → void
  Append(Cord&& src) → void
  Append(absl::string_view src) → void
class CordTestAccess
  FlatOverhead() → size_t
  MaxFlatLength() → size_t
  SizeofCordRepExternal() → size_t
  SizeofCordRepSubstring() → size_t
  FlatTagToLength(uint8_t tag) → size_t
  LengthToTag(size_t s) → uint8_t
chunk_iterator_(cord) → :
AbslFormatFlush(absl::Cord* absl_nonnull cord, absl::string_view part) → friend void
AbslStringify(Sink& sink, const absl::Cord& cord) → friend void
AbslHashValue(H hash_state, const absl::Cord& c) → friend H
ConvertCompareResultToStrongOrdering(int c) → static inline std::strong_orde
if(c == 0)
IsSame(const InlineRep& other) → bool
CopyTo(std::string* absl_nonnull dst) → void
is_tree() → bool
```

### absl/strings/cord_analysis.cc
```
struct CordRepRef
  CordRepRef(child) → return
struct RawUsage
Child(const CordRep* absl_nonnull child) → CordRepRef
Add(size_t size, CordRepRef<mode>) → void
Add(size_t size, CordRepRef<Mode::kTotalMorePrecise> repref) → void
MaybeDiv(double d, refcount_t refcount) → double
Child(const CordRep* absl_nonnull child) → CordRepRef
Add(size_t size, CordRepRef<Mode::kFairShare> rep) → void
AnalyzeDataEdge(CordRepRef<mode> rep, RawUsage<mode>& raw_usage) → void
if(rep.rep->tag == SUBSTRING)
AnalyzeBtree(CordRepRef<mode> rep, RawUsage<mode>& raw_usage) → void
GetEstimatedUsage(const CordRep* absl_nonnull rep) → size_t
if(repref.rep->tag == CRC)
GetEstimatedMemoryUsage(const CordRep* absl_nonnull rep) → size_t
GetEstimatedFairShareMemoryUsage(const CordRep* absl_nonnull rep) → size_t
GetMorePreciseMemoryUsage(const CordRep* absl_nonnull rep) → size_t
```

### absl/strings/cord_buffer.h
```
class CordBuffer
  MaximumPayload() → constexpr size_t
  MaximumPayload(size_t block_size) → constexpr size_t
  CreateWithDefaultLimit(size_t capacity) → CordBuffer
  CreateWithCustomLimit(size_t block_size, size_t capacity) → CordBuffer
  available() → absl::Span<char>
  available_up_to(size_t size) → absl::Span<char>
  data() → char*
  data() → const char*
is_short() → bool
short_available() → absl::Span<char>
long_available() → absl::Span<char>
short_length() → size_t
set_short_length(size_t length) → void
add_short_length(size_t n) → void
data() → char*
data() → const char*
rep() → cord_internal::CordRepFlat*
IsPow2(size_t size) → static bool
Log2Floor(size_t size) → static size_t
Log2Ceil(size_t size) → static size_t
ConsumeValue(absl::string_view& short_value) → cord_internal::CordRep*
if(capacity > Rep::kInlineCapacity)
if(capacity + kOverhead >= block_size)
if(slop >= kOverhead && slop <= kMaxPageSlop + kOverhead)
```

### absl/strings/cord_buffer_test.cc
```
class CordBufferTestPeer
ConsumeValue(CordBuffer& buffer, absl::string_view& short_value) → static cord_internal::CordRep*
MaxCapacityFor(size_t block_size, size_t requested) → size_t
for(size_t size = kMaxFlatSize; size <= kCustomLimit; size *= 2)
for(size_t dist_from_max = 0; dist_from_max <= 32; ++dist_from_max)
for(size_t limit = kMaxFlatSize; limit <= kCustomLimit; limit *= 2)
```

### absl/strings/cord_test.cc
```
class CordTestPeer
class TestData
struct CordCompareTestCase
class AfterExitCordTester
  EXPECT_EQ(*cord_, expected_)
struct ShortView
struct LongView
class PopulatedCordFactory
class CordMutator
GetUniformRandomUpTo(RandomEngine* rng, int upper_bound) → static int
if(upper_bound > 0)
GetUniformRandomUpTo(RandomEngine* rng, size_t upper_bound) → static size_t
if(upper_bound > 0)
GenerateSkewedRandom(RandomEngine* rng, int max_log) → static int32_t
RandomLowercaseString(RandomEngine* rng) → static std::string
RandomLowercaseString(RandomEngine* rng, size_t length) → static std::string
DoNothing(absl::string_view , void*) → static void
DeleteExternalString(absl::string_view data, void* arg) → static void
AddExternalMemory(absl::string_view s, absl::Cord* dst) → static void
DumpGrowth() → static void
for(int i = 0; i < 1000; i++)
AppendWithFragments(const std::string& s, RandomEngine* rng, absl::Cord* cord) → static size_t
if(N < min_size)
AddNewStringBlock(const std::string& str, absl::Cord* dst) → static void
MakeComposite() → static absl::Cord
```

### absl/strings/cord_test_helpers.h
```
ToString(TestCordSize size) → inline absl::string_view
switch(size)
Length(TestCordSize size) → inline size_t
MakeFragmentedCord(const Container& c) → Cord
for(const auto& s : c)
MakeFragmentedCord(std::initializer_list<absl::string_view> list) → inline Cord
```

### absl/strings/cordz_test.cc
```
PrintTo(const Cord& cord, std::ostream* s) → inline void
MakeString(size_t size) → absl::string_view
MakeString(TestCordSize size) → absl::string_view
MakeAppendStringCord(TestCordSize size) → absl::Cord
TestParamToString(::testing::TestParamInfo<TestCordSize> size) → std::string
cord() → Cord&
InitialOr(Method method) → Method
ParamToString(::testing::TestParamInfo<T> param) → std::string
```

### absl/strings/cordz_test_helpers.h
```
class CordzSamplingIntervalHelper
struct TestCordRep
struct TestCordData
GetCordzInfoForTesting(const Cord& cord) → inline const cord_internal::Co
CordzInfoIsListed(const cord_internal::CordzInfo* cordz_info, cord_internal::CordzSampleToken token = {}) → inline bool
for(const cord_internal::CordzInfo& info : token)
if(cord_info == nullptr)
if(cord_info == nullptr)
TestCordRep()
UnsampledCord(Args... args) → Cord
```

### absl/strings/escaping.cc
```
is_octal_digit(char c) → inline bool
hex_digit_to_int(char c) → inline unsigned int
if(x > '9')
IsSurrogate(char32_t c, absl::string_view src, std::string* absl_nullable error) → inline bool
if(c >= 0xD800 && c <= 0xDFFF)
if(error)
CUnescapeInternal(absl::string_view src, bool leave_nulls_escaped, char* absl_nonnull dst, size_t* absl_nonnull dst_size, std::string* absl_nullable error) → bool
if(src[p] != '\\')
if(error != nullptr)
switch(src[p])
if(ch > 0xff)
if(error != nullptr)
while(octal_start <= p)
if(error != nullptr)
if(error != nullptr)
if(ch > 0xFF)
if(error != nullptr)
while(hex_start <= p)
if(error != nullptr)
for(int i = 0; i < 4; ++i)
if(error != nullptr)
while(hex_start <= p)
if(error != nullptr)
for(int i = 0; i < 8; ++i)
if(newrune > 0x10FFFF)
```

### absl/strings/escaping.h
```
CUnescape(absl::string_view source, std::string* absl_nonnull dest) → inline bool
```

### absl/strings/escaping_benchmark.cc
```
BM_CUnescapeHexString(benchmark::State& state) → void
for(int i = 0; i < 50; i++)
for(auto _ : state)
BM_WebSafeBase64Escape_string(benchmark::State& state) → void
for(int i = 0; i < 10; ++i)
for(auto _ : state)
BM_HexStringToBytes(benchmark::State& state) → void
for(auto _ : state)
BM_HexStringToBytes_Fail(benchmark::State& state) → void
for(auto _ : state)
CEscapeBenchmarkHelper(benchmark::State& state, const char* string_value, int max_len) → void
for(auto _ : state)
BM_CEscape_NoEscape(benchmark::State& state) → void
BM_CEscape_SomeEscaped(benchmark::State& state) → void
BM_CEscape_MostEscaped(benchmark::State& state) → void
```

### absl/strings/escaping_test.cc
```
struct epair
class CUnescapeTest
for(int kind = 0; kind < 4; kind++)
for(const std::string& original : inputs)
switch(kind)
for(int char0 = 0; char0 < 256; char0++)
for(int char1 = 0; char1 < 256; char1++)
for(const epair& val : oct_values)
for(const epair& val : hex_values)
for(const epair& val : utf8_oct_values)
for(const epair& val : utf8_hex_values)
for(const epair& val : tests)
for(const auto e : bad)
TestEscapeAndUnescape() → void
for(const auto& tc : base64_tests)
if('=' == websafe[c])
for(absl::string_view bad_data : data_set)
for(absl::string_view b64 : good_padding)
for(absl::string_view b64 : bad_padding)
for(size_t i = 0; i < kSize / 3; ++i)
```

### absl/strings/has_absl_stringify.h
```
class UnimplementedSink
  Append(size_t count, char ch) → void
  Append(string_view v) → void
  AbslFormatFlush(UnimplementedSink* sink, absl::string_view v) → friend void
```

### absl/strings/has_absl_stringify_test.cc
```
struct TypeWithoutAbslStringify
struct TypeWithAbslStringify
AbslStringify(Sink&, const TypeWithAbslStringify&) → friend void
```

### absl/strings/has_ostream_operator_test.cc
```
struct TypeWithoutOstreamOp
struct TypeWithOstreamOp
```

### absl/strings/internal/append_and_overwrite.h
```
StringAppendAndOverwrite(T& str, typename T::size_type append_n, Op append_op) → void
```

### absl/strings/internal/append_and_overwrite_test.cc
```
struct AppendAndOverwriteParam
for(int i = 0; i < 1000; ++i)
if(new_cap > prev_cap)
```

### absl/strings/internal/charconv_bigint.cc
```
LargePowerOfFiveData(int i) → const uint32_t*
LargePowerOfFiveSize(int i) → int
if(fp.subrange_begin == nullptr)
if(words_[1])
while(begin < end && *begin == '0')
if(dp != end)
for(; begin != end && significant_digits > 0; ++begin)
if(*begin == '.')
if(after_decimal_point)
if(digits_queued == kMaxSmallPowerOfTen)
if(digits_queued)
if(begin < end && !after_decimal_point)
while(n >= kLargePowerOfFiveStep)
if(first_pass)
for(; this_i >= 0 && other_i < other_size; --this_i, ++other_i)
if(this_word > 0 && size_ <= step)
```

### absl/strings/internal/charconv_bigint.h
```
class BigUnsigned
  static_assert(max_words == 4 || max_words == 84, "unsupported max_words value") → public:
  MultiplyByTenToTheNth(exponent_adjust)
  ReadFloatMantissa(const ParsedFloat& fp, int significant_digits) → int
  SetToZero()
  SetToZero()
  MultiplyBy(words[0])
  MultiplyBy(2, words)
  MultiplyBy(kFiveToNth[kMaxSmallPowerOfFive])
if(exponent_adjust > 0)
Digits10() → static constexpr int
ShiftLeft(int count) → void
if(count > 0)
if(word_shift >= max_words)
if(count == 0)
if(size_ < max_words && words_[size_])
MultiplyBy(uint32_t v) → void
if(size_ == 0 || v == 1)
if(v == 0)
for(int i = 0; i < size_; ++i)
if(window && size_ < max_words)
MultiplyBy(uint64_t v) → void
if(words[1] == 0)
MultiplyByFiveToTheNth(int n) → void
while(n >= kMaxSmallPowerOfFive)
```

### absl/strings/internal/charconv_bigint_test.cc
```
for(int i = 1; i <= 84 * 32; ++i)
for(int i = 1; i < 84*32; ++i)
for(uint32_t i = 1; i <= 100; ++i)
for(uint32_t i = 1; i < 200; i += 2)
for(int a = 0 ; a < 700; a += 25)
for(int i = 0; i < 1160; ++i)
for(int j = 0; j < i; j++)
for(int i = 0; i < 1160; ++i)
for(int i = 0; i < 800; ++i)
for(int j = 0; j < i; j++)
for(int i = 0; i < 200; ++i)
```

### absl/strings/internal/charconv_parse.cc
```
AllowExponent(chars_format flags) → bool
RequireExponent(chars_format flags) → bool
ConsumeDigits(const char* begin, const char* end, int max_digits, T* out, bool* dropped_nonzero_digit) → int
if(base == 10)
if(dropped_nonzero && dropped_nonzero_digit != nullptr)
IsNanChar(char v) → bool
ParseInfinityOrNan(const char* begin, const char* end, strings_internal::ParsedFloat* out) → bool
if(end - begin < 3)
switch(*begin)
if(begin < end && *begin == '(')
ParseFloat(const char* begin, const char* end, chars_format format_flags) → strings_internal::ParsedFloat
while(begin < end && *begin == '0')
if(begin < end && *begin == '.')
if(mantissa == 0)
while(begin < end && *begin == '0')
if(mantissa_begin == begin)
if(begin - mantissa_begin == 1 && *mantissa_begin == '.')
if(mantissa_is_inexact)
if(base == 10)
if(begin < end && *begin == '-')
if(begin == exponent_digits_begin)
if(negative_exponent)
if(result.mantissa > 0)
```

### absl/strings/internal/charconv_parse.h
```
struct ParsedFloat
```

### absl/strings/internal/charconv_parse_test.cc
```
ExpectParsedFloat(std::string s, absl::chars_format format_flags, FloatType expected_type, uint64_t expected_mantissa, int expected_exponent, int expected_literal_exponent = -999) → void
if(open_bracket_pos != std::string::npos)
if(parsed.end == nullptr)
if(begin_subrange == -1)
if(parsed.type == FloatType::kNumber)
if(expected_literal_exponent != -999)
ExpectNumber(std::string s, absl::chars_format format_flags, uint64_t expected_mantissa, int expected_exponent, int expected_literal_exponent = -999) → void
ExpectSpecial(const std::string& s, absl::chars_format format_flags, FloatType type) → void
ExpectFailedParse(absl::string_view s, absl::chars_format format_flags) → void
```

### absl/strings/internal/cord_data_edge.h
```
IsDataEdge(const CordRep* edge) → inline bool
EdgeData(const CordRep* edge) → inline absl::string_view
```

### absl/strings/internal/cord_internal.cc
```
LogFatalNodeType(CordRep* rep) → void
while(true)
if(rep->tag == BTREE)
```

### absl/strings/internal/cord_internal.h
```
class RefcountAndFlags
  assert(refcount > 0 || refcount & kImmortalFlag)
  assert(refcount > 0 || refcount & kImmortalFlag)
struct CordRep
  substring() → CordRepSubstring*
  substring() → const CordRepSubstring*
  crc() → CordRepCrc*
  crc() → const CordRepCrc*
  external() → CordRepExternal*
  external() → const CordRepExternal*
  flat() → CordRepFlat*
  flat() → const CordRepFlat*
struct CordRepSubstring
  Create(CordRep* child, size_t pos, size_t n) → inline CordRepSubstring*
  Substring(CordRep* rep, size_t pos, size_t n) → inline CordRep*
struct CordRepExternal
  Delete(CordRep* rep) → void
struct Rank0
struct ConstInitExternalStorage
class InlineData
  poison_this()
  ABSL_ASSERT(rep != nullptr)
  poison()
  poison() → constexpr void
  unpoison() → constexpr void
```

### absl/strings/internal/cord_rep_btree.cc
```
struct StackOperations
  assert(height <= CordRepBtree::kMaxHeight)
  ABSL_UNREACHABLE()
  assert(!propagate) → case CordRepBtree::kPopped:
  Finalize(tree, result) → return
DumpAll(const CordRep* rep, bool include_contents, std::ostream& stream, size_t depth = 0) → void
if(include_contents)
CreateSubstring(CordRep* rep, size_t offset, size_t n) → CordRepSubstring*
if(rep->tag == SUBSTRING)
MakeSubstring(CordRep* rep, size_t offset, size_t n) → inline CordRep*
MakeSubstring(CordRep* rep, size_t offset) → inline CordRep*
ResizeEdge(CordRep* edge, size_t length, bool is_mutable) → CordRep*
Consume(absl::string_view s, size_t n) → inline absl::string_view
Consume(char* dst, absl::string_view s, size_t n) → inline absl::string_view
if(edge_type == kBack)
FastUnref(R* r, Fn&& fn) → inline void
DeleteSubstring(CordRepSubstring* substring) → void
if(rep->tag >= FLAT)
DeleteLeafEdge(CordRep* rep) → void
if(rep->tag >= FLAT)
owned(int depth) → inline bool
node(int depth) → inline CordRepBtree*
BuildStack(CordRepBtree* tree, int depth) → inline CordRepBtree*
while(current_depth < depth)
BuildOwnedStack(CordRepBtree* tree, int height) → inline void
```

### absl/strings/internal/cord_rep_btree.h
```
class CordRepBtree
  Create(CordRep* rep) → CordRepBtree*
  Destroy(CordRepBtree* tree) → void
  Unref(absl::Span<CordRep* const> edges) → void
  Append(CordRepBtree* tree, CordRep* rep) → CordRepBtree*
  Prepend(CordRepBtree* tree, CordRep* rep) → CordRepBtree*
  Append(CordRepBtree* tree, string_view data, size_t extra = 0) → CordRepBtree*
  Prepend(CordRepBtree* tree, string_view data, size_t extra = 0) → CordRepBtree*
  SubTree(size_t offset, size_t n) → CordRep*
Delete(CordRepBtree* tree) → static void
height() → int
begin() → size_t
back() → size_t
end() → size_t
index(EdgeType edge) → size_t
size() → size_t
capacity() → size_t
set_begin(size_t begin) → void
set_end(size_t end) → void
sub_fetch_begin(size_t n) → size_t
fetch_add_end(size_t n) → size_t
for(CordRep* edge : edges)
for(size_t i = 0; i < new_end; ++i)
if(delta != 0)
for(size_t i = new_end - 1; i >= new_begin; --i)
```

### absl/strings/internal/cord_rep_btree_navigator.cc
```
Substring(CordRep* rep, size_t offset, size_t n) → inline CordRep*
if(rep->tag == SUBSTRING)
Substring(CordRep* rep, size_t offset) → inline CordRep*
while(n >= edge->length)
while(height > 0)
while(n >= edge->length)
if(length < edge->length)
if(++height > height_)
if(length != 0)
if(length >= edge->length)
while(height > 0)
if(length != 0)
while(length >= edge->length)
if(length != 0)
```

### absl/strings/internal/cord_rep_btree_navigator.h
```
class CordRepBtreeNavigator
  bool() → explicit operator
  btree() → CordRepBtree*
  Current() → CordRep*
  InitFirst(CordRepBtree* tree) → CordRep*
  InitLast(CordRepBtree* tree) → CordRep*
  InitOffset(CordRepBtree* tree, size_t offset) → Position
  Next() → CordRep*
  Previous() → CordRep*
bool() → inline CordRepBtreeNavigator::
while(--height >= 0)
while(--height >= 0)
```

### absl/strings/internal/cord_rep_btree_navigator_test.cc
```
CordRepBtreeNavigatorTest()
count() → size_t
tree() → CordRepBtree*
data() → const std::string&
flats() → const std::vector<CordRep*>&
ToString(testing::TestParamInfo<size_t> param) → static std::string
for(size_t char_offset = 0; char_offset < kCharsPerFlat; ++char_offset)
for(size_t index1 = 0; index1 < count; ++index1)
for(size_t index2 = index1; index2 < count; ++index2)
for(size_t char_offset = 0; char_offset < kCharsPerFlat; ++char_offset)
for(size_t char_offset = 0; char_offset < kCharsPerFlat; ++char_offset)
for(size_t index = 0; index < count; ++index)
for(size_t char_offset = 0; char_offset < kCharsPerFlat; ++char_offset)
if(result.tree->tag == BTREE)
```

### absl/strings/internal/cord_rep_btree_reader.cc
```
if(consumed_by_read >= remaining_)
```

### absl/strings/internal/cord_rep_btree_reader.h
```
class CordRepBtreeReader
  length() → size_t
  Init(CordRepBtree* tree) → absl::string_view
  Next() → absl::string_view
  Skip(size_t skip) → absl::string_view
  Read(size_t n, size_t chunk_size, CordRep*& tree) → absl::string_view
  Seek(size_t offset) → absl::string_view
bool() → explicit operator
btree() → CordRepBtree*
node() → CordRep*
remaining() → size_t
Reset() → void
```

### absl/strings/internal/cord_rep_btree_reader_test.cc
```
for(size_t count : counts)
while(remaining > 0)
for(size_t count : counts)
for(size_t count : counts)
for(size_t count : counts)
for(size_t read_size : {kChars - 1, kChars, kChars + 7, cap * cap})
while(remaining > 0)
if(tree)
if(remaining > 0)
```

### absl/strings/internal/cord_rep_btree_test.cc
```
class CordRepBtreeTestPeer
class DataConsumer
SetEdge(CordRepBtree* node, size_t idx, CordRep* edge) → static void
AddEdge(CordRepBtree* node, CordRep* edge) → static void
if(arg->tag < FLAT)
if(actual != data)
if(arg == nullptr)
if(arg->tag != BTREE)
if(arg == nullptr)
if(arg->tag != SUBSTRING)
if(substr->start != start || substr->length != length)
if(arg.tree != tree || arg.extracted != rep)
Next(size_t n) → absl::string_view
Consumed() → absl::string_view
BtreeAdd(CordRepBtree* node, bool append, absl::string_view data) → CordRepBtree*
GetLeafEdges(const CordRepBtree* tree, std::vector<CordRep*>& edges) → void
GetLeafEdges(const CordRepBtree* tree) → std::vector<CordRep*>
MakeHexFlat(size_t i) → CordRepFlat*
MakeLeaf(size_t size = CordRepBtree::kMaxCapacity) → CordRepBtree*
for(size_t i = 1; i < size; ++i)
MakeTree(size_t size, bool append = true) → CordRepBtree*
for(size_t i = 1; i < size; ++i)
CreateTree(absl::Span<CordRep* const> reps) → CordRepBtree*
CreateTree(absl::string_view data, size_t chunk_size) → CordRepBtree*
CreateTreeReverse(absl::string_view data, size_t chunk_size) → CordRepBtree*
```

### absl/strings/internal/cord_rep_consume.cc
```
ClipSubstring(CordRepSubstring* substring) → CordRep*
if(rep->tag == SUBSTRING)
```

### absl/strings/internal/cord_rep_crc.cc
```
if(node->child != nullptr)
```

### absl/strings/internal/cord_rep_crc.h
```
struct CordRepCrc
  New(CordRep* child, crc_internal::CrcCordState state) → CordRepCrc*
  Destroy(CordRepCrc* node) → void
RemoveCrcNode(CordRep* rep) → inline CordRep*
SkipCrcNode(CordRep* rep) → inline CordRep*
SkipCrcNode(const CordRep* rep) → inline const CordRep*
```

### absl/strings/internal/cord_rep_crc_test.cc
```
MakeCrcCordState(uint32_t crc) → absl::crc_internal::CrcCordSta
```

### absl/strings/internal/cord_rep_flat.h
```
struct CordRepFlat
  assert(rep->tag >= FLAT && rep->tag <= MAX_FLAT_TAG)
  delete(rep, size) → ::operator
  delete(rep) → ::operator
AllocatedSizeToTagUnchecked(size_t size) → constexpr uint8_t
TagToAllocatedSize(uint8_t tag) → constexpr size_t
RoundUp(size_t n, size_t m) → constexpr size_t
RoundUpForTag(size_t size) → inline size_t
AllocatedSizeToTag(size_t size) → inline uint8_t
TagToLength(uint8_t tag) → constexpr size_t
NewImpl(size_t len, Args... args ABSL_ATTRIBUTE_UNUSED) → static CordRepFlat*
if(len <= kMinFlatLength)
New(size_t len) → static CordRepFlat*
New(Large, size_t len) → static CordRepFlat*
Delete(CordRep*rep) → static void
Create(absl::string_view data, size_t extra = 0) → static CordRepFlat*
Data() → char*
Data() → const char*
Capacity() → size_t
AllocatedSize() → size_t
```

### absl/strings/internal/cord_rep_test_util.h
```
class AutoUnref
MakeSubstring(size_t start, size_t len, cord_internal::CordRep* rep) → inline cord_internal::CordRepS
MakeFlat(absl::string_view value) → inline cord_internal::CordRepF
MakeExternal(absl::string_view s) → inline cord_internal::CordRepE
CreateRandomString(size_t n) → inline std::string
for(size_t i = 0; i < n; ++i)
CreateFlatsFromString(absl::string_view data, size_t chunk_size) → inline std::vector<cord_intern
CordRepBtreeFromFlats(absl::Span<cord_internal::CordRep* const> flats) → inline cord_internal::CordRepB
CordVisitReps(cord_internal::CordRep* rep, Fn&& fn) → inline void
while(rep->tag == cord_internal::SUBSTRING)
if(rep->tag == cord_internal::BTREE)
CordCollectRepsIf(Predicate&& predicate, cord_internal::CordRep* rep) → inline std::vector<cord_intern
CordVisitReps(rep, [&reps, &predicate](cord_internal::CordRep* rep)
CordCollectReps(cord_internal::CordRep* rep) → inline std::vector<cord_intern
CordToString(cord_internal::CordRep* rep, std::string& s) → inline void
while(rep->tag == cord_internal::SUBSTRING)
if(rep->tag == cord_internal::BTREE)
CordToString(cord_internal::CordRep* rep) → inline std::string
Add(CordRepType* rep) → CordRepType*
Ref(CordRepType* rep) → CordRepType*
RefIf(bool condition, CordRepType* rep) → CordRepType*
```

### absl/strings/internal/cordz_functions.cc
```
cordz_should_profile_slow(SamplingState& state) → ABSL_ATTRIBUTE_NOINLINE int64_
if(mean_interval <= 0)
if(mean_interval == 1)
if(cordz_next_sample.next_sample <= 0)
cordz_set_next_sample_for_testing(int64_t next_sample) → void
get_cordz_mean_interval() → int32_t
set_cordz_mean_interval(int32_t mean_interval) → void
```

### absl/strings/internal/cordz_functions.h
```
struct SamplingState
cordz_should_profile() → inline int64_t
cordz_should_profile() → inline int64_t
cordz_set_next_sample_for_testing(int64_t) → inline void
```

### absl/strings/internal/cordz_functions_test.cc
```
for(int i = 0; i < kSamples; i++)
```

### absl/strings/internal/cordz_handle.cc
```
struct Queue
ABSL_GUARDED_BY(mutex) → std::atomic<CordzHandle*> dq_t
GlobalQueue() → static Queue&
if(is_snapshot)
if(dq_tail != nullptr)
if(is_snapshot_)
if(dq_prev_ == nullptr)
while(next && !next->is_snapshot_)
if(next)
for(CordzHandle* handle : to_delete)
if(handle)
if(dq_tail != nullptr)
for(const CordzHandle* p = dq_tail; p; p = p->dq_prev_)
for(const CordzHandle* p = global_queue.dq_tail; p; p = p->dq_prev_)
for(const CordzHandle* p = dq_next_; p != nullptr; p = p->dq_next_)
```

### absl/strings/internal/cordz_handle.h
```
class CordzSnapshot
is_snapshot() → bool
```

### absl/strings/internal/cordz_handle_test.cc
```
struct CordzHandleDeleteTracker
DeleteQueue() → std::vector<const CordzHandle*
for(int i = 0; i < 3; ++i)
for(int i = 0; i < kNumThreads; ++i)
if(old_handle != nullptr)
for(const CordzHandle* handle : safe_to_inspect)
for(auto& h : handles)
```

### absl/strings/internal/cordz_info.cc
```
class CordRepAnalyzer
  ABSL_ASSERT(rep != nullptr)
  AnalyzeBtree(repref) → case CordRepKind::BTREE:
  ABSL_ASSERT(rep == nullptr || rep->tag != CordRepKind::UNUSED_0)
  CountFlat(size)
statistics_(statistics) → :
AnalyzeCordRep(const CordRep* rep) → void
Child(const CordRep* child) → RepRef
tag() → constexpr CordRepKind
Add(size_t size, size_t refcount) → void
CountFlat(size_t size) → void
if(size <= 64)
CountLinearReps(RepRef rep, MemoryUsage& memory_usage) → RepRef
AnalyzeBtree(RepRef rep) → void
if(src->parent_stack_depth_)
sampling_stride_(sampling_stride)
if(src)
if(head != nullptr)
if(next)
if(prev)
ABSL_EXCLUSIVE_LOCK_FUNCTION(mutex_)
if(!tracked)
```

### absl/strings/internal/cordz_info.h
```
sampling_stride() → int64_t
ABSL_GUARDED_BY(mutex) → CordzInfo* head
ODRCheck() → void
```

### absl/strings/internal/cordz_info_statistics_test.cc
```
struct RefHelper
PrintTo(const CordzStatistics& stats, std::ostream* s) → inline void
Flat(size_t size) → CordRepFlat*
External(size_t length = 512) → CordRepExternal*
Substring(CordRep* rep) → CordRepSubstring*
for(CordRep* rep : refs)
NeedsUnref(T* rep) → T*
Ref(T* rep, size_t n = 1) → T*
while(n--)
SizeOf(const T* rep) → size_t
SizeOf(const CordRepFlat* rep) → size_t
SizeOf(const CordRepExternal* rep) → size_t
FairShareImpl(CordRep* rep, size_t ref) → double
if(rep->tag >= FLAT)
FairShare(CordRep* rep, size_t ref = 1) → size_t
SampleCord(CordRep* rep) → CordzStatistics
if(stats.member != arg.member)
for(size_t i = 0; i < flat3_count; ++i)
for(int i = 0; i < leaf_count; ++i)
if(i == 0)
for(size_t i = 0; i < flat3_count; ++i)
if(i == 0)
for(const CordzInfo& cord_info : token)
for(int i = 0; i < kNumThreads; ++i)
for(InlineData& cord : cords)
```

### absl/strings/internal/cordz_info_test.cc
```
DeleteQueue() → std::vector<const CordzHandle*
FormatStack(absl::Span<void* const> raw_stack) → std::string
for(void* stackp : raw_stack)
TrackChildCord(InlineData& data, const InlineData& parent) → CordzInfo*
TrackParentCord(InlineData& data) → CordzInfo*
```

### absl/strings/internal/cordz_sample_token.cc
```
if(current_)
```

### absl/strings/internal/cordz_sample_token.h
```
class CordzSampleToken
  Iterator(const CordzSampleToken* token) → explicit
begin() → Iterator
end() → Iterator
```

### absl/strings/internal/cordz_sample_token_test.cc
```
for(const CordzInfo& cord_info : token)
for(int i = 0; i < kNumThreads; ++i)
if(token)
for(const CordzInfo& info : *token)
for(TestCordData& cord : cords)
```

### absl/strings/internal/cordz_statistics.h
```
struct CordzStatistics
```

### absl/strings/internal/cordz_update_scope.h
```
info_(info) → :
SetCordRep(CordRep* rep) → void
info() → CordzInfo*
```

### absl/strings/internal/cordz_update_tracker.h
```
class CordzUpdateTracker
  LossyAdd(method, value)
for(int i = 0; i < kNumMethods; ++i)
Value(MethodIdentifier method) → int64_t
LossyAdd(MethodIdentifier method, int64_t n = 1) → void
LossyAdd(const CordzUpdateTracker& src) → void
for(int i = 0; i < kNumMethods; ++i)
```

### absl/strings/internal/cordz_update_tracker_test.cc
```
AllMethods() → Methods
```

### absl/strings/internal/damerau_levenshtein_distance.cc
```
CappedDamerauLevenshteinDistance(absl::string_view s1, absl::string_view s2, uint8_t cutoff) → uint8_t
if(i > lower_diag)
for(size_t j = j_begin; j <= j_end; ++j)
```

### absl/strings/internal/damerau_levenshtein_distance_benchmark.cc
```
MakeTestString(int desired_length, int num_edits) → std::string
BenchmarkArgs(benchmark::internal::Benchmark* benchmark) → void
for(const int s : string_size)
for(const int n : num_edits)
for(const int d : distance_cap)
BM_Distance(benchmark::State& state) → void
for(auto _ : state)
```

### absl/strings/internal/damerau_levenshtein_distance_test.cc
```
for(auto res : {"", "ca", "efg", "ea", "ce", "ceb", "eca", "cae", "cea", "bea"})
for(auto res : {"a", "b", "c", "ba", "cb", "bca", "cab", "cba", "ace", "efc", "ebf", "aef", "ae", "be", "eb", "ec", "ecb", "bec", "bce", "cbe", "ace", "eac", "aeb", "bae", "eab", "eba"})
for(auto res : {"ab", "ac", "bc", "acb", "bac", "ebc", "aec", "abe"})
```

### absl/strings/internal/escaping.cc
```
CalculateBase64EscapedLenInternal(size_t input_len, bool do_padding) → size_t
if(input_len % 3 == 0)
if(do_padding)
if(do_padding)
Base64EscapeInternal(const unsigned char* src, size_t szsrc, char* dest, size_t szdest, const char* base64, bool do_padding) → size_t
if(szsrc >= 3)
while(cur_src < limit_src - 3)
switch(szsrc)
if(do_padding)
if(do_padding)
```

### absl/strings/internal/escaping.h
```
Base64EscapeInternal(const unsigned char* src, size_t szsrc, String* dest, bool do_padding, const char* base64_chars) → void
StringResizeAndOverwrite(*dest, calc_escaped_size, [src, szsrc, base64_chars, do_padding](char* buf, size_t buf_size)
```

### absl/strings/internal/escaping_test_common.h
```
struct base64_testcase
```

### absl/strings/internal/generic_printer.cc
```
PrintEscapedString(std::ostream& os, absl::string_view v) → std::ostream&
TryShorten(T v, F strtox) → std::string
if(parsed != v)
PrintPreciseFP(std::ostream& os, float v) → std::ostream&
TryShorten(v, [](const char* buf) → return os <<
PrintPreciseFP(std::ostream& os, double v) → std::ostream&
TryShorten(v, [](const char* buf) → return os <<
PrintPreciseFP(std::ostream& os, long double v) → std::ostream&
TryShorten(v, [](const char* buf) → return os <<
hexnib(int v) → inline char
PrintCharImpl(std::ostream& os, T v) → static std::ostream&
PrintChar(std::ostream& os, char c) → std::ostream&
PrintChar(std::ostream& os, signed char c) → std::ostream&
PrintChar(std::ostream& os, unsigned char c) → std::ostream&
PrintByte(std::ostream& os, std::byte b) → std::ostream&
```

### absl/strings/internal/generic_printer_internal.h
```
struct ContainerLogPolicy
class GenericPrinter
struct GenericPrintStreamAdapter
  Test(...) → std::false_type
struct GenericPrintAdapterFactory
LogOpening(std::ostream& os) → void
LogClosing(std::ostream& os) → void
LogFirstSeparator(std::ostream& os) → void
LogSeparator(std::ostream& os) → void
MaxElements() → int64_t
Log(std::ostream& os, const T& element) → void
LogEllipsis(std::ostream& os) → void
PrintTuple(std::ostream& os, const std::tuple<Ts...>& tuple) → std::ostream&
PrintPair(std::ostream& os, const std::pair<T, U>& p) → std::ostream&
PrintOptionalLike(std::ostream& os, const T& v) → std::ostream&
PrintVariant(std::ostream& os, const std::variant<Ts...>& v) → std::ostream&
PrintStatusOrLike(std::ostream& os, const StatusOrLike& v) → std::ostream&
PrintSmartPointerContents(std::ostream& os, const SmartPointer& v) → std::ostream&
if(v == nullptr)
GenericPrintImpl(std::ostream& os, const T& v) → std::ostream&
constexpr(is_any_string<T> || std::is_same_v<T, absl::string_view>) → if
Flush() → void
constexpr(HasFlushMethod<StreamT>::value) → if
```

### absl/strings/internal/generic_printer_test.cc
```
struct NotStreamable
struct Streamable
struct AbslStringifiable
struct CustomPrinter
struct CustomRecursive
AbslStringify(S& sink, const AbslStringifiable&) → friend void
IsUnprintable() → auto
HasExactlyNInstancesOf(int n, absl::string_view me) → auto
GenericPrintToString(const T& v) → std::string
DebugString() → std::string
AbslStringify(Sink& sink, const CustomContainer& c) → friend void
SpecializablePrint(const T& v) → std::string
CustomPrinter(int) → explicit
for(int i = 0; i < 4; ++i)
AbslStringify(Sink& sink, CStyleEnumWithStringify e) → void
switch(e)
AbslStringify(Sink& sink, CppStyleEnumWithStringify e) → void
switch(e)
AbslStringify(Sink& sink, CharBasedEnumWithStringify e) → void
switch(e)
```

### absl/strings/internal/memutil.cc
```
memcasecmp(const char* s1, const char* s2, size_t len) → int
for(size_t i = 0; i < len; i++)
if(c1 != c2)
```

### absl/strings/internal/memutil_benchmark.cc
```
MakeHaystack() → const char*
case_eq(const char a, const char b) → bool
BM_Searchcase(benchmark::State& state) → void
for(auto _ : state)
BM_SearchcaseMedium(benchmark::State& state) → void
for(auto _ : state)
BM_SearchcasePathological(benchmark::State& state) → void
for(auto _ : state)
memcasechr(const char* s, int c, size_t slen) → char*
for(; slen; ++s, --slen)
memcasematch(const char* phaystack, size_t haylen, const char* pneedle, size_t neelen) → const char*
if(0 == neelen)
BM_Memcasematch(benchmark::State& state) → void
for(auto _ : state)
BM_MemcasematchMedium(benchmark::State& state) → void
for(auto _ : state)
BM_MemcasematchPathological(benchmark::State& state) → void
for(auto _ : state)
```

### absl/strings/internal/numbers_test_common.h
```
struct uint32_test_case
struct uint64_test_case
Itoa(IntType value, int base, std::string* destination) → inline bool
if(base <= 1 || base > 36)
if(value == 0)
while(value != 0)
if(negative)
```

### absl/strings/internal/ostringstream.h
```
buf_(that.buf_)
str() → std::string*
str() → const std::string*
str(std::string* str) → void
str() → std::string*
str() → const std::string*
str(std::string* str) → void
```

### absl/strings/internal/ostringstream_benchmark.cc
```
BM_StdStream(benchmark::State& state) → void
for(auto _ : state)
for(int i = 0; i != num_writes; ++i)
switch(kOutput)
BM_CustomStream(benchmark::State& state) → void
for(auto _ : state)
for(int i = 0; i != num_writes; ++i)
switch(kOutput)
```

### absl/strings/internal/pow10_helper.cc
```
Pow10(int exp) → double
if(exp < -324)
```

### absl/strings/internal/pow10_helper_test.cc
```
struct TestCase
for(const TestCase& test_case : kTestCases)
```

### absl/strings/internal/resize_uninitialized.h
```
struct ResizeUninitializedTraits
Resize(string_type* s, size_t new_size) → static void
Resize(string_type* s, size_t new_size) → static void
STLStringSupportsNontrashingResize(string_type*) → inline constexpr bool
STLStringResizeUninitialized(string_type* s, size_t new_size) → inline void
STLStringResizeUninitializedAmortized(string_type* s, size_t new_size) → void
```

### absl/strings/internal/resize_uninitialized_test.cc
```
struct resizable_string
struct default_init_string
size() → size_t
capacity() → size_t
data() → char*
resize(size_t) → void
reserve(size_t) → void
erase(size_t = 0, size_t = 0) → resizable_string&
max_size() → size_t
size() → size_t
capacity() → size_t
data() → char*
resize(size_t) → void
reserve(size_t) → void
erase(size_t = 0, size_t = 0) → default_init_string&
max_size() → size_t
for(int i = 0; i < 1000; ++i)
```

### absl/strings/internal/str_format/arg.cc
```
class IntDigits
  static_assert(!IsSigned<T>::value, "")
  static_assert(std::is_integral<T>::value, "")
  PrintAsDec(u, add_neg)
  static_assert(!IsSigned<T>::value, "")
  static_assert(!IsSigned<T>::value, "")
  static_assert('-' < '0', "The check below verifies both.")
ReducePadding(string_view s, size_t *capacity) → void
ReducePadding(size_t n, size_t *capacity) → void
PrintAsOct(T v) → void
PrintAsDec(T v) → void
PrintAsDec(int128 v) → void
if(v < 0)
PrintAsDec(uint128 v, bool add_neg = false) → void
if(p[0] == '0')
if(add_neg)
PrintAsHexLower(T v) → void
if(p[0] == '0')
PrintAsHexUpper(T v) → void
with_neg_and_zero() → string_view
without_neg_or_zero() → string_view
is_negative() → bool
BaseIndicator(const IntDigits &as_digits, const FormatConversionSpecImpl conv) → string_view
SignColumn(bool neg, const FormatConversionSpecImpl conv) → string_view
ConvertCharImpl(char v, const FormatConversionSpecImpl conv, FormatSinkImpl* sink) → bool
```

### absl/strings/internal/str_format/arg.h
```
struct ArgConvertResult
struct VoidPtr
struct FormatCountCaptureHelper
struct FormatArgImplFriend
class FormatArgImpl
  static_assert(std::is_same<D, const T&>::value || storage_policy<D>::value == ByValue, "Decayed types must be stored by value")
ExtractCharSet(FormatConvertResult<C>) → constexpr FormatConversionChar
ExtractCharSet(ArgConvertResult<C>) → constexpr FormatConversionChar
FormatConvertImpl(std::string_view v, FormatConversionSpecImpl conv, FormatSinkImpl* sink) → inline StringConvertResult
FormatConvertImpl(const AbslCord& value, FormatConversionSpecImpl conv, FormatSinkImpl* sink) → StringConvertResult
if(to_write == 0)
FormatConvertImpl(T v, FormatConversionSpecImpl conv, FormatSinkImpl* sink) → IntegralConvertResult
FormatConvertImpl(const StreamedWrapper<T>& v, FormatConversionSpecImpl conv, FormatSinkImpl* out) → StringConvertResult
ConvertHelper(const FormatCountCapture& v, FormatConversionSpecImpl conv, FormatSinkImpl* sink) → static ArgConvertResult<Format
FormatConvertImpl(const FormatCountCapture& v, FormatConversionSpecImpl conv, FormatSinkImpl* sink) → ArgConvertResult<FormatConvers
ToInt(Arg arg, int* out) → static bool
Convert(Arg arg, FormatConversionSpecImpl conv, FormatSinkImpl* out) → static bool
GetVTablePtrForTest(Arg arg) → static typename Arg::Dispatche
ArgumentToConv() → constexpr FormatConversionChar
FormatArgImpl(const T& value) → explicit
SetValue(const T& value) → static Data
Value(Data arg) → static const T&
SetValue(const T& value) → static Data
Value(Data arg) → static const T&
SetValue(const T& value) → static Data
```

### absl/strings/internal/str_format/arg_test.cc
```
class FormatArgImplTest
```

### absl/strings/internal/str_format/bind.cc
```
class ArgContext
  Bind(const UnboundConversion* unbound, BoundConversion* bound) → bool
class ConverterConsumer
class DefaultConverter
class SummarizingConverter
  spec("%d") → UntypedFormatSpecImpl
BindFromPosition(int position, int* value, absl::Span<const FormatArgImpl> pack) → inline bool
if(unbound->flags != Flags::kBasic)
if(width < 0)
if(force_left)
Append(string_view s) → bool
ConvertOne(const UnboundConversion& conv, string_view conv_string) → bool
ConvertAll(const UntypedFormatSpecImpl format, absl::Span<const FormatArgImpl> args, Converter converter) → bool
Append(string_view s) → void
ConvertOne(const BoundConversion& bound, string_view) → bool
Append(string_view s) → void
ConvertOne(const BoundConversion& bound, string_view) → bool
BindWithPack(const UnboundConversion* props, absl::Span<const FormatArgImpl> pack, BoundConversion* bound) → bool
Summarize(const UntypedFormatSpecImpl format, absl::Span<const FormatArgImpl> args) → std::string
FormatUntyped(FormatRawSinkImpl raw_sink, const UntypedFormatSpecImpl format, absl::Span<const FormatArgImpl> args) → bool
AppendPack(std::string* out, const UntypedFormatSpecImpl format, absl::Span<const FormatArgImpl> args) → std::string&
FormatPack(UntypedFormatSpecImpl format, absl::Span<const FormatArgImpl> args) → std::string
FprintF(std::FILE* output, const UntypedFormatSpecImpl format, absl::Span<const FormatArgImpl> args) → int
SnprintF(char* output, size_t size, const UntypedFormatSpecImpl format, absl::Span<const FormatArgImpl> args) → int
```

### absl/strings/internal/str_format/bind.h
```
class BoundConversion
class UntypedFormatSpecImpl
struct MakeDependent
class Streamable
  Print(std::ostream& os) → std::ostream&
class StreamedWrapper
arg() → const FormatArgImpl*
set_arg(const FormatArgImpl* a) → void
has_parsed_conversion() → bool
str() → string_view
parsed_conversion() → const str_format_internal::Par
Extract(const T& s) → static const UntypedFormatSpec
CheckArity(ErrorMaker<true> SpecifierCount = {}, ErrorMaker<i == j> ParametersPassed = {}) → static constexpr bool
CheckMatches(absl::index_sequence<I...>) → static bool
Base("to avoid noise in the compiler error") → :
Base(s) → :
Base(s) → :
Base(&pc) → :
```

### absl/strings/internal/str_format/bind_test.cc
```
class FormatBindTest
Extract(const char *s, UnboundConversion *props, int *next) → bool
for(const Expectation &e : kExpect)
if(e.arg != nullptr)
for(const Expectation &e : kExpect)
```

### absl/strings/internal/str_format/checker.h
```
ValidFormatImpl(string_view format) → constexpr bool
while(p != end)
if(p == end)
if(p[1] == '%')
if(conv.arg_position <= 0 || conv.arg_position > kNumArgs)
for(auto extra : {conv.width, conv.precision})
for(bool b : used)
```

### absl/strings/internal/str_format/checker_test.cc
```
struct Case
ConvToString(FormatConversionCharSet conv) → std::string
ValidFormat(const char* format) → constexpr Case
for(Case c : trues)
for(Case c : falses)
```

### absl/strings/internal/str_format/constexpr_parser.h
```
struct UnboundConversion
  assert(value >= 0)
  assert(value > 0)
class ConvTag
struct ConvTagHolder
UnboundConversion()
set_value(int value) → constexpr void
value() → constexpr int
set_from_arg(int value) → constexpr void
is_from_arg() → constexpr bool
get_from_arg() → constexpr int
is_conv() → constexpr bool
is_length() → constexpr bool
is_flags() → constexpr bool
as_conv() → constexpr FormatConversionChar
as_length() → constexpr LengthMod
as_flags() → constexpr Flags
GetTagForChar(char c) → constexpr ConvTag
CheckFastPathSetting(const UnboundConversion& conv) → constexpr bool
if(width_precision_needed && conv.flags == Flags::kBasic)
ParseDigits(char& c, const char*& pos, const char* const end) → constexpr int
for(;;)
ConsumeConversion(const char* pos, const char* const end, UnboundConversion* conv, int* next_arg) → constexpr const char*
if(is_positional)
if(c < 'A')
```

### absl/strings/internal/str_format/convert_test.cc
```
struct NativePrintfTraits
class FormatConvertTest
struct Cardinal
class TypedFormatConvertTest
struct NullSink
LengthModFor() → std::string
EscCharImpl(int v) → std::string
Esc(char v) → std::string
Esc(signed char v) → std::string
Esc(unsigned char v) → std::string
Esc(wchar_t v) → std::string
Esc(const T &v) → std::string
StrAppendV(std::string *dst, const char *format, va_list ap) → void
if(result < kSpaceLength)
if(result >= 0)
if(result < 0)
StrAppend(std::string *out, const char *format, ...) → void
StrPrint(const char *format, ...) → std::string
VerifyNativeImplementationImpl() → NativePrintfTraits
IsNativeHexFloatConversion(char f) → bool
TestStringConvert(const T& str) → void
for(const Expectation &e : kExpect)
AllFlagCombinations() → std::vector<std::string>
if(is_unsigned_to_signed)
StrPrintChar(T c) → std::optional<std::string>
```

### absl/strings/internal/str_format/extension.cc
```
FlagsToString(Flags v) → std::string
```

### absl/strings/internal/str_format/extension.h
```
class FormatRawSinkImpl
class FormatSinkImpl
  memset(pos_, c, count)
  Flush()
  raw_append(n)
  Flush()
  PutPaddedString(string_view v, int width, int precision, bool left) → bool
  T(this) → return
struct FormatConversionCharInternal
class FormatConversionSpecImpl
  FlagsContains(flags_, Flags::kShowPos) → return
  FlagsContains(flags_, Flags::kSignCol) → return
  T(*this) → return
struct FormatConversionCharSetInternal
  FormatConversionCharToConvValue(#c[0])
  FormatConversionCharToConvValue('*')
  FormatConversionCharSetUnion(d, i, u, o, x, X)
  FormatConversionCharSetUnion(a, e, f, g, A, E, F, G)
  FormatConversionCharSetUnion(kIntegral, kFloating)
Write(string_view s) → void
Extract(T s) → static FormatRawSinkImpl
Flush(void* r, string_view s) → static void
Flush() → void
Append(size_t n, char c) → void
Append(string_view v) → void
```

### absl/strings/internal/str_format/extension_test.cc
```
class UserDefinedType
Append(absl::string_view str) → void
Value() → const std::string&
AbslFormatFlush(UserDefinedType* x, absl::string_view str) → friend void
MakeRandomString(size_t len) → std::string
for(char& c : s)
for(size_t chunk_size : {1, 10, 100, 1000, 10000})
for(size_t chunks = 0; chunks < 10; ++chunks)
for(size_t chunk_size : {1, 10, 100, 1000, 10000})
for(size_t chunks = 0; chunks < 10; ++chunks)
```

### absl/strings/internal/str_format/float_conversion.cc
```
class StackArray
  assert(capacity <= kMaxCapacity)
  assert(step <= kNumSteps)
  assert(false && "Invalid capacity")
class BinaryToDecimal
  assert(exp > 0)
  assert(exp <= std::numeric_limits<MaxFloatType>::max_exponent)
  for(v >>= (32 - offset)
  assert(decimal_start_ != after_chunk_index - 1)
class FractionalDigitGenerator
  assert(-exp < 0)
  assert(-exp >= Limits::min_exponent - margin)
struct FractionalDigitPrinterResult
struct FormatState
struct Padding
struct HexFloatTypeParams
  assert(leading_digit_size_bits >= 1 && leading_digit_size_bits <= 4)
struct Buffer
  assert(begin > data)
  assert(begin < end)
  assert(begin < end)
struct Decomposed
struct DigitRun
RunWithCapacityImpl(Func f) → ABSL_ATTRIBUTE_NOINLINE static
RunWithCapacity(size_t capacity, Func f) → static void
```

### absl/strings/internal/str_format/output.cc
```
struct ClearErrnoGuard
if(to_write > 0)
if(errno == EINTR)
```

### absl/strings/internal/str_format/output.h
```
class BufferRawSink
  Write(string_view v) → void
class FILERawSink
  Write(string_view v) → void
total_written() → size_t
count() → size_t
error() → int
AbslFormatFlush(std::string* out, string_view s) → inline void
AbslFormatFlush(std::ostream* out, string_view s) → inline void
AbslFormatFlush(FILERawSink* sink, string_view v) → inline void
AbslFormatFlush(BufferRawSink* sink, string_view v) → inline void
```

### absl/strings/internal/str_format/parser.cc
```
ConsumeUnboundConversionNoInline(const char* p, const char* end, UnboundConversion* conv, int* next_arg) → ABSL_ATTRIBUTE_NOINLINE const 
LengthModToString(LengthMod v) → std::string
switch(v)
Append(string_view s) → bool
ConvertOne(const UnboundConversion &conv, string_view s) → bool
AppendText(string_view s) → size_t
for(const ConversionItem &item : items_)
```

### absl/strings/internal/str_format/parser.h
```
class ParsedFormatBase
  ParsedFormatBase(string_view format, bool allow_ignored, std::initializer_list<FormatConversionCharSet> convs) → explicit
  text(base, 0) → string_view
  MatchesConversions(bool allow_ignored, std::initializer_list<FormatConversionCharSet> convs) → bool
class ExtendedParsedFormat
  New(format, false) → return
  New(format, true) → return
ParseFormatString(string_view src, Consumer consumer) → bool
while(p != end)
if(!percent)
EnsureConstexpr(string_view s) → constexpr bool
ParsedFormatBase(const ParsedFormatBase& other)
ParsedFormatBase(ParsedFormatBase&& other)
if(text_size > 0)
ProcessFormat(Consumer consumer) → bool
for(const auto& item : items_)
if(item.is_conversion)
has_error() → bool
ExtendedParsedFormat(format, false) → :
New(string_view format) → static std::unique_ptr<Extende
NewAllowIgnored(string_view format) → static std::unique_ptr<Extende
New(string_view format, bool allow_ignored) → static std::unique_ptr<Extende
ParsedFormatBase(s, allow_ignored, {C...}) → :
```

### absl/strings/internal/str_format/parser_test.cc
```
class ConsumeUnboundConversionTest
struct SummarizeConsumer
class ParsedFormatTest
struct ExpectParse
for(auto e : kExpect)
for(auto e : kExpect)
Run(const char *fmt, bool force_positional = false) → bool
for(const auto& e : kExpect)
for(int rev = 0; rev < 2; ++rev)
for(int i = 0; i < 1 << kNumFlags; ++i)
if(rev == 1)
for(const char* fmt : {"d", "llx", "G", "1$X"})
for(const char* fmt : {"3d", ".llx", "-G", "1$#X", "lc"})
Append(string_view s) → bool
ConvertOne(const UnboundConversion& conv, string_view s) → bool
SummarizeParsedFormat(const ParsedFormatBase& pc) → std::string
for(const auto& e : kExpect)
for(const auto& e : kExpect)
```

### absl/strings/internal/str_join_internal.h
```
struct AlphaNumFormatterImpl
  StrAppend(out, t)
struct NoFormatter
class StreamFormatterImpl
class PairFormatterImpl
  f1_(out, p.first)
  f2_(out, p.second)
  f1_(out, p.first)
  f2_(out, p.second)
class DereferenceFormatterImpl
  f_(out, *t)
  f_(out, *t)
struct DefaultFormatter
struct JoinTupleLoop
if(strm_)
JoinAlgorithm(Iterator start, Iterator end, absl::string_view s, Formatter&& f) → std::string
for(Iterator it = start; it != end; ++it)
JoinAlgorithm(Iterator start, Iterator end, absl::string_view s, NoFormatter) → std::string
if(start != end)
for(Iterator it = start; ++it != end;)
if(result_size > 0)
for(Iterator it = start; ++it != end;)
JoinAlgorithm(const std::tuple<T...>& tup, absl::string_view sep, Formatter&& fmt) → std::string
JoinRange(Iterator first, Iterator last, absl::string_view separator) → std::string
JoinRange(const Range& range, absl::string_view separator, Formatter&& fmt) → std::string
```

### absl/strings/internal/str_split_internal.h
```
class ConvertibleToStringView
  assert(s != nullptr)
  assert(s != nullptr)
class SplitIterator
  old(*this) → SplitIterator
class Splitter
  for(std::size_t i = 0; i < Size && it != end()
value_(s) → :
value_(s) → :
value_(s) → :
value() → absl::string_view
if(state_ == kEndState)
at_end() → bool
if(state_ == kLastState)
text() → absl::string_view
delimiter() → const Delimiter&
predicate() → const Predicate&
begin() → const_iterator
end() → const_iterator
Container() → operator
for(const auto& sp : splitter)
for(const absl::string_view sv : splitter)
if(insert)
ToIter(std::pair<iterator, bool> pair) → static iterator
ToIter(iterator iter) → static iterator
```

### absl/strings/internal/string_constant.h
```
struct StringConstant
TryConstexprEval(absl::string_view view) → static constexpr bool
MakeStringConstant(T) → constexpr StringConstant<T>
```

### absl/strings/internal/string_constant_test.cc
```
struct Callable
```

### absl/strings/internal/stringify_sink.h
```
class StringifySink
  Append(size_t count, char ch) → void
  Append(string_view v) → void
  ExtractStringification(StringifySink& sink, const T& v) → friend string_view
AbslFormatFlush(StringifySink* sink, absl::string_view v) → friend void
ExtractStringification(StringifySink& sink, const T& v) → string_view
AbslStringify(Sink& sink, SourceLocation l) → void
```

### absl/strings/internal/stringify_stream.h
```
class StringifyStream
  AbslStringify(stream.sink_, t)
constexpr(HasStreamInsertion<T>::value) → if
Append(size_t count, char ch) → void
Append(absl::string_view v) → void
AbslFormatFlush(Sink* sink, absl::string_view v) → friend void
```

### absl/strings/internal/stringify_stream_test.cc
```
struct AppendNCharsTest
struct AppendStringViewTest
struct AbslFormatFlushTest
struct PreferStreamInsertionOverAbslStringifyTest
AbslStringify(Sink& sink, const AppendNCharsTest& t) → friend void
AbslStringify(Sink& sink, const AppendStringViewTest& t) → friend void
AbslStringify(Sink& sink, const AbslFormatFlushTest& t) → friend void
AbslStringify(Sink& sink, const PreferStreamInsertionOverAbslStringifyTest&) → friend void
```

### absl/strings/internal/utf8.cc
```
EncodeUTF8Char(char* buffer, char32_t utf8_char) → size_t
if(utf8_char <= 0x7F)
WideToUtf8(wchar_t wc, char* buf, ShiftState& s) → size_t
if(v <= 0x007F)
if(!s.saw_high_surrogate)
if(0x10000 <= v && v <= 0x10FFFF)
```

### absl/strings/internal/utf8.h
```
struct ShiftState
```

### absl/strings/internal/utf8_test.cc
```
struct WideToUtf8TestCase
for(auto& test : tests)
while(buf0[apparent_length - 1] == '\x00' && buf1[apparent_length - 1] == '\xFF')
GetWideToUtf8TestCases() → std::vector<WideToUtf8TestCase
GetUtf8TestString() → absl::string_view
for(const wchar_t wc : WIDE_STRING_LITERAL)
```

### absl/strings/match.cc
```
if(upper_needle == lower_needle)
FindLongestCommonPrefix(absl::string_view a, absl::string_view b) → absl::string_view
FindLongestCommonSuffix(absl::string_view a, absl::string_view b) → absl::string_view
while(count < limit && *pa == *pb)
```

### absl/strings/numbers.cc
```
struct ExpDigits
struct LookupTables
SimpleAtof(absl::string_view str, float* absl_nonnull out) → bool
if(str[0] == '+')
if(result.ec == std::errc::invalid_argument)
if(result.ec == std::errc::result_out_of_range)
if(*out > 1.0)
SimpleAtod(absl::string_view str, double* absl_nonnull out) → bool
if(str[0] == '+')
if(result.ec == std::errc::invalid_argument)
if(result.ec == std::errc::result_out_of_range)
if(*out > 1.0)
SimpleAtob(absl::string_view str, bool* absl_nonnull out) → bool
EncodeHundred(uint32_t n, char* absl_nonnull out_str) → inline char*
EncodeTenThousand(uint32_t n, char* absl_nonnull out_str) → inline char*
PrepareEightDigits(uint32_t i) → inline uint64_t
EncodePadded16(uint64_t v, char* absl_nonnull buffer) → inline char*
EncodeFullU32(uint32_t n, char* absl_nonnull out_str) → inline ABSL_ATTRIBUTE_ALWAYS_I
if(n < 10)
if(n < 100'000'000)
EncodeFullU64(uint64_t i, char* absl_nonnull buffer) → inline ABSL_ATTRIBUTE_ALWAYS_I
if(i < 1'0000'0000'0000'0000ull)
EncodeFullU128(uint128 i, char* absl_nonnull buffer) → inline ABSL_ATTRIBUTE_ALWAYS_I
if(high == 0)
if(i < 0)
```

### absl/strings/numbers.h
```
is_signed() → constexpr bool
constexpr(std::is_arithmetic<int_type>::value) → if
if(kIsSigned)
constexpr(kUse128Bit) → if
constexpr(kUse128Bit) → if
if(kIsSigned)
if(kIntTypeSize == 64)
if(kIntTypeSize == 64)
FastHexToBufferZeroPad16(uint64_t val, char* absl_nonnull out) → inline size_t
for(int i = 0; i < 8; ++i)
```

### absl/strings/numbers_benchmark.cc
```
BM_FastIntToBuffer(benchmark::State& state) → void
for(auto _ : state)
RepeatedSevens(int num_digits, int base) → int64_t
BM_safe_strto32_string(benchmark::State& state) → void
for(auto _ : state)
BM_safe_strto64_string(benchmark::State& state) → void
for(auto _ : state)
BM_safe_strtou32_string(benchmark::State& state) → void
for(auto _ : state)
BM_safe_strtou64_string(benchmark::State& state) → void
for(auto _ : state)
MakeFloatStrings(int num_strings, int num_digits) → std::vector<std::string>
for(std::string& s : float_strings)
for(int i = 0; i < num_digits; ++i)
for(int i = 0; i < num_digits; ++i)
GetStringAs(const std::string& s) → StringType
GetStringsAs(const std::vector<std::string>& strings) → std::vector<StringType>
for(const std::string& s : strings)
BM_SimpleAtof(benchmark::State& state) → void
for(auto _ : state)
for(const T& input : inputs)
BM_SimpleAtod(benchmark::State& state) → void
for(auto _ : state)
for(const T& input : inputs)
BM_FastHexToBufferZeroPad16(benchmark::State& state) → void
```

### absl/strings/numbers_test.cc
```
struct MyInteger
class SimpleDtoaTest
  feholdexcept(&fp_env_)
  feenableexcept(FE_DIVBYZERO | FE_INVALID | FE_OVERFLOW)
  fedisableexcept(FE_DIVBYZERO | FE_INVALID | FE_OVERFLOW)
  fesetenv(&fp_env_)
PerfectDtoa(double d) → std::string
for(int64_t num = mantissa, pos = maxlen; --pos >= 0;)
for(char* p = end; --p != begin;)
if(exp < 0)
for(int i = 0; i < 100; ++i)
for(double multiplier : {1e-300, 1e-200, 1e-100, 0.1, 1.0, 10.0, 1e100, 1e300})
integer() → constexpr operator
as_integer() → integer
CheckInt32(int32_t x) → void
CheckInt64(int64_t x) → void
CheckUInt32(uint32_t x) → void
CheckUInt64(uint64_t x) → void
CheckUInt128(absl::uint128 x) → void
CheckInt128(absl::int128 x) → void
CheckHex64(uint64_t v) → void
for(int i = -100; i <= 100; i++)
for(int i = 0; i <= 100; i++)
for(int i = 0; i < 10000; i++)
VerifySimpleAtoiGood(in_val_type in_value, int_type exp_value) → void
```

### absl/strings/resize_and_overwrite.h
```
ProbeResizeAndOverwriteOp(char*, size_t) → inline size_t
StringResizeAndOverwriteFallback(T& str, typename T::size_type n, Op op) → void
if(old_size < n)
StringResizeAndOverwriteImpl(T& str, typename T::size_type n, Op op) → void
constexpr(strings_internal:: has__google_nonstandard_backport_resize_and_overwrite< T>::value) → if
StringResizeAndOverwrite(T& str, typename T::size_type n, Op op) → void
```

### absl/strings/resize_and_overwrite_test.cc
```
struct ResizeAndOverwriteParam
if(param.final_size >= param.initial_size)
if(param.final_size >= param.initial_size)
if(param.final_size >= param.initial_size)
if(param.final_size >= param.initial_size)
if(!kMSan)
if(param.initial_size < param.final_size)
```

### absl/strings/str_cat.cc
```
Append(char* absl_nonnull out, const AlphaNum& x) → inline char* absl_nonnull
StrCat(const AlphaNum& a, const AlphaNum& b) → std::string
StrCat(const AlphaNum& a, const AlphaNum& b, const AlphaNum& c) → std::string
StrCat(const AlphaNum& a, const AlphaNum& b, const AlphaNum& c, const AlphaNum& d) → std::string
CatPieces(std::initializer_list<absl::string_view> pieces) → std::string
for(absl::string_view piece : pieces)
for(absl::string_view piece : pieces)
if(this_size != 0)
AppendPieces(std::string* absl_nonnull dest, std::initializer_list<absl::string_view> pieces) → void
for(absl::string_view piece : pieces)
StringAppendAndOverwrite(*dest, to_append, [&pieces](char* const buf, size_t buf_size)
for(absl::string_view piece : pieces)
if(this_size != 0)
StrAppend(std::string* absl_nonnull dest, const AlphaNum& a) → void
StrAppend(std::string* absl_nonnull dest, const AlphaNum& a, const AlphaNum& b) → void
StrAppend(std::string* absl_nonnull dest, const AlphaNum& a, const AlphaNum& b, const AlphaNum& c) → void
StrAppend(std::string* absl_nonnull dest, const AlphaNum& a, const AlphaNum& b, const AlphaNum& c, const AlphaNum& d) → void
```

### absl/strings/str_cat.h
```
struct AlphaNumBuffer
struct Hex
  static_assert(numbers_internal::kFastToBufferSize >= 32, "This function only works when output buffer >= 32 bytes long")
struct Dec
  assert(dec.width <= numbers_internal::kFastToBufferSize)
class AlphaNum
AbslStringify(S& sink, Hex hex) → friend void
if(real_width >= hex.width)
fill(spec >= absl::kSpacePad2 ? ' ' : '0')
neg(v < 0)
AbslStringify(S& sink, Dec dec) → friend void
while(val > 9)
if(fillers > 0)
if(dec.neg && dec.fill == '0')
piece_(&buf.data[0], buf.size) → :
piece_(pc) → :
piece_(str) → :
size() → absl::string_view::size_type
data() → const char* absl_nullable
Piece() → absl::string_view
AlphaNum(+e) → :
IntegerToString(Integer i) → std::string
StringResizeAndOverwrite(result, kMaxDigits10, [i](char* start, size_t buf_size)
FloatToString(Float f) → std::string
StringResizeAndOverwrite(result, numbers_internal::kSixDigitsToBufferSize, [f](char* start, size_t buf_size)
```

### absl/strings/str_cat_benchmark.cc
```
IncrementAlternatingSign(int i) → inline int
BM_Sum_By_StrCat(benchmark::State& state) → void
for(auto _ : state)
for(char* f = &foo[0]; *f != 0; ++f)
BM_StrCat_By_snprintf(benchmark::State& state) → void
for(auto _ : state)
BM_StrCat_By_Strings(benchmark::State& state) → void
for(auto _ : state)
BM_StrCat_By_StringOpPlus(benchmark::State& state) → void
for(auto _ : state)
BM_StrCat_By_StrCat(benchmark::State& state) → void
for(auto _ : state)
BM_HexCat_By_StrCat(benchmark::State& state) → void
for(auto _ : state)
BM_HexCat_By_StrFormat(benchmark::State& state) → void
for(auto _ : state)
BM_HexCat_By_Substitute(benchmark::State& state) → void
for(auto _ : state)
BM_FloatToString_By_StrCat(benchmark::State& state) → void
for(auto _ : state)
BM_DoubleToString_By_SixDigits(benchmark::State& state) → void
for(auto _ : state)
BM_FloatToString_By_StrFormat(benchmark::State& state) → void
for(auto _ : state)
BM_StrAppendImpl(benchmark::State& state, Table table, size_t total_bytes, std::index_sequence<Index...>) → void
```

### absl/strings/str_cat_test.cc
```
struct Mallocator
struct PointStringify
struct PointStringifyUsingFormat
max_size() → size_type
Mallocator(const Mallocator<U>&) → template <class U>
allocate(size_t n) → T*
deallocate(T* p, size_t) → void
for(std::string result : {"hello", "a string too long to fit in the SSO"})
CheckHex(IntType v, const char* nopad_format, const char* zeropad_format, const char* spacepad_format) → void
for(int spec = absl::kZeroPad2; spec <= absl::kZeroPad20; ++spec)
for(int spec = absl::kSpacePad2; spec <= absl::kSpacePad20; ++spec)
CheckDec(IntType v, const char* nopad_format, const char* zeropad_format, const char* spacepad_format) → void
for(int spec = absl::kZeroPad2; spec <= absl::kZeroPad20; ++spec)
for(int spec = absl::kSpacePad2; spec <= absl::kSpacePad20; ++spec)
CheckHexDec64(uint64_t v) → void
CheckHexDec32(uint32_t uv) → void
CheckAll(uint64_t v) → void
TestFastPrints() → void
for(int i = 0; i < 10000; i++)
AbslStringify(FormatSink& sink, const PointStringify& p) → friend void
AbslStringify(FormatSink& sink, const PointStringifyUsingFormat& p) → friend void
AbslStringify(Sink& sink, EnumWithStringify e) → void
CheckSingleArgumentIntegerLimits() → void
```

### absl/strings/str_format.h
```
class UntypedFormatSpec
class FormatCountCapture
class FormatRawSink
class FormatConversionSpec
class FormatSink
struct FormatConvertResult
spec_(pc) → :
FormatStreamed(const T& v) → str_format_internal::StreamedW
Unused() → int* absl_nonnull
StrAppendFormat(std::string* absl_nonnull dst, const FormatSpec<Args...>& format, const Args&... args) → std::string&
PrintF(const FormatSpec<Args...>& format, const Args&... args) → int
FPrintF(std::FILE* absl_nonnull output, const FormatSpec<Args...>& format, const Args&... args) → int
SNPrintF(char* absl_nonnull output, std::size_t size, const FormatSpec<Args...>& format, const Args&... args) → int
sink_(raw) → :
Format(FormatRawSink raw_sink, const FormatSpec<Args...>& format, const Args&... args) → bool
is_basic() → bool
has_left_flag() → bool
has_show_pos_flag() → bool
has_sign_col_flag() → bool
has_alt_flag() → bool
has_zero_flag() → bool
conversion_char() → FormatConversionChar
width() → int
precision() → int
impl_(impl) → :
```

### absl/strings/str_format_test.cc
```
struct X
class TempFile
  EXPECT_GT(size, 0)
  str(2 * size, ' ') → std::string
  EXPECT_EQ(read_bytes, size)
struct SummarizeConsumer
struct Point
struct PointStringify
struct PointStringifyUsingFormat
for(const char* fmt : formats)
for(const auto& fmt : formats)
WithSnprintf(const char* fmt, ...) → std::string
file() → std::FILE*
ReadFile() → std::string
Append(string_view s) → bool
ConvertOne(const str_format_internal::UnboundConversion& conv, string_view s) → bool
SummarizeParsedFormat(const ParsedFormatBase& pc) → std::string
WrappedFormat(const absl::FormatSpec<Args...>& format, const Args&... args) → std::string
AbslFormatConvert(const Point& p, const absl::FormatConversionSpec& spec, absl::FormatSink* s) → absl::FormatConversionCharSet:
AbslStringify(FormatSink& sink, const PointStringify& p) → friend void
AbslStringify(FormatSink& sink, const PointStringifyUsingFormat& p) → friend void
AbslStringify(Sink& sink, EnumClassWithStringify e) → void
AbslStringify(Sink& sink, EnumWithStringify e) → void
AbslStringify(Sink& sink, EnumWithLargerValue e) → void
CodegenAbslStrFormatInt(int i) → std::string
```

### absl/strings/str_join.h
```
AlphaNumFormatter() → inline strings_internal::Alpha
StreamFormatter() → inline strings_internal::Strea
PairFormatter(absl::string_view sep) → strings_internal::AlphaNumForm
DereferenceFormatter(Formatter&& f) → strings_internal::DereferenceF
DereferenceFormatter() → strings_internal::AlphaNumForm
StrJoin(Iterator start, Iterator end, absl::string_view sep, Formatter&& fmt) → std::string
StrJoin(const Range& range, absl::string_view separator, Formatter&& fmt) → std::string
StrJoin(std::initializer_list<T> il, absl::string_view separator, Formatter&& fmt) → std::string
StrJoin(std::initializer_list<absl::string_view> il, absl::string_view separator, Formatter&& fmt) → inline std::string
StrJoin(const std::tuple<T...>& value, absl::string_view separator, Formatter&& fmt) → std::string
StrJoin(Iterator start, Iterator end, absl::string_view separator) → std::string
StrJoin(const Range& range, absl::string_view separator) → std::string
StrJoin(std::initializer_list<T> il, absl::string_view separator) → std::string
StrJoin(std::initializer_list<absl::string_view> il, absl::string_view separator) → inline std::string
StrJoin(const std::tuple<T...>& value, absl::string_view separator) → std::string
```

### absl/strings/str_join_benchmark.cc
```
BM_Join2_Strings(benchmark::State& state) → void
for(auto _ : state)
BM_Join2_Ints(benchmark::State& state) → void
for(auto _ : state)
BM_Join2_KeysAndValues(benchmark::State& state) → void
for(auto _ : state)
BM_JoinStreamable(benchmark::State& state) → void
for(auto _ : state)
BM_JoinTuple(benchmark::State& state) → void
for(auto _ : state)
```

### absl/strings/str_join_test.cc
```
struct StreamableType
struct TestingParenFormatter
class TestValue
class TestIterator
  TestIterator(&data, 0) → return
class TestIteratorRange
ImmovableFormatter()
data() → const char*
size() → size_t
begin(const std::vector<absl::string_view>& data) → static TestIterator
end(const std::vector<absl::string_view>& data) → static TestIterator
begin() → const TestIterator<ValueT>&
end() → const TestIterator<ValueT>&
```

### absl/strings/str_replace.cc
```
ApplySubstitutions(absl::string_view s, std::vector<strings_internal::ViableSubstitution>* absl_nonnull subs_ptr, std::string* absl_nonnull result_ptr) → int
if(sub.offset >= pos)
if(sub.offset == s.npos)
StrReplaceAll(absl::string_view s, strings_internal::FixedMapping replacements) → std::string
StrReplaceAll(strings_internal::FixedMapping replacements, std::string* absl_nonnull target) → int
```

### absl/strings/str_replace.h
```
struct ViableSubstitution
OccursBefore(const ViableSubstitution& y) → bool
FindSubstitutions(absl::string_view s, const StrToStrMapping& replacements) → std::vector<ViableSubstitution
for(const auto& rep : replacements)
StrReplaceAll(absl::string_view s, const StrToStrMapping& replacements) → std::string
StrReplaceAll(const StrToStrMapping& replacements, std::string* absl_nonnull target) → int
```

### absl/strings/str_replace_benchmark.cc
```
struct Replacement
SetUpStrings() → void
if(big_string == nullptr)
for(std::string phrase : {"the quick brown fox jumped over the lazy dogs", "pack my box with the five dozen liquor jugs"})
for(int i = 0; i < 10 * 1000; ++i)
for(size_t pos = 0;;)
for(const auto& r : replacements)
if(needlepos != std::string::npos && needlepos < next_pos)
BM_StrReplaceAllOneReplacement(benchmark::State& state) → void
for(auto _ : state)
BM_StrReplaceAll(benchmark::State& state) → void
for(auto _ : state)
```

### absl/strings/str_replace_test.cc
```
struct Cont
get(const Cont& c) → absl::string_view
```

### absl/strings/str_split.cc
```
struct LiteralPolicy
struct AnyOfPolicy
GenericFind(absl::string_view text, absl::string_view delimiter, size_t pos, FindPolicy find_policy) → absl::string_view
if(found_pos != absl::string_view::npos)
Find(absl::string_view text, absl::string_view delimiter, size_t pos) → static size_t
Length(absl::string_view delimiter) → static size_t
Find(absl::string_view text, absl::string_view delimiter, size_t pos) → static size_t
Length(absl::string_view) → static size_t
```

### absl/strings/str_split.h
```
class ByString
  ByString(absl::string_view sp) → explicit
  Find(absl::string_view text, size_t pos) → absl::string_view
class ByAsciiWhitespace
  Find(absl::string_view text, size_t pos) → absl::string_view
class ByChar
  Find(absl::string_view text, size_t pos) → absl::string_view
class ByAnyChar
  ByAnyChar(absl::string_view sp) → explicit
  Find(absl::string_view text, size_t pos) → absl::string_view
class ByLength
  ByLength(ptrdiff_t length) → explicit
  Find(absl::string_view text, size_t pos) → absl::string_view
struct SelectDelimiter
class MaxSplitsImpl
struct AllowEmpty
struct SkipEmpty
struct SkipWhitespace
Find(absl::string_view text, size_t pos) → absl::string_view
if(count_++ == limit_)
MaxSplits(Delimiter delimiter, int limit) → typename strings_internal::Sel
StrSplit(strings_internal::ConvertibleToStringView text, Delimiter d) → absl::string_view>
StrSplit(StringType&& text, Delimiter d) → std::string>
StrSplit(strings_internal::ConvertibleToStringView text, Delimiter d, Predicate p) → absl::string_view>
StrSplit(StringType&& text, Delimiter d, Predicate p) → std::string>
```

### absl/strings/str_split_benchmark.cc
```
struct OneCharLiteral
struct OneCharStringLiteral
MakeTestString(int desired_length) → std::string
BM_Split2StringView(benchmark::State& state) → void
for(auto _ : state)
MakeMultiDelimiterTestString(int desired_length) → std::string
BM_Split2StringViewByAnyChar(benchmark::State& state) → void
for(auto _ : state)
BM_Split2StringViewLifted(benchmark::State& state) → void
for(auto _ : state)
BM_Split2String(benchmark::State& state) → void
for(auto _ : state)
BM_Split2SplitStringUsing(benchmark::State& state) → void
for(auto _ : state)
BM_SplitStringToUnorderedSet(benchmark::State& state) → void
for(int i = 1; i < len; i += 2)
for(auto _ : state)
BM_SplitStringToUnorderedMap(benchmark::State& state) → void
for(int i = 1; i < len; i += 2)
for(auto _ : state)
BM_SplitStringAllowEmpty(benchmark::State& state) → void
for(int i = 1; i < len; i += 2)
for(auto _ : state)
BM_SplitStringWithOneChar(benchmark::State& state) → void
for(auto _ : state)
```

### absl/strings/str_split_test.cc
```
class Skip
for(const auto& spec : specs)
for(const auto& expected : spec.expect)
for(; it != it2; ++it)
for(absl::string_view p : splitter)
TestConversionOperator(const Splitter& splitter) → void
TestMapConversionOperator(const Splitter& splitter) → void
TestPairConversionOperator(const Splitter& splitter) → void
TestArrayConversionOperator(const Splitter& splitter) → void
ReturnStringView() → absl::string_view
ReturnConstCharP() → const char*
ReturnCharP() → char*
for(absl::string_view letter : splitter)
for(absl::string_view letter : std_splitter)
CopyToHeap(const T& value) → static std::unique_ptr<T>
IsFoundAtStartingPos(absl::string_view text, Delimiter d, size_t starting_pos, int expected_pos) → static bool
IsFoundAt(absl::string_view text, Delimiter d, int expected_pos) → static bool
TestComma(Delimiter d) → void
```

### absl/strings/string_view.h
```
ClippedSubstr(string_view s ABSL_ATTRIBUTE_LIFETIME_BOUND, size_t pos, size_t n = string_view::npos) → inline string_view
NullSafeStringView(const char* absl_nullable p) → constexpr string_view
```

### absl/strings/strip.h
```
ConsumePrefix(absl::string_view* absl_nonnull str, absl::string_view expected) → inline constexpr bool
ConsumeSuffix(absl::string_view* absl_nonnull str, absl::string_view expected) → inline constexpr bool
```

### absl/strings/strip_test.cc
```
for(int i = 0; i < NUM_TESTS; i++)
```

### absl/strings/substitute.cc
```
SubstituteAndAppendArray(std::string* absl_nonnull output, absl::string_view format, const absl::string_view* absl_nullable args_array, size_t num_args) → void
if(format[i] == '$')
if(format[i] == '$')
if(value == nullptr)
if(end - writer < hex.width)
while(value > 9)
if(fillers > 0)
if(neg && dec.fill == '0')
```

### absl/strings/substitute.h
```
class Arg
  Arg(Hex hex)
  Arg(const void* absl_nullable value)
piece_(value) → :
piece_(value) → :
piece_(scratch_, 1) → :
piece_(value ? "true" : "false") → :
piece() → absl::string_view
CalculateOneBit(const char* absl_nonnull format) → constexpr int
SkipNumber(const char* absl_nonnull format) → constexpr const char* absl_non
PlaceholderBitmask(const char* absl_nonnull format) → constexpr int
SubstituteAndAppend(std::string* absl_nonnull output, absl::string_view format) → inline void
SubstituteAndAppend(std::string* absl_nonnull output, absl::string_view format, const substitute_internal::Arg& a0) → inline void
SubstituteAndAppend(std::string* absl_nonnull output, absl::string_view format, const substitute_internal::Arg& a0, const substitute_internal::Arg& a1) → inline void
SubstituteAndAppend(std::string* absl_nonnull output, absl::string_view format, const substitute_internal::Arg& a0, const substitute_internal::Arg& a1, const substitute_internal::Arg& a2) → inline void
SubstituteAndAppend(std::string* absl_nonnull output, absl::string_view format, const substitute_internal::Arg& a0, const substitute_internal::Arg& a1, const substitute_internal::Arg& a2, const substitute_internal::Arg& a3) → inline void
SubstituteAndAppend(std::string* absl_nonnull output, absl::string_view format, const substitute_internal::Arg& a0, const substitute_internal::Arg& a1, const substitute_internal::Arg& a2, const substitute_internal::Arg& a3, const substitute_internal::Arg& a4) → inline void
SubstituteAndAppend(std::string* absl_nonnull output, absl::string_view format, const substitute_internal::Arg& a0, const substitute_internal::Arg& a1, const substitute_internal::Arg& a2, const substitute_internal::Arg& a3, const substitute_internal::Arg& a4, const substitute_internal::Arg& a5) → inline void
SubstituteAndAppend(std::string* absl_nonnull output, absl::string_view format, const substitute_internal::Arg& a0, const substitute_internal::Arg& a1, const substitute_internal::Arg& a2, const substitute_internal::Arg& a3, const substitute_internal::Arg& a4, const substitute_internal::Arg& a5, const substitute_internal::Arg& a6) → inline void
SubstituteAndAppend(std::string* absl_nonnull output, absl::string_view format, const substitute_internal::Arg& a0, const substitute_internal::Arg& a1, const substitute_internal::Arg& a2, const substitute_internal::Arg& a3, const substitute_internal::Arg& a4, const substitute_internal::Arg& a5, const substitute_internal::Arg& a6, const substitute_internal::Arg& a7) → inline void
SubstituteAndAppend(std::string* absl_nonnull output, absl::string_view format, const substitute_internal::Arg& a0, const substitute_internal::Arg& a1, const substitute_internal::Arg& a2, const substitute_internal::Arg& a3, const substitute_internal::Arg& a4, const substitute_internal::Arg& a5, const substitute_internal::Arg& a6, const substitute_internal::Arg& a7, const substitute_internal::Arg& a8) → inline void
SubstituteAndAppend(std::string* absl_nonnull output, absl::string_view format, const substitute_internal::Arg& a0, const substitute_internal::Arg& a1, const substitute_internal::Arg& a2, const substitute_internal::Arg& a3, const substitute_internal::Arg& a4, const substitute_internal::Arg& a5, const substitute_internal::Arg& a6, const substitute_internal::Arg& a7, const substitute_internal::Arg& a8, const substitute_internal::Arg& a9) → inline void
```

### absl/strings/substitute_benchmark.cc
```
BM_Substitute(benchmark::State& state) → void
for(auto _ : state)
BM_SubstituteCstr(benchmark::State& state) → void
for(auto _ : state)
BM_StringPrintf(benchmark::State& state) → void
for(auto _ : state)
BM_SubstituteNumber(benchmark::State& state) → void
for(auto _ : state)
BM_StrCatNumber(benchmark::State& state) → void
for(auto _ : state)
BM_SubstituteSimpleText(benchmark::State& state) → void
for(auto _ : state)
BM_StrCatSimpleText(benchmark::State& state) → void
for(auto _ : state)
MakeFormatByDensity(int density, bool subs_mode) → std::string
for(size_t i = 0; i < 10; ++i)
BM_SubstituteDensity(benchmark::State& state) → void
for(auto _ : state)
BM_StrCatDensity(benchmark::State& state) → void
for(auto _ : state)
```

### absl/strings/substitute_test.cc
```
struct MyStruct
AbslStringify(Sink& sink, const MyStruct& s) → friend void
AbslStringify(Sink& sink, EnumWithStringify e) → void
```

### absl/synchronization/barrier.cc
```
IsZero(void *arg) → static bool
if(this->num_to_block_ < 0)
```

### absl/synchronization/barrier.h
```
class Barrier
  Block() → bool
  ABSL_GUARDED_BY(lock_) → int num_to_block_
  ABSL_GUARDED_BY(lock_) → int num_to_exit_
```

### absl/synchronization/barrier_test.cc
```
for(int i = 0; i < kNumThreads - 1; ++i)
for(auto& thread : threads)
```

### absl/synchronization/blocking_counter.cc
```
IsDone(void *arg) → bool
if(count == 0)
```

### absl/synchronization/blocking_counter.h
```
class BlockingCounter
  BlockingCounter(int initial_count) → explicit
  DecrementCount() → bool
  Wait() → void
  ABSL_GUARDED_BY(lock_) → int num_waiting_
  ABSL_GUARDED_BY(lock_) → bool done_
TraceObjectKind() → static inline constexpr base_i
```

### absl/synchronization/blocking_counter_benchmark.cc
```
BM_BlockingCounter_SingleThread(benchmark::State& state) → void
for(auto _ : state)
for(int i = 0; i < iterations; ++i)
BM_BlockingCounter_DecrementCount(benchmark::State& state) → void
for(auto _ : state)
BM_BlockingCounter_Wait(benchmark::State& state) → void
for(auto _ : state)
for(int i = 0; i < num_threads; ++i)
```

### absl/synchronization/blocking_counter_test.cc
```
PauseAndDecreaseCounter(BlockingCounter* counter, int* done) → void
for(int k = 0; k < num_workers; k++)
for(int k = 0; k < num_workers; k++)
for(std::thread& w : workers)
```

### absl/synchronization/internal/create_thread_identity.cc
```
ReclaimThreadIdentity(void* v) → static void
if(identity->per_thread_synch.all_locks != nullptr)
RoundUp(intptr_t addr, intptr_t align) → static intptr_t
OneTimeInitThreadIdentity(base_internal::ThreadIdentity* identity) → void
ResetThreadIdentityBetweenReuse(base_internal::ThreadIdentity* identity) → static void
NewThreadIdentity() → static base_internal::ThreadId
if(thread_identity_freelist)
if(identity == nullptr)
CreateThreadIdentity() → base_internal::ThreadIdentity*
```

### absl/synchronization/internal/create_thread_identity.h
```
GetOrCreateCurrentThreadIdentity() → inline base_internal::ThreadId
```

### absl/synchronization/internal/futex.h
```
struct FutexTimespec
class FutexImpl
  WaitAbsoluteTimeout(v, val, nullptr) → return
class Futex
Wait(std::atomic<int32_t>* v, int32_t val) → static int
WaitAbsoluteTimeout(std::atomic<int32_t>* v, int32_t val, const struct timespec* abs_timeout) → static int
if(err != 0)
WaitRelativeTimeout(std::atomic<int32_t>* v, int32_t val, const struct timespec* rel_timeout) → static int
if(err != 0)
Wake(std::atomic<int32_t>* v, int32_t count) → static int
ToFutexTimespec(const struct timespec* userspace_ts, FutexTimespec* futex_ts) → static FutexTimespec*
if(userspace_ts == nullptr)
if(userspace_ts->tv_sec > kMaxSeconds)
```

### absl/synchronization/internal/futex_waiter.cc
```
while(true)
while(x != 0)
if(err != 0)
if(err == -EINTR || err == -EWOULDBLOCK)
```

### absl/synchronization/internal/graphcycles.cc
```
class Vec
  Discard()
  Init()
  for(uint32_t i = 0; i < size()
  resize(src->size_)
  Discard()
  Discard()
class NodeSet
struct Node
class PointerMap
InitArenaIfNecessary() → static void
if(arena == nullptr)
Vec() → public:
clear() → void
empty() → bool
size() → uint32_t
begin() → T*
end() → T*
back() → const T&
pop_back() → void
push_back(const T& v) → void
resize(uint32_t n) → void
fill(const T& val) → void
MoveFrom(Vec<T>* src) → void
if(src->ptr_ == src->space_)
```

### absl/synchronization/internal/graphcycles.h
```
struct GraphId
class GraphCycles
  GraphCycles() → public:
  GetId(void* ptr) → GraphId
  RemoveNode(void* ptr) → void
  Ptr(GraphId id) → void*
  InsertEdge(GraphId source_node, GraphId dest_node) → bool
  RemoveEdge(GraphId source_node, GraphId dest_node) → void
  HasNode(GraphId node) → bool
  HasEdge(GraphId source_node, GraphId dest_node) → bool
InvalidGraphId() → inline GraphId
```

### absl/synchronization/internal/graphcycles_benchmark.cc
```
BM_StressTest(benchmark::State& state) → void
for(int i = 0; i < num_nodes; i++)
for(int i = 0; i < num_nodes; i++)
for(int j = i + 1; j < end; j++)
```

### absl/synchronization/internal/graphcycles_test.cc
```
struct Edge
class GraphCyclesTest
  CheckInvariants(g_)
  CheckInvariants(g_)
Get(const IdMap& id, int num) → static GraphId
IsReachable(Edges *edges, int from, int to, std::unordered_set<int> *seen) → static bool
for(const auto &edge : *edges)
if(edge.from == from)
if(edge.to == to)
PrintEdges(Edges *edges) → static void
for(const auto &edge : *edges)
PrintGCEdges(Nodes *nodes, const IdMap &id, GraphCycles *gc) → static void
for(int a : *nodes)
for(int b : *nodes)
PrintTransitiveClosure(Nodes *nodes, Edges *edges) → static void
for(int a : *nodes)
for(int b : *nodes)
PrintGCTransitiveClosure(Nodes *nodes, const IdMap &id, GraphCycles *gc) → static void
for(int a : *nodes)
for(int b : *nodes)
CheckTransitiveClosure(Nodes *nodes, Edges *edges, const IdMap &id, GraphCycles *gc) → static void
for(const auto &a : *nodes)
for(const auto &b : *nodes)
if(gc_reachable != reachable)
CheckEdges(Nodes *nodes, Edges *edges, const IdMap &id, GraphCycles *gc) → static void
```

### absl/synchronization/internal/kernel_timeout.cc
```
if(unix_nanos < 0)
if(unix_nanos >= kMaxNanos)
if(nanos < 0)
if(nanos > kMaxNanos - now)
if(nanos > kMaxNanos - now)
if(ns_from_now >= kMaxValueNanos)
if(ms_from_now > kInfinite)
```

### absl/synchronization/internal/kernel_timeout.h
```
class KernelTimeout
  KernelTimeout(absl::Time t) → explicit
  KernelTimeout(absl::Duration d) → explicit
  MakeAbsTimespec() → struct timespec
  MakeRelativeTimespec() → struct timespec
  MakeClockAbsoluteTimespec(clockid_t c) → struct timespec
  MakeAbsNanos() → int64_t
  InMillisecondsFromNow() → DWord
  ToChronoTimePoint() → std::chrono::time_point<std::c
Never() → static constexpr KernelTimeout
has_timeout() → bool
is_absolute_timeout() → bool
is_relative_timeout() → bool
SupportsSteadyClock() → static constexpr bool
RawAbsNanos() → int64_t
```

### absl/synchronization/internal/kernel_timeout_test.cc
```
for(auto duration : kDurationsToTest)
for(auto duration : kDurationsToTest)
for(auto duration : kDurationsToTest)
```

### absl/synchronization/internal/per_thread_sem.cc
```
if(identity->blocked_count_ptr != nullptr)
if(identity->blocked_count_ptr != nullptr)
```

### absl/synchronization/internal/per_thread_sem.h
```
class PerThreadSem
  Tick(base_internal::ThreadIdentity* identity) → void
  SetThreadBlockedCounter(std::atomic<int> *counter) → void
  Init(base_internal::ThreadIdentity* identity) → inline void
  Post(base_internal::ThreadIdentity* identity) → inline void
  Wait(KernelTimeout t) → inline bool
  OneTimeInitThreadIdentity(absl::base_internal::ThreadIdentity*) → friend void
```

### absl/synchronization/internal/per_thread_sem_test.cc
```
class SimpleSemaphore
  lock(mu_) → std::unique_lock<std::mutex>
  lock(mu_) → std::lock_guard<std::mutex>
struct ThreadData
class PerThreadSemTest
  Wait(t->timeout)
  Post(t->identity1)
  Post(t.identity2)
  Wait(t.timeout)
Wait() → void
Post() → void
TimingThread(ThreadData* t) → static void
while(t->num_iterations--)
TestTiming(const char *msg, bool timeout) → void
for(int i = 0; i < kNumIterations; ++i)
Post(base_internal::ThreadIdentity *id) → static void
Wait(KernelTimeout t) → static bool
Wait(absl::Time t) → static bool
Tick(base_internal::ThreadIdentity *identity) → static void
for(int i = 0; i < 10000; i++)
t([]() → std::thread
```

### absl/synchronization/internal/pthread_waiter.cc
```
class PthreadMutexHolder
  ABSL_RAW_LOG(FATAL, "pthread_mutex_lock failed: %d", err)
  ABSL_RAW_LOG(FATAL, "pthread_mutex_unlock failed: %d", err)
if(err != 0)
if(err != 0)
if(err != 0)
if(err2 != 0)
while(wakeup_count_ == 0)
if(err != 0)
if(err == ETIMEDOUT)
if(err != 0)
if(waiter_count_ != 0)
```

### absl/synchronization/internal/sem_waiter.cc
```
while(true)
while(x != 0)
while(true)
```

### absl/synchronization/internal/stdcpp_waiter.cc
```
while(wakeup_count_ == 0)
if(wait_result == std::cv_status::timeout)
if(waiter_count_ != 0)
```

### absl/synchronization/internal/thread_pool.h
```
class ThreadPool
  l(mu_) → absl::MutexLock
  for(size_t i = 0; i < threads_.size()
  assert(func != nullptr)
  l(mu_) → absl::MutexLock
  l(mu_) → absl::MutexLock
  func()
ThreadPool(int num_threads) → explicit
for(int i = 0; i < num_threads; ++i)
for(auto &t : threads_)
WorkLoop() → void
while(true)
if(func == nullptr)
```

### absl/synchronization/internal/waiter_base.cc
```
if(!is_idle && ticker - wait_start > kIdlePeriods)
```

### absl/synchronization/internal/waiter_base.h
```
class WaiterBase
  MaybeBecomeIdle() → void
class WaiterCrtp
GetWaiter(base_internal::ThreadIdentity* identity) → static T*
```

### absl/synchronization/internal/waiter_test.cc
```
class WaiterTest
WaiterTest() → public:
WithTolerance(absl::Duration d) → absl::Duration
```

### absl/synchronization/internal/win32_waiter.cc
```
class LockHolder
  AcquireSRWLockExclusive(mu_)
  ReleaseSRWLockExclusive(mu_)
while(wakeup_count_ == 0)
if(err == ERROR_TIMEOUT)
if(waiter_count_ != 0)
```

### absl/synchronization/lifetime_test.cc
```
class OnConstruction
class OnDestruction
ThreadOne(absl::Mutex* mutex, absl::CondVar* condvar, absl::Notification* notification, bool* state) → void
while(*state == false)
ThreadTwo(absl::Mutex* mutex, absl::CondVar* condvar, absl::Notification* notification, bool* state) → void
RunTests(absl::Mutex* mutex, absl::CondVar* condvar) → void
if(!mutex)
if(!condvar)
TestLocals() → void
TestConstInitGlobal() → void
OnConstruction(Function fn) → explicit
main() → int
```

### absl/synchronization/mutex.cc
```
struct SynchWaitParams
struct SynchLocksHeld
struct MuHowS
struct DeadlockReportBuffers
struct ScopedDeadlockReportBuffers
MeasureTimeToYield() → absl::Duration
GetMutexGlobals() → const MutexGlobals&
MutexDelay(int32_t c, int mode) → int
if(c < limit)
if(c == limit)
AtomicSetBits(std::atomic<intptr_t>* pv, intptr_t bits, intptr_t wait_until_clear) → static bool
for(;;)
EnsureSynchEvent(std::atomic<intptr_t>* addr, const char* name, intptr_t bits, intptr_t lockbit) → static SynchEvent*
if(++synch_event_count > kMaxSynchEventCount)
for(auto*& head : synch_event)
for(auto* e = head; e != nullptr;)
if(e == nullptr)
if(name == nullptr)
UnrefSynchEvent(SynchEvent* e) → static void
if(e != nullptr)
if(del)
GetSynchEvent(const void* addr) → static SynchEvent*
if(e != nullptr)
PostSynchEvent(void* obj, int ev) → static void
if(e == nullptr || e->log)
```

### absl/synchronization/mutex.h
```
class Condition
  Condition(const bool* absl_nonnull cond) → explicit
  Eval() → bool
  GuaranteedEqual(const Condition* absl_nullable a, const Condition* absl_nullable b) → bool
  CallVoidPtrFunction(const Condition* absl_nonnull c) → bool
  CastAndCallFunction(const Condition* absl_nonnull c) → bool
  CastAndCallMethod(const Condition* absl_nonnull c) → bool
class CondVar
  CondVar() → public:
  Signal() → void
  SignalAll() → void
  EnableDebugLog(const char* absl_nullable name) → void
  WaitCommon(Mutex* absl_nonnull mutex, synchronization_internal::KernelTimeout t) → bool
  Remove(base_internal::PerThreadSynch* absl_nonnull s) → void
Await(const Condition& cond) → void
AwaitWithTimeout(const Condition& cond, absl::Duration timeout) → bool
AwaitWithDeadline(const Condition& cond, absl::Time deadline) → bool
ABSL_EXCLUSIVE_LOCK_FUNCTION()
ABSL_SHARED_LOCK_FUNCTION()
ABSL_EXCLUSIVE_LOCK_FUNCTION()
ABSL_EXCLUSIVE_LOCK_FUNCTION()
ABSL_SHARED_LOCK_FUNCTION()
ABSL_EXCLUSIVE_LOCK_FUNCTION()
Mutex(const volatile Mutex* absl_nullable) → explicit
mu_(mu) → :
```

### absl/synchronization/mutex_benchmark.cc
```
class ScopedThreadMutexPriority
BM_Mutex(benchmark::State& state) → void
for(auto _ : state)
BM_ReaderLock(benchmark::State& state) → void
for(auto _ : state)
BM_TryLock(benchmark::State& state) → void
for(auto _ : state)
BM_ReaderTryLock(benchmark::State& state) → void
for(auto _ : state)
DelayNs(int64_t ns, int* data) → static void
ScopedThreadMutexPriority(int priority) → explicit
BM_MutexEnqueue(benchmark::State& state) → void
for(int i = 0; i < kBatchSize; i++)
BM_Contended(benchmark::State& state) → void
for(auto _ : state)
SetupBenchmarkArgs(benchmark::internal::Benchmark* bm, bool do_test_priorities) → void
for(int critical_section_ns : {1, 20, 50, 200, 2000})
for(int num_priorities = 1; num_priorities <= max_num_priorities; num_priorities++)
BM_ConditionWaiters(benchmark::State& state) → void
Waiter(absl::BlockingCounter* init, absl::Mutex* m, int* p) → static void
if(num_classes == 0)
for(int i = 0; i < num_waiters; i++)
for(auto _ : state)
for(int i = 0; i < num_classes; i++)
```

### absl/synchronization/mutex_method_pointer_test.cc
```
class Callback
class M2
Callback() → public:
method() → bool
M2() → public:
method2() → bool
VirtualInheritance() → public:
method() → bool
```

### absl/synchronization/mutex_test.cc
```
struct ScopedInvariantDebugging
struct TestContext
struct MyContext
  MyTurn() → bool
struct TimeoutBugStruct
struct DequeueAllWakeableBugStruct
struct LockWhenTestStruct
struct ReaderDecrementBugStruct
struct Base
struct Constable
struct TrueViaDeducingThis
struct True
struct AcquireFromConditionStruct
class ScopedDisableBazelTestWarnings
  SetEnvironmentVariableA(kVarName, nullptr)
  unsetenv(kVarName)
struct TimeoutTestParam
CreatePool(int threads) → std::unique_ptr<absl::synchron
CreateDefaultPool() → std::unique_ptr<absl::synchron
ScopedInvariantDebugging()
GetInvariantChecked() → static bool
SetInvariantChecked(bool new_value) → static void
CheckSumG0G1(void *v) → static void
TestMu(TestContext *cxt, int c) → static void
for(int i = 0; i != cxt->iterations; i++)
```

### absl/synchronization/notification.cc
```
if(!notified)
if(!notified)
```

### absl/synchronization/notification.h
```
class Notification
  WaitForNotification() → void
  WaitForNotificationWithTimeout(absl::Duration timeout) → bool
  WaitForNotificationWithDeadline(absl::Time deadline) → bool
  Notify() → void
TraceObjectKind() → static inline constexpr base_i
HasBeenNotifiedInternal(const std::atomic<bool>* notified_yet) → static inline bool
```

### absl/synchronization/notification_test.cc
```
class ThreadSafeCounter
  lock(mutex_) → MutexLock
  lock(mutex_) → MutexLock
  lock(mutex_) → MutexLock
Increment() → void
Get() → int
WaitUntilGreaterOrEqual(int n) → void
RunWorker(int i, ThreadSafeCounter* ready_counter, Notification* notification, ThreadSafeCounter* done_counter) → static void
BasicTests(bool notify_before_waiting, Notification* notification) → static void
if(notify_before_waiting)
for(int i = 0; i < kNumThreads; ++i)
if(!notify_before_waiting)
for(std::thread& worker : workers)
```

### absl/time/civil_time.cc
```
NormalizeYear(civil_year_t year) → inline civil_year_t
FormatYearAnd(string_view fmt, CivilSecond cs) → std::string
ParseYearAnd(string_view fmt, string_view s, CivilT* c) → bool
ParseAs(string_view s, CivilT2* c) → bool
ParseLenient(string_view s, CivilT* c) → bool
FormatCivilTime(CivilSecond c) → std::string
FormatCivilTime(CivilMinute c) → std::string
FormatCivilTime(CivilHour c) → std::string
FormatCivilTime(CivilDay c) → std::string
FormatCivilTime(CivilMonth c) → std::string
FormatCivilTime(CivilYear c) → std::string
ParseCivilTime(string_view s, CivilSecond* c) → bool
ParseCivilTime(string_view s, CivilMinute* c) → bool
ParseCivilTime(string_view s, CivilHour* c) → bool
ParseCivilTime(string_view s, CivilDay* c) → bool
ParseCivilTime(string_view s, CivilMonth* c) → bool
ParseCivilTime(string_view s, CivilYear* c) → bool
ParseLenientCivilTime(string_view s, CivilSecond* c) → bool
ParseLenientCivilTime(string_view s, CivilMinute* c) → bool
ParseLenientCivilTime(string_view s, CivilHour* c) → bool
ParseLenientCivilTime(string_view s, CivilDay* c) → bool
ParseLenientCivilTime(string_view s, CivilMonth* c) → bool
ParseLenientCivilTime(string_view s, CivilYear* c) → bool
AbslParseFlag(string_view s, CivilSecond* c, std::string*) → bool
AbslParseFlag(string_view s, CivilMinute* c, std::string*) → bool
```

### absl/time/civil_time.h
```
GetWeekday(CivilSecond cs) → inline Weekday
NextWeekday(CivilDay cd, Weekday wd) → inline CivilDay
PrevWeekday(CivilDay cd, Weekday wd) → inline CivilDay
GetYearDay(CivilSecond cs) → inline int
AbslStringify(Sink& sink, CivilSecond c) → void
AbslStringify(Sink& sink, CivilMinute c) → void
AbslStringify(Sink& sink, CivilHour c) → void
AbslStringify(Sink& sink, CivilDay c) → void
AbslStringify(Sink& sink, CivilMonth c) → void
AbslStringify(Sink& sink, CivilYear c) → void
```

### absl/time/civil_time_benchmark.cc
```
BM_Difference_Days(benchmark::State& state) → void
BM_Step_Days(benchmark::State& state) → void
BM_Format(benchmark::State& state) → void
BM_Parse(benchmark::State& state) → void
BM_RoundTripFormatParse(benchmark::State& state) → void
BM_CivilTimeAbslHash(benchmark::State& state) → void
for(const T civil_time : civil_times)
BM_CivilYearAbslHash(benchmark::State& state) → void
BM_CivilMonthAbslHash(benchmark::State& state) → void
BM_CivilDayAbslHash(benchmark::State& state) → void
BM_CivilHourAbslHash(benchmark::State& state) → void
BM_CivilMinuteAbslHash(benchmark::State& state) → void
BM_CivilSecondAbslHash(benchmark::State& state) → void
```

### absl/time/civil_time_test.cc
```
struct HasDiff
  test(...) → std::false_type
for(int day = 1; day <= 146097; ++day)
for(absl::CivilDay day = june_1; day < july; ++day)
```

### absl/time/clock.cc
```
class UnscaledCycleClockWrapperForGetCurrentTime
struct TimeSampleAtomic
struct TimeSample
Now() → Time
if(n >= 0)
GetCurrentTimeNanos() → int64_t
Now() → static int64_t
SeqAcquire(std::atomic<uint64_t>* seq) → static inline uint64_t
SeqRelease(std::atomic<uint64_t>* seq, uint64_t x) → static inline void
ABSL_GUARDED_BY(lock) → uint64_t last_now_cycles
ABSL_EXCLUSIVE_LOCKS_REQUIRED(time_state.lock)
if(elapsed_cycles >= local_approx_syscall_time_in_cycles && ++loops == 20)
if(local_approx_syscall_time_in_cycles < 1000 * 1000)
ReadTimeSampleAtomic(const struct TimeSampleAtomic* atomic, struct TimeSample* sample) → static void
GetCurrentTimeNanos() → int64_t
SafeDivideAndScale(uint64_t a, uint64_t b) → static uint64_t
if(scaled_b != 0)
ABSL_LOCKS_EXCLUDED(time_state.lock)
if(delta_cycles < sample.min_cycles_per_sample)
ABSL_EXCLUSIVE_LOCKS_REQUIRED(time_state.lock)
if(sample->nsscaled_per_cycle != 0)
if(new_nsscaled_per_cycle != 0 && diff_ns < 100 * 1000 * 1000 && -diff_ns < 100 * 1000 * 1000)
MaxSleep() → constexpr absl::Duration
SleepOnce(absl::Duration to_sleep) → void
```

### absl/time/clock_benchmark.cc
```
BM_Clock_Now_AbslTime(benchmark::State& state) → void
BM_Clock_Now_GetCurrentTimeNanos(benchmark::State& state) → void
BM_Clock_Now_AbslTime_ToUnixNanos(benchmark::State& state) → void
BM_Clock_Now_CycleClock(benchmark::State& state) → void
BM_Clock_Now_gettimeofday(benchmark::State& state) → static void
BM_Clock_Now_clock_gettime(benchmark::State& state) → static void
```

### absl/time/clock_interface.h
```
class Clock
  GetRealClock() → Clock&
  TimeNow() → absl::Time
  Sleep(absl::Duration d) → void
  SleepUntil(absl::Time wakeup_time) → void
  AwaitWithDeadline(absl::Mutex* absl_nonnull mu, const absl::Condition& cond, absl::Time deadline) → bool
```

### absl/time/clock_interface_test.cc
```
AwaitWithDeadlineAndNotify(absl::Clock* clock, absl::Mutex* mu, absl::Condition* cond, absl::Time wakeup_time, absl::Notification* note, bool* return_val) → void
if(kSkipFlakyReason != nullptr)
if(kSkipFlakyReason != nullptr)
if(kSkipFlakyReason != nullptr)
for(int i = 0; i < 5; ++i)
```

### absl/time/clock_test.cc
```
AlarmHandler(int signo) → void
SleepForBounded(absl::Duration d, absl::Duration lower_bound, absl::Duration upper_bound, absl::Duration timeout, AlarmPolicy alarm_policy, int* attempts) → bool
if(alarm_policy == AlarmPolicy::kWithAlarm)
if(alarm_policy == AlarmPolicy::kWithAlarm)
if(lower_bound <= actual && actual <= upper_bound)
AssertSleepForBounded(absl::Duration d, absl::Duration early, absl::Duration late, absl::Duration timeout, AlarmPolicy alarm_policy) → testing::AssertionResult
```

### absl/time/duration.cc
```
struct SafeMultiply
struct DisplayUnit
IsFinite(double d) → inline bool
IsValidDivisor(double d) → inline bool
NormalizeTicks(int64_t* sec, int64_t* ticks) → inline void
if(*ticks < 0)
MakeU128(int64_t a) → inline uint128
if(a < 0)
MakeU128Ticks(Duration d) → inline uint128
if(rep_hi < 0)
MakeDurationFromU128(uint128 u128, bool is_neg) → inline Duration
if(h64 == 0)
if(h64 >= kMaxRepHi64)
if(is_neg && h64 == kMaxRepHi64 && l64 == 0)
if(is_neg)
if(rep_lo != 0)
EncodeTwosComp(int64_t v) → inline uint64_t
DecodeTwosComp(uint64_t v) → inline int64_t
SafeAddRepHi(double a_hi, double b_hi, Duration* d) → inline bool
ScaleFixed(Duration d, int64_t r) → inline Duration
ScaleDouble(Duration d, double r) → inline Duration
IDivFastPath(const Duration num, const Duration den, int64_t* q, Duration* rem) → inline bool
if(den_hi == 0)
if(den_lo == kTicksPerNanosecond)
if(num_hi >= 0)
```

### absl/time/duration_benchmark.cc
```
BM_Duration_Factory_Nanoseconds(benchmark::State& state) → void
BM_Duration_Factory_Microseconds(benchmark::State& state) → void
BM_Duration_Factory_Milliseconds(benchmark::State& state) → void
BM_Duration_Factory_Seconds(benchmark::State& state) → void
BM_Duration_Factory_Minutes(benchmark::State& state) → void
BM_Duration_Factory_Hours(benchmark::State& state) → void
BM_Duration_Factory_DoubleNanoseconds(benchmark::State& state) → void
BM_Duration_Factory_DoubleMicroseconds(benchmark::State& state) → void
BM_Duration_Factory_DoubleMilliseconds(benchmark::State& state) → void
BM_Duration_Factory_DoubleSeconds(benchmark::State& state) → void
BM_Duration_Factory_DoubleMinutes(benchmark::State& state) → void
BM_Duration_Factory_DoubleHours(benchmark::State& state) → void
BM_Duration_Addition(benchmark::State& state) → void
BM_Duration_Subtraction(benchmark::State& state) → void
BM_Duration_Multiplication_Fixed(benchmark::State& state) → void
BM_Duration_Multiplication_Double(benchmark::State& state) → void
BM_Duration_Division_Fixed(benchmark::State& state) → void
BM_Duration_Division_Double(benchmark::State& state) → void
BM_Duration_FDivDuration_Nanoseconds(benchmark::State& state) → void
BM_Duration_IDivDuration_Nanoseconds(benchmark::State& state) → void
BM_Duration_IDivDuration_Microseconds(benchmark::State& state) → void
BM_Duration_IDivDuration_Milliseconds(benchmark::State& state) → void
BM_Duration_IDivDuration_Seconds(benchmark::State& state) → void
BM_Duration_IDivDuration_Minutes(benchmark::State& state) → void
BM_Duration_IDivDuration_Hours(benchmark::State& state) → void
```

### absl/time/duration_test.cc
```
ApproxYears(int64_t n) → absl::Duration
TestToConversion() → void
TestFromChronoBasicEquality() → void
if(minutes_max_count > kint64max / 60)
if(minutes_min_count < kint64min / 60)
if(hours_max_count > kint64max / 3600)
if(hours_min_count < kint64min / 3600)
TestToChrono() → void
for(int unit_sign : {1, -1})
for(int unit_sign : {1, -1})
for(int unit_sign : {1, -1})
for(int64_t i = LOW; i < HIGH; ++i)
for(const auto& test : to_ts)
for(const auto& test : from_ts)
for(const auto& test : to_tv)
for(const auto& test : from_tv)
VerifyApproxSameAsMul(double time_as_seconds, int* const misses) → void
for(int64_t seconds = 0; seconds < 99; ++seconds)
for(uint32_t ticks : tick_vals)
for(absl::Duration d : {s_plus_t, -s_plus_t})
for(;;)
if(mid_duration == d)
for(int i = 0; i < 1000000; ++i)
```

### absl/time/flag_test.cc
```
SetFlagValue(absl::string_view flag_name, absl::string_view value) → bool
GetFlagValue(absl::string_view flag_name, std::string& value) → bool
```

### absl/time/format.cc
```
struct cctz_parts
unix_epoch() → inline cctz::time_point<cctz::
Split(absl::Time t) → cctz_parts
Join(const cctz_parts& parts) → absl::Time
FormatTime(absl::string_view format, absl::Time t, absl::TimeZone tz) → std::string
FormatTime(absl::Time t, absl::TimeZone tz) → std::string
FormatTime(absl::Time t) → std::string
ParseTime(absl::string_view format, absl::string_view input, absl::Time* time, std::string* err) → bool
ParseTime(absl::string_view format, absl::string_view input, absl::TimeZone tz, absl::Time* time, std::string* err) → bool
for(const auto& lit : literals)
if(b)
AbslParseFlag(absl::string_view text, absl::Time* t, std::string* error) → bool
AbslUnparseFlag(absl::Time t) → std::string
ParseFlag(const std::string& text, absl::Time* t, std::string* error) → bool
UnparseFlag(absl::Time t) → std::string
```

### absl/time/format_benchmark.cc
```
BM_Format_FormatTime(benchmark::State& state) → void
BM_Format_ParseTime(benchmark::State& state) → void
```

### absl/time/format_test.cc
```
TestFormatSpecifier(absl::Time t, absl::TimeZone tz, const std::string& fmt, const std::string& ans) → void
```

### absl/time/internal/cctz/include/cctz/civil_time_detail.h
```
struct fields
struct second_tag
class civil_time
  civil_time(max_year, 12, 31, 23, 59, 59) → return
  civil_time(min_year, 1, 1, 0, 0, 0) → return
  difference(T{}, lhs.f_, rhs.f_) → return
if(cd < 0)
if(d > 0)
if(d > 146097)
if(d > -365)
if(d > 365)
for(;;)
for(;;)
for(;;)
if(d > 28)
for(;;)
if(++m > 12)
if(m != 12)
if(m <= 0)
if(hh < 0)
if(mm < 0)
if(0 <= ss && ss < 60)
if(0 <= mm && mm < 60)
if(0 <= hh && hh < 24)
if(1 <= d && d <= 28 && 1 <= m && m <= 12)
```

### absl/time/internal/cctz/include/cctz/time_zone.h
```
class time_zone
  name() → std::string
  lookup(const time_point<seconds>& tp) → absolute_lookup
  lookup(const civil_second& cs) → civil_lookup
  next_transition(const time_point<seconds>& tp, civil_transition* trans) → bool
  prev_transition(const time_point<seconds>& tp, civil_transition* trans) → bool
  version() → std::string
  description() → std::string
  effective_impl() → const Impl&
lookup(const time_point<D>& tp) → absolute_lookup
next_transition(const time_point<D>& tp, civil_transition* trans) → bool
prev_transition(const time_point<D>& tp, civil_transition* trans) → bool
AbslHashValue(H h, time_zone tz) → friend H
convert(const time_point<D>& tp, const time_zone& tz) → inline civil_second
convert(const civil_second& cs, const time_zone& tz) → inline time_point<seconds>
format(const std::string& fmt, const time_point<D>& tp, const time_zone& tz) → inline std::string
parse(const std::string& fmt, const std::string& input, const time_zone& tz, time_point<D>* tpp) → inline bool
join_seconds(const time_point<seconds>& sec, const femtoseconds& fs, time_point<std::chrono::duration<Rep, std::ratio<1, Denom>>>* tpp) → bool
join_seconds(const time_point<seconds>& sec, const femtoseconds&, time_point<std::chrono::duration<Rep, std::ratio<Num, 1>>>* tpp) → bool
if(count >= 0 || count % Num == 0)
join_seconds(const time_point<seconds>& sec, const femtoseconds&, time_point<std::chrono::duration<Rep, std::ratio<1, 1>>>* tpp) → bool
join_seconds(const time_point<seconds>& sec, const femtoseconds&, time_point<seconds>* tpp) → inline bool
```

### absl/time/internal/cctz/include/cctz/zone_info_source.h
```
class ZoneInfoSource
  Read(void* ptr, std::size_t size) → std::size_t
  Skip(std::size_t offset) → int
  Version() → std::string
```

### absl/time/internal/cctz/src/cctz_benchmark.cc
```
BM_Difference_Days(benchmark::State& state) → void
BM_Step_Days(benchmark::State& state) → void
BM_GetWeekday(benchmark::State& state) → void
BM_NextWeekday(benchmark::State& state) → void
for(const auto from : kDays)
for(const auto to : kWeekdays)
BM_PrevWeekday(benchmark::State& state) → void
for(const auto from : kDays)
for(const auto to : kWeekdays)
AllTimeZoneNames() → std::vector<std::string>
for(const char* const* namep = cctz::kTimeZoneNames; *namep != nullptr; ++namep)
TestTimeZone() → cctz::time_zone
BM_Zone_LoadUTCTimeZoneFirst(benchmark::State& state) → void
BM_Zone_LoadUTCTimeZoneLast(benchmark::State& state) → void
BM_Zone_LoadTimeZoneFirst(benchmark::State& state) → void
BM_Zone_LoadTimeZoneCached(benchmark::State& state) → void
BM_Zone_LoadLocalTimeZoneCached(benchmark::State& state) → void
BM_Zone_LoadAllTimeZonesFirst(benchmark::State& state) → void
if(index == 0)
BM_Zone_LoadAllTimeZonesCached(benchmark::State& state) → void
for(const auto& name : names)
BM_Zone_TimeZoneEqualityImplicit(benchmark::State& state) → void
BM_Zone_TimeZoneEqualityExplicit(benchmark::State& state) → void
BM_Zone_UTCTimeZone(benchmark::State& state) → void
BM_Time_ToCivil_CCTZ(benchmark::State& state) → void
```

### absl/time/internal/cctz/src/civil_time_detail.cc
```
switch(wd)
```

### absl/time/internal/cctz/src/civil_time_test.cc
```
struct HasDifference
  test(...) → std::false_type
Format(const T& t) → std::string
for(const auto& e : kLeapYearTable)
```

### absl/time/internal/cctz/src/time_zone_fixed.cc
```
Format02d(char* p, int v) → char*
Parse02d(const char* p) → int
FixedOffsetFromName(const std::string& name, seconds* offset) → bool
if(name == "UTC" || name == "UTC0")
FixedOffsetToName(const seconds& offset) → std::string
if(sign == '-')
if(offset_seconds > 0)
FixedOffsetToAbbr(const seconds& offset) → std::string
if(abbr[5] == '0' && abbr[6] == '0')
if(abbr[3] == '0' && abbr[4] == '0')
```

### absl/time/internal/cctz/src/time_zone_format.cc
```
strptime(const char* s, const char* fmt, std::tm* tm) → char*
ToTmWday(weekday wd) → int
switch(wd)
FromTmWday(int tm_wday) → weekday
switch(tm_wday)
ToTM(const time_zone::absolute_lookup& al) → std::tm
ToWeek(const civil_day& cd, weekday week_start) → int
Format64(char* ep, int width, std::int_fast64_t v) → char*
if(v < 0)
if(last_digit < 0)
Format02d(char* ep, int v) → char*
FormatOffset(char* ep, int offset, const char* mode) → char*
if(offset < 0)
if(!ccc || minutes != 0 || seconds != 0)
FormatTM(std::string* out, const std::string& fmt, const std::tm& tm) → void
ParseInt(const char* dp, int width, T min, T max, T* vp) → const char*
if(dp != nullptr)
if(*dp == '-')
if(width <= 0 || --width != 0)
if(const char* const bp = dp)
if(value < kmin / 10)
if(value < kmin + d)
if(!neg || value != 0)
if(min <= value && value <= max)
format(const std::string& format, const time_point<seconds>& tp, const detail::femtoseconds& fs, const time_zone& tz) → std::string
```

### absl/time/internal/cctz/src/time_zone_format_test.cc
```
TestFormatSpecifier(time_point<D> tp, time_zone tz, const std::string& fmt, const std::string& ans) → void
for(int prec = 1; prec <= 15; ++prec)
for(int prec = 1; prec <= 15; ++prec)
for(const std::string prec : precisions)
for(int ms = 0; ms < 1000; ms += 111)
for(int us = 0; us < 1000; us += 27)
for(int ns = 0; ns < 1000; ns += 9)
for(const std::string prec : precisions)
for(int ms = 0; ms < 1000; ms += 111)
for(int us = 0; us < 1000; us += 27)
for(int ns = 0; ns < 1000; ns += 9)
for(int ps = 0; ps < 1000; ps += 250)
for(auto fmt : {"%Ez", "%z"})
for(auto fmt : {"%Ez", "%E*z", "%:z", "%::z", "%:::z"})
```

### absl/time/internal/cctz/src/time_zone_if.h
```
class TimeZoneIf
  UTC() → std::unique_ptr<TimeZoneIf>
  Make(const std::string& name) → std::unique_ptr<TimeZoneIf>
  BreakTime(const time_point<seconds>& tp) → time_zone::absolute_lookup
  MakeTime(const civil_second& cs) → time_zone::civil_lookup
  NextTransition(const time_point<seconds>& tp, time_zone::civil_transition* trans) → bool
  PrevTransition(const time_point<seconds>& tp, time_zone::civil_transition* trans) → bool
  Version() → std::string
  Description() → std::string
ToUnixSeconds(const time_point<seconds>& tp) → inline std::int_fast64_t
FromUnixSeconds(std::int_fast64_t t) → inline time_point<seconds>
```

### absl/time/internal/cctz/src/time_zone_impl.cc
```
TimeZoneMutex() → std::mutex&
if(time_zone_map != nullptr)
if(impl == nullptr)
if(time_zone_map != nullptr)
for(const auto& element : *time_zone_map)
```

### absl/time/internal/cctz/src/time_zone_impl.h
```
Name() → const std::string&
BreakTime(const time_point<seconds>& tp) → time_zone::absolute_lookup
MakeTime(const civil_second& cs) → time_zone::civil_lookup
NextTransition(const time_point<seconds>& tp, time_zone::civil_transition* trans) → bool
PrevTransition(const time_point<seconds>& tp, time_zone::civil_transition* trans) → bool
Version() → std::string
Description() → std::string
```

### absl/time/internal/cctz/src/time_zone_info.cc
```
struct Header
  Build(const tzhead& tzh) → bool
  DataLength(std::size_t time_len) → std::size_t
class FileZoneInfoSource
  Open(const std::string& name) → std::unique_ptr<ZoneInfoSource
class AndroidZoneInfoSource
  Open(const std::string& name) → std::unique_ptr<ZoneInfoSource
class FuchsiaZoneInfoSource
  Open(const std::string& name) → std::unique_ptr<ZoneInfoSource
IsLeap(year_t year) → inline bool
ToPosixWeekday(weekday wd) → inline int
switch(wd)
Decode8(const char* cp) → inline std::uint_fast8_t
Decode32(const char* cp) → std::int_fast32_t
Decode64(const char* cp) → std::int_fast64_t
AllYearDST(const PosixTimeZone& posix) → bool
TransOffset(bool leap_year, int jan1_weekday, const PosixTransition& pt) → std::int_fast64_t
switch(pt.date.fmt)
if(last_week)
MakeUnique(const time_point<seconds>& tp) → inline time_zone::civil_lookup
MakeUnique(std::int_fast64_t unix_time) → inline time_zone::civil_lookup
MakeSkipped(const Transition& tr, const civil_second& cs) → inline time_zone::civil_lookup
MakeRepeated(const Transition& tr, const civil_second& cs) → inline time_zone::civil_lookup
YearShift(const civil_second& cs, year_t shift) → inline civil_second
if(tt.utc_offset == utc_offset && tt.is_dst == is_dst)
```

### absl/time/internal/cctz/src/time_zone_info.h
```
struct Transition
struct TransitionType
class TimeZoneInfo
  UTC() → std::unique_ptr<TimeZoneInfo>
  Make(const std::string& name) → std::unique_ptr<TimeZoneInfo>
  BreakTime(const time_point<seconds>& tp) → time_zone::absolute_lookup
  MakeTime(const civil_second& cs) → time_zone::civil_lookup
  NextTransition(const time_point<seconds>& tp, time_zone::civil_transition* trans) → bool
  PrevTransition(const time_point<seconds>& tp, time_zone::civil_transition* trans) → bool
  Version() → std::string
  Description() → std::string
```

### absl/time/internal/cctz/src/time_zone_libc.cc
```
gm_time(const std::time_t* timep, std::tm* result) → inline std::tm*
local_time(const std::time_t* timep, std::tm* result) → inline std::tm*
make_time(const civil_second& cs, int is_dst, std::time_t* t, std::tm* tm) → bool
if(*t == std::time_t{-1})
if(tmp == nullptr || tmp->tm_year != tm->tm_year || tmp->tm_mon != tm->tm_mon || tmp->tm_mday != tm->tm_mday || tmp->tm_hour != tm->tm_hour || tmp->tm_min != tm->tm_min || tmp->tm_sec != tm->tm_sec)
find_trans(std::time_t lo, std::time_t hi, tm_gmtoff_t offset) → std::time_t
while(lo + 1 != hi)
if(tmp != nullptr)
while(++lo != hi)
if(tmp != nullptr)
if(tmp == nullptr)
if(!local_)
if(tm0.tm_isdst == tm1.tm_isdst)
if(t0 < t1)
if(tm0.tm_isdst)
local_(name == "localtime") → :
```

### absl/time/internal/cctz/src/time_zone_libc.h
```
class TimeZoneLibC
  Make(const std::string& name) → std::unique_ptr<TimeZoneLibC>
  BreakTime(const time_point<seconds>& tp) → time_zone::absolute_lookup
  MakeTime(const civil_second& cs) → time_zone::civil_lookup
  NextTransition(const time_point<seconds>& tp, time_zone::civil_transition* trans) → bool
  PrevTransition(const time_point<seconds>& tp, time_zone::civil_transition* trans) → bool
  Version() → std::string
  Description() → std::string
  TimeZoneLibC(const std::string& name) → explicit
```

### absl/time/internal/cctz/src/time_zone_lookup.cc
```
if(impl_ == nullptr)
load_time_zone(const std::string& name, time_zone* tz) → bool
utc_time_zone() → time_zone
fixed_time_zone(const seconds& offset) → time_zone
local_time_zone() → time_zone
if(status != ZX_OK)
if(status != ZX_OK)
```

### absl/time/internal/cctz/src/time_zone_lookup_test.cc
```
LoadZone(const std::string& name) → time_zone
VersionCmp(time_zone tz, const std::string& target) → int
for(const char* const* np = kTimeZoneNames; *np != nullptr; ++np)
for(std::size_t i = 0; i != n_threads; ++i)
for(auto& thread : threads)
for(const auto& thread_failure : thread_failures)
for(const char* const* np = kTimeZoneNames; *np != nullptr; ++np)
if(fcl.kind == time_zone::civil_lookup::UNIQUE)
if(tcl.kind == time_zone::civil_lookup::UNIQUE)
if(pal.is_dst == tal.is_dst)
if(tzname == "Africa/Casablanca" || tzname == "Africa/El_Aaiun")
if(ep == nullptr)
for(const char* const* np = kTimeZoneNames; *np != nullptr; ++np)
if(offset_delta == 0)
if(trans_al.is_dst == pre_trans_al.is_dst)
```

### absl/time/internal/cctz/src/time_zone_name_win.cc
```
U_SUCCESS(UErrorCode error) → bool
AsProcAddress(HMODULE module, const char* name) → static T
GetSystem32Dir() → std::wstring
LoadIcuGetTimeZoneIDForWindowsID() → ucal_getTimeZoneIDForWindowsID
if(ucal_getTimeZoneIDForWindowsIDRef != nullptr)
if(icu_dll == nullptr)
if(ucal_getTimeZoneIDForWindowsIDRef != nullptr)
Utf16ToUtf8(const wchar_t* ptr, size_t size) → std::string
GetWindowsLocalTimeZone() → std::string
if(getTimeZoneIDForWindowsID == nullptr)
for(;;)
if(status != U_BUFFER_OVERFLOW_ERROR)
```

### absl/time/internal/cctz/src/time_zone_posix.cc
```
ParseInt(const char* p, int min, int max, int* vp) → const char*
ParseAbbr(const char* p, std::string* abbr) → const char*
if(*p == '<')
while(*++p != '>')
while(*p != '\0')
ParseOffset(const char* p, int min_hour, int max_hour, int sign, std::int_fast32_t* offset) → const char*
if(*p == '+' || *p == '-')
if(*p == ':')
if(*p == ':')
ParseDateTime(const char* p, PosixTransition* res) → const char*
if(p != nullptr && *p == ',')
if(*++p == 'M')
if(p != nullptr)
ParsePosixSpec(const std::string& spec, PosixTimeZone* res) → bool
```

### absl/time/internal/cctz/src/time_zone_posix.h
```
struct PosixTransition
struct PosixTimeZone
```

### absl/time/internal/cctz/src/tzfile.h
```
struct tzhead
```

### absl/time/internal/test_util.cc
```
LoadTimeZone(const std::string& name) → TimeZone
```

### absl/time/simulated_clock.cc
```
wakeup_called_(false)
WakeUp() → void
AwaitConditionOrWakeUp() → void
CancelOrAwaitWakeUp() → void
if(wakeup_called && !wakeup_time_passed_)
Ready() → bool
for(auto& iter : waiters)
ABSL_EXCLUSIVE_LOCKS_REQUIRED(lock_)
ABSL_EXCLUSIVE_LOCKS_REQUIRED(lock_)
for(const auto& wakeup_call : wakeup_calls)
if(deadline <= now_ || ready)
```

### absl/time/simulated_clock.h
```
class SimulatedClock
  SimulatedClock(absl::Time t) → explicit
  TimeNow() → absl::Time
  Sleep(absl::Duration d) → void
  SleepUntil(absl::Time wakeup_time) → void
  SetTime(absl::Time t) → int64_t
  AdvanceTime(absl::Duration d) → int64_t
  ABSL_SHARED_LOCKS_REQUIRED(mu)
  GetEarliestWakeupTime() → std::optional<absl::Time>
```

### absl/time/simulated_clock_test.cc
```
class SimulatedClockTorturer
  await_threads_done(num_threads_) → absl::BlockingCounter
  signal_threads_done(num_threads_) → absl::BlockingCounter
  lock(mu) → absl::MutexLock
  lock(mutex) → absl::MutexLock
SleepAndNotify(absl::Clock* clock, absl::Duration sleep_secs, absl::Notification* note) → void
if(kSkipFlakyReason != nullptr)
if(kSkipFlakyReason != nullptr)
if(kSkipFlakyReason != nullptr)
if(kSkipFlakyReason != nullptr)
if(kSkipFlakyReason != nullptr)
SleepUntilAndNotify(absl::Clock* clock, absl::Time wakeup_time, absl::Notification* note) → void
if(kSkipFlakyReason != nullptr)
if(kSkipFlakyReason != nullptr)
if(kSkipFlakyReason != nullptr)
if(kSkipFlakyReason != nullptr)
if(kSkipFlakyReason != nullptr)
AwaitWithDeadlineAndNotify(absl::Clock* clock, absl::Mutex* mu, absl::Condition* cond, absl::Time wakeup_time, absl::Notification* note, bool* return_val) → void
if(kSkipFlakyReason != nullptr)
if(kSkipFlakyReason != nullptr)
if(kSkipFlakyReason != nullptr)
RacerMakesConditionTrue(absl::Notification* start_note, absl::Mutex* mu, bool* f, absl::BlockingCounter* threads_done) → void
RacerAdvancesTime(absl::Notification* start_note, absl::SimulatedClock* simclock, absl::Duration d, absl::BlockingCounter* threads_done) → void
for(int iteration = 0; iteration < 100; ++iteration)
if(f)
```

### absl/time/time.cc
```
unix_epoch() → inline cctz::time_point<cctz::
FloorToUnit(absl::Duration d, absl::Duration unit) → inline int64_t
InfiniteFutureBreakdown() → inline absl::Time::Breakdown
InfinitePastBreakdown() → inline absl::Time::Breakdown
InfiniteFutureCivilInfo() → inline absl::TimeZone::CivilIn
InfinitePastCivilInfo() → inline absl::TimeZone::CivilIn
InfiniteFutureTimeConversion() → inline absl::TimeConversion
InfinitePastTimeConversion() → inline TimeConversion
MakeTimeWithOverflow(const cctz::time_point<cctz::seconds>& sec, const cctz::civil_second& cs, const cctz::time_zone& tz, bool* normalized = nullptr) → Time
if(sec == max)
if(cs > al.cs)
if(sec == min)
if(cs < al.cs)
MapWeekday(const cctz::weekday& wd) → inline int
switch(wd)
FromUDate(double udate) → absl::Time
FromUniversal(int64_t universal) → absl::Time
ToUnixNanos(Time t) → int64_t
ToUnixMicros(Time t) → int64_t
ToUnixMillis(Time t) → int64_t
ToUnixSeconds(Time t) → int64_t
ToTimeT(Time t) → time_t
ToUDate(Time t) → double
ToUniversal(absl::Time t) → int64_t
TimeFromTimespec(timespec ts) → absl::Time
```

### absl/time/time.h
```
class Duration
class Time
  In(TimeZone tz) → Breakdown
  UniversalEpoch() → friend constexpr Time
  InfiniteFuture() → friend constexpr Time
  InfinitePast() → friend constexpr Time
class TimeZone
  At(Time t) → CivilInfo
  At(CivilSecond ct) → TimeInfo
  NextTransition(Time t, CivilTransition* trans) → bool
  PrevTransition(Time t, CivilTransition* trans) → bool
AbslHashValue(H h, Duration d) → friend H
Get() → constexpr int64_t
ZeroDuration() → ABSL_ATTRIBUTE_CONST_FUNCTION 
AbsDuration(Duration d) → ABSL_ATTRIBUTE_CONST_FUNCTION 
Nanoseconds(T n) → ABSL_ATTRIBUTE_CONST_FUNCTION 
Microseconds(T n) → ABSL_ATTRIBUTE_CONST_FUNCTION 
Milliseconds(T n) → ABSL_ATTRIBUTE_CONST_FUNCTION 
Seconds(T n) → ABSL_ATTRIBUTE_CONST_FUNCTION 
Minutes(T n) → ABSL_ATTRIBUTE_CONST_FUNCTION 
Hours(T n) → ABSL_ATTRIBUTE_CONST_FUNCTION 
Nanoseconds(T n) → ABSL_ATTRIBUTE_CONST_FUNCTION 
Microseconds(T n) → ABSL_ATTRIBUTE_CONST_FUNCTION 
Milliseconds(T n) → ABSL_ATTRIBUTE_CONST_FUNCTION 
Seconds(T n) → ABSL_ATTRIBUTE_CONST_FUNCTION 
```

### absl/time/time_benchmark.cc
```
BM_Time_Arithmetic(benchmark::State& state) → void
BM_Time_Difference(benchmark::State& state) → void
BM_Time_ToDateTime_Absl(benchmark::State& state) → void
BM_Time_ToDateTime_Libc(benchmark::State& state) → void
BM_Time_ToDateTimeUTC_Absl(benchmark::State& state) → void
BM_Time_ToDateTimeUTC_Libc(benchmark::State& state) → void
BM_Time_FromUnixMicros(benchmark::State& state) → void
BM_Time_ToUnixNanos(benchmark::State& state) → void
BM_Time_ToUnixMicros(benchmark::State& state) → void
BM_Time_ToUnixMillis(benchmark::State& state) → void
BM_Time_ToUnixSeconds(benchmark::State& state) → void
BM_Time_FromCivil_Absl(benchmark::State& state) → void
BM_Time_FromCivil_Libc(benchmark::State& state) → void
BM_Time_FromCivilUTC_Absl(benchmark::State& state) → void
BM_Time_FromCivilDay0_Absl(benchmark::State& state) → void
BM_Time_FromCivilDay0_Libc(benchmark::State& state) → void
BM_Time_ToTimespec(benchmark::State& state) → void
BM_Time_FromTimespec(benchmark::State& state) → void
if(++ts.tv_nsec == 1000 * 1000 * 1000)
BM_Time_InfiniteFuture(benchmark::State& state) → void
BM_Time_InfinitePast(benchmark::State& state) → void
```

### absl/time/time_test.cc
```
for(const auto& test : to_ts)
for(const auto& test : from_ts)
for(const auto& test : to_tv)
for(const auto& test : from_tv)
MakeChronoUnixTime(const Duration& d) → std::chrono::system_clock::tim
for(const auto base : bases)
```

### absl/types/any_span.h
```
struct IdentityT
struct DerefT
  ABSL_RAW_DCHECK(ptr, "Cannot dereference null pointer")
class Range
  static_assert(std::is_same<typename std::iterator_traits<Iter>::iterator_category, std::random_access_iterator_tag>::value, "Iter must be a random access iterator.") → public:
  ABSL_HARDENING_ASSERT(begin <= end)
Identity() → inline const IdentityT&
Deref() → inline const DerefT&
Range(Iter begin, Iter end)
size() → std::size_t
MakeAdaptorFromRange(Iter begin, Iter end) → Range<Iter>
AnySpan(array, N, transform) → :
AnySpan(array, N, transform) → :
AnySpan(array, N, transform) → :
AnySpan(array, N, transform) → :
subspan(size_type pos, size_type len) → constexpr AnySpan
if(len == AnySpan<T>::npos)
subspan(size_type pos) → constexpr AnySpan
first(size_type len) → constexpr AnySpan
last(size_type len) → constexpr AnySpan
size() → constexpr size_type
empty() → constexpr bool
at(size_type index) → constexpr reference
front() → constexpr reference
back() → constexpr reference
```

### absl/types/any_span_benchmark.cc
```
MakeElement(int) → T
MakeContainer() → Container
for(size_t i = 0; i < container_size; ++i)
MakePointerContainer(SourceContainer* source) → Container
for(auto& value : *source)
BM_Sequential(benchmark::State& state) → void
for(auto _ : state)
for(auto& v : a)
BM_Sequential_Span(benchmark::State& state) → void
for(auto _ : state)
for(auto& v : array_span)
BM_Sequential_DerefSpan(benchmark::State& state) → void
for(auto _ : state)
for(const auto& v : array_span)
BM_Sequential_AnySpan(benchmark::State& state) → void
for(auto _ : state)
for(T& s : span)
BM_Sequential_AnySpanDeref(benchmark::State& state) → void
for(auto _ : state)
for(auto& s : span)
BM_Random(benchmark::State& state) → void
for(auto _ : state)
BM_Random_Span(benchmark::State& state) → void
for(auto _ : state)
BM_Random_AnySpan(benchmark::State& state) → void
```

### absl/types/any_span_test.cc
```
class AnySpanTest
  span(c, t) → AnySpan<MaybeConst<T>>
  EXPECT_EQ(expected, s)
  EXPECT_EQ(expected, span[index])
  EXPECT_EQ(&expected, &span[index])
struct Base1
struct Base2
class MarkIfCopied
IsHardened() → bool
ABSL_HARDENING_ASSERT([&hardened]()
ExpectSpanEqualsContainer(AnySpan<T> span, const Container& c) → void
for(const T& s : span)
TestContainer(Container c, const Transform& t) → static void
TestPointerContainer(Container* c) → static void
for(const T& s : span)
for(const std::string& a : empty)
for(std::string& s : v)
for(const std::string& s : std::vector<std::string>{"a", "b", "c", "d"})
for(int& i : v)
for(std::string& s : v)
for(size_t i = 0; i < 4; ++i)
for(int i = 0; i < 4; ++i)
for(int i = 0; i < 4; ++i)
for(int i = 0; i < 3; ++i)
Fun(SRef p) → static T&
```

### absl/types/compare.h
```
class OnlyLiteralZero
struct partial_ordering_base
  ABSL_COMPARE_INLINE_BASECLASS_DECL(less)
struct weak_ordering_base
  ABSL_COMPARE_INLINE_BASECLASS_DECL(less)
struct strong_ordering_base
  ABSL_COMPARE_INLINE_BASECLASS_DECL(less)
OnlyLiteralZero(T)
compare_result_as_less_than(const BoolT r) → constexpr bool
compare_result_as_less_than(const absl::weak_ordering r) → constexpr bool
do_less_than_comparison(const Compare& compare, const K& x, const LK& y) → constexpr bool
compare_result_as_ordering(const Int c) → constexpr absl::weak_ordering
compare_result_as_ordering(const absl::weak_ordering c) → constexpr absl::weak_ordering
do_three_way_comparison(const Compare& compare, const K& x, const LK& y) → constexpr absl::weak_ordering
do_three_way_comparison(const Compare& compare, const K& x, const LK& y) → constexpr absl::weak_ordering
```

### absl/types/compare_test.cc
```
struct WeakOrderingLess
Identity(bool b) → bool
for(const auto& lhs : values)
for(const auto& rhs : values)
for(const auto& lhs : values)
for(const auto& rhs : values)
for(const auto& lhs : values)
for(const auto& rhs : values)
```

### absl/types/internal/any_span.h
```
struct IsAnySpan
class TransformPtr
  static_assert(CopiedTransform::value)
  static_assert(!CopiedTransform::value)
struct TransformedContainer
struct HasSize
struct NoData
struct TypeOfData
struct Getter
  ABSL_RAW_DCHECK(other.offset == 0u, "offset must be zero")
  ABSL_RAW_DCHECK(other.offset == 0u, "offset must be zero")
  ABSL_RAW_DCHECK(fun != nullptr, "pointer cannot be null")
  ABSL_RAW_DCHECK(array != nullptr, "pointer cannot be null")
  ABSL_RAW_DCHECK(ptr_array != nullptr, "pointer cannot be null")
  fun(container, index + offset) → return
  ABSL_RAW_DCHECK(fun != nullptr, "pointer cannot be null")
  ABSL_RAW_DCHECK(array != nullptr, "pointer cannot be null")
struct MakeArrayGetterImpl
  TransformPtr(transform, IsTransformCopied<Transform>{})
ptr_(&t) → :
get() → auto
constexpr(std::is_function_v<Transform>) → if
ApplyTransform(TransformPtr transform, U& u) → T&
GetFromArray(const TransformedContainer& container, std::size_t i) → GetterFunctionResult<T>
GetFromContainer(const TransformedContainer& container, std::size_t i) → GetterFunctionResult<T>
```

### absl/types/internal/span.h
```
struct ElementType
struct IsView
EqualImpl(SpanT<T> a, SpanT<T> b) → ABSL_INTERNAL_CONSTEXPR_SINCE_
LessThanImpl(SpanT<T> a, SpanT<T> b) → ABSL_INTERNAL_CONSTEXPR_SINCE_
```

### absl/types/optional_ref.h
```
class optional_ref
  ABSL_HARDENING_ASSERT(ptr_ != nullptr)
  ABSL_HARDENING_ASSERT(ptr_ != nullptr)
  static_assert(!std::is_same_v<std::nullopt_t, std::remove_cv_t<T>>, "optional_ref<nullopt_t> is not allowed.")
ptr_(nullptr) → :
ptr_(input) → :
has_value() → constexpr bool
value() → constexpr T&
value_or(U&& default_value) → constexpr T
if(false)
as_pointer() → constexpr T*
as_optional() → constexpr std::optional<U>
```

### absl/types/optional_ref_test.cc
```
class TestInterface
class TestDerivedClass
struct TestStructWithCopy
TestStructWithCopy(TestStructWithCopy&&)
TestStructWithCopy(const TestStructWithCopy&)
AddExclamation(optional_ref<const std::string> input) → std::string
```

### absl/types/source_location.h
```
class SourceLocation
  SourceLocation(line, file_name) → return
  SourceLocation(1, "<source_location>") → return
current() → static constexpr SourceLocatio
UseUnused() → friend constexpr int
```

### absl/types/source_location_test.cc
```
Echo(const SourceLocation& location) → SourceLocation
TryPassLineAndFile(...) → bool
constexpr(!std::is_same_v<absl::SourceLocation, StdSourceLocation>) → if
```

### absl/types/span.h
```
Span(a, N) → :
at(size_type i) → constexpr reference
subspan(size_type pos = 0, size_type len = npos) → constexpr Span
first(size_type len) → constexpr Span
last(size_type len) → constexpr Span
AbslHashValue(H h, Span v) → friend H
```

### absl/types/span_test.cc
```
struct ContainerWithShallowConstData
struct TypeWithMisleadingData
struct TypeWithMisleadingSize
struct Base
class IntSpanOrderComparisonTest
struct ConstexprTester
struct ContainerWithConstexprMethods
struct BigStruct
MakeRamp(int len, int offset = 0) → std::vector<int>
TakesGenericSpan(absl::Span<T>) → void
data() → int*
size() → int
data() → int&
size() → int
data() → int*
size() → const char*
data() → int*
size() → int
TestInitializerList(absl::Span<const int> s, const std::vector<int>& v) → void
CheckType(const T&) → void
cafter_(vec_after_)
size() → constexpr int
data() → constexpr const int*
```

### absl/types/variant.h
```
struct ConversionVisitor
ConvertVariantTo(Variant&& variant) → To
```

### absl/types/variant_test.cc
```
struct Convertible1
struct Convertible2
Convertible1()
Convertible1(const Convertible1&)
Convertible1(const Convertible2&)
Convertible2()
Convertible2(const Convertible2&)
Convertible2(const Convertible1&)
```

### absl/utility/utility.h
```
struct nontype_t
```

## ci

### ci/cmake_common.sh
```
# Copyright 2020 The Abseil Authors.
```

### ci/cmake_install_test.sh
```
# Copyright 2019 The Abseil Authors.
```

### ci/linux_arm_clang-latest_libcxx_bazel.sh
```
# Copyright 2019 The Abseil Authors.
```

### ci/linux_clang-latest_libcxx_asan_bazel.sh
```
# Copyright 2019 The Abseil Authors.
```

### ci/linux_clang-latest_libcxx_bazel.sh
```
# Copyright 2019 The Abseil Authors.
```

### ci/linux_clang-latest_libcxx_tsan_bazel.sh
```
# Copyright 2019 The Abseil Authors.
```

### ci/linux_clang-latest_libstdcxx_bazel.sh
```
# Copyright 2019 The Abseil Authors.
```

### ci/linux_docker_containers.sh
```
# Copyright 2019 The Abseil Authors.
```

### ci/linux_gcc-floor_libstdcxx_bazel.sh
```
# Copyright 2020 The Abseil Authors.
```

### ci/linux_gcc-latest_libstdcxx_bazel.sh
```
# Copyright 2019 The Abseil Authors.
```

### ci/linux_gcc-latest_libstdcxx_cmake.sh
```
# Copyright 2019 The Abseil Authors.
```

### ci/linux_gcc_alpine_cmake.sh
```
# Copyright 2019 The Abseil Authors.
```

### ci/macos_xcode_bazel.sh
```
# Copyright 2019 The Abseil Authors.
```

### ci/macos_xcode_cmake.sh
```
# Copyright 2019 The Abseil Authors.
export CMAKE_BUILD_PARALLEL_LEVEL
export CTEST_PARALLEL_LEVEL
```
