

## Auto-generated signatures
<!-- Updated by gen-context.js -->
<!-- Generated by SigMap gen-context.js v3.3.2 -->
<!-- Updated: 2026-04-14T06:21:12.384Z -->
<!-- Do not edit below — regenerate with: node gen-context.js -->

# Code signatures

<!-- Generated by SigMap gen-context.js v3.3.2 -->
<!-- DO NOT EDIT below the marker line — run gen-context.js to regenerate -->

# Code signatures

## deps
```
src/flask/app.py ← __future__, types, werkzeug, ., ctx
src/flask/blueprints.py ← __future__, cli, globals, helpers, sansio
src/flask/cli.py ← __future__, types, click, werkzeug, globals
src/flask/config.py ← __future__, werkzeug, errno, types
src/flask/ctx.py ← __future__, types, werkzeug, ., globals
src/flask/debughelpers.py ← __future__, jinja2, werkzeug, blueprints, globals
src/flask/helpers.py ← __future__, types, werkzeug, globals, signals
src/flask/json/__init__.py ← __future__, globals, provider
src/flask/json/provider.py ← __future__, werkzeug
src/flask/json/tag.py ← __future__, markupsafe, werkzeug, json
src/flask/logging.py ← __future__, werkzeug, globals
src/flask/sansio/app.py ← __future__, werkzeug, .., config, ctx
src/flask/sansio/blueprints.py ← __future__, .., scaffold
src/flask/sansio/scaffold.py ← __future__, jinja2, werkzeug, .., helpers
src/flask/sessions.py ← __future__, itsdangerous, werkzeug, json
src/flask/templating.py ← __future__, jinja2, ctx, globals, helpers
src/flask/testing.py ← __future__, types, click, werkzeug, cli
src/flask/views.py ← __future__, ., globals
src/flask/wrappers.py ← __future__, werkzeug, ., globals, helpers
tests/conftest.py ← _pytest, flask, pytest
tests/test_appctx.py ← flask, pytest
tests/test_apps/blueprintapp/apps/admin/__init__.py ← flask
tests/test_apps/blueprintapp/apps/frontend/__init__.py ← flask
tests/test_apps/cliapp/factory.py ← flask
tests/test_apps/helloworld/hello.py ← flask
tests/test_async.py ← flask, pytest
tests/test_basic.py ← platform, markupsafe, werkzeug, flask, importlib
tests/test_blueprints.py ← jinja2, werkzeug, pytest, flask
tests/test_cli.py ← _pytest, click, flask, importlib, platform
tests/test_config.py ← pytest, flask
tests/test_converters.py ← werkzeug, flask
tests/test_helpers.py ← flask, pytest, werkzeug
tests/test_instance_config.py ← pytest, flask
tests/test_json.py ← werkzeug, flask, pytest
tests/test_json_tag.py ← markupsafe, flask, pytest
tests/test_logging.py ← flask, pytest
tests/test_regression.py ← flask
tests/test_reqctx.py ← flask, pytest
tests/test_request.py ← __future__, flask
tests/test_session_interface.py ← flask
tests/test_signals.py ← flask
tests/test_subclassing.py ← flask
tests/test_templating.py ← jinja2, markupsafe, pytest, werkzeug, flask
tests/test_testing.py ← flask, importlib, click, pytest
tests/test_user_error_handler.py ← werkzeug, pytest, flask
tests/test_views.py ← werkzeug, flask, pytest
tests/type_check/typing_app_decorators.py ← __future__, flask
tests/type_check/typing_error_handler.py ← __future__, werkzeug, flask
tests/type_check/typing_route.py ← __future__, flask
examples/celery/src/task_app/__init__.py ← celery, flask
examples/celery/src/task_app/tasks.py ← celery
examples/celery/src/task_app/views.py ← celery, flask, .
examples/javascript/js_example/views.py ← flask, .
examples/javascript/tests/conftest.py ← js_example, pytest
examples/javascript/tests/test_js_example.py ← flask, pytest
examples/tutorial/flaskr/__init__.py ← flask
examples/tutorial/flaskr/auth.py ← flask, werkzeug, db
examples/tutorial/flaskr/blog.py ← flask, werkzeug, auth, db
examples/tutorial/flaskr/db.py ← flask, click
examples/tutorial/tests/conftest.py ← flaskr, pytest
examples/tutorial/tests/test_auth.py ← flask, flaskr, pytest
examples/tutorial/tests/test_blog.py ← flaskr, pytest
examples/tutorial/tests/test_db.py ← flaskr, pytest
examples/tutorial/tests/test_factory.py ← flaskr
docs/conf.py ← pallets_sphinx_themes, packaging
```

## todos
```
tests/test_testing.py:131  # XXX: .example.com:1234/foo/"
tests/test_testing.py:137  # XXX: .example.com:1234/foo/"
```

## docs

### docs/conf.py
```
def github_link(name, rawtext, text, lineno, inliner, options, content)
def setup(app)
```

## examples

### examples/celery/src/task_app/__init__.py
```
def create_app() → Flask
def celery_init_app(app: Flask) → Celery
```

### examples/celery/src/task_app/tasks.py
```
def add(a: int, b: int) → int
def block() → None
def process(self: Task, total: int) → object
```

### examples/celery/src/task_app/templates/index.html
```
title: Celery Example
form#add action="{{"
form#block action="{{"
form#process action="{{"
```

### examples/celery/src/task_app/views.py
```
def result(id: str) → dict[str, object]
def add() → dict[str, object]
def block() → dict[str, object]
def process() → dict[str, object]
GET /result/<id>  →  result()
POST /add  →  add()
POST /block  →  block()
POST /process  →  process()
```

### examples/javascript/js_example/templates/base.html
```
title: JavaScript Example
form#calc
form#calc
span#result
```

### examples/javascript/js_example/views.py
```
def index(js)
def add()
```

### examples/javascript/tests/conftest.py
```
def fixture_app()
def client(app)
```

### examples/javascript/tests/test_js_example.py
```
def test_index(app, client, path, template_name)
def test_add(client, a, b, result)
```

### examples/tutorial/flaskr/__init__.py
```
def create_app(test_config)  # Create and configure an instance of the Flask application
```

### examples/tutorial/flaskr/auth.py
```
def login_required(view)  # View decorator that redirects anonymous users to the login p
def load_logged_in_user()  # If a user id is stored in the session, load the user object 
def register()  # Register a new user
def login()  # Log in a registered user by adding the user id to the sessio
def logout()  # Clear the current session, including the stored user id
```

### examples/tutorial/flaskr/blog.py
```
def index()  # Show all the posts, most recent first
def get_post(id, check_author)  # Get a post and its author by id
def create()  # Create a new post for the current user
def update(id)  # Update a post if the current user is the author
def delete(id)  # Delete a post
```

### examples/tutorial/flaskr/db.py
```
def get_db()  # Connect to the application's configured database
def close_db(e)  # If this request connected to the database, close the
def init_db()  # Clear existing data and create new tables
def init_db_command()  # Clear existing data and create new tables
def init_app(app)  # Register database functions with the Flask app
```

### examples/tutorial/flaskr/schema.sql
```
TABLE user
TABLE post
```

### examples/tutorial/flaskr/templates/auth/login.html
```
input#username
input#password
```

### examples/tutorial/flaskr/templates/auth/register.html
```
input#username
input#password
```

### examples/tutorial/flaskr/templates/base.html
```
title: {% block title %}{% endblock %} - Flaskr
```

### examples/tutorial/flaskr/templates/blog/create.html
```
input#title
textarea#body
```

### examples/tutorial/flaskr/templates/blog/update.html
```
form#post action="{{"
input#title
textarea#body
```

### examples/tutorial/tests/conftest.py
```
class AuthActions
  def __init__(client)
  def login(username, password)
  def logout()
def app()  # Create and configure a new app instance for each test
def client(app)  # A test client for the app
def runner(app)  # A test runner for the app's Click commands
def auth(client)
```

### examples/tutorial/tests/test_auth.py
```
def test_register(client, app)
def test_register_validate_input(client, username, password, message)
def test_login(client, auth)
def test_login_validate_input(auth, username, password, message)
def test_logout(client, auth)
```

### examples/tutorial/tests/test_blog.py
```
def test_index(client, auth)
def test_login_required(client, path)
def test_author_required(app, client, auth)
def test_exists_required(client, auth, path)
def test_create(client, auth, app)
def test_update(client, auth, app)
def test_create_update_validate(client, auth, path)
def test_delete(client, auth, app)
```

### examples/tutorial/tests/test_db.py
```
def test_get_close_db(app)
def test_init_db_command(runner, monkeypatch)
```

### examples/tutorial/tests/test_factory.py
```
def test_config()  # Test create_app without passing test config
def test_hello(client)
```

## src

### src/flask/app.py
```
class Flask(App)
  def get_send_file_max_age(filename: str | None) → int | None
  def send_static_file(filename: str) → Response
  def create_jinja_environment() → Environment
  def create_url_adapter(request: Request | None) → MapAdapter | None
  def raise_routing_exception(request: Request) → t.NoReturn
  def make_shell_context() → dict[str, t.Any]
  def test_client(use_cookies: bool, **kwargs: t.Any) → FlaskClient
  def test_cli_runner(**kwargs: t.Any) → FlaskCliRunner
def remove_ctx(f: F) → F
def add_ctx(f: F) → F
```

### src/flask/blueprints.py
```
class Blueprint(SansioBlueprint)
  def get_send_file_max_age(filename: str | None) → int | None
  def send_static_file(filename: str) → Response
```

### src/flask/cli.py
```
class NoAppException(click.UsageError)
class ScriptInfo
  def load_app() → Flask
class AppGroup(click.Group)
  def decorator(f: t.Callable[..., t.Any]) → click.Command
class FlaskGroup(AppGroup)
  def get_command(ctx: click.Context, name: str) → click.Command | None
  def list_commands(ctx: click.Context) → list[str]
  def parse_args(ctx: click.Context, args: list[str]) → list[str]
class CertParamType(click.ParamType)
  def __init__() → None
class SeparatedPathType(click.Path)
def find_best_app(module: ModuleType) → Flask  # Given a module instance this tries to find the best possible
def find_app_by_string(module: ModuleType, app_name: str) → Flask  # Check if the given string is a variable name or a function
def prepare_import(path: str) → str  # Given a filename this will try to calculate the python path,
def locate_app(module_name: str, app_name: str | None, raise_if_not_found: t.Literal[True]) → Flask
def locate_app(module_name: str, app_name: str | None, raise_if_not_found: t.Literal[False]) → Flask | None
def locate_app(module_name: str, app_name: str | None, raise_if_not_found: bool) → Flask | None
def get_version(ctx: click.Context, param: click.Parameter, value: t.Any) → None
def with_appcontext(f: F) → F  # Wraps a callback so that it's guaranteed to be executed with
def load_dotenv(path: str | os.PathLike[str] | None, load_defaults: bool) → bool
def show_server_banner(debug: bool, app_import_path: str | None) → None  # Show extra startup messages the first time the server is run
def run_command(info: ScriptInfo, host: str, port: int, reload: bool, debugger: bool, with_threads: bool, cert: ssl.SSLContext | tuple[str, str | None] | t.Literal["adhoc"] | None, extra_files: list[str] | None, exclude_patterns: list[str] | None) → None  # Run a local development server
def shell_command() → None  # Run an interactive Python shell in the context of a given
def routes_command(sort: str, all_methods: bool) → None  # Show all registered routes with endpoints and methods
```

### src/flask/config.py
```
class ConfigAttribute(t.Generic[T])
class Config(dict)
  def from_envvar(variable_name: str, silent: bool) → bool
  def from_object(obj: object | str) → None
```

### src/flask/ctx.py
```
class _AppCtxGlobals
  def get(name: str, default: t.Any | None) → t.Any
  def pop(name: str, default: t.Any) → t.Any
  def setdefault(name: str, default: t.Any) → t.Any
class AppContext
  def from_environ(app: Flask, environ: WSGIEnvironment, /) → te.Self
  def has_request() → bool
  def copy() → te.Self
  def request() → Request
  def session() → SessionMixin
  def match_request() → None
  def push() → None
  def pop(exc: BaseException | None) → None
def after_this_request(f: ft.AfterRequestCallable[t.Any]) → ft.AfterRequestCallable[t.Any]  # Decorate a function to run after the current request
def copy_current_request_context(f: F) → F
def has_request_context() → bool
def has_app_context() → bool
```

### src/flask/debughelpers.py
```
class UnexpectedUnicodeError(AssertionError, UnicodeError)
class DebugFilesKeyError(KeyError, AssertionError)
  def __init__(request: Request, key: str) → None
class FormDataRoutingRedirect(AssertionError)
  def __init__(request: Request) → None
def attach_enctype_error_multidict(request: Request) → None  # Patch ``request
def explain_template_loading_attempts(app: App, template: str, attempts: list[
        tuple[
            BaseLoader, Scaffold, tuple[str, str | None, t.Callable[[], bool] | None] | None, ]
    ]) → None  # This should help developers understand what failed
```

### src/flask/helpers.py
```
class _CollectErrors
  def __init__() → None
  def raise_any(message: str) → None
def get_debug_flag() → bool  # Get whether debug mode should be enabled for the app, indica
def get_load_dotenv(default: bool) → bool  # Get whether the user has disabled loading default dotenv fil
def stream_with_context(generator_or_function: t.Iterator[t.AnyStr]) → t.Iterator[t.AnyStr]
def stream_with_context(generator_or_function: t.Callable[..., t.Iterator[t.AnyStr]]) → t.Callable[[t.Iterator[t.An...
def stream_with_context(generator_or_function: t.Iterator[t.AnyStr] | t.Callable[..., t.Iterator[t.AnyStr]]) → t.Iterator[t.AnyStr] | t.Ca...
def make_response(*args: t.Any) → Response
def url_for(endpoint: str, *, _anchor: str | None, _method: str | None, _scheme: str | None, _external: bool | None, **values: t.Any) → str
def redirect(location: str, code: int, Response: type[BaseResponse] | None) → BaseResponse
def abort(code: int | BaseResponse, *args: t.Any, **kwargs: t.Any) → t.NoReturn  # Raise an :exc:`~werkzeug
def get_template_attribute(template_name: str, attribute: str) → t.Any
def flash(message: str, category: str) → None
def send_from_directory(directory: os.PathLike[str] | str, path: os.PathLike[str] | str, **kwargs: t.Any) → Response
def get_root_path(import_name: str) → str  # Find the root path of a package, or the path that contains a
GET /stream  →  streamed_response()
GET /stream  →  streamed_response()
```

### src/flask/json/__init__.py
```
def dumps(obj: t.Any, **kwargs: t.Any) → str
def dump(obj: t.Any, fp: t.IO[str], **kwargs: t.Any) → None
def loads(s: str | bytes, **kwargs: t.Any) → t.Any
def load(fp: t.IO[t.AnyStr], **kwargs: t.Any) → t.Any
def jsonify(*args: t.Any, **kwargs: t.Any) → Response
```

### src/flask/json/provider.py
```
class JSONProvider
  def __init__(app: App) → None
  def dumps(obj: t.Any, **kwargs: t.Any) → str
  def dump(obj: t.Any, fp: t.IO[str], **kwargs: t.Any) → None
  def loads(s: str | bytes, **kwargs: t.Any) → t.Any
  def load(fp: t.IO[t.AnyStr], **kwargs: t.Any) → t.Any
  def response(*args: t.Any, **kwargs: t.Any) → Response
class DefaultJSONProvider(JSONProvider)
  def dumps(obj: t.Any, **kwargs: t.Any) → str
  def loads(s: str | bytes, **kwargs: t.Any) → t.Any
  def response(*args: t.Any, **kwargs: t.Any) → Response
```

### src/flask/json/tag.py
```
class JSONTag
  def __init__(serializer: TaggedJSONSerializer) → None
  def check(value: t.Any) → bool
  def to_json(value: t.Any) → t.Any
  def to_python(value: t.Any) → t.Any
  def tag(value: t.Any) → dict[str, t.Any]
class TagDict(JSONTag)
  def check(value: t.Any) → bool
  def to_json(value: t.Any) → t.Any
  def to_python(value: t.Any) → t.Any
class PassDict(JSONTag)
  def check(value: t.Any) → bool
  def to_json(value: t.Any) → t.Any
class TagTuple(JSONTag)
  def check(value: t.Any) → bool
  def to_json(value: t.Any) → t.Any
  def to_python(value: t.Any) → t.Any
class PassList(JSONTag)
  def check(value: t.Any) → bool
  def to_json(value: t.Any) → t.Any
class TagBytes(JSONTag)
  def check(value: t.Any) → bool
  def to_json(value: t.Any) → t.Any
  def to_python(value: t.Any) → t.Any
class TagMarkup(JSONTag)
```

### src/flask/logging.py
```
def wsgi_errors_stream() → t.TextIO  # Find the most appropriate error stream for the application
def has_level_handler(logger: logging.Logger) → bool  # Check if there is a handler in the logging chain that will h
def create_logger(app: App) → logging.Logger  # Get the Flask app's logger and configure it if needed
```

### src/flask/sansio/app.py
```
class App(Scaffold)
  def name() → str
  def logger() → logging.Logger
  def jinja_env() → Environment
  def create_jinja_environment() → Environment
  def make_config(instance_relative: bool) → Config
  def make_aborter() → Aborter
  def auto_find_instance_path() → str
  def create_global_jinja_loader() → DispatchingJinjaLoader
```

### src/flask/sansio/blueprints.py
```
class BlueprintSetupState
class Blueprint(Scaffold)
  def record(func: DeferredSetupFunction) → None
  def record_once(func: DeferredSetupFunction) → None
  def wrapper(state: BlueprintSetupState) → None
  def register_blueprint(blueprint: Blueprint, **options: t.Any) → None
  def register(app: App, options: dict[str, t.Any]) → None
  def app_template_filter(name: T_template_filter) → T_template_filter
  def decorator(f: T_template_filter) → T_template_filter
  def register_template_filter(state: BlueprintSetupState) → None
```

### src/flask/sansio/scaffold.py
```
class Scaffold
  def static_folder() → str | None
  def static_folder(value: str | os.PathLike[str] | None) → None
  def has_static_folder() → bool
  def static_url_path() → str | None
  def static_url_path(value: str | None) → None
  def jinja_loader() → BaseLoader | None
  def get(rule: str, **options: t.Any) → t.Callable[[T_route], T_route]
  def post(rule: str, **options: t.Any) → t.Callable[[T_route], T_route]
def setupmethod(f: F) → F
def find_package(import_name: str) → tuple[str | None, str]  # Find the prefix that a package is installed under, and the p
```

### src/flask/sessions.py
```
class SessionMixin(MutableMapping[str, t.Any])
  def permanent() → bool
  def permanent(value: bool) → None
class SecureCookieSession(CallbackDict[str, t.Any], SessionMixin)
  def on_update(: te.Self) → None
class NullSession(SecureCookieSession)
class SessionInterface
  def make_null_session(app: Flask) → NullSession
  def is_null_session(obj: object) → bool
  def get_cookie_name(app: Flask) → str
  def get_cookie_domain(app: Flask) → str | None
  def get_cookie_path(app: Flask) → str
  def get_cookie_httponly(app: Flask) → bool
  def get_cookie_secure(app: Flask) → bool
  def get_cookie_samesite(app: Flask) → str | None
class SecureCookieSessionInterface(SessionInterface)
  def get_signing_serializer(app: Flask) → URLSafeTimedSerializer | None
  def open_session(app: Flask, request: Request) → SecureCookieSession | None
```

### src/flask/templating.py
```
class Environment(BaseEnvironment)
  def __init__(app: App, **options: t.Any) → None
class DispatchingJinjaLoader(BaseLoader)
  def __init__(app: App) → None
  def list_templates() → list[str]
def render_template(template_name_or_list: str | Template | list[str | Template], **context: t.Any) → str  # Render a template by name with the given context
def render_template_string(source: str, **context: t.Any) → str  # Render a template from the given source string with the give
def stream_template(template_name_or_list: str | Template | list[str | Template], **context: t.Any) → t.Iterator[str]  # Render a template by name with the given context as a stream
def stream_template_string(source: str, **context: t.Any) → t.Iterator[str]  # Render a template from the given source string with the give
```

### src/flask/testing.py
```
class EnvironBuilder(werkzeug.test.EnvironBuilder)
  def json_dumps(obj: t.Any, **kwargs: t.Any) → str
class FlaskClient(Client)
  def __init__(*args: t.Any, **kwargs: t.Any) → None
class FlaskCliRunner(CliRunner)
  def __init__(app: Flask, **kwargs: t.Any) → None
```

### src/flask/views.py
```
class View
  def dispatch_request() → ft.ResponseReturnValue
  def view(**kwargs: t.Any) → ft.ResponseReturnValue
  def view(**kwargs: t.Any) → ft.ResponseReturnValue
class MethodView(View)
  def dispatch_request(**kwargs: t.Any) → ft.ResponseReturnValue
```

### src/flask/wrappers.py
```
class Request(RequestBase)
  def max_content_length() → int | None
  def max_content_length(value: int | None) → None
  def max_form_memory_size() → int | None
  def max_form_memory_size(value: int | None) → None
  def max_form_parts() → int | None
  def max_form_parts(value: int | None) → None
  def endpoint() → str | None
  def blueprint() → str | None
class Response(ResponseBase)
  def max_cookie_size() → int
```

## tests

### tests/conftest.py
```
def app()
def app_ctx(app)
def req_ctx(app)
def client(app)
def test_apps(monkeypatch)
def leak_detector()  # Fails if any app contexts are still pushed when a test ends
def modules_tmp_path(tmp_path, monkeypatch)  # A temporary directory added to sys
def modules_tmp_path_prefix(modules_tmp_path, monkeypatch)
def site_packages(modules_tmp_path, monkeypatch)  # Create a fake site-packages
def purge_module(request)
```

### tests/test_appctx.py
```
def test_basic_url_generation(app)
def test_url_generation_requires_server_name(app)
def test_url_generation_without_context_fails()
def test_request_context_means_app_context(app)
def test_app_context_provides_current_app(app)
def test_app_tearing_down(app)
def test_app_tearing_down_with_previous_exception(app)
def test_app_tearing_down_with_handled_exception_by_except_block(app)
def test_app_tearing_down_with_handled_exception_by_app_handler(app, client)
def test_app_tearing_down_with_unhandled_exception(app, client)
def test_app_ctx_globals_methods(app, app_ctx)
def test_custom_app_ctx_globals_class(app)
def test_context_refcounts(app, client)
def test_clean_pop(app)
def test_robust_teardown(app: flask.Flask, client: FlaskClient) → None
GET /  →  index()
```

### tests/test_apps/blueprintapp/apps/admin/__init__.py
```
def index()
def index2()
```

### tests/test_apps/blueprintapp/apps/frontend/__init__.py
```
def index()
def missing_template()
```

### tests/test_apps/cliapp/factory.py
```
def create_app()
def create_app2(foo, bar)
def no_app()
```

### tests/test_apps/helloworld/hello.py
```
def hello()
```

### tests/test_async.py
```
class AppError(Exception)
class BlueprintError(Exception)
class AsyncView(View)
  async def dispatch_request()
class AsyncMethodView(MethodView)
  async def get()
  async def post()
def test_async_route(path, async_app)
def test_async_error_handler(path, async_app)
def test_async_before_after_request()
```

### tests/test_basic.py
```
def test_options_work(app, client)
def test_options_on_multiple_rules(app, client)
def test_method_route(app, client, method)
def test_method_route_no_methods(app)
def test_provide_automatic_options_attr_disable(app: flask.Flask, client: FlaskClient) → None  # Automatic options can be disabled by the view func attribute
def test_provide_automatic_options_attr_enable(app: flask.Flask, client: FlaskClient) → None  # When default automatic options is disabled in config, it can
def test_provide_automatic_options_arg_disable(app: flask.Flask, client: FlaskClient) → None  # Automatic options can be disabled by the route argument
def test_provide_automatic_options_method_disable(app: flask.Flask, client: FlaskClient) → None  # Automatic options is ignored if the route handles options
def test_request_dispatching(app, client)
def test_disallow_string_for_allowed_methods(app)
def test_url_mapping(app, client)
def test_werkzeug_routing(app, client)
def test_endpoint_decorator(app, client)
def test_session_accessed(app: flask.Flask, client: FlaskClient) → None
def test_session_path(app, client)
def test_session_using_application_root(app, client)
def test_session_using_session_settings(app, client)
def test_session_using_samesite_attribute(app, client)
def test_missing_session(app)
def test_session_secret_key_fallbacks(app, client) → None
def test_session_expiration(app, client)
def test_session_stored_last(app, client)
def test_session_special_types(app, client)
def test_session_cookie_setting(app)
def test_session_vary_cookie(app, client)
```

### tests/test_blueprints.py
```
def test_blueprint_specific_error_handling(app, client)
def test_blueprint_specific_user_error_handling(app, client)
def test_blueprint_app_error_handling(app, client)
def test_blueprint_prefix_slash(app, client, prefix, rule, url)
def test_blueprint_url_defaults(app, client)
def test_blueprint_url_processors(app, client)
def test_templates_and_static(test_apps)
def test_default_static_max_age(app: flask.Flask) → None
def test_templates_list(test_apps)
def test_dotted_name_not_allowed(app, client)
def test_empty_name_not_allowed(app, client)
def test_dotted_names_from_app(app, client)
def test_empty_url_defaults(app, client)
def test_route_decorator_custom_endpoint(app, client)
def test_route_decorator_custom_endpoint_with_dots(app, client)
def test_endpoint_decorator(app, client)
def test_template_filter(app)
def test_add_template_filter(app)
def test_template_filter_with_name(app)
def test_add_template_filter_with_name(app)
def test_template_filter_with_template(app, client)
def test_template_filter_after_route_with_template(app, client)
def test_add_template_filter_with_template(app, client)
def test_template_filter_with_name_and_template(app, client)
def test_add_template_filter_with_name_and_template(app, client)
```

### tests/test_cli.py
```
class TestRoutes
  def app()
  def invoke(app, runner)
  def expect_order(order, output)
  def test_simple(invoke)
  def test_sort(app, invoke)
  def test_all_methods(invoke)
  def test_no_routes(runner)
  def test_subdomain(runner)
def runner()
def test_cli_name(test_apps)  # Make sure the CLI object's name is the app's name and not th
def test_find_best_app(test_apps)
def test_prepare_import(request, value, path, result)  # Expect the correct path to be set and the correct import and
def test_locate_app(test_apps, iname, aname, result)
def test_locate_app_raises(test_apps, iname, aname)
def test_locate_app_suppress_raise(test_apps)
def test_get_version(test_apps, capsys)
def test_scriptinfo(test_apps, monkeypatch)
def test_app_cli_has_app_context(app, runner)
def test_with_appcontext(runner)
def test_appgroup_app_context(runner)
def test_flaskgroup_app_context(runner)
def test_flaskgroup_debug(runner, set_debug_flag)
def test_flaskgroup_nested(app, runner)
def test_no_command_echo_loading_error()
```

### tests/test_config.py
```
def common_object_test(app)
def test_config_from_pyfile()
def test_config_from_object()
def test_config_from_file_json()
def test_config_from_file_toml()
def test_from_prefixed_env(monkeypatch)
def test_from_prefixed_env_custom_prefix(monkeypatch)
def test_from_prefixed_env_nested(monkeypatch)
def test_config_from_mapping()
def test_config_from_class()
def test_config_from_envvar(monkeypatch)
def test_config_from_envvar_missing(monkeypatch)
def test_config_missing()
def test_config_missing_file()
def test_custom_config_class()
def test_session_lifetime()
def test_get_namespace()
def test_from_pyfile_weird_encoding(tmp_path, encoding)
```

### tests/test_converters.py
```
def test_custom_converters(app, client)
def test_context_available(app, client)
GET /<ctx:name>  →  index()
```

### tests/test_helpers.py
```
class FakePath
  def __init__(path)
class PyBytesIO
  def __init__(*args, **kwargs)
class TestSendfile
  def test_send_file(app, req_ctx)
  def test_static_file(app, req_ctx)
  def get_send_file_max_age(filename)
  def test_send_from_directory(app, req_ctx)
class TestUrlFor
  def test_url_for_with_anchor(app, req_ctx)
  def index()
  def test_url_for_with_scheme(app, req_ctx)
  def index()
  def test_url_for_with_scheme_not_external(app, req_ctx)
  def test_url_for_with_alternating_schemes(app, req_ctx)
  def index()
  def test_url_with_method(app, req_ctx)
class TestNoImports
  def test_name_with_import_error(modules_tmp_path)
class TestStreaming
  def test_streaming_with_context(app, client)
  def index()
  def generate()
  def test_streaming_with_context_as_decorator(app, client)
```

### tests/test_instance_config.py
```
def test_explicit_instance_paths(modules_tmp_path)
def test_uninstalled_module_paths(modules_tmp_path, purge_module)
def test_uninstalled_package_paths(modules_tmp_path, purge_module)
def test_uninstalled_namespace_paths(tmp_path, monkeypatch, purge_module)
def test_installed_module_paths(modules_tmp_path, modules_tmp_path_prefix, purge_module, site_packages)
def test_installed_package_paths(modules_tmp_path, modules_tmp_path_prefix, purge_module, monkeypatch)
def test_prefix_package_paths(modules_tmp_path, modules_tmp_path_prefix, purge_module, site_packages)
```

### tests/test_json.py
```
class FixedOffset(datetime.tzinfo)
  def __init__(hours, name)
  def utcoffset(dt)
  def tzname(dt)
  def dst(dt)
def test_bad_request_debug_message(app, client, debug)
def test_json_bad_requests(app, client)
def test_json_custom_mimetypes(app, client)
def test_json_as_unicode(test_value, expected, app, app_ctx)
def test_json_dump_to_file(app, app_ctx)
def test_jsonify_basic_types(test_value, app, client)
def test_jsonify_dicts(app, client)
def test_jsonify_arrays(app, client)  # Test jsonify of lists and args unpacking
def test_jsonify_datetime(app, client, value)
def test_jsonify_aware_datetimes(tz)  # Test if aware datetime
def test_jsonify_uuid_types(app, client)  # Test jsonify with uuid
def test_json_decimal()
def test_json_attr(app, client)
def test_tojson_filter(app, req_ctx)
def test_json_customization(app, client)
def test_json_key_sorting(app, client)
def test_html_method()
```

### tests/test_json_tag.py
```
def test_dump_load_unchanged(data)
def test_duplicate_tag()
def test_custom_tag()
def test_tag_interface()
def test_tag_order()
```

### tests/test_logging.py
```
def reset_logging(pytestconfig)
def test_logger(app)
def test_logger_debug(app)
def test_existing_handler(app)
def test_wsgi_errors_stream(app, client)
def test_has_level_handler()
def test_log_view_exception(app, client)
```

### tests/test_regression.py
```
def test_aborting(app)
```

### tests/test_reqctx.py
```
class TestGreenletContextCopying
  def test_greenlet_context_copying(app, client)
  def index()
  def g()
  def test_greenlet_context_copying_api(app, client)
  def index()
  def g()
def test_teardown_on_pop(app)
def test_teardown_with_previous_exception(app)
def test_teardown_with_handled_exception(app)
def test_proper_test_request_context(app)
def test_context_binding(app)
def test_context_test(app)
def test_manual_context_binding(app)
def test_session_error_pops_context()
def test_session_dynamic_cookie_name()
```

### tests/test_request.py
```
def test_max_content_length(app: Flask, client: FlaskClient) → None
def test_limit_config(app: Flask)
def test_trusted_hosts_config(app: Flask) → None
POST /  →  index()
GET /  →  index()
```

### tests/test_session_interface.py
```
def test_open_session_with_endpoint()  # If request
GET /  →  index()
```

### tests/test_signals.py
```
def test_template_rendered(app, client)
def test_before_render_template()
def test_request_signals()
def test_request_exception_signal()
def test_appcontext_signals(app, client)
def test_flash_signal(app)
def test_appcontext_tearing_down_signal(app, client)
```

### tests/test_subclassing.py
```
def test_suppressed_exception_logging()
```

### tests/test_templating.py
```
def test_context_processing(app, client)
def test_original_win(app, client)
def test_simple_stream(app, client)
def test_request_less_rendering(app, app_ctx)
def test_standard_context(app, client)
def test_escaping(app, client)
def test_no_escaping(app, client)
def test_escaping_without_template_filename(app, client, req_ctx)
def test_macros(app, req_ctx)
def test_template_filter(app)
def test_add_template_filter(app)
def test_template_filter_with_name(app)
def test_add_template_filter_with_name(app)
def test_template_filter_with_template(app, client)
def test_add_template_filter_with_template(app, client)
def test_template_filter_with_name_and_template(app, client)
def test_add_template_filter_with_name_and_template(app, client)
def test_template_test(app)
def test_add_template_test(app)
def test_template_test_with_name(app)
def test_add_template_test_with_name(app)
def test_template_test_with_template(app, client)
def test_add_template_test_with_template(app, client)
def test_template_test_with_name_and_template(app, client)
def test_add_template_test_with_name_and_template(app, client)
```

### tests/test_testing.py
```
def test_environ_defaults_from_config(app, client)
def test_environ_defaults(app, client, app_ctx, req_ctx)
def test_environ_base_default(app, client)
def test_environ_base_modified(app, client)
def test_client_open_environ(app, client, request)
def test_specify_url_scheme(app, client)
def test_path_is_url(app)
def test_environbuilder_json_dumps(app)  # EnvironBuilder
def test_blueprint_with_subdomain()
def test_redirect_session(app, client, app_ctx)
def test_session_transactions(app, client)
def test_session_transactions_no_null_sessions()
def test_session_transactions_keep_context(app, client, req_ctx)
def test_session_transaction_needs_cookies(app)
def test_test_client_context_binding(app, client)
def test_reuse_client(client)
def test_full_url_request(app, client)
def test_json_request_and_response(app, client)
def test_client_json_no_app_context(app, client)
def test_subdomain()
def test_nosubdomain(app, client)
def test_cli_runner_class(app)
def test_cli_invoke(app)
def test_cli_custom_obj(app)
def test_client_pop_all_preserved(app, req_ctx, client)
```

### tests/test_user_error_handler.py
```
class TestGenericHandlers
  def app(app)
  def do_custom()
  def do_error()
  def do_abort()
  def do_raise()
  def report_error(e)
  def test_handle_class_or_code(app, client, to_handle)
  def handle_500(e)
def test_error_handler_no_match(app, client)
def test_error_handler_subclass(app)
def test_error_handler_http_subclass(app)
def test_error_handler_blueprint(app)
def test_default_error_handler()
```

### tests/test_views.py
```
def common_test(app)
def test_basic_view(app)
def test_method_based_view(app)
def test_view_patching(app)
def test_view_inheritance(app, client)
def test_view_decorators(app, client)
def test_view_provide_automatic_options_attr_disable(app: flask.Flask, client: FlaskClient) → None  # Automatic options can be disabled by the view class attribut
def test_view_provide_automatic_options_attr_enable(app: flask.Flask, client: FlaskClient) → None  # When default automatic options is disabled in config, it can
def test_provide_automatic_options_method_disable(app: flask.Flask, client: FlaskClient) → None  # Automatic options is ignored if the route handles options
def test_implicit_head(app, client)
def test_explicit_head(app, client)
def test_endpoint_override(app)
def test_methods_var_inheritance(app, client)
def test_multiple_inheritance(app, client)
def test_remove_method_from_parent(app, client)
def test_init_once(app, client)
```

### tests/type_check/typing_app_decorators.py
```
def after_sync(response: Response) → Response
async def after_async(response: Response) → Response
def before_sync() → None
async def before_async() → None
def teardown_sync(exc: BaseException | None) → None
async def teardown_async(exc: BaseException | None) → None
```

### tests/type_check/typing_error_handler.py
```
def handle_400(e: BadRequest) → str
def handle_custom(e: ValueError) → str
def handle_accept_base(e: Exception) → str
def handle_multiple(e: BadRequest | NotFound) → str
```

### tests/type_check/typing_route.py
```
class StatusJSON(t.TypedDict)
class RenderTemplateView(View)
  def __init__(: RenderTemplateView, template_name: str) → None
  def dispatch_request(: RenderTemplateView) → str
def hello_str() → str
def hello_bytes() → bytes
def hello_json() → Response
def hello_json_dict() → dict[str, t.Any]
def hello_json_list() → list[t.Any]
def typed_dict() → StatusJSON
def hello_generator() → t.Generator[str, None, None]
def hello_generator_expression() → t.Iterator[bytes]
def hello_iterator() → t.Iterator[str]
def tuple_status(code: int) → tuple[str, int]
def tuple_status_enum() → tuple[str, int]
def tuple_headers() → tuple[str, dict[str, str]]
def return_template(name: str | None) → str
def return_template_stream() → t.Iterator[str]
async def async_route() → str
```
