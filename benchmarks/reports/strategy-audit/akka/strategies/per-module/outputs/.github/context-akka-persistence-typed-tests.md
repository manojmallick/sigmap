<!-- Generated by SigMap gen-context.js v3.3.2 -->
<!-- DO NOT EDIT below the marker line — run gen-context.js to regenerate -->

# Code signatures

## todos
```
akka-persistence-typed-tests/src/test/scala/docs/akka/persistence/typed/ReplicatedAuctionExampleSpec.scala:190  # TODO: this is not as described
```

## akka-persistence-typed-tests

### akka-persistence-typed-tests/src/test/scala/akka/persistence/typed/scaladsl/OptionalSnapshotStoreSpec.scala
```
object OptionalSnapshotStoreSpec
  def persistentBehavior(probe, name)
  def persistentBehaviorWithSnapshotPlugin(probe)
class OptionalSnapshotStoreSpec
```

### akka-persistence-typed-tests/src/test/scala/akka/persistence/typed/scaladsl/PerformanceSpec.scala
```
object PerformanceSpec
  def startMeasure → Unit
  def stopMeasure → Double
  def every(num) → Boolean
  def shouldFail → Boolean
  def failureWasDefined → Boolean
  def behavior(name, probe)
  def eventSourcedTestPersistenceBehavior(name, probe)
class PerformanceSpec
  def stressPersistentActor(persistentActor, probe, failAt, description) → Unit
  def stressEventSourcedPersistentActor(failAt) → Unit
```

### akka-persistence-typed-tests/src/test/scala/akka/persistence/typed/scaladsl/PrimitiveStateSpec.scala
```
object PrimitiveStateSpec
class PrimitiveStateSpec
  def primitiveState(persistenceId, probe) → Behavior[Int]
```

### akka-persistence-typed-tests/src/test/scala/akka/persistence/typed/scaladsl/SlowInMemorySnapshotStore.scala
```
class SlowInMemorySnapshotStore
  def loadAsync(persistenceId, criteria) → Future[Option[SelectedSna
  def saveAsync(metadata, snapshot) → Future[Unit]
```

### akka-persistence-typed-tests/src/test/scala/akka/persistence/typed/scaladsl/SnapshotMutableStateSpec.scala
```
object SnapshotMutableStateSpec
  def conf → Config
  def counter(persistenceId, probe) → EventSourcedBehavior[Comm
class SnapshotMutableStateSpec
  def snapshotState3 → Behavior[Command]
```

### akka-persistence-typed-tests/src/test/scala/akka/persistence/typed/scaladsl/SnapshotRecoveryWithEmptyJournalSpec.scala
```
object SnapshotRecoveryWithEmptyJournalSpec
  def conf → Config
  def apply(name, probe) → Behavior[String]
class SnapshotRecoveryWithEmptyJournalSpec
```

### akka-persistence-typed-tests/src/test/scala/akka/persistence/typed/state/scaladsl/ChangeEventSpec.scala
```
object ChangeEventSpec
  def conf → Config
  def behaviorWithChangeEvent(persistenceId, probe) → Behavior[Command]
class ChangeEventSpec
```

### akka-persistence-typed-tests/src/test/scala/akka/persistence/typed/state/scaladsl/DurableStateBehaviorInterceptorSpec.scala
```
object DurableStateBehaviorInterceptorSpec
  def conf → Config
  def testBehavior(persistenceId, probe) → Behavior[String]
class DurableStateBehaviorInterceptorSpec
```

### akka-persistence-typed-tests/src/test/scala/akka/persistence/typed/state/scaladsl/DurableStateBehaviorReplySpec.scala
```
object DurableStateBehaviorReplySpec
  def conf → Config
  def counter(persistenceId) → Behavior[Command[_]]
  def counter(ctx, persistenceId) → DurableStateBehavior[Comm
class DurableStateBehaviorReplySpec
```

### akka-persistence-typed-tests/src/test/scala/akka/persistence/typed/state/scaladsl/DurableStateBehaviorSpec.scala
```
object DurableStateBehaviorSpec
  def conf → Config
  def counter(persistenceId) → DurableStateBehavior[Comm
class DurableStateBehaviorSpec
```

### akka-persistence-typed-tests/src/test/scala/akka/persistence/typed/state/scaladsl/DurableStateBehaviorTimersSpec.scala
```
object DurableStateBehaviorTimersSpec
  def conf → Config
  def testBehavior(persistenceId, probe) → Behavior[String]
  def testTimerFromSetupBehavior(persistenceId, probe) → Behavior[String]
class DurableStateBehaviorTimersSpec
```

### akka-persistence-typed-tests/src/test/scala/akka/persistence/typed/state/scaladsl/DurableStateRevisionSpec.scala
```
object DurableStateRevisionSpec
  def conf → Config
class DurableStateRevisionSpec
```

### akka-persistence-typed-tests/src/test/scala/akka/persistence/typed/state/scaladsl/DurableStateStashOverflowSpec.scala
```
object DurableStateStashOverflowSpec
  def apply(persistenceId) → Behavior[Command]
  def conf → Config
class DurableStateStashOverflowSpec
```

### akka-persistence-typed-tests/src/test/scala/akka/persistence/typed/state/scaladsl/NullEmptyStateSpec.scala
```
object NullEmptyStateSpec
  def conf → Config
class NullEmptyStateSpec
  def nullState(persistenceId, probe) → Behavior[String]
```

### akka-persistence-typed-tests/src/test/scala/akka/persistence/typed/state/scaladsl/PrimitiveStateSpec.scala
```
object PrimitiveStateSpec
  def conf → Config
class PrimitiveStateSpec
  def primitiveState(persistenceId, probe) → Behavior[Int]
```

### akka-persistence-typed-tests/src/test/scala/docs/akka/persistence/typed/ReplicatedAuctionExampleSpec.scala
```
object ReplicatedAuctionExampleSpec
  def applyEvent(event) → AuctionState
  def withNewHighestBid(bid) → AuctionState
  def withTooLowBid(bid) → AuctionState
  def isHigherBid(first, second) → Boolean
  def apply(replica, name, initialBid, closingAt, responsibleForClosing, allReplicas) → Behavior[Command]
class ReplicatedAuctionExampleSpec
```

### akka-persistence-typed-tests/src/test/scala/docs/akka/persistence/typed/ReplicatedBlogExampleSpec.scala
```
object ReplicatedBlogExampleSpec
  def withContent(newContent, timestamp) → BlogState
  def isEmpty → Boolean
  def apply(entityId, replicaId, allReplicaIds) → Behavior[Command]
class ReplicatedBlogExampleSpec
```

### akka-persistence-typed-tests/src/test/scala/docs/akka/persistence/typed/ReplicatedEventSourcingCompileOnlySpec.scala
```
object ReplicatedEventSourcingCompileOnlySpec
  def apply(system, entityId, replicaId) → EventSourcedBehavior[Comm
  def apply(system, entityId, replicaId) → EventSourcedBehavior[Comm
```

### akka-persistence-typed-tests/src/test/scala/docs/akka/persistence/typed/ReplicatedMovieWatchListExampleSpec.scala
```
object ReplicatedMovieWatchListExampleSpec
  def apply(entityId, replicaId, allReplicaIds) → Behavior[Command]
class ReplicatedMovieWatchListExampleSpec
```

### akka-persistence-typed-tests/src/test/scala/docs/akka/persistence/typed/ReplicatedShoppingCartExampleSpec.scala
```
object ReplicatedShoppingCartExampleSpec
  def apply(entityId, replicaId, allReplicaIds) → Behavior[Command]
class ReplicatedShoppingCartExampleSpec
```
