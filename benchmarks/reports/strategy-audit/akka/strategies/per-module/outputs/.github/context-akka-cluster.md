<!-- Generated by SigMap gen-context.js v3.3.2 -->
<!-- DO NOT EDIT below the marker line — run gen-context.js to regenerate -->

# Code signatures

## todos
```
akka-cluster/src/test/scala/akka/cluster/GossipSpec.scala:391  # TODO: test coverage for when leaderOf returns None - I have not been able to
```

## akka-cluster

### akka-cluster/src/test/scala/akka/cluster/ClusterDomainEventPublisherSpec.scala
```
object ClusterDomainEventPublisherSpec
class ClusterDomainEventPublisherSpec
```

### akka-cluster/src/test/scala/akka/cluster/ClusterDomainEventSpec.scala
```
class ClusterDomainEventSpec
```

### akka-cluster/src/test/scala/akka/cluster/ClusterHeartbeatReceiverSpec.scala
```
class ClusterHeartbeatReceiverSpec
```

### akka-cluster/src/test/scala/akka/cluster/ClusterHeartbeatSenderSpec.scala
```
object ClusterHeartbeatSenderSpec
class ClusterHeartbeatSenderSpec
```

### akka-cluster/src/test/scala/akka/cluster/ClusterHeartbeatSenderStateSpec.scala
```
object ClusterHeartbeatSenderStateSpec
  def markNodeAsUnavailable → Unit
  def markNodeAsAvailable → Unit
class ClusterHeartbeatSenderStateSpec
```

### akka-cluster/src/test/scala/akka/cluster/ClusterLogSpec.scala
```
object ClusterLogSpec
class ClusterLogDefaultSpec
class ClusterLogVerboseDefaultSpec
class ClusterLogVerboseEnabledSpec
```

### akka-cluster/src/test/scala/akka/cluster/ClusterSpec.scala
```
object ClusterSpec
class ClusterSpec
  def clusterView
  def leaderActions → Unit
```

### akka-cluster/src/test/scala/akka/cluster/ClusterTestKit.scala
```
trait ClusterTestKit
  def register(actorSystem)
  def registerAsFirst(actorSystem)
  def newActorSystem(config) → ActorSystem
  def newActorSystemAsFirst(config) → ActorSystem
  def formCluster → Unit
  def joinCluster(actorSystem) → Unit
  def shutdownAll → Unit
  def quitAndRestart(actorSystem, config) → ActorSystem
```

### akka-cluster/src/test/scala/akka/cluster/CrossDcHeartbeatSenderSpec.scala
```
object CrossDcHeartbeatSenderSpec
class CrossDcHeartbeatSenderSpec
```

### akka-cluster/src/test/scala/akka/cluster/DowningProviderSpec.scala
```
class FailingDowningProvider
class DummyDowningProvider
class DowningProviderSpec
```

### akka-cluster/src/test/scala/akka/cluster/FailureDetectorPuppet.scala
```
class FailureDetectorPuppet
  def markNodeAsUnavailable → Unit
  def markNodeAsAvailable → Unit
```

### akka-cluster/src/test/scala/akka/cluster/GossipSpec.scala
```
class GossipSpec
```

### akka-cluster/src/test/scala/akka/cluster/GossipTargetSelectorSpec.scala
```
class GossipTargetSelectorSpec
```

### akka-cluster/src/test/scala/akka/cluster/HeartbeatNodeRingPerfSpec.scala
```
class HeartbeatNodeRingPerfSpec
  def createHeartbeatNodeRingOfSize(size) → HeartbeatNodeRing
```

### akka-cluster/src/test/scala/akka/cluster/HeartbeatNodeRingSpec.scala
```
class HeartbeatNodeRingSpec
```

### akka-cluster/src/test/scala/akka/cluster/JoinConfigCompatCheckClusterSpec.scala
```
class JoinConfigCompatCheckClusterSpec
  def checkInitJoin(oldConfig, newConfig) → ConfigValidation
  def checkInitJoinAck(oldConfig, newConfig) → ConfigValidation
```

### akka-cluster/src/test/scala/akka/cluster/JoinConfigCompatCheckerRollingUpdateSpec.scala
```
object JoinConfigCompatCheckerRollingUpdateSpec
class JoinConfigCompatCheckerRollingUpdateSpec
class JoinConfigCompatRollingUpdateChecker
```

### akka-cluster/src/test/scala/akka/cluster/JoinConfigCompatCheckerSpec.scala
```
object JoinConfigCompatCheckerSpec
class JoinConfigCompatCheckerSpec
class JoinConfigCompatCheckerTest
class JoinConfigCompatCheckerExtraTest
class RogueJoinConfigCompatCheckerTest
```

### akka-cluster/src/test/scala/akka/cluster/JoinConfigCompatPreDefinedChecksSpec.scala
```
class JoinConfigCompatPreDefinedChecksSpec
```

### akka-cluster/src/test/scala/akka/cluster/MemberOrderingSpec.scala
```
class MemberOrderingSpec
  def m(address, status) → Member
```

### akka-cluster/src/test/scala/akka/cluster/MembershipStateSpec.scala
```
class MembershipStateSpec
```

### akka-cluster/src/test/scala/akka/cluster/NativeImageMetadataSpec.scala
```
object NativeImageMetadataSpec
  def main(args) → Unit
class NativeImageMetadataSpec
```

### akka-cluster/src/test/scala/akka/cluster/ReachabilityPerfSpec.scala
```
class ReachabilityPerfSpec
```

### akka-cluster/src/test/scala/akka/cluster/ReachabilitySpec.scala
```
class ReachabilitySpec
```

### akka-cluster/src/test/scala/akka/cluster/ResetSystemMessageSeqNrSpec.scala
```
class ResetSystemMessageSeqNrSpec
```

### akka-cluster/src/test/scala/akka/cluster/ShutdownAfterJoinSeedNodesSpec.scala
```
object ShutdownAfterJoinSeedNodesSpec
class ShutdownAfterJoinSeedNodesSpec
```

### akka-cluster/src/test/scala/akka/cluster/StartupWithOneThreadSpec.scala
```
object StartupWithOneThreadSpec
  def testProps
  def receive
class StartupWithOneThreadSpec
  def this
```

### akka-cluster/src/test/scala/akka/cluster/TestMember.scala
```
object TestMember
  def apply(address, status) → Member
  def apply(address, status, upNumber, dc) → Member
  def apply(address, status, roles, dataCenter, upNumber, appVersion) → Member
  def withUniqueAddress(uniqueAddress, status, roles, dataCenter, upNumber, appVersion) → Member
```

### akka-cluster/src/test/scala/akka/cluster/VectorClockPerfSpec.scala
```
object VectorClockPerfSpec
  def createVectorClockOfSize(size) → (VectorClock, SortedSet[N
  def copyVectorClock(vc) → VectorClock
class VectorClockPerfSpec
  def checkThunkFor(vc1, vc2, thunk, VectorClock)
  def compareTo(order)
  def notEqual(vc1, vc2) → Unit
```

### akka-cluster/src/test/scala/akka/cluster/VectorClockSpec.scala
```
class VectorClockSpec
```

### akka-cluster/src/test/scala/akka/cluster/protobuf/ClusterMessageSerializerSpec.scala
```
class ClusterMessageSerializerSpec
  def roundtrip
  def checkSerialization(obj) → Unit
```

### akka-cluster/src/test/scala/akka/cluster/routing/ClusterRouterSupervisorSpec.scala
```
object ClusterRouterSupervisorSpec
  def receive
class ClusterRouterSupervisorSpec
```

### akka-cluster/src/test/scala/akka/cluster/sbr/LeaseMajoritySpec.scala
```
class LeaseMajoritySpec
```

### akka-cluster/src/test/scala/akka/cluster/sbr/SplitBrainResolverIndirectlyConnectedSpec.scala
```
class SplitBrainResolverIndirectlyConnectedSpec
```

### akka-cluster/src/test/scala/akka/cluster/sbr/SplitBrainResolverSpec.scala
```
object SplitBrainResolverSpec
  def props(stableAfter, strategy, probe, selfUniqueAddress, selfDc, downAllWhenUnstable, tickInterval) → Props
class SplitBrainResolverSpec
```

### akka-cluster/src/test/scala/akka/cluster/sbr/TestAddresses.scala
```
object TestAddresses
  def dcMember(dc, m) → Member
  def dataCenter(dc, members) → Set[Member]
  def joining(m) → Member
  def leaving(m) → Member
  def exiting(m) → Member
  def downed(m) → Member
```

### akka-cluster/src/test/scala/akka/cluster/testkit/AutoDownSpec.scala
```
object AutoDownSpec
class AutoDownSpec
  def autoDownActor(autoDownUnreachableAfter) → ActorRef
```
