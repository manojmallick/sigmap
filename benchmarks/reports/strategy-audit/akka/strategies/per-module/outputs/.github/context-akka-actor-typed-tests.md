<!-- Generated by SigMap gen-context.js v3.3.2 -->
<!-- DO NOT EDIT below the marker line — run gen-context.js to regenerate -->

# Code signatures

## akka-actor-typed-tests

### akka-actor-typed-tests/src/test/scala/docs/akka/typed/AggregatorSpec.scala
```
object AggregatorSpec
  def apply(hotel1, hotel2) → Behavior[Command]
class AggregatorSpec
  def sendRequests(replyTo) → Unit
  def sendRequests(replyTo) → Unit
```

### akka-actor-typed-tests/src/test/scala/docs/akka/typed/DispatchersDocSpec.scala
```
object DispatchersDocSpec
class DispatchersDocSpec
```

### akka-actor-typed-tests/src/test/scala/docs/akka/typed/FSMDocSpec.scala
```
object FSMDocSpec
  def apply → Behavior[Event]
class FSMDocSpec
```

### akka-actor-typed-tests/src/test/scala/docs/akka/typed/FaultToleranceDocSpec.scala
```
object FaultToleranceDocSpec
  def apply → Behavior[Command]
  def apply → Behavior[Command]
  def apply → Behavior[Command]
class FaultToleranceDocSpec
```

### akka-actor-typed-tests/src/test/scala/docs/akka/typed/GracefulStopDocSpec.scala
```
object GracefulStopDocSpec
  def apply → Behavior[Command]
  def apply(name) → Behavior[Command]
  def apply → Behavior[Command]
  def apply → Behavior[Command]
class GracefulStopDocSpec
```

### akka-actor-typed-tests/src/test/scala/docs/akka/typed/InteractionPatternsSpec.scala
```
class InteractionPatternsSpec
  def apply → Behavior[PrintMe]
  def apply → Behaviors.Receive[Request
  def apply(backend) → Behavior[Command]
  def active(inProgress, ActorRef[URI]], count) → Behavior[Command]
```

### akka-actor-typed-tests/src/test/scala/docs/akka/typed/IntroSpec.scala
```
object IntroSpec
  def apply → Behavior[Greet]
  def apply → Behavior[SayHello]
  def apply → Behavior[RoomCommand]
  def apply → Behavior[SessionEvent]
  def apply → Behavior[NotUsed]
class IntroSpec
```

### akka-actor-typed-tests/src/test/scala/docs/akka/typed/LoggingDocExamples.scala
```
object LoggingDocExamples
  def identifier → String
  def apply → Behavior[Command]
  def howToUse → Unit
  def logMessages → Unit
  def withMdc → Unit
  def logging → Unit
  def tagsExample → Unit
```

### akka-actor-typed-tests/src/test/scala/docs/akka/typed/MailboxDocSpec.scala
```
class MailboxDocSpec
```

### akka-actor-typed-tests/src/test/scala/docs/akka/typed/OOIntroSpec.scala
```
object OOIntroSpec
  def apply → Behavior[RoomCommand]
  def apply(room, screenName, client) → Behavior[SessionCommand]
  def apply → Behavior[SessionEvent]
  def apply → Behavior[NotUsed]
  def main(args) → Unit
class OOIntroSpec
```

### akka-actor-typed-tests/src/test/scala/docs/akka/typed/RouterSpec.scala
```
object RouterSpec
  def apply → Behavior[Command]
  def apply(text)
class RouterSpec
  def apply → Behavior[RouterSpec.Worke
  def DoLog(text)
  def apply → Behavior[RouterSpec.Worke
  def DoLog(text)
  def mapping(message)
  def apply(monitor) → Behavior[Message]
```

### akka-actor-typed-tests/src/test/scala/docs/akka/typed/SpawnProtocolDocSpec.scala
```
object SpawnProtocolDocSpec
  def apply → Behavior[SpawnProtocol.Co
class SpawnProtocolDocSpec
```

### akka-actor-typed-tests/src/test/scala/docs/akka/typed/StashDocSpec.scala
```
object StashDocSpec
  def save(id, value) → Future[Done]
  def load(id) → Future[String]
  def apply(id, db) → Behavior[Command]
class StashDocSpec
```

### akka-actor-typed-tests/src/test/scala/docs/akka/typed/StyleGuideDocExamples.scala
```
object StyleGuideDocExamples
  def apply → Behavior[Command]
  def apply → Behavior[Command]
  def apply(name) → Behavior[Command]
  def apply(name) → Behavior[Command]
```

### akka-actor-typed-tests/src/test/scala/docs/akka/typed/TailChopping.scala
```
object TailChopping
  def apply
  def waiting(requestCount) → Behavior[Command]
  def sendNextRequest(requestCount) → Behavior[Command]
```

### akka-actor-typed-tests/src/test/scala/docs/akka/typed/coexistence/ClassicWatchingTypedSpec.scala
```
object ClassicWatchingTypedSpec
  def props
  def apply → Behavior[Command]
class ClassicWatchingTypedSpec
```

### akka-actor-typed-tests/src/test/scala/docs/akka/typed/coexistence/TypedWatchingClassicSpec.scala
```
object TypedWatchingClassicSpec
  def props → classic.Props
class TypedWatchingClassicSpec
```

### akka-actor-typed-tests/src/test/scala/docs/akka/typed/extensions/ExtensionDocSpec.scala
```
class ExpensiveDatabaseConnection
  def executeQuery(query) → Future[Any]
object DatabasePool
  def createExtension(system) → DatabasePool
  def get(system) → DatabasePool
class DatabasePool
  def connection → ExpensiveDatabaseConnecti
object ExtensionDocSpec
```

### akka-actor-typed-tests/src/test/scala/docs/akka/typed/fromclassic/ClassicSample.scala
```
object ClassicSample
  def props → Props
```

### akka-actor-typed-tests/src/test/scala/docs/akka/typed/fromclassic/TypedSample.scala
```
object TypedSample
  def apply → Behavior[HelloWorld.Greet
  def apply → Behavior[Command]
  def updated(children, ActorRef[Child.Command]]) → Behavior[Command]
  def apply → Behavior[Command]
```

### akka-actor-typed-tests/src/test/scala/docs/akka/typed/pubsub/PubSubExample.scala
```
object PubSubExample
  def example → Behavior[Any]
  def registry → Behavior[Any]
```

### akka-actor-typed-tests/src/test/scala/docs/akka/typed/supervision/SupervisionCompileOnly.scala
```
object SupervisionCompileOnly
  def apply → Behavior[Command]
  def child(size) → Behavior[String]
  def parent → Behavior[String]
  def parent2 → Behavior[String]
  def close → Unit
  def process(parts) → Unit
  def claimResource → Resource
  def withPreRestart → Behavior[String]
```

### akka-actor-typed-tests/src/test/scala-3/docs/akka/typed/InteractionPatternsScala3Spec.scala
```
class InteractionPatternsScala3Spec
  def apply → Behaviors.Receive[Request
  def apply(cookieFabric) → Behavior[Command]
  def apply(backend) → Behavior[Command]
  def active(inProgress, ActorRef[URI]], count) → Behavior[CommandOrRespons
```
