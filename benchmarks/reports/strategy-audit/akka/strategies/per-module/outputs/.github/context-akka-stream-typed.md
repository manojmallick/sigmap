<!-- Generated by SigMap gen-context.js v3.3.2 -->
<!-- DO NOT EDIT below the marker line — run gen-context.js to regenerate -->

# Code signatures

## todos
```
akka-stream-typed/src/main/scala/akka/stream/typed/scaladsl/ActorFlow.scala:23  # TODO: would be nice to provide Implicits to allow .ask() directly on Flow/So
```

## akka-stream-typed

### akka-stream-typed/src/main/scala/akka/stream/typed/javadsl/ActorSink.scala
```
object ActorSink
  def actorRef(ref, onCompleteMessage, onFailureMessage, T]) → Sink[T, NotUsed]
  def actorRefWithBackpressure(ref, messageAdapter, T, M], onInitMessage, M], ackMessage, onCompleteMessage, onFailureMessage, M]) → Sink[T, NotUsed]
  def actorRefWithBackpressure(ref, messageAdapter, T, M], onInitMessage, M], onCompleteMessage, onFailureMessage, M]) → Sink[T, NotUsed]
```

### akka-stream-typed/src/main/scala/akka/stream/typed/javadsl/ActorSource.scala
```
object ActorSource
  def actorRef(completionMatcher, failureMatcher, java.util.Optional[Throwable]], bufferSize, overflowStrategy) → Source[T, ActorRef[T]]
  def actorRefWithBackpressure(ackTo, ackMessage, completionMatcher, java.util.Optional[CompletionStrategy]], failureMatcher, java.util.Optional[Throwable]]) → Source[T, ActorRef[T]]
  def actorRefWithAck(ackTo, ackMessage, completionMatcher, java.util.Optional[CompletionStrategy]], failureMatcher, java.util.Optional[Throwable]]) → Source[T, ActorRef[T]]
```

### akka-stream-typed/src/main/scala/akka/stream/typed/javadsl/PubSub.scala
```
object PubSub
  def source(topicActor, bufferSize, overflowStrategy) → Source[T, NotUsed]
  def sink(topicActor) → Sink[T, NotUsed]
```

### akka-stream-typed/src/main/scala/akka/stream/typed/scaladsl/ActorFlow.scala
```
object ActorFlow
  def ask(ref)
  def ask(parallelism)
  def askWithStatus(ref)
  def askWithStatus(parallelism)
  def askWithContext(ref)
  def askWithContext(parallelism)
  def askWithStatusAndContext(ref)
  def askWithStatusAndContext(parallelism)
```

### akka-stream-typed/src/main/scala/akka/stream/typed/scaladsl/ActorSink.scala
```
object ActorSink
  def actorRef(ref, onCompleteMessage, onFailureMessage) → Sink[T, NotUsed]
  def actorRefWithBackpressure(ref, messageAdapter, T)
  def actorRefWithBackpressure(ref, messageAdapter, T)
```

### akka-stream-typed/src/main/scala/akka/stream/typed/scaladsl/ActorSource.scala
```
object ActorSource
  def actorRef(completionMatcher, Unit], failureMatcher, Throwable], bufferSize, overflowStrategy) → Source[T, ActorRef[T]]
  def actorRefWithBackpressure(ackTo, ackMessage, completionMatcher, CompletionStrategy], failureMatcher, Throwable]) → Source[T, ActorRef[T]]
  def actorRefWithAck(ackTo, ackMessage, completionMatcher, CompletionStrategy], failureMatcher, Throwable]) → Source[T, ActorRef[T]]
```

### akka-stream-typed/src/main/scala/akka/stream/typed/scaladsl/PubSub.scala
```
object PubSub
  def source(topicActor, bufferSize, overflowStrategy) → Source[T, NotUsed]
  def sink(topicActor) → Sink[T, NotUsed]
```

### akka-stream-typed/src/test/java/akka/stream/typed/javadsl/ActorSourceSinkCompileTest.java
```
class ActorSourceSinkCompileTest
```

### akka-stream-typed/src/test/java/akka/stream/typed/javadsl/CustomGuardianAndMaterializerTest.java
```
class CustomGuardianAndMaterializerTest
  useSystemWideMaterialiser() → void
  createCustomSystemLevelMaterialiser() → void
  createCustomActorLevelMaterializer() → void
```

### akka-stream-typed/src/test/java/docs/akka/stream/typed/ActorSinkExample.java
```
class ActorSinkExample
```

### akka-stream-typed/src/test/java/docs/akka/stream/typed/ActorSinkWithAckExample.java
```
class ActorSinkWithAckExample
```

### akka-stream-typed/src/test/java/docs/akka/stream/typed/ActorSourceExample.java
```
class ActorSourceExample
  apply(Protocol p, boolean isCheck) → String
```

### akka-stream-typed/src/test/java/docs/akka/stream/typed/ActorSourceWithBackpressureExample.java
```
class StreamFeeder
  toString() → String
  create() → Behavior<Emitted>
  createReceive() → Receive<Emitted>
class ActorSourceWithBackpressureExample
  main(String[] args) → void
```

### akka-stream-typed/src/test/java/docs/javadsl/ActorFlowCompileTest.java
```
class ActorFlowCompileTest
```

### akka-stream-typed/src/test/scala/akka/stream/typed/NativeImageMetadataSpec.scala
```
object NativeImageMetadataSpec
  def main(args) → Unit
class NativeImageMetadataSpec
```

### akka-stream-typed/src/test/scala/akka/stream/typed/scaladsl/ActorSourceSinkSpec.scala
```
object ActorSourceSinkSpec
class ActorSourceSinkSpec
```

### akka-stream-typed/src/test/scala/akka/stream/typed/scaladsl/MaterializerForTypedSpec.scala
```
class MaterializerForTypedSpec
```

### akka-stream-typed/src/test/scala/akka/stream/typed/scaladsl/PubSubSpec.scala
```
class PubSubSpec
```

### akka-stream-typed/src/test/scala/docs/akka/stream/typed/ActorSourceSinkExample.scala
```
object ActorSourceSinkExample
  def compileOnlySourceRef
  def main(args) → Unit
  def apply → Behavior[Emitted.type]
  def compileOnlyAcotrRef
  def targetActor → ActorRef[Protocol]
  def compileOnlySinkWithBackpressure
  def targetActor → ActorRef[Protocol]
```

### akka-stream-typed/src/test/scala/docs/scaladsl/ActorFlowSpec.scala
```
object ActorFlowSpec
class ActorFlowSpec
```
