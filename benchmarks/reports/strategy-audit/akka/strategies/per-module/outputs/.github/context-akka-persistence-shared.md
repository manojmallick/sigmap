<!-- Generated by SigMap gen-context.js v3.3.2 -->
<!-- DO NOT EDIT below the marker line — run gen-context.js to regenerate -->

# Code signatures

## akka-persistence-shared

### akka-persistence-shared/src/test/scala/akka/persistence/journal/leveldb/PersistencePluginProxySpec.scala
```
object PersistencePluginProxySpec
  def targetAddressConfig(system)
  def receive
class PersistencePluginProxySpec
```

### akka-persistence-shared/src/test/scala/akka/persistence/journal/leveldb/SharedLeveldbJournalSpec.scala
```
object SharedLeveldbJournalSpec
  def receive
class SharedLeveldbJournalSpec
```

### akka-persistence-shared/src/test/scala/akka/persistence/serialization/SerializerSpec.scala
```
object SerializerSpecConfigs
  def config(configs) → Config
class SnapshotSerializerPersistenceSpec
class MessageSerializerPersistenceSpec
object MessageSerializerRemotingSpec
  def receive
  def receive
  def port(system)
  def address(system)
class MessageSerializerRemotingSpec
class MyPayloadSerializer
  def identifier → Int
  def includeManifest → Boolean
  def toBinary(o) → Array[Byte]
  def fromBinary(bytes, manifest) → AnyRef
class MyPayload2Serializer
  def identifier → Int
  def manifest(o) → String
  def toBinary(o) → Array[Byte]
  def fromBinary(bytes, manifest) → AnyRef
class MySnapshotSerializer
  def identifier → Int
  def includeManifest → Boolean
  def toBinary(o) → Array[Byte]
  def fromBinary(bytes, manifest) → AnyRef
```
