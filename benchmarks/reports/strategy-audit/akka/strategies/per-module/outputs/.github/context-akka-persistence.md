<!-- Generated by SigMap gen-context.js v3.3.2 -->
<!-- DO NOT EDIT below the marker line — run gen-context.js to regenerate -->

# Code signatures

## akka-persistence

### akka-persistence/src/test/scala/akka/persistence/PersistenceSpec.scala
```
object PersistenceSpec
  def config(plugin, test, serialization, extraConfig)
trait Cleanup
trait TurnOffRecoverOnStart
class TestException
  def beIndependentlyOrdered(prefixes)
```

### akka-persistence/src/test/scala/akka/persistence/PersistentActorBoundedStashingSpec.scala
```
object PersistentActorBoundedStashingSpec
  def receiveRecover
class SteppingInMemPersistentActorBoundedStashingSpec
class ThrowExceptionStrategyPersistentActorBoundedStashingSpec
class DiscardStrategyPersistentActorBoundedStashingSpec
class ReplyToStrategyPersistentActorBoundedStashingSpec
```

### akka-persistence/src/test/scala/akka/persistence/PersistentActorJournalProtocolSpec.scala
```
object PersistentActorJournalProtocolSpec
  def persistenceId
  def receiveRecover
  def receiveCommand
  def P(p) → Unit
  def PA(p) → Unit
object JournalPuppet
class JournalProbe
class JournalPuppet
  def receive
class PersistentActorJournalProtocolSpec
  def expectWrite(subject, msgs) → WriteMessages
  def confirm(w) → Unit
  def startActor(name) → ActorRef
  def msgs(start, end)
  def commands(start, end)
  def expectDone(start, end)
```

### akka-persistence/src/test/scala/akka/persistence/PersistentActorRecoveryTimeoutSpec.scala
```
object PersistentActorRecoveryTimeoutSpec
  def config
class PersistentActorRecoveryTimeoutSpec
```

### akka-persistence/src/test/scala/akka/persistence/PersistentActorSpec.scala
```
object PersistentActorSpec
  def receiveRecover
class InmemPersistentActorSpec
```

### akka-persistence/src/test/scala/akka/persistence/PersistentActorStashingSpec.scala
```
object PersistentActorStashingSpec
  def unstashBehavior → Receive
  def receiveRecover
  def unstashBehavior → Receive
  def unstashBehavior → Receive
  def unstashBehavior → Receive
class SteppingInMemPersistentActorStashingSpec
  def stash
```

### akka-persistence/src/test/scala/akka/persistence/RecoveryPermitterSpec.scala
```
object RecoveryPermitterSpec
  def testProps(name, probe, throwFromRecoveryCompleted) → Props
class RecoveryPermitterSpec
  def requestPermit(p) → Unit
```

### akka-persistence/src/test/scala/akka/persistence/SliceRangesSpec.scala
```
class SliceRangesSpec
```

### akka-persistence/src/test/scala/akka/persistence/SnapshotDecodeFailureSpec.scala
```
object SnapshotDecodeFailureSpec
  def receiveCommand
  def receiveRecover
  def receiveCommand
  def receiveRecover
class SnapshotDecodeFailureSpec
```

### akka-persistence/src/test/scala/akka/persistence/SnapshotDirectoryFailureSpec.scala
```
object SnapshotDirectoryFailureSpec
class SnapshotDirectoryFailureSpec
```

### akka-persistence/src/test/scala/akka/persistence/SnapshotFailureRobustnessSpec.scala
```
object SnapshotFailureRobustnessSpec
class SnapshotFailureRobustnessSpec
class SnapshotIsOptionalSpec
```

### akka-persistence/src/test/scala/akka/persistence/SnapshotRecoveryLocalStoreSpec.scala
```
object SnapshotRecoveryLocalStoreSpec
  def receiveCommand
  def receiveRecover
  def receiveCommand
  def receiveRecover
class SnapshotRecoveryLocalStoreSpec
```

### akka-persistence/src/test/scala/akka/persistence/SnapshotRecoveryWithEmptyJournalSpec.scala
```
object SnapshotRecoveryWithEmptyJournalSpec
class SnapshotRecoveryWithEmptyJournalSpec
```

### akka-persistence/src/test/scala/akka/persistence/SnapshotSerializationSpec.scala
```
object SnapshotSerializationSpec
  def includeManifest → Boolean
  def identifier
  def toBinary(obj) → Array[Byte]
  def fromBinary(bytes, clazz) → AnyRef
class SnapshotSerializationSpec
```

### akka-persistence/src/test/scala/akka/persistence/SnapshotSpec.scala
```
object SnapshotSpec
  def receiveDelete → Receive
class SnapshotSpec
```

### akka-persistence/src/test/scala/akka/persistence/TimerPersistentActorSpec.scala
```
object TimerPersistentActorSpec
  def testProps(name) → Props
  def testJavaProps(name) → Props
class TimerPersistentActorSpec
```

### akka-persistence/src/test/scala/akka/persistence/journal/InmemEventAdaptersSpec.scala
```
class InmemEventAdaptersSpec
class ExampleEventAdapter
class MarkerInterfaceAdapter
class PreciseAdapter
case class ReadMeEvent
class AnotherReaderAdapter
case class WriteMeEvent
```

### akka-persistence/src/test/scala/akka/persistence/journal/ReplayFilterSpec.scala
```
class ReplayFilterSpec
```

### akka-persistence/src/test/scala/akka/persistence/journal/SteppingInmemJournal.scala
```
object SteppingInmemJournal
  def step(journal)
  def config(instanceId) → Config
  def getRef(instanceId) → ActorRef
```

### akka-persistence/src/test/scala/akka/persistence/journal/chaos/ChaosJournal.scala
```
class WriteFailedException
class ReplayFailedException
class ReadHighestFailedException
  def random
  def asyncReplayMessages(persistenceId, fromSequenceNr, toSequenceNr, max)
  def asyncReadHighestSequenceNr(persistenceId, fromSequenceNr) → Future[Long]
  def shouldFail(rate) → Boolean
```

### akka-persistence/src/test/scala/akka/persistence/journal/inmem/InmemJournalSpec.scala
```
object InmemJournalSpec
  def testProps(name) → Props
class InmemJournalSpec
```

### akka-persistence/src/test/scala/akka/persistence/journal/leveldb/CompactionSegmentManagementSpec.scala
```
class CompactionSegmentManagementSpec
```

### akka-persistence/src/test/scala/akka/persistence/journal/leveldb/JournalCompactionSpec.scala
```
class JournalNoCompactionSpec
class JournalCompactionSpec
class JournalCompactionThresholdSpec
object JournalCompactionSpec
  def config → Config
  def createLogger(system, watcher) → ActorRef
  def apply(specId) → SpecComponentBuilder
  def apply(specId, compactionInterval) → SpecComponentBuilder
  def props(specId, watcher) → Props
```

### akka-persistence/src/test/scala/akka/persistence/serialization/MessageSerializerSpec.scala
```
class MessageSerializerSpec
```

### akka-persistence/src/test/scala/akka/persistence/serialization/PayloadSerializerSpec.scala
```
class PayloadSerializerSpec
```
