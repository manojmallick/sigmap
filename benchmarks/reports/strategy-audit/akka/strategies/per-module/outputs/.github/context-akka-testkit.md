<!-- Generated by SigMap gen-context.js v3.3.2 -->
<!-- DO NOT EDIT below the marker line — run gen-context.js to regenerate -->

# Code signatures

## todos
```
akka-testkit/src/main/scala/akka/testkit/internal/NativeImageUtils.scala:134  # FIXME: dispatcher types?
akka-testkit/src/main/scala/akka/testkit/internal/NativeImageUtils.scala:235  # FIXME: we could have separate/extensible to define these in the modules they 
akka-testkit/src/test/java/akka/testkit/javadsl/TestKitApiTest.java:79  # FIXME: why are these called PF when they take a total function, how are they 
akka-testkit/src/test/java/akka/testkit/javadsl/TestKitApiTest.java:99  # FIXME: how is this supposed to be used, scaladoc talks about a partial functi
akka-testkit/src/test/java/akka/testkit/javadsl/TestKitApiTest.java:108  # FIXME: how is this supposed to be used, scaladoc talks about a partial functi
akka-testkit/src/test/java/akka/testkit/javadsl/TestKitApiTest.java:122  # FIXME: how is this supposed to be used, scaladoc talks about a partial functi
akka-testkit/src/test/java/akka/testkit/javadsl/TestKitApiTest.java:130  # FIXME: how is this supposed to be used, scaladoc talks about a partial functi
akka-testkit/src/test/java/akka/testkit/javadsl/TestKitApiTest.java:141  # FIXME: how is this supposed to be used, scaladoc talks about a partial functi
```

## akka-testkit

### akka-testkit/src/main/scala/akka/testkit/TestKitExtension.scala
```
object TestKitExtension
  def createExtension(system) → TestKitSettings
class TestKitSettings
```

### akka-testkit/src/main/scala/akka/testkit/TestLatch.scala
```
object TestLatch
  def apply(count)
class TestLatch
  def countDown
  def isOpen → Boolean
  def open
  def reset
  def ready(atMost)
  def result(atMost)
```

### akka-testkit/src/main/scala/akka/testkit/internal/NativeImageUtils.scala
```
object NativeImageUtils
class NativeImageUtils
  def writeMetadata → Unit
  def verifyMetadata → (String, String)
  def generateMetadata → String
```

### akka-testkit/src/main/scala/akka/testkit/javadsl/EventFilter.scala
```
class EventFilter
  def intercept(code) → T
  def message(msg) → EventFilter
  def startsWith(msg) → EventFilter
  def matches(regex) → EventFilter
  def from(source) → EventFilter
  def occurrences(number) → EventFilter
```

### akka-testkit/src/main/scala/akka/testkit/javadsl/TestKit.scala
```
class TestKit
  def getTestActor → ActorRef
  def getRef → ActorRef
  def getSystem → ActorSystem
  def duration(s) → FiniteDuration
  def dilated(duration) → java.time.Duration
  def msgAvailable → Boolean
  def getLastSender → ActorRef
  def send(actor, msg) → Unit
object TestKit
  def shutdownActorSystem(actorSystem, duration, verifySystemShutdown) → Unit
  def shutdownActorSystem(actorSystem) → Unit
  def shutdownActorSystem(actorSystem, duration) → Unit
  def shutdownActorSystem(actorSystem, verifySystemShutdown) → Unit
```

### akka-testkit/src/test/java/akka/testkit/AkkaJUnitActorSystemResource.java
```
class AkkaJUnitActorSystemResource
  before() → void
  after() → void
  getSystem() → ActorSystem
```

### akka-testkit/src/test/java/akka/testkit/TestActorRefJavaCompile.java
```
class TestActorRefJavaCompile
  shouldBeAbleToCompileWhenUsingApply() → void
```

### akka-testkit/src/test/java/akka/testkit/javadsl/TestKitApiTest.java
```
class TestKitApiTest
  compileOnlyTestCase() → void
```

### akka-testkit/src/test/scala/akka/testkit/AkkaSpec.scala
```
object AkkaSpec
  def mapToConfig(map, Any]) → Config
```

### akka-testkit/src/test/scala/akka/testkit/AkkaSpecSpec.scala
```
class AkkaSpecSpec
  def receive
```

### akka-testkit/src/test/scala/akka/testkit/Coroner.scala
```
object Coroner
  def cancel → Unit
  def waitForStart → Unit
  def started → Unit
  def finished → Unit
  def expired → Unit
  def watch(duration, reportTitle, out, startAndStopDuration, displayThreadCounts) → WatchHandle
  def triggerReportIfOverdue(duration) → Unit
  def printReport(reportTitle, out) → Unit
trait WatchedByCoroner
  def expectedTestDuration → FiniteDuration
  def displayThreadCounts → Boolean
  def startAndStopDuration → FiniteDuration
```

### akka-testkit/src/test/scala/akka/testkit/CoronerSpec.scala
```
class CoronerSpec
  def lockingThread(name, initialLocks) → LockingThread
  def run
  def recursiveLock(locks) → Unit
```

### akka-testkit/src/test/scala/akka/testkit/DefaultTimeoutSpec.scala
```
class DefaultTimeoutSpec
```

### akka-testkit/src/test/scala/akka/testkit/ExplicitlyTriggeredSchedulerSpec.scala
```
class ExplicitlyTriggeredSchedulerSpec
```

### akka-testkit/src/test/scala/akka/testkit/ImplicitSenderSpec.scala
```
class ImplicitSenderSpec
```

### akka-testkit/src/test/scala/akka/testkit/TestActorRefSpec.scala
```
object TestActorRefSpec
  def receive
  def isDefinedAt(o)
  def apply(o) → Unit
  def receiveT → Receive
  def receiveT
  def receiveT
  def receiveT
  def receive
class TestActorRefSpec
  def receive
  def receive
  def receive
  def receiveT
  def receiveT
  def receiveT
```

### akka-testkit/src/test/scala/akka/testkit/TestActorsSpec.scala
```
class TestActorsSpec
```

### akka-testkit/src/test/scala/akka/testkit/TestEventListenerSpec.scala
```
class TestEventListenerSpec
```

### akka-testkit/src/test/scala/akka/testkit/TestFSMRefSpec.scala
```
class TestFSMRefSpec
  def fsmActorFactory
```

### akka-testkit/src/test/scala/akka/testkit/TestProbeSpec.scala
```
class TestProbeSpec
  def assertFailureMessageContains(expectedHint)
  def run(sender, msg) → TestActor.AutoPilot
```

### akka-testkit/src/test/scala/akka/testkit/TestTimeSpec.scala
```
class TestTimeSpec
```

### akka-testkit/src/test/scala/akka/testkit/WithLogCapturing.scala
```
trait WithLogCapturing
class DebugLogSilencingTestEventListener
class SilenceAllTestEventListener
```

### akka-testkit/src/test/scala/akka/testkit/metrics/AveragingGauge.scala
```
class AveragingGauge
  def add(n) → Unit
  def add(ns) → Unit
```

### akka-testkit/src/test/scala/akka/testkit/metrics/KnownOpsInTimespanTimer.scala
```
class KnownOpsInTimespanTimer
  def stop → Boolean
  def elapsedTime → Long
  def avgDuration → Long
  def opsPerSecond → Double
```

### akka-testkit/src/test/scala/akka/testkit/metrics/MetricKeyDSL.scala
```
trait MetricKeyDSL
  def fromString(root)
```

### akka-testkit/src/test/scala/akka/testkit/metrics/MetricsKit.scala
```
trait AkkaMetricRegistry
  def getKnownOpsInTimespanCounters
  def getHdrHistograms
  def getAveragingGauges
```

### akka-testkit/src/test/scala/akka/testkit/metrics/MetricsKitSpec.scala
```
class MetricsKitSpec
```

### akka-testkit/src/test/scala/akka/testkit/metrics/reporter/AkkaConsoleReporter.scala
```
class AkkaConsoleReporter
  def printMetrics
```
