<!-- Generated by SigMap gen-context.js v3.3.2 -->
<!-- DO NOT EDIT below the marker line — run gen-context.js to regenerate -->

# Code signatures

## akka-slf4j

### akka-slf4j/src/main/scala/akka/event/slf4j/Slf4jLogger.scala
```
trait SLF4JLogging
object Logger
  def apply(logger) → SLFLogger
  def apply(logClass, logSource) → SLFLogger
  def root → SLFLogger
class Slf4jLogger
  def receive
class Slf4jLoggingFilter
  def isErrorEnabled(logClass, logSource)
  def isWarningEnabled(logClass, logSource)
  def isInfoEnabled(logClass, logSource)
  def isDebugEnabled(logClass, logSource)
object Slf4jLogMarker
  def apply(marker) → Slf4jLogMarker
  def create(marker) → Slf4jLogMarker
```

### akka-slf4j/src/test/scala/akka/event/slf4j/NativeImageMetadataSpec.scala
```
object NativeImageMetadataSpec
  def main(args) → Unit
class NativeImageMetadataSpec
```

### akka-slf4j/src/test/scala/akka/event/slf4j/Slf4jLoggerSpec.scala
```
object Slf4jLoggerSpec
  def receive
  def outputString → String
class Slf4jLoggerSpec
```

### akka-slf4j/src/test/scala/akka/event/slf4j/Slf4jLoggingFilterSpec.scala
```
object Slf4jLoggingFilterSpec
  def receive
  def receive
class Slf4jLoggingFilterSpec
```
