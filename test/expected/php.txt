class UserService
  function getUser(string $id)
  function createUser(array $data)
  function validateUser(User $user)
interface Repository
  function findById(string $id)
  function save(mixed $entity)
function hashPassword(string $password)
